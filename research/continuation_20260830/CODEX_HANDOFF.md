# Codex handoff — rec canonical context and physical reference

Preserve all worktrees, PR #34/PR #36 evidence, old archives, untracked files,
and source tables. Use a new isolated worktree from the exact terminal package
commit recorded in `REMOTE_PUBLICATION.json`. Never clean, reset, stash,
rebase, amend, squash or force-push.

## Retained scope

```text
PR #34
BLOCKED_REC_SPLIT_DEFECT

PR #36
component-only context/restart/deposition proof
NO_PASS_REC_PHYSICAL_SPLIT
```

The helper `physical_inputs.py` is a reference utility. It binds declared
physical inputs and computes the differential of an explicitly declared
positive-interior KL reconstruction when the moment matrix and prior move. It
is not atomic physics, a COM closure, or an interval certificate.

## REC-LOCAL-01_CANONICAL_CONTEXT_INTEGRATION

Run the nine canonical context/restart tests already present in PR #36, then
the directly affected restart selectors and component mutation runner. Record
actual counts and environment. Do not inherit `NOT_RUN` as PASS. The new
context-bound API must reject altered Doppler width, energy constants, source
identity, frame, time variable, measure and event topology before restore.

## REC-LOCAL-02_SOURCE_BOUND_PHYSICAL_DEPOSITION_AND_FULL_JVP

Build a locked-z~1100 isotropic physical reference using the existing
source-defined occupation/flux owners, the actual 35-frequency x 26-direction
COM measure, and explicit source kernels. Preserve indices 136..143 and edges
(135,136), (143,144). Do not infer cells from native spikes or call
`Aup*x_left-Adn*x_right` a physical photon flux.

The actual unknown must contain physical COM occupation. Establish positive
moment feasibility before selecting a deposition candidate. If no positive map
exists, retain an exact/verified infeasibility certificate. If the map or
measure depends on state, the coupled JVP must include `dM`, source-target
moment changes, prior changes, Doppler/frame derivatives and the moving-measure
term. The fixed-map JVP in PR #36 and the reference helper here are partial
building blocks, not the completed coupled derivative.

Compare native and physical representations only through a justified map.
Assemble photon and atomic four-force independently with metric (-,+,+,+),
explicit hydrogen frame and units. Prove source flux parity, number/energy/
four-force balance, thermal/spectral response, independent directional JVP,
and accepted/rejected restart/history transactions.

One PHYS-MATH and one PHYS-MATH-CODE review after actual execution; at most one
reproduced P0/P1 repair and one differential rereview. Ordinary push one
stacked draft implementation PR against this continuation branch, exact remote
readback, then stop. No full-suite reassurance, timing/GPU/Wolfram, dynamic
macro, rec-to-rei export, merge or ready transition.

Bounded success may be `PASS_REC_ISOTROPIC_PHYSICAL_REFERENCE_ONLY`; full split
PASS requires every original physical requirement. Otherwise preserve a
measured `BLOCKED_REC_PHYSICAL_INTERFACE_DEFECT`.
