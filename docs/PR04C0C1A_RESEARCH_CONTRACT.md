# PR-04C0/C1A research contract — v0.55

## Question

Can canonical October-2012 original HyRec and the 35-state COM–KHW domain be
coupled without a global state remap by assigning every operator term exactly
one owner and extracting source-identical positive photon packets at
`x=+-21.25`?

## Fixed inputs

- canonical archive SHA-256
  `48cd597519606cdafd0ee6405b781d28467cd323278d16596055a8d0577a1d27`;
- predeclared trajectory targets `z=1300,1100,900`;
- physical interfaces `x=-21.25,+21.25` in the hydrogen tetrad;
- ordinary frequency in Hz and explicit `c,h,k_B`;
- no fitted normalization, frequency-cell reconstruction, or packet deposition
  into COM states in this bounded stage.

## Primary hypotheses

1. Every native, COM, analytic-reference and cross-interface process admits
   exactly one declared owner.
2. The source free-streaming history defines a positive total occupation at
   both interfaces by the canonical two-point time interpolation.
3. One packet evaluated once and applied to the two representation ledgers
   with opposite signs conserves photon number and transported photon energy.
4. A pure representation crossing has zero atomic source. Atomic recoil remains
   owned by the physical collision operators.
5. At fixed local hydrogen-frame state the packet is Bianchi-label independent.

## Fail-closed boundaries

PR-04 remains open unless the later far-boundary deposition, coupled implicit
residual, analytic block JVP, positivity and branch-zero gates pass. This stage
must not claim direct native/COM state equality or full trajectory parity.
