# rec_bianchi split-domain bootstrap after ODE four-loop audit

This package converts the external-audit branch into an executable, bounded recovery path. It does **not** merge the audit delivery wholesale and it does not promote the dynamic macro or a production recombination history.

## Exact audit evidence

```text
audit branch: audit/ode-four-loop-external-audit-20260823
audit commit: 4cd2c7bff00ca91c57997d7e6e1ff4c67f7fccd3
audit tree:   3f8731cfab9c9493fcdaa18d855d95768eee1d47
verdict:      REWORK / HOLD
```

The implementation base is the parent of this package stack, created from the live `main` branch. Local Codex must read the package branch and its exact parent chain before mutation; it must not substitute the audit branch as an implementation base.

## Scientific next stage

```text
PR-05C2C1B2B1E1C
exterior-native / interior-COM / single-interface replacement
```

The replacement closes only when residual, analytic JVP, ownership, number/energy/four-force ledgers, primitive/exterior-Schur/interface parity, event localization and restart parity coexist. The existing positive COM subblock and canonical HyRec source remain frozen.

## rec → rei dependency

`rei_bianchi` may continue its rec-independent first canonical FLRW interval, but its recombination splice and hydrogen-frame/Bianchi adapters remain locked until this repo emits `PASS_REC_SPLIT_DOMAIN_REPLACEMENT_AND_INTERFACE_V1` with an exact interface receipt.

Read `PACKAGE.json`, `AUDIT.md`, `REC_REI_INTERFACE_BRIDGE.json`, `WORK_UNITS.json`, and `CODEX_HANDOFF.md`.