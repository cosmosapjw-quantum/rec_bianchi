# rec_bianchi split-domain bootstrap R3

Current execution authority for PR #26. The earlier R2 package remains a
historical preimage and must not be executed.

The exact next action is `REC-BOOTSTRAP-00`, followed by genuine ownership RED
and the exterior-native / interior-COM / single-interface implementation.
Point spikes 136..143 do not define finite-cell support. Crossing edges are
(135,136) and (143,144).

The provisional rec→rei export is metadata-only until the terminal producer
gate passes. No rate/history import or adapter activation is allowed before it.
