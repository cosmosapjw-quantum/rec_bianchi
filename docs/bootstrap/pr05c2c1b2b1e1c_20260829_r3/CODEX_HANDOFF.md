# Codex handoff — rec split-domain R3

Use the exact-pinned entry prompt delivered alongside the publication receipt.
It supplies immutable package and receipt commits.

Preserve all worktrees/untracked files. Materialize R3, verify its manifest,
offline validator and live validator, then create
`agent/implementation/pr05c2c1b2b1e1c-20260829-r1` from the immutable R3
package commit.

Execute the work units in PACKAGE.json. Do not run the old R2 package, merge
the audit branch, infer support, import a surrogate, or start the dynamic macro.
`rei_bianchi` may monitor but cannot consume numerical rec output before the
provider gate.

Final report: STATUS / ACTUAL PROGRESS / VERIFIED / DEFERRED / BLOCKERS / NEXT.
