# Codex handoff — rec_bianchi split-domain replacement

```text
repository: cosmosapjw-quantum/rec_bianchi
source branch: audit/ode-four-loop-external-audit-20260823
source HEAD: 4cd2c7bff00ca91c57997d7e6e1ff4c67f7fccd3
source tree: 3f8731cfab9c9493fcdaa18d855d95768eee1d47
intended implementation branch:
agent/implementation/rec-split-domain-replacement-20260829-r1
exact next action: REC-SPLIT-01_GENUINE_RED
```

Use a new isolated worktree. Preserve every existing checkout and untracked
artifact. Never clean, reset, stash, amend, rebase, squash, or force-push.

## Intake

Run the repository's offline bootstrap and quick verification from the exact
source. Verify the canonical HyRec archive and the v0.75 stage. Read:

1. `HANDOFF_PROMPT.md`
2. `state/PROJECT_STATE.json`
3. `docs/CURRENT_STATE.md`
4. the v0.75 expanded artifact
5. this package's `PACKAGE.json`, `WORK_UNITS.json`,
   `ACCEPTANCE_MATRIX.json`, and `CROSS_REPO_CONTRACT.json`

Do not rerun historical campaigns for reassurance.

## Implement

Start with genuine RED that reaches ownership/numerical assertions. Then build
the exterior-native/interior-COM/interface replacement at the locked z~1100
snapshot. Preserve exact indices 136..143 and crossing edges (135,136),
(143,144). Do not infer cells or fit a normalization.

Use an independent dense direct reference for the exterior-Schur comparison.
Do not accept a production-reported residual as its own oracle. Test analytic
JVP against an independent directional derivative with a fixed, recorded
step schedule; do not widen tolerance after seeing results.

The owner swap is admissible only if overlap and unowned counts are both zero,
number/energy/four-force ledgers close, and restart/history parity passes.
A contract witness is never implementation evidence.

Perform one PHYS-MATH and one PHYS-MATH-CODE review, repair at most one
reproduced P0/P1 defect, ordinary-push one draft PR against the audit branch,
read back exact head/tree/paths/evidence, and stop.

Terminal ceiling:

```text
PASS_REC_PR05C2C1B2B1E1C_SPLIT_DOMAIN_REPLACEMENT_LOCKED_Z1100
```

This is not a full recombination history and does not authorize a numerical
splice into `rei_bianchi`.
