# PLAN-V2 · 충돌항 오라클 설계안

**작성 2026-07-31 · 상태: ✅ 전부 완료 — V2a·V2d(27차), V2b·V2c(28차), V2e·V2f(29차)**

---

## 0. 결론 먼저

> **몬테카를로를 주 오라클로 쓰지 않는다.**  주 오라클은 **결정론적 각도구적**
> (discrete ordinates)이고, MC 는 5% 수준의 **보조 교차검증**으로 강등한다.
> 그리고 오라클을 **궤적**이 아니라 **국소 연산자**로 재정의한다 — 그러면
> Bianchi 배경의 진동이 오라클에 아예 들어오지 않는다.

측정 근거 (본 설계를 위해 새로 잰 값, §3):

| 경로 | 비용 | p₂ = 1/10 추정 상대오차 |
|---|---|---|
| 몬테카를로 | 광자 10⁶개 | **9.0 %** |
| 몬테카를로 | 광자 10⁸개 (외삽) | 0.9 % |
| **결정론 각도구적** | **격자 32점** | **4 × 10⁻¹⁵** |

지적하신 진동 문제는 **맞고, 생각보다 더 나쁘다**.  다만 진짜 이유는 진동 자체가
아니라 **오라클을 궤적으로 잡았을 때 진동이 스텝 수를 지수적으로 늘리는 것**이다.
MC 오차는 스텝을 늘려도 바닥에 붙는다 (§3.4 측정).

---

## 1. V2 가 검증해야 하는 대상 — 범위를 좁힌다

H3 (충돌항) 에서 이미 두 독립 경로로 검증한 것과 **안 한 것**을 분리한다.

**이미 검증됨**
- λ_l 값 자체 — 수치 위상함수 경로 vs 해석 다극 경로 (두 경로 일치).
- Legendre 3항 점화식 (Rust 판이 numpy `legval` 과 독립).

**검증된 적 없음 (= V2 의 표적)**
1. λ_l 을 **계층에 붙인 결과** — 결합 구조, 부호, 정규화.
2. **tilted 프레임**에서의 충돌항 — 전자가 움직일 때 (boost 대수와의 결합).
3. Π 조합의 계수 — H3 이 `8/27 vs 4/15` 로 **정직하게 미결 표시**해 둔 항목.

### 1.1 요구 정확도를 유도한다 (가정하지 않는다)

오라클이 구별해야 하는 최소 간극:

| 구별 대상 | 값 | 상대 간극 |
|---|---|---|
| 감쇠 (1−λ₂) vs 감쇠 없음 | 9/10 vs 1 | **10 %** |
| Π 계수 후보 | 8/27 = 0.2963 vs 4/15 = 0.2667 | **11 %** |
| λ₂ vs 인접 오답 (1/10 vs 1/5) | 0.1 vs 0.2 | 100 % |

⇒ **판정에 필요한 정확도 ≤ 1 %**, 여유 있게 **0.1 %** 를 목표로 잡는다.
(프로젝트 관례상 게이트는 구별 간극보다 최소 10배 조여야 한다.)

---

## 2. 문헌 기반

### 2.1 Bianchi 배경의 진동은 실재하고, 진동수가 **지수적으로** 증가한다

van den Hoogen 외의 Bianchi VII₀ 미래 해석에 따르면, 늦은 시각에 **두 개의 서로 다른
진동수**가 나타난다 — 하나는 전단·곡률 변수, 다른 하나는 tilt 속도 —
그리고 각변수가

```
psi ≈ psi_hat + (√3 / M) e^{ρτ},        M ∝ e^{-τ}
```

로 가므로 **진동수가 τ 에 대해 지수적으로 증가**한다.  Weyl 스칼라도
`(W₁,W₂) ≈ −(12/M̂²)[σ̂₁² − γ²v̂₁²] e^{6(γ−1)τ}(cos 2ψ₂, sin 2ψ₂)` 형태로 진동한다.
저자들은 이 진동을 다루려고 **부록에서 변수 변환을 새로 도입**해 진동 성분과 세속
성분을 분리했고, 강성 대응 솔버(`ode23t`, atol/rtol 10⁻⁶)를 썼다.

Mixmaster (VIII, IX) 는 더 험하다: Kasner 국면과 **급격한 바운스**가 번갈아 나오고
곡률이 전단을 순간적으로 압도한다.

⇒ **지적이 정확하다.**  궤적을 오라클로 삼으면, 진동을 풀기 위해 스텝 수가
지수적으로 늘고, 매 스텝에 잡음이 있는 추정량을 쓰면 답이 없다.

### 2.2 우리와 **같은 설정**의 선행연구는 MC 를 쓰지 않는다

Pontzen & Challinor (2007), Bianchi VII_h 에서 Thomson 산란을 포함한 CMB 편광:

- **다극 계층**을 그대로 적분한다 (MC 아님, 이산종좌표 아님).
- l = 60 에서 특별한 경계조건 없이 절단하고, **l = 120 으로 재실행해 l < 30 에서
  차이가 없음을 확인**한다.
- 매 스텝 **`δη ≪ 1`, `σ δη ≪ 1`, `τ δη ≪ 1`** 를 강제한다.
  ★ 세 번째가 **Thomson 불투명도에 대한 강성 제약**이다 — 우리 D4(강성 스캔)가
  재현해야 할 바로 그 조건.
- 재결합사(史)에 대단히 민감: 순간 재결합 근사 대비 **진폭이 약 5배** 달라진다.

⇒ 문헌의 표준 대응은 "**l_max 를 올리고 스텝을 조인다**" 이지 "MC 로 검증한다" 가
아니다.  V2 를 MC 로 잡는 것은 문헌 관행에서 벗어난 선택이었다.

### 2.3 MC 가 **가장 못하는 양**이 하필 우리 표적이다

Richers 외 (2017)의 다차원 Boltzmann 중성미자 수송법 비교 — 이산종좌표(DO),
몬테카를로(MC), 모멘트법을 나란히 돌린 정밀 비교:

> MC 는 **차감 상쇄(subtractive cancellation)가 지배하는 양** — 예컨대
> **Eddington 텐서의 비대각 성분** — 에서 잡음을 낮추기 어렵다.

> 근사 2-모멘트 닫힘의 오차가 **DO 와 MC 의 차이와 비슷한 수준**이었다.

Eddington 텐서의 비대각·무대각합 성분은 우리의 **π_ab (이방응력)** 와 같은 대상이다.
즉 문헌이 "MC 가 특히 못한다" 고 지목한 양이 정확히 V2 의 표적이다.
두 번째 인용은 더 뼈아프다 — **MC 의 잡음이 우리가 재려는 효과와 같은 크기**라면
오라클로서 실격이다.

### 2.4 광학두께가 커지면 MC 비용이 폭발한다 (산란차수 문제)

Krieger & Wolf (2021), MC 복사전달의 "scattering order problem":

- τ_max = 80, 알베도 1 에서 **검출 광자당 평균 산란 횟수 ≈ 1119회**.
- τ_max = 75 를 제대로 풀려면 Split & Stretch 로 **직렬 환산 52일**이 들고도
  결과가 여전히 잡음투성이였다.

Thomson 산란은 **알베도 = 1 (순수 산란)** 이다 — 위 표에서 최악의 칸이다.
재결합 이전 (τ ≫ 1, 강결합) 을 MC 로 재려는 시도는 문헌이 이미 비용을 보고해 뒀다.

### 2.5 결정론적 대안은 성숙해 있다

Nagakura 외 (2025), S_N (이산종좌표) 이중해상도 처방:

- S_N 의 알려진 약점은 다차원에서 **각해상도 부족**이다.
- 그런데 **충돌항은 이류항만큼의 고해상도를 요구하지 않는다** — 이것이 그 논문의
  핵심 주장이자 우리에게 결정적인 사실이다.
  Thomson 위상함수는 μ 의 **2차 다항식**이므로 각적분이 **낮은 차수 구적으로 정확**하다.
- Richers 2017 도 DO 가 "광학적으로 두꺼운 영역의 가열·냉각률 결정에서 뛰어나다" 고
  적는다 — MC 가 무너지는 바로 그 영역이다.

---

## 3. 사전측정 (이번 설계를 위해 새로 잼)

모두 리포지토리와 무관한 **독립 프로토타입**에서 쟀다.
표적은 Rayleigh 위상함수의 l=2 고유값 `p₂ = 1/10` (= 감쇠 9/10).

### 3.1 몬테카를로 — 광자 10⁶개에 9 %

```
      N       추정 평균     표준편차     p₂ 상대오차     (분자,분모) 상관계수
  10,000       0.0836     5.95e-02       59.5 %            0.104
 100,000       0.1014     2.33e-02       23.3 %            0.104
1,000,000      0.0998     9.01e-03        9.0 %            0.103
```

**1 % 에는 N ≈ 10⁸, 0.1 % 에는 N ≈ 10¹⁰** 이 필요하다.
§1.1 의 요구(≤1 %)를 겨우 만족하는 수준이 10⁸ 이다.

### 3.2 ★ 분산 감소가 듣지 않는 이유 (측정)

공통난수/상관표본은 MC 의 표준 처방이다.  그런데 여기서는 **상관계수가 0.10** 이다.

기전: 우리가 재려는 것이 "l=2 정보가 산란으로 **90 % 파괴된다**" 는 사실이다.
그런데 상관표본이 이득을 내려면 산란 전후가 강하게 상관돼야 한다.
**측정 대상 자체(강한 감쇠)가 분산 감소의 전제(강한 상관)를 파괴한다.**
⇒ 이 문제에서 상관표본은 구조적으로 무력하다.  MC 를 살리는 표준 수단이 막힌다.

### 3.3 결정론 각도구적 — 32점에 4 × 10⁻¹⁵

Gauss–Legendre(θ) × 균일(φ) 곱격자에서 산란적분을 **직접 축약**한다
(Legendre 재귀·PSTF·λ_l 을 전혀 쓰지 않는다):

```
 nθ   nφ    점수        추정 p₂           |오차|
  4    8      32   0.100000000000     4.07e-15
  6   12      72   0.100000000000     1.73e-15
 12   24     288   0.100000000000     4.12e-15
 24   48    1152   0.100000000000     1.43e-14
```

부수 게이트도 함께 통과했다:
- **광자수 보존** `|∫Cf − ∫f| / ∫f = 1.1e-16`
- **p₁ = 0.000000000000**, **p₃ = 0.000000000000** (위상함수가 μ 의 2차식이므로
  l ≥ 3 이 정확히 죽는다 — 구적이 이 구조를 스스로 재현한다)

> ★ 정직한 주석: 계층의 l=1 감쇠 1/3 은 **위상함수에서 오는 것이 아니라**
> 전자의 벌크 속도(Doppler) 항에서 온다.  위 p₁ = 0 은 그 사실의 확인이지 모순이 아니다.
> V2 설계는 이 둘을 **분리해서** 시험한다 (§5의 V2a vs V2b).

### 3.4 진동 배경 — 스텝을 늘려도 MC 는 바닥에 붙는다

문헌(§2.1)의 VII₀ 형 진동수 증가 `ω(τ) = ω₀e^{ρτ}` 를 넣은 모형 적분
(τ ∈ [0, 1.2] 에서 진동수 6 → 36):

```
 스텝수     결정론 상대오차      MC(N=10⁶)      MC(N=10⁸)
   200        1.23e-04        5.94e-03       5.41e-04
   800        7.65e-06        3.20e-03       2.54e-04
  3200        4.78e-07        1.48e-03       1.44e-04
 12800        2.99e-08        6.05e-04       8.20e-05
 51200        1.87e-09        3.66e-04       2.94e-05
```

**해석**: 결정론 오차는 스텝을 늘리면 계속 내려간다 (진동을 풀면 그만이다).
MC 는 스텝을 256배 늘려도 16배밖에 못 줄인다 (독립 잡음의 1/√n 평균).
즉 **진동을 풀려고 계산을 늘릴수록 "비용은 늘고 정확도는 정체"** 한다.
같은 3.7e−4 를 얻는 데 MC 는 5 × 10¹⁰ 연산, 결정론은 1.6 × 10⁶ 연산이었다.

### 3.5 진폭 의존성 — 우주론적 값에서는 절망적이다

잡음은 `1/(a√N)` 로 간다 (a = 이방성 진폭).  측정:

```
   a     N=10⁵ 오차%   N=10⁶ 오차%    1 % 에 필요한 N
 0.300      22.3          6.0            3.6e+07
 0.100      67.1         19.5            3.8e+08
 0.030     178.5         52.1            2.7e+09
```

정확히 `a⁻²` 로 간다.  CMB 의 실제 이방성은 **a ~ 10⁻⁵** 이므로 외삽하면
1 % 에 **N ~ 10¹⁶**.  선형 영역에서 MC 를 직접 쓰는 것은 불가능하다.
(우회로는 "이방성을 인위적으로 크게 키운 시험" 인데, 그러면 **선형 응답 영역을
벗어나** 검증 대상이 달라진다 — 정직하게 한계로 기록한다.)

---

## 4. 설계 판정

### 4.1 오라클을 **궤적**에서 **국소 연산자**로 옮긴다 (가장 중요한 결정)

지금까지 V2 를 "광자를 배경에서 전파시키며 산란시켜 모멘트를 재는" **궤적 시험**으로
구상했다.  그런데 §1 을 다시 보면 검증 표적은 **충돌 연산자 자체**이지 전파가 아니다.
전파(자유흐름)는 이미 정확 구적 오라클로 H1–H5 에서 검증됐다.

⇒ 오라클을 이렇게 잡는다:

```
임의의 f(n̂) 에 대해:   [계층이 계산한 충돌항의 다극]  vs  [각격자에서 직접 축약한 값]
```

이러면 **배경이 아예 등장하지 않는다.**  진동하는 Bianchi 배경이든 Mixmaster 든
오라클에 영향을 못 준다.  지적하신 문제가 **설계 단계에서 소거**된다.

배경이 필요한 시험(결합 궤적)은 §5의 V2c 로 따로 두되, **짧은 구간 + 매끄러운 배경**
으로 제한하고, 진동 배경에서의 적분은 **D1(이벤트 검출)·D3(혼합정밀)** 의 문제로
넘긴다 — 그건 충돌항의 문제가 아니라 적분기의 문제다.

### 4.2 순환 논증 차단 — 위상함수를 **재유도**한다

D-오라클이 우리 코드의 `P(μ) = (3/16π)(1+μ²)` 를 그대로 쓰면 **다극 축약만** 검증하고
**물리 입력은 검증하지 않는다**.  그래서 위상함수를 Thomson 미분단면적에서 조립한다:

```
dσ/dΩ ∝ ½ Σ_{i,j} |ê_i · ê′_j|²        (입사 비편광, 출사 편광 합산)
```

**측정 결과**: 이 쌍극복사 조립이 `(1+μ²)/2` 와 **최대 차이 2.2e−16** 으로 일치했고,
규격화 `∫(3/16π)(1+μ²)dΩ = 1.000000000000000` 도 확인됐다.
⇒ D-오라클은 위상함수를 **인용하지 않고 만들 수 있다**.  순환이 끊긴다.

### 4.3 MC 를 완전히 버리지는 않는다 — 다만 역할을 바꾼다

MC 만이 검증할 수 있는 것이 하나 있다: 충돌 연산자가 **진짜 마르코프 커널**인가
(양성, 규격화, 실제 표본추출 가능성).  구적은 적분값이 맞아도 이 성질을 못 본다.
그래서 MC 를 **5 % 게이트의 존재증명 시험**으로 남긴다 (N ~ 10⁶, a ~ 0.3).
"정확도 오라클" 이 아니라 "구조 오라클" 이다.  이 구분을 문서와 시험 docstring 에
명시한다 — H3 이 "오라클이 없다" 고 적었던 자리를 **정확히 무엇으로 메웠는지**
남기기 위해서다.

---

## 5. 설계안 (구현 단위)

| ID | 내용 | 신규 파일 | 수락 게이트 |
|---|---|---|---|
| **V2a** | **국소 연산자 오라클** — 임의 f(n̂) 에 대해 계층 충돌항 vs 각격자 직접 축약.  위상함수는 §4.2 대로 쌍극복사에서 재유도 | `audit/v2_collision_oracle.py`<br>`bianchi/matter/collision_oracle.py` | ① p₀…p₄ 를 격자에서 뽑아 계층의 λ_l 과 **1e−12** 이내<br>② 광자수 보존 1e−14<br>③ 격자 수렴: 32점과 1152점이 1e−13 이내<br>④ **대조군**: λ₂ 를 1/5 로 바꾸면 게이트가 깨질 것 |
| **V2b** | **Doppler(l=1) 분리 시험** — 전자 벌크속도 항을 위상함수와 **분리**해 검증.  §3.3 의 p₁=0 이 모순이 아님을 코드로 고정 | 위와 동일 | ① v_e = 0 에서 l=1 감쇠가 위상함수만으로는 0<br>② v_e ≠ 0 에서 1/3 구조가 재현<br>③ v_e 1차에서 선형 |
| **V2c** | **tilted 프레임 충돌항** — 전자가 움직이는 틀에서 boost 대수와의 결합.  K2/H5-d 의 boosted 구적을 재사용 | `audit/v2_tilted_collision.py` | ① v→0 에서 V2a 로 환원<br>② T^{μν} 프레임 무관성 잔차가 충돌 전후 모두 유지<br>③ 에너지틀에서 q′ 감쇠가 예상 부호 |
| **V2d** | **Π 계수 판정** — H3 이 미결로 남긴 `8/27 vs 4/15`.  격자에서 직접 재서 **둘 중 하나를 고른다** (또는 셋 다 아님을 보고) | `audit/v2_pi_coefficient.py` | ① 측정값이 두 후보 중 하나와 **0.1 %** 이내<br>② 아니면 **정직하게 미결로 남기고 측정값을 기록** |
| **V2e** | **강성 스캔** — 문헌의 `τ δη ≪ 1` 을 우리 수치로 재현.  τ̇/H 를 0.1 → 10³ 로 올리며 명시적 RK4 가 언제 깨지는지 측정 | `bianchi/matter/collision_stiffness.py` | ① 임계 δη 가 τ̇ 에 반비례<br>② 임계를 넘으면 **조용히 틀리지 않고 경고**<br>③ 암묵적 적분으로 넘어가는 기준을 수치로 제시 |
| **V2f** | **MC 구조 오라클 (강등)** — 마르코프 커널 성질만 확인 | `audit/v2_monte_carlo.py` | ① p₂ 가 5 % 이내 (N=10⁶)<br>② 위상함수 표본추출이 실제 pdf (KS 시험)<br>③ 광자수 보존<br>★ docstring 에 "정확도 오라클이 아님" 명시 |

### 5.1 순서와 근거

```
V2a → V2b → V2d      국소 연산자.  배경 없음 ⇒ 진동 무관.  V2d 가 미결 항목을 닫는다
V2c                  tilted (K2 의 boost 재사용)
V2e                  강성 — 문헌 제약을 우리 수치로
V2f                  MC 구조 시험 (마지막, 싸게)
```

V2a 가 끝나는 순간 "충돌항에 오라클이 없다" 는 부채가 사라진다.
나머지는 그 위의 확장이다.

### 5.2 비용 추정

| 단위 | 예상 계산량 | 근거 |
|---|---|---|
| V2a | 격자 288점 × f 몇 종 → **밀리초** | §3.3 측정 |
| V2c | boosted 구적 재사용 → 초 단위 | H5-d 실측 |
| V2e | RK4 스캔 ~10² 회 → 분 단위 | H5-e3 실측 |
| V2f | N=10⁶ × 24 반복 → **분 단위** | §3.1 실측 (24반복 약 40초) |

⇒ 전체가 기존 증분들보다 **싸다**.  MC 를 주 오라클로 잡았다면 V2f 하나가
N=10⁸ × 반복으로 수 시간이었을 것이다.

---

## 6. 위험표 (정직하게)

| 위험 | 실체 | 대응 |
|---|---|---|
| **D-오라클이 사실은 독립이 아니다** | 위상함수를 코드에서 가져오면 다극 축약만 검증 | §4.2 의 쌍극복사 재유도 (측정 2.2e−16).  시험이 재유도 경로를 쓰는지 **소스 검사로 강제** (K2 에서 손 전사를 금지한 회귀시험과 같은 방식) |
| **격자가 우연히 맞을 수 있다** | 저차 구적이 저차 다항식을 정확히 적분하는 건 당연 — 게이트가 무의미해질 수 있다 | 대조군을 넣는다: λ₂ 를 1/5 로 바꾸면 **깨져야** 한다.  또 위상함수에 인위적 l=4 성분을 넣어 구적이 그것도 잡아내는지 확인 |
| **선형 영역 밖 시험** | MC 게이트가 a ~ 0.3 이라 CMB 의 a ~ 10⁻⁵ 와 다른 영역 | V2f docstring 에 명시.  V2a 는 선형 연산자 시험이라 a 와 무관 — 이것이 D-오라클을 주로 삼는 또 하나의 이유 |
| **편광을 안 본다** | 현재 H3 은 비편광 스칼라 판.  실제 Thomson 은 E-모드와 결합 | V2 범위에서 **제외**하고 별도 항목으로 기록.  Pontzen & Challinor 가 편광까지 다루므로 비교 대상이 있다 |
| **진동 배경 자체는 여전히 미해결** | V2 가 진동을 회피할 뿐 풀지는 않는다 | 맞다.  진동은 **적분기 문제(D1 이벤트 검출, D3 혼합정밀, R1 암묵적 BDF)** 로 이관.  V2 문서에 이관 사실을 남긴다 |
| **재결합사 민감도** | 문헌이 5배 차이를 보고 | B2b (다준위 재결합) 와 묶어야 함을 기록.  V2 게이트는 τ̇ 를 **처방**으로 두고 재결합사와 분리 |

---

## 7. 지적에 대한 최종 답

> "Bianchi universe 에서는 oscillative decay/expansion 이 자주 일어나서 몬테카를로
>  이용 적분법이 적합하지 않을 수도 있다"

**적합하지 않다 — 다만 이유가 두 겹이다.**

1. **진동 때문에** (지적하신 부분): 진동수가 지수적으로 증가하므로 (§2.1 문헌)
   궤적을 풀려면 스텝이 지수적으로 늘고, 잡음 있는 추정량은 스텝을 늘려도
   정확도가 정체한다 (§3.4 측정: 256배 스텝에 16배 개선).

2. **진동과 무관하게도** (더 근본적): 우리 표적인 π_ab 급 양은 MC 가 문헌에서
   **가장 못한다고 지목된 양**이고 (§2.3), 강한 감쇠가 분산 감소의 전제를 스스로
   파괴하며 (§3.2 상관계수 0.10), 우주론적 진폭에서는 N ~ 10¹⁶ 이 필요하다 (§3.5).

그리고 **해법은 진동을 잘 다루는 MC 를 만드는 것이 아니라, 오라클을 배경과
무관하게 재정의하는 것**이다 (§4.1).  그러면 32점 구적으로 4 × 10⁻¹⁵ 이 나온다.

---

## 부록 A · 사전측정 프로토타입

| 파일 | 내용 |
|---|---|
| `probe_mc.py` | MC 로 p₂ 추정, 상관표본 효과 측정 |
| `probe_det.py` | 결정론 각도구적, 광자수 보존, p₁·p₃ |
| `probe_osc.py` | 진동 배경에서의 오차 누적 비교 |
| `probe_amp.py` | 이방성 진폭에 따른 요구 표본수 |
| `probe_phase.py` | 위상함수의 쌍극복사 재유도 (순환 차단) |

모두 리포지토리 없이 독립 실행된다.  구현 시 `audit/` 로 옮기고 시험으로 고정한다.

---

## 부록 B · 참고문헌

- van den Hoogen 외, *The futures of Bianchi type VII₀ cosmologies with vorticity* —
  진동수의 지수적 증가, 진동 분리 변수변환, `ode23t`
- Wainwright, Hancock & Uggla, *Asymptotic self-similarity breaking at late times in
  cosmology* (gr-qc/9812010) — VII₀ 의 자기유사성 깨짐, Weyl 곡률 지배
- Pontzen & Challinor (2007), *Bianchi model CMB polarization…*, MNRAS 380, 1387 —
  Bianchi VII_h + Thomson, 다극 계층, l_max=60/120, `τδη ≪ 1`
- Richers 외 (2017), *A Detailed Comparison of Multi-Dimensional Boltzmann Neutrino
  Transport Methods…* (arXiv:1706.06187) — DO vs MC vs 모멘트, MC 의 차감 상쇄 약점
- Krieger & Wolf (2021), *The scattering order problem in Monte Carlo radiative
  transfer*, A&A — τ=80 에서 1119회 산란, τ=75 에 직렬 52일
- Nagakura 외 (2025), *A Dual-Resolution Prescription in the S_N Method…*
  (arXiv:2510.25274) — 충돌항은 이류항보다 낮은 각해상도로 충분
