# Lewis–Challinor 다극 계층 이식 계획 (H 티어) — 비섭동 배경 Boltzmann + imperfect fluid

**출처**: A. Lewis & A. Challinor, *Evolution of cosmological dark matter perturbations*,
[astro-ph/0203507v2](https://arxiv.org/abs/astro-ph/0203507) (Phys. Rev. D 66, 023531).
**목표**: 식 (12) — **정확(비선형화) 운동량적분 다극 계층** — 을 균질 배경에 이식해
(i) imperfect fluid 를 *가정* 대신 *유도된 절단*으로 얻고, (ii) 제대로 된 Boltzmann
계층(충돌항 포함)을 배경 수준에서 갖춘다.

---

## 0. 논문 원문 확보 — 식 (12) 전사 완료

사용자가 TeX 소스(`neutrinos.tex`)를 제공해 **식 (12) 를 원문 그대로 확보**했다
(label `JAl`). 부호규약은 소스 247행에서 **(+−−−)** 확인.

**식 (12)** — 정확(비선형화) 운동량적분 다극 계층, `n ≡ l + 2i`:

$$
\begin{aligned}
{}_\perp\!\dot J^{(i)}_{A_l}
&+ H\Big[(3+n)J^{(i)}_{A_l} + (1-n)J^{(i+1)}_{A_l}\Big]
\;\underbrace{+\;\mathrm{D}^a J^{(i)}_{aA_l} - \tfrac{l}{2l+1}\mathrm{D}_{\langle a_l}J^{(i+1)}_{A_{l-1}\rangle}}_{\text{(S) 공간미분}}
\;\underbrace{-\;l\,J^{(i)}_{a\langle A_{l-1}}\omega_{a_l\rangle}{}^{a}}_{\text{(V) vorticity}}\\[2pt]
&\underbrace{+\;\tfrac{l}{2l+1}\Big[(l{+}n{+}1)J^{(i)}_{\langle A_{l-1}} + (2{-}n)J^{(i+1)}_{\langle A_{l-1}}\Big]A_{a_l\rangle}
+\Big[(l{-}n)J^{(i-1)}_{aA_l} + (n{-}2)J^{(i)}_{aA_l}\Big]A^{a}}_{\text{(A) 가속도}}\\[2pt]
&+\tfrac{l}{2l+3}\Big[(2n{+}3)J^{(i)}_{a\langle A_{l-1}} + (2{-}2n)J^{(i+1)}_{a\langle A_{l-1}}\Big]\sigma_{a_l\rangle}{}^{a}
+\Big[(l{-}n)J^{(i-1)}_{abA_l} + (n{-}1)J^{(i)}_{abA_l}\Big]\sigma^{ab}\\[2pt]
&+\tfrac{l(l-1)}{4l^2-1}\Big[(n{-}1)J^{(i+2)}_{\langle A_{l-2}} - (l{+}n{+}1)J^{(i+1)}_{\langle A_{l-2}}\Big]\sigma_{a_{l-1}a_l\rangle}
\;=\; 0
\end{aligned}
$$

우변은 무충돌(Vlasov)이라 0 이고, **충돌물질이면 여기에 충돌항이 온다** (논문 명시) → H3.

부수 확인:
- 식 (2) `p^a = E u^a + λ e^a`
- 식 (10) `J^{(i)}_{A_l} ≡ [4π(−2)^l (l!)²/(2l+1)!] ∫₀^∞ dλ λ² E F_{A_l}(λ/E)^n`
- 식 (11) `ρ=J^(0)`, `q_a=J^(0)_a`, `π_ab=J^(0)_ab`, `p=⅓J^(1)`
- **`A_a = u̇_a ≡ u^b∇_b u_a` (소스 확인)** — 즉 **가속도**이며, 우리 코드의 Bianchi
  구조상수 `a_a` 와 **무관**하다. §3 의 경고가 원문으로 확정되었다.
- 식 (13) 은 FRW 주변 **선형화** — 우리는 쓰지 않는다.

---

## 0b. 균질 축약과 **−8/15 자체검증** (H0 의 핵심 성과)

### 어느 항이 죽는가

| 항 | 균질 배경에서 |
|---|---|
| (S) 공간미분 `D^a J_{aA_l}`, `D_{⟨a_l}J_{A_{l-1}⟩}` | **Bianchi I(N=A_struct=0)**: 정확히 0.  **일반유형**: 0 이 아니라 3-접속(N, A_struct)의 **대수적** 결합으로 남는다 (미분은 사라지고 접속만) — 우리 `P_double = A\|x\|²−(A·x)x+x×(Nx)` 와 같은 구조 |
| (V) vorticity `ω_ab` | **법선 합동**(u=e₀, 측지·비회전): 0.  tilted 유체: `congruence.py` 의 on-shell ω (H5) |
| (A) 가속도 `A_a=u̇_a` | 법선 합동: 0.  tilted: on-shell u̇_a (H5) |
| H 항, σ 항 3종 | **모두 살아남는다** ← 계층의 본체 |

★ 사용자 지적("공간미분이 균질해서 사라진다")은 **Bianchi I 에서 정확**하고, 일반유형에서는
"미분→대수" 로 약화된다. 이 구분을 H0 산출물에 명시한다.

### 법선 합동·균질 축약 (H1 의 구현 대상)

$$
\dot J^{(i)}_{A_l} + H\Big[(3{+}n)J^{(i)}_{A_l} + (1{-}n)J^{(i+1)}_{A_l}\Big]
+ \tfrac{l}{2l+3}\Big[(2n{+}3)J^{(i)}_{a\langle A_{l-1}} + (2{-}2n)J^{(i+1)}_{a\langle A_{l-1}}\Big]\sigma_{a_l\rangle}{}^{a}
$$
$$
+\Big[(l{-}n)J^{(i-1)}_{abA_l} + (n{-}1)J^{(i)}_{abA_l}\Big]\sigma^{ab}
+\tfrac{l(l-1)}{4l^2-1}\Big[(n{-}1)J^{(i+2)}_{\langle A_{l-2}} - (l{+}n{+}1)J^{(i+1)}_{\langle A_{l-2}}\Big]\sigma_{a_{l-1}a_l\rangle} = 0
$$

### 자체검증 1 — `l=0, i=0` (n=0) 이 EMT 보존을 준다

`J^(0)=ρ`, `J^(1)=3p`, `J^(0)_ab=π_ab`. l=0 이라 l-비례 항은 소멸:
$$
\dot\rho + H\big[3\rho + 3p\big] + \big[(0{-}0)J^{(-1)}_{ab} + (0{-}1)J^{(0)}_{ab}\big]\sigma^{ab}
=\dot\rho + 3H(\rho{+}p) - \pi_{ab}\sigma^{ab} = 0
$$
우리 검증식(잔차 3.9e−10)은 `ρ̇ + 3H(ρ+p) + σ_ab π^ab = 0` → **σ↔π 축약 부호가 반대**.
이것이 (+−−−)↔(−+++) 변환이 물리는 유일한 자리다 (§2).

### 자체검증 2 — `l=2, i=0` (n=2) 이 **−8/15 를 재생산**

마지막 항의 계수 `l(l−1)/(4l²−1) = 2·1/15 = 2/15`, 괄호는
`(n−1)J^(2) − (l+n+1)J^(1) = J^(2) − 5J^(1)`.
**무질량**(λ=E ⇒ `(λ/E)^n=1` ⇒ **J^(i) 가 i 무관** ⇒ `J^(1)=J^(2)=ρ`) 이면
$$
\tfrac{2}{15}\big[\rho - 5\rho\big]\sigma_{ab} = -\tfrac{8}{15}\rho\,\sigma_{ab}
$$
→ **Misner 중성미자 점성의 −8/15 가 계층에서 그대로 나온다.**
우리 `freestream.sudden_response_coefficient()` 가 독립적으로 검증한 그 계수다.

**수치 확인 (이 세션에서 실행)**: 위 무질량 전제와 결과를 우리 정확 구적해로 대조했다.
```
무질량   3p/ρ = 1.000000000000     ⇒ J^(1) = J^(0) (i-무관) ✓
유질량   3p/ρ = 0.305530  (m=5)    ⇒ i-의존 ✓ (전제가 무질량 한정임을 확인)
급작응답 계수 (freestream 독립 측정) = -0.532569   vs  -8/15 = -0.533333   (0.14%, 구적·차분 수준)
```

**두 자체검증의 정합성**: 검증 1 과 검증 2 는 **같은 하나의 부호 반전**을 가리킨다
(둘 다 σ↔π 결합 섹터에서 우리 규약과 반대). 즉 변환 규칙은 **단일 규칙으로 결정**되며
서로 모순하지 않는다. 다만 그 규칙의 **기원**(π_ab 정의 부호인지, h_ab 공간성분 −δ_ab
때문인지)은 추상지표 계산으로 단정하지 않고 H0 에서 성분식으로 확정한다 — §2 의 방침대로,
판정자는 오라클이다.

> **이 두 자체검증이 이 계획의 신뢰 근거다.** 식 (12) 를 옮겨 적고 균질 축약했더니,
> 우리가 3년 전(v1.x) 독립적으로 검증해 둔 두 결과(EMT 보존식, −8/15)가 **계수까지**
> 재생산되었다. 전사·축약이 옳다는 강한 증거이며, 동시에 부호 변환의 두 앵커를 제공한다.

---

## 1. 왜 이 작업이 "연구규모가 아닌"가 — 기존 자산

| 자산 | 무엇 | 이 작업에서의 역할 |
|---|---|---|
| `matter/freestream.py` (PR-38) | 균질 Vlasov **정확 구적**해. Bianchi I 대각, p_i=const 특성곡선 | **수치 오라클** — 계층의 모든 계수를 여기에 맞춰 확정·검증 |
| 검증 잔차 | EMT 보존 `ρ̇+3H(ρ+p)+σ_abπ^ab=0` → 3.9e−10;  등방극한 π=0 → 2.6e−13;  **급작응답 −8/15** | 계층이 통과해야 할 게이트 |
| `matter/viscous.py` (PR-39) | Eckart / Israel-Stewart (현재는 *가정*) | 계층의 l=2 절단으로 **유도**됨을 보이는 대상 |
| `matter/species.py` (PR-30) | Species 프로토콜 (rho/p/pi/q/rhs/Q, ΣQ=0) | 계층을 종족으로 꽂는 인터페이스 — 배경 솔버 수정 불필요 |
| `_rustcore/src/thermo/fd.rs` (B1) | Gauss-Legendre FD 적분 µs 급 | 계층 초기조건·정규화 상수의 고속 평가 |
| `charts/general.py` | 비대각 σ, N, A, R 지원 | 계층이 요구하는 일반유형 배경 |

**결정적 단순화 — 균질성**: 식 (12) 의 공간미분항이 Bianchi I 에서는 **정확히 사라지고**,
일반유형에서는 **미분이 사라지고 3-접속(N, A_struct)의 대수항만 남는다** (§0b 표).
어느 경우든 **k-모드가 없다** → 계층은 (l, i) 두 지표만 갖는 **유한 절단 가능한 ODE 계**가
된다. 섭동 코드의 k×l 격자가 아니라는 점이 이 작업을 연구규모에서 끌어내린다.

---

## 2. 함정 1 — 부호규약 (+−−−) → (−+++) 변환 프로토콜

우리 코드는 `_ETA = diag(−1,1,1,1)` 즉 **(−+++)**. 논문은 **(+−−−)**. 기계적 치환은 위험하다.

| 양 | 논문 (+−−−) | 우리 (−+++) |
|---|---|---|
| 계량 | `g_ab` | `g_μν = −g_ab^(paper)` |
| 4-속도 규격 | `u^a u_a = +1` | `u^μ u_μ = −1` |
| 에너지 추출 | `E = p^a u_a` | `E = −p^μ u_μ` |
| 방향벡터 | `e^a e_a = −1` | `e^μ e_μ = +1` |
| 사영자 | `h_ab = g_ab − u_a u_b` | `h_μν = g_μν + u_μ u_ν` |
| 질량껍질 | `p^a p_a = m²` | `p^μ p_μ = −m²` |

**규칙**: 계량 인자를 **홀수 개** 포함한 축약은 부호가 뒤집히고, **짝수 개**는 불변.
그러나 이 계산을 항마다 손으로 하면 반드시 실수가 난다.

**채택 전략 — 추상지표를 거치지 않는다.**
1. 식 (12) 를 **직교틀 성분식**으로 먼저 옮긴다. 우리 코드의 σ_ab, π_ab, N_ab, A_a 는
   모두 3×3/3-벡터 **numpy 성분 배열**이고, 공간 지표는 두 규약에서 동일한 유클리드
   3-계량 δ_ab 로 올린다/내린다 → **공간 지표만 가진 양(σ_ab, π_ab, J_{A_l})은 규약 무관**.
2. 규약이 실제로 물리는 곳은 **E·λ 의 정의와 (0-지표를 포함한) 축약** 뿐이다.
   여기만 위 표로 명시 변환하고, 나머지는 손대지 않는다.
3. **그리고 각 항의 최종 부호는 오라클로 확정한다** (§4). 규약 변환은 후보를 좁히는
   도구이고, 판정자는 `freestream.py` 다.

> 이 저장소의 v1.1 교훈: 회전 부호는 shear/N 섹터만 보면 **검출 불가**였고 tilt·자기장에서만
> 드러났다. 같은 이유로 부호는 "유도했으니 맞다"가 아니라 "오라클이 통과했으니 맞다"로
> 확정한다.

---

## 3. 함정 2 — Θ vs H, 그리고 우리 정규화

| 논문 | 우리 코드 |
|---|---|
| 팽창 스칼라 `Θ` | `H = Θ/3` (Hubble) — 계수에 3 이 들어온다 |
| shear `σ_ab` (차원량) | `Σ_ab = σ_ab/H` (Hubble 정규화) 와 차원량 `σ_ab` 병용 |
| 시간 | 우주시간 `t` | 배경 솔버는 `τ = ln ℓ` (dτ = H dt), 광선·운동론은 `t` |
| vorticity `ω_ab` | 법선 합동은 ω=0; **유체 합동**의 ω 는 `physical/congruence.py` (D22 on-shell) |
| 가속도 `A_a` | class B 의 `A_a` (구조상수) 와 유체 `u̇_a` 를 **혼동 금지** |

★ `A_a` 중의성이 실질 위험이다. 논문의 `A_a` 는 기준 4-속도 `u` 의 **가속도**다.
우리 코드의 `A` 는 Bianchi **구조상수** a_a 다 (Jacobi n^{ab}a_b=0 의 그 A). 계층을
법선 합동(u = e_0, 측지·비회전)에 태우면 **논문의 A_a = 0, ω_ab = 0** 이 되어 크게 단순화된다.
기울어진 유체 합동에 태우려면 `congruence.py` 의 on-shell u̇_a, ω_ab 를 공급한다.
→ **H1 에서 법선 합동 버전을 먼저** 구현하고, tilted 는 H5 로 분리한다.

---

## 4. 부호 확정·검증 전략 (이 계획의 핵심)

원문 확보로 **계수는 이미 정확히 안다** (§0). 남은 미지수는 **(+−−−)→(−+++) 부호 변환**
하나이고, 이는 추측하지 않고 오라클로 확정한다.

**(a) 부호 자유도의 범위.** §0b 의 두 자체검증이 자유도를 **단일 전역 부호**로 좁혔다
(σ↔π 축약 섹터). 후보는 유한하며(각 σ-결합 항군의 부호), l=0·l=2 두 앵커가 이를 결정한다.

**(b) 오라클 확정.** `freestream.py` 가 같은 물리의 **정확해**를 준다:
- 임의 대각 배경 (H, σ_diag) 과 임의 f₀ 에서 정확 구적으로 `ρ, p, π_ab` 및 시간도함수를 얻는다
- 계층 예측과 성분별로 맞춰 부호를 확정 (회전 부호·curl 상대부호·³S_ab 계수를 확정했던
  것과 동일한 방법)
- 잔차 목표 **≤1e−10** (기존 EMT 잔차 3.9e−10 과 같은 급)

**(c) 독립 게이트.** 확정된 계층이 통과해야 하는, 계수동정에 **쓰지 않은** 검증:

| 게이트 | 기준 |
|---|---|
| 등방 극한 (σ=0) | 모든 l≥1 다극 → 0, `π_ab`=0 |
| 무질량 극한 | `w = p/ρ = 1/3`, `ρ ∝ ℓ⁻⁴` |
| 비상대론 극한 | `w → 0`, `ρ ∝ ℓ⁻³` |
| **EMT 보존** | `ρ̇ + 3H(ρ+p) + σ_ab π^ab = 0` ≤1e−10 |
| **Misner 급작응답** | `π_ab = −(8/15) ρ δ(ln a)_⟨ab⟩` — 계수 **−8/15 를 계층이 재생산** |
| 절단 수렴 | l_max 2→4→6→8 에서 `π_ab` 가 수렴 (자유흐름은 무한계층) |
| **정확 구적 대조** | 동일 초기조건에서 `freestream.moments()` 와 궤적 일치 |
| Eckart/IS 환원 | l=2 절단 + 충돌 완화시간 → `viscous.py` 형태 복원 (계수까지) |

★ **−8/15 게이트가 가장 강력하다.** 이 계수는 Misner 중성미자 점성의 기원이고
계층의 σ↔l=2 결합과 l=2↔l=3 누출을 동시에 시험한다. 계수동정에 쓰지 말고 남겨둔다.

---

## 5. 작업 DAG

```
H0 식(12) 전사·구조확정 ──> H1 법선합동 Vlasov 계층 (충돌 없음)
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
        H2 절단·닫힘 규칙   H3 충돌항(Thomson)   H4 imperfect fluid 유도
                    │               │               │
                    └───────────────┴───────────────┘
                                    ▼
                          H5 tilted 합동 (ω, u̇ 포함)
                                    ▼
                          H6 Rust 이식 (계수 확정 후에만)
```

| 단계 | 산출 | 인수 게이트 | 규모 |
|---|---|---|---|
| **H0** | ✅ **완료** — 식(12) 전사 + 균질 축약 + −8/15 자체검증 (§0, §0b).  남은 것: 부호 기원 성분식 확정 | 두 앵커 정합 확인됨 | 소 |
| **H1** | ✅ **H1-a·H1-b 완료** — `matter/hierarchy.py` (J 상태·초기조건·법선합동 RHS), `audit/h_hierarchy.py` | −8/15 상대오차 0.00e+00;  RHS = 정확구적 시간미분 (81 조합, ≤1.2e−8);  부호 유일성 마진 1.5e6× | **중** |
| **H2** | ✅ **완료** — l 단순절단 + **i 기하외삽** 닫힘, RK4 적분, l_max·i_max 수렴 | l_max=4 에서 π 8.2e−9 수렴;  0-절단 대비 7자리 우월;  정확구적 궤적 일치 | 소 |
| **H3** | ✅ **완료** — `matter/collision.py`, 두 경로 분리 (수치 위상함수 / 해석 다극) | λ₂=1/10 (감쇠 **9/10**) 두 경로 독립 재생산 8.8e−15;  자유흐름 잔차 **정확히 0**;  긴밀결합 π/ρ ∝ 1/(n_eσ_T) | 중 |
| **H4** | ✅ **완료** — `matter/viscous_derived.py`, 두 경로 분리 측정 | τ_π=1/(4H), η=ρ/(15H) **유도**;  두 경로 교차검증 감쇠 1e−9 / 소스 1.5e−3;  Eckart≡IS 매칭 일치 | 소 |
| **H3R/H4R** | ✅ **완료** — Rust 이식 `_rustcore/src/kinetic/{collision,viscous}.rs`, 두 경로 **양쪽 언어에서 각각** 유지 | 차등테스트 42 신규 (λ_l 1e−13, 계층 1e−10, 자유흐름 **비트-정확 0**);  충돌 RK4 ~20×, λ_l 14×, 긴밀결합 스캔 8× | 소 |
| **H5** | tilted 합동 (congruence.py 의 on-shell ω, u̇ 주입) | v→0 에서 H1 로 환원, D22 성질 유지 | 중 |
| **H6** | ✅ **완료 (조기 이식)** — `kinetic/{pstf,quad,hierarchy}.rs` + codegen PSTF | 차등 17건, ρ 정규화 잔차 ≤1e−12;  구적 7.9×, 계층 적분 15.3× | 소 |

**H1 이 임계경로다.** H0 은 문서, H2·H4·H6 은 H1 이 서면 짧다.

---

## 6. 설계 결정 (미리 못 박아 둘 것)

1. **상태 표현**: `J[(l, i)]` 를 dict 가 아니라 **평탄 배열 + 인덱스 맵**으로.
   (l, i) 를 (l ≤ l_max, i ≤ i_max) 사각격자로 두고, PSTF 텐서 `J_{A_l}` 은 독립
   성분 (2l+1) 개만 저장 → `conventions.tracefree_from_5` 와 같은 규율을 l 일반으로 확장.
   ★ 함정 3(trace-free 를 행렬연산으로 만들지 말 것)이 여기서 재발할 수 있다.
2. **정규화 상수는 코드에 한 번만**: 식(10) 의 `4π(−2)^l (l!)²/(2l+1)!` 를 `_J_NORM(l)`
   단일 함수로. 이 인자의 `(−2)^l` 부호가 l 홀수에서 부호 반전을 만들므로,
   `freestream.py` 의 π_ab 정의와의 상대부호를 **오라클로 고정**한다.
3. **Species 프로토콜 준수**: 계층은 `rho/p/pi/q/rhs/Q` 를 내놓는 종족으로 감싼다.
   배경 솔버(charts/*)는 손대지 않는다 — B 티어에서 확립한 규율.
4. **freestream.py 는 폐기하지 않는다.** 정확 구적해는 영구 오라클로 남긴다
   (`force_python` 처럼 `force_quadrature` 경로 유지).
5. **Rust 이식은 H6 에서만.** B 티어의 교훈: 계수가 확정되기 전에 이식하면 틀린 물리를
   빠르게 계산한다. 계층은 (l_max, i_max) 격자 ODE 라 Python 으로도 충분히 빠를 수 있으므로,
   H6 는 **필요성이 측정된 뒤** 착수 (B1 처럼 "ODE RHS 안에서 매 스텝" 비용을 먼저 재고).

---

## 7. 위험과 완화

| 위험 | 완화 |
|---|---|
| 식(12) 계수 오전사 | 오라클 수치동정 (§4b) + 계수동정에 안 쓴 독립 게이트 (§4c) |
| 부호규약 혼용 | 추상지표 우회 → 직교틀 성분 + 오라클 판정 (§2) |
| `A_a` 중의성 (구조상수 vs 가속도) | H1 은 법선합동(둘 다 무관)부터; 명칭 분리 (`A_struct` vs `u_dot`) |
| PSTF trace-free 붕괴 | 독립성분 저장 + 런타임 가드 (함정 3 규율 확장) |
| 절단 인공물 | l_max 수렴 스캔을 시험으로 박제, 절단 오차 보고 |
| 무한계층 vs 유한 ODE | 자유흐름 극한을 정확 구적과 직접 대조 (§4c) |

---

## 8. 이 작업이 주는 것 (왜 할 가치가 있나)

1. **imperfect fluid 가 가정에서 유도로.** 현재 `viscous.py` 의 Eckart/IS 는 *postulate* 다.
   계층 절단으로 유도되면 η, ζ, τ_π 가 분포함수·충돌항에서 나온다 — 자유 파라미터 소멸.
2. **비선형·비섭동.** 논문 식(13)(선형화)을 **쓰지 않는다.** 배경의 σ_ab 는 크기 제한이
   없으므로 Kasner 근방·큰 이방성에서도 유효 — 섭동 코드가 못 가는 영역이 우리 주력이다.
3. **일반유형 확장.** 현재 정확 구적해는 **대각 Bianchi I 전용**이다. 계층은 비대각 σ,
   회전, tilt 를 자연히 담아 C1(일반유형 조석)과 결이 맞는다.
4. **충돌항 확보.** Vlasov(무충돌)에서 진짜 Boltzmann 으로 — Thomson 산란이 들어오면
   재결합기 광자-바리온 결합을 배경 수준에서 다룰 수 있다 (B2b 와 인접).

---

## 9. 착수 순서 (즉시 가능한 첫 조각)

**H0 는 완료됐다** (§0, §0b) — 논문 TeX 소스로 식(12) 전사, 균질 축약, 그리고 −8/15
자체검증까지. 따라서 바로 H1 로 들어간다.

1. **H1-a**: `_J_NORM(l) = 4π(−2)^l(l!)²/(2l+1)!`, PSTF 독립성분 저장·가드,
   `freestream.moments()` → 초기 `J^(i)_{A_l}` 사영기.
2. **H1-b**: §0b 의 축약식을 (l ≤ l_max, i ≤ i_max) 격자 RHS 로 구현
   (`matter/boltzmann_hierarchy.py`), σ-결합 부호는 §4b 오라클로 확정
   (`audit/h_hierarchy.py`).
3. **H1-c**: §4c 게이트 시험 (`tests/test_h1_hierarchy.py`) — 특히 **−8/15** 와
   **정확 구적 궤적 대조**.
4. 그 다음 H2(절단 수렴) → H4(viscous 유도) 는 짧다.

---

## 부록 · 기존 DAG 와의 관계

이 H 티어는 **B2b(Lyα 복사전달)보다 먼저** 놓는다 — 연구규모가 아니고, 기존 정확해
오라클이 있어 검증이 확실하며, C1(일반유형)·D(견고성)과 상승작용이 있다.

```
[완료] A1 A2 A3 · C1 · B1 B3 · B2a
[신규] H0 → H1 → {H2, H3, H4} → H5 → H6      ← 여기
[대기] B2b (Lyα 복사전달, 연구규모) · C2 · D · E
```
