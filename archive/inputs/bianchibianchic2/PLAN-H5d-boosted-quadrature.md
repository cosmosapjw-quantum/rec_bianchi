# PLAN-H5-d · boosted 정확구적 오라클 (l ≥ 2)

작성 2026-07-30.  H5-a/b/c 완료 후.  **왜 필요한가**: H5-b 의 보존법칙 오라클은
(l,i) = (0,0), (1,0) 만 통제한다.  l ≥ 2 의 (Ω), (D), (E), (div) 계수는 아직 독립
검증이 없다.  H1 이 σ 항을 정확 구적으로 확정했듯, tilted 항도 정확 구적으로 확정한다.

---

## 1. 왜 tilted 에도 정확 오라클이 있는가

Bianchi I 무충돌 Vlasov 의 정확해는 **법선 프레임**에서

```
f(t, p) = f₀(q_i),        q_i = 불변기저 공변운동량 = const
법선관측자 물리운동량:  P^i = q n̂_i / a_i(t),   λ = |P|,   E = √(m² + λ²)
```

tilted 관측자는 **같은 분포함수 f** 를 보되 4-운동량을 다르게 분해한다.  boost 는
순수 대수이므로 tilted 모멘트도 **정확 구적**으로 나온다.  이것이 H5-d 의 전부다.

### 1.1 측도 변환 (핵심)

`d³P / E` 는 로런츠 불변이므로  `d³P′ = (E′/E) d³P`.  기존 모멘트 정의

```
J^(i)_{A_l} = ∫ d³P  E (λ/E)^n  e_{⟨A_l⟩} f,        n = l + 2i
```

의 tilted 판을 **법선 프레임 격자 위에서** 계산할 수 있다:

```
J′^(i)_{A_l} = ∫ d³P′ E′ (λ′/E′)^n e′_{⟨A_l⟩} f
             = ∫ (d³q / V) · (E′/E) · E′ (λ′/E′)^n · e′_{⟨A_l⟩} · f₀(q)
```
`V = a₁a₂a₃` (기존 `quad.rs` / `freestream.py` 와 같은 규약: `d³P = d³q/V`).
⇒ **기존 격자를 그대로 쓰고 피적분함수만 바꾸면 된다.**

### 1.2 boost 대수 (코드에서 반드시 수치 검증할 것)

법선 정규직교기저 성분 `(E, P^A)`, tilt 속도 `v_A`, `γ = (1−v²)^{−1/2}`,
`k ≡ γ²/(γ+1)` (= (γ−1)/v², v→0 특이점 제거형).  순수 boost 사틀
`E_A = γv_A n + [δ_AB + k v_A v_B] ê_B` 에 대해

```
E′  = γ (E − v·P)
P′^A = P^A + v_A [ k (v·P) − γ E ]
λ′ = |P′|,   e′^A = P′^A / λ′
```

★ **자기검증 필수**: `λ′² = E′² − m²` 가 격자 전점에서 성립하는지 확인한다.
  이 한 줄이 boost 대수 오타를 전부 잡는다 (부호 하나만 틀려도 깨진다).
  위 `P′^A` 는 `p·E_A` 를 전개해 얻은 것이지만 **유도를 믿지 말고 측정한다.**

---

## 2. 게이트 — 계층을 경유하지 않는 것부터

### 2.1 ★ T^{μν} 프레임 무관성 (가장 결정적)

T^{μν} 는 텐서다.  법선 프레임 구적이 주는 `(ρ, q_a = 0, p, π_ab)` 로 T^{μν} 를 세우고
tilted 사틀로 사영한 값이, **boosted 구적이 직접 준** `(ρ′, q′_A, π′_AB)` 와 같아야 한다.

```
ρ′   = T^{μν} u_μ u_ν
q′_A = − T^{μν} u_μ E_{Aν}
π′_AB = T^{μν} E_{Aμ} E_{Bν} − ⅓ δ_AB (T^{μν} h_{μν})
```
계층 방정식을 전혀 쓰지 않으므로 **boost 대수 + 구적을 한 번에** 검증한다.
여기서 어긋나면 이후 어떤 계층 검증도 의미가 없다 — 그래서 이 게이트가 1순위다.

### 2.2 v → 0 환원
`v = 0` 에서 `J′ = J` 가 **비트-정확**해야 한다 (E′=E, P′=P 이므로 피적분함수가 동일).

### 2.3 ★ 홀수 l 이 인공 왜곡 없이 켜진다
H1 에서는 등방 f₀ 가 `f(p) = f(−p)` 라 홀수 l 이 항등적으로 0 이었고 (|J_a| ~ 3e−15),
(A) 항군을 시험하려고 `f_dipole(ε, axis)` 을 **인공 도입**해야 했다.

tilted 에서는 **boost 자체가 물리적 쌍극자원**이다: 등방 f₀ + 등방 a_vec 이어도
`q′_A = J′^(0)_A ≠ 0` (에너지 유속).  ⇒ H5-d 는 홀수 l 과 (A),(Ω),(D),(E) 를
**분포 왜곡 없이** 시험한다 — H1 보다 물리적으로 정직한 게이트.

측정 예정: `|q′|/ρ′` 대 `v` 의 관계 (작은 v 에서 `≈ (4/3)v` for 무질량 — 확인 대상).

---

## 3. ⊥J̇ 의 함정 — 사틀이 시간의존이다

`J′_{A_l}(t)` 는 **t-의존 사틀** `E_A(t)` 성분이다.  따라서

```
⊥J̇_{A_l}  ≠  d J′_{A_l} / dt        (일반적으로)
```
성분의 시간미분에는 사틀 회전/boost 항이 섞인다.  H1 에서 프레임 회전 규약
(`Γ^a_{b0} = −Ω`) 때문에 실제로 부호 사고가 났던 지점과 같은 함정이다.

**대응 (H5-c 에서 이미 검증된 방식 그대로)**: 성분을 직접 미분하지 말고
**텐서로 계산**한다.

```
J′^{μ₁…μ_l}(t) = Σ J′_{A_l} E_{A₁}^{μ₁} … E_{A_l}^{μ_l}      (좌표성분, 공간균질)
⊥J̇^{μ…} = h…h · u^λ ∇_λ J′^{μ…}
⊥J̇_{A_l} = E_{A₁μ₁}…E_{A_lμ_l} ⊥J̇^{μ…}
```
좌표성분이 공간균질이라 `∂_i = 0` 이고 `u^λ∇_λ = γ∂_t + Γ` 로 닫힌다.
`∂_t(좌표성분)` 에는 `dJ′_A/dt` 와 `J′_A ∂_t(E…E)` 가 함께 들어가는데,
후자가 바로 사틀 회전항이며 이 방식은 그것을 **자동으로** 처리한다.

`tilted.py` 의 `_matter_terms` 가 rank 1·2 에 대해 이미 이 패턴으로 동작한다
(∇T 오라클과 1e−11 일치).  일반 rank 로 올리는 것이 H5-d 의 구현 작업이다.

---

## 4. 구현 DAG

```
H5-d1  `matter/tilted_moments.py`
         · boost 대수 + λ′²=E′²−m² 자기검증
         · J′^(i)_{A_l}(a, v, m, l, i) 정확 구적 (기존 격자 재사용)
         · PSTF 사영은 `hierarchy.pstf` 그대로
       게이트: T^{μν} 프레임 무관성 / v→0 비트-정확 / q′≠0 (인공 쌍극자 불필요)
         │
H5-d2  일반 rank ⊥J̇ 와 D 항  (tilted.py `_matter_terms` 의 rank-l 일반화)
         │
H5-d3  l = 2 잔차 시험 → (Ω),(D),(E),(div) 계수를 l≥2 에서 **독립 확인**
         · 부호 전수 스캔 + 유일성 마진 (H1 은 1.5e6×, H5-b 는 1.4e14×)
         │
H5-d4  격자 적합성: boost 후 E′ 가 방향의존 ⇒ γ 스캔 수렴 측정
         · **유효 |v| 범위를 조용히 쓰지 않고 측정해 문서화** (H5-c 의 cond 스캔과 같은 취지)
         · 차분 조건수 dt 스캔 (H3R 에서 겪은 함정 선제 대응)
```

임계경로는 **H5-d1** 이다.  §2.1 게이트가 서면 나머지는 기계적이다.

---

## 5. 예상 위험

| 위험 | 대응 |
|---|---|
| boost 후 반경격자가 부적합 (E′ 가 n̂ 의존) | γ 스캔 수렴 시험 — H2 의 l_max·i_max 수렴 패턴 재사용 |
| γ ≫ 1 에서 구적 꼬리 절단 | ρ′ 대 해석 로런츠 변환값 비교로 **유효 v 범위를 측정**해 문서화 |
| 유한차분 조건수 (H3R 재현) | 처음부터 dt 스캔으로 반올림 지배 구간 확인, 허용오차 근거를 시험으로 고정 |
| `close_i` 기하외삽이 tilted 에서 무효화 | H2 근거(무질량 i-무관성)가 boost 후에도 성립하는지 **재측정** — 가정하지 않는다 |
| 일반 rank D 항의 지표 실수 | rank 1·2 는 ∇T 오라클과 대조 가능 → 일반 코드가 rank 1·2 에서 H5-c 값을 재생산하는지 **회귀로 고정** |
