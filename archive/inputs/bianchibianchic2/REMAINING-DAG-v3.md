# 잔여 작업 DAG (v3.0) — 우선순위와 "Rust 가 실제로 무엇을 풀어주는가"

## 0. 질문에 대한 답 — Rust 로 열역학을 더 정밀하게, 더 빠르게?

**측정 먼저.** 추측 대신 현재 Python 열역학의 실제 비용을 쟀다:

| 항목 | 현재 Python 비용 | 병목인가? |
|---|---|---|
| Peebles 3-준위 재결합 (700 pts) | 15 ms | ✗ 아니다 |
| 동결 (해석식) | 0.1 ms | ✗ |
| 중성미자 FD 적분 (a 1개) | 1.0 ms | **경계** |
| 중성미자 FD 적분 (a 200개) | 159 ms | **경계** |
| 동결 Boltzmann λ=10¹⁰, LSODA | **실패 (nan)** | 솔버 선택 문제 |
| 동결 Boltzmann λ=10¹⁰, **Radau** | 267 ms, 성공 | ✓ Python 으로 됨 |
| 동결 Boltzmann λ=10¹⁰, **BDF** | 114 ms, 성공 | ✓ Python 으로 됨 |

**결론 (정직하게):**

1. **정밀도 향상 자체는 대체로 Rust 문제가 아니다.**
   - 동결: 이전 세션이 해석식으로 후퇴한 건 **LSODA 선택 실수**였다. Radau/BDF 를 쓰면
     Python 에서 그냥 풀린다. Rust 불필요.
   - `g_*(T)` 표: 데이터 문제. 아래 A1 에서 확인했듯 **현재 표는 T=1 GeV 에서 106.75
     인데 참값은 76.34 (40% 오차)** — 계산량이 아니라 표가 틀린 것이다.
   - r_s 2%: 헬륨 반영 c_s, 자기일관 z_drag 등 **모델링** 문제. 1-D 구적은 ms 급이다.

2. **Rust 가 진짜로 푸는 것은 "계산량이 막던 것"뿐이다.** 세 가지:
   - **(a) ODE RHS 안에서 매 스텝 비싼 적분**: 중성미자 FD 가 1 ms/호출이다. 완전한
     열역사 ODE 가 매 스텝 이를 부르면 1e4 스텝 → **궤적당 ~8 초**. 우주론 스캔
     1e4 개면 22 시간. Rust 로 µs 급이 되면 비로소 가능해진다. ← **가장 실질적**
   - **(b) 다준위 재결합** (RECFAST 3-준위 → HyRec/CosmoRec 급 300+ 준위): 강성
     대규모 계 + Lyα 복사전달. Python 으로는 무겁고, Rust+diffsol BDF 의 정확한 표적.
   - **(c) 이방 스캔**: shear 값·우주론 파라미터 배치 → Rayon (이미 R3 로 확보).

3. **따라서 순서가 중요하다.** 싸고 즉시 이득인 물리 수정(A 티어)을 Python 에서 먼저
   하고, 계산량이 진짜 장벽인 것(B 티어)만 Rust 로 옮긴다. 반대로 하면 틀린 물리를
   빠르게 계산하게 된다.

---

## 1. DAG

```
[A 티어 · 물리 정확도 — Python 으로 충분, 저비용]
 A1 g_*(T) 격자QCD 표 교체 ──┬──> A2 동결 Boltzmann 복원 (Radau/BDF)
                             │
 A3 r_s 물리 개선 (He c_s, 자기일관 z_drag) ──> θ_* 2% 해소
                             │
[B 티어 · Rust 가 실제로 푸는 것]         │
 B1 열역학 커널 Rust (FD 적분·g_* 보간) ──┴──> B2 다준위 재결합 (강성 대규모)
                             └──────────────> B3 이방 열역사 스캔 (Rayon)
[C 티어 · 커버리지 — 과학적 유용성]
 C1 일반유형 조석/광선 codegen (VII_h!) ──> Planck 비교 확장
 C2 나머지 차트 RHS 이식 (class_b_tilted, exceptional, type_ix_d)
[D 티어 · 수치 견고성]
 D1 이벤트 검출 (Kasner 바운스·재붕괴·유형전이; diffsol 내장)
 D2 구속 사영/감시,  D3 혼합정밀 (특이점 근방)
[E 티어 · 유예]
 E1 R5 GPU (diffsol CUDA 재활용)
```

## 2. 우선순위 근거

| 순위 | 항목 | 근거 |
|---|---|---|
| **1** | A1, A2 | 비용 대비 이득 최대. 현재 **틀린 값**(g_* 40% 오차)을 고치고, 포기했던 물리(동결 Boltzmann)를 되살린다. Rust 불필요. |
| **2** | C1 | **현재 Rust 고속 경로는 대각 Bianchi I 만 커버한다.** Planck 의 Bianchi 탐색은 VII_h — 일반유형 조석이 없으면 12,000× 가속의 과학적 쓸모가 type I 에 갇힌다. |
| **3** | A3 | θ_* 2% 는 문서화된 마지막 물리 부채. |
| **4** | B1 → B2/B3 | Rust 가 진짜 필요한 구간. B1(커널)이 B2·B3 를 모두 연다. |
| **5** | D1–D3 | 견고성. Kasner/재붕괴에서 신뢰도 상승. |
| **6** | E1 | CPU 가 이미 12,000× — 수익 체감. 마지막. |

## 3. 진행 상태

- [x] **A1** g_*(T) 격자QCD 표 교체 — Husdal 2016 (격자QCD 반영) 36점
- [x] **A2** 동결 Boltzmann 복원 — Radau, 해석식 대체
- [x] **C1** 일반유형 조석 codegen — sympy→Rust 자동생성, Riemann 3.6e-15 / T_AB 4.5e-13
- [x] **A3** r_s 절단오차 수정 + 자기일관 z_drag — 100θ_* 2% → **0.10%**
- [x] **B1** Rust 열역학 커널 (FD 적분 GL, g_* 표 codegen 공유) — FD 7e-14, g_* 비트-정확, 266×
- [x] **B3** 자기일관 g_* 동결 + 이방 스캔
- [~] **B2a** 헬륨 재결합 + Peebles H(z) 복사항 버그 수정
- [ ] **B2b** 다준위 재결합 — ★진단 결과 표적이 **α_B 가 아니라 Lyα 복사전달**로 확정
- [ ] D1–D3 수치 견고성
- [ ] E1 GPU
