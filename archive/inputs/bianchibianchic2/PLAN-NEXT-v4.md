# 확장·검증 계획 v4 (2026-07-31)

H5 완결(tilted 계층 + 질량행렬 + 적분 + 문헌 기반 정확도 개선) 시점의 잔여 작업 정리.
**현 상태 요약**: pytest 627 / cargo 49.  tilted 계층이 l≤3 까지 두 독립 오라클로
검증됐고, 무질량 궤적 오차가 ρ 7.8e−8 이다.

---

## 0. 가장 큰 구조적 공백 — 지금까지는 전부 **시험장(test-field)** 이다

`charts/class_a.py` 의 RHS 에는 이방응력 입력이 없다.  `charts/general.py` 에만
훅이 있다:

```python
Pi = args.get("Pi", jnp.zeros((3, 3)))
dS = -(2.0 - q) * S - S3 + Pi + (W @ S - S @ W)     # ← 훅은 있으나 아무도 안 채운다
```

그리고 `tilted_integrate.Background` 는 a(t), v(t) 를 **처방**한다.  즉:

> 운동론 계층은 π_ab, q_a 를 정확히 계산하지만 **기하를 전혀 움직이지 않는다.**
> 결합은 단방향이고, 배경은 손으로 준 것이다.

프로젝트의 원래 목표("background level 에서 구현 가능한 우주론 현상을 cosmological
evolution 에 반영")를 기준으로 보면 **이것이 최대 공백**이다.  아래 K 티어가 그것을 메운다.

---

## 1. K 티어 — 계층 ↔ Einstein 결합 (최우선)

| ID | 내용 | 수락 게이트 |
|---|---|---|
| **K1** | 계층의 π_ab 를 `general.rhs` 의 `Pi` 로, q_a·Ω 를 `args["q"], args["Omega"]` 로 되먹임.  차트 ODE 와 계층을 **한 상태벡터로** 결합 적분 | ① γ-law 극한(π→Eckart, H4 의 η)에서 기존 차트 궤적 재현<br>② Ω+Σ²+K=1 (Gauss) 이 적분 내내 유지<br>③ 계층을 끄면(무물질) 진공 Kasner 재현 |
| **K2** | ✅ **완료** — 에너지틀 q′_a(v)=0 근찾기 + tilted Euler 교차검증 (1.6e−8) | |
| **K2b** | ✅ **no-go 확립** — **Bianchi I 은 tilt 를 담을 수 없다** (Codazzi 가 q=0 강제; 대각 N·Σ 이면 class A 전체가 그렇다).  type V 에서는 A 를 정확히 풀 수 있음을 구성적으로 보임 | |
| **K4** | ✅ **완료 (32~33차)** — 특성곡선 두 경로 확정 (2e−16), 보존량 4종, **역방향 특성곡선** 모멘트 (A→0 에서 type I 구적과 9.5e−17), boosted 판까지 (**T^{μν} 프레임 무관성 1.9e−15**).  ★★ 부수 물리: **type V 자유흐름이 법선틀 열유속을 스스로 만든다** (|q|/ρ 가 A 에 선형, t=0 에서 0) — K2b 가 연 자리가 실제로 채워지고, 그 q 를 담는 A 를 Codazzi 로 정확히 풀었다 (잔차 8.7e−19) | 다음: **K5 결합계** — 푼 A(0.035)와 넣은 A(0.7)의 20배 차이를 없애려면 Σ·a·A 를 함께 진화시켜야 한다 |
| **K5a** | ✅ **완료 (34차)** — type V Einstein 텐서를 **계량에서 직접** (자기검증: 사영 성분에 x 잔존 0개).  Friedmann 곡률항 −3A²/a₁², Codazzi G₀₁ = 3σ₁A/a₁ 를 측정으로 확정하고, **교환자 계산**으로 a_a = (−A/a₁,0,0) 을 얻어 프로젝트 규약과 잔차 0 으로 일치.  ★ K4c 의 '20배 차이' 는 **정규화 문제**였고 물리 단위로는 O(1).  ★★ **운동량 구속을 정확히 만족하는 구성 발견** (σ₁ = −0.02712, 잔차 0.0) | 다음 K5b: Friedmann·진화까지 함께 푸는 결합계 |
| **K3** | **Bianchi 항등식 게이트** — 결합계에서 ∇_μT^{μν}=0 이 *자동으로* 성립하는지 감시 | 잔차가 적분 오차 수준(≤1e−8)에서 머물 것.  ★ 이건 결합이 옳은지 아닌지를 가르는 **독립** 검사다 (H5-b 의 오라클을 동역학으로 재사용) |

**왜 최우선인가**: K1 없이는 지금까지의 정확도(1e−8)가 우주론적으로 아무것도 바꾸지
못한다.  그리고 K3 는 **이미 가진 오라클을 공짜로 재사용**하는 강력한 게이트다.

**위험**: `general.py` 주석이 경고하듯 `Pi` 만 주고 `q` 를 안 고치면 **조용히 틀린다**
((2−γ)V²/G₊ 보정 누락).  → K1 의 첫 시험을 "q 를 안 고치면 Gauss 가 깨진다" 로 잡는다.

---

## 2. L 티어 — 문헌이 지목한 **미검증 예측** (싸고 결론이 분명)

| ID | 내용 | 수락 게이트 |
|---|---|---|
| **L1** | ✅ **완료 (30차)** — 홀짝 예측 **확인** (누출: 홀수 0.0000e+00, 짝수 1.33/1.60/1.82).  ★ 부수: 논문의 변환식이 Bianchi 배경에서 **불완전**함을 발견하고 빠진 l=2 항 두 개의 계수 −2i, (2i−1) 을 측정 (논문 2항만 쓰면 6–8 % 어긋난다).  ★ 그리고 "궤적오차에 홀짝 계단" 기대는 **반증** — v=0 에서 짝·홀이 비트 단위로 같고, tilt 를 켜야 홀수가 1.5배 낫다 | |
| **L2** | ✅ **완료 (31차)** — 문헌 공식의 **적용 범위를 측정으로 채웠다**: √(3w) 는 l≥2 상대론 영역에서 4e−5, **5w 는 l=1 전용** (l≥2 에서는 낮은 w 에서 상대오차 1.02 로 발산).  ★ r 이 **l 의존** (r₃/r₁ = 2.54 at w=0.005) 이라 단일 r 처방 자체가 근사.  ★★ **부정 결과**: 닫힘으로 쓰면 데이터 구동을 못 이기고, "frame-invariant" 단서도 tilt 오염도 감소로 **나타나지 않는다** (frozen 이 가장 둔감) | |
| **L3** | ✅ **완료** — 측정 결과 유질량도 **i-닫힘 지배** (l 은 포화).  더 중요한 발견: 닫힘의 한계는 **J 값이 아니라 버려진 J̇** 이었다 (격자 밖 J 를 정확 구적으로 채워도 오히려 나빠진다).  같은 비로 J̇ 도 닫으니 무질량 6800배, 유질량 2–22배.  ★ 무질량 1차원 환원은 이 J̇ 닫힘의 **r=1 특수경우**였음이 드러남 (값이 비트 단위로 일치) | |

**왜 여기가 싼가**: 전부 기존 오라클(정확 구적)로 바로 잰다.  새 이론이 필요 없다.

---

## 3. V 티어 — 정직하게 남아 있는 **검증 부채**

| ID | 부채 | 제안 |
|---|---|---|
| **V1** | **l ≥ 4 tilted 부호 미검증**.  잔차 시험은 l+2 이웃을 요구해 L_MAX=5 로는 l≤3 만 가능 | codegen 을 L_MAX=6 으로 (rank-6 PSTF, 현재 100 ms/회 → Rust 로) → l=4 잔차·부호 스캔 |
| **V2** | ✅ **완료 (V2a/V2d)** — 다만 **몬테카를로가 아니라 결정론적 각도구적**으로.  MC 는 광자 10⁶개에 9 % 인데 격자 32점이 4e−15 다 (설계문서 `PLAN-V2-collision-oracle.md` 참조).  오라클을 **궤적이 아니라 국소 연산자**로 재정의해 진동 배경 문제를 소거했다.  전체 사슬 잔차 3e−15, i-무관성 확인.  ★ 부수: **편광 판을 만들자 `8/27 vs 4/15` 미결이 닫혔다 — 정답 16/45** (유효감쇠 3/4, 비 6/5 가 Hu 강의노트와 일치) | ✅ **V2 티어 완결** (27~29차): V2b 는 쌍극 원천 계수 **4/3 을 측정**해 `dipole_source` 를 구현, V2c 는 l=2 감쇠가 v_e 1차에서 불변임을 확인, V2e 는 문헌의 `τ̇δη ≪ 1` 을 **τ̇δη ≲ 1.86** 으로 숫자화(+경고 감시자), V2f 는 MC 를 구조 오라클로 강등 |
| **V3** | 🟡 **부분 완료 (L3)** — J̇ 닫힘이 기본값이 되면서 위반이 2.2e−1 → 기계정밀도로 내려갔고, `degeneracy_violation(jdot_closure=…)` 로 옛 거동과 나란히 잴 수 있다.  남은 것: 적분 **중** 임계값 감시(경고 발행)와 다른 축퇴(등방 극한 π=0) | |
| **V4** | ✅ **완료 (L3 과 함께)** — 노름 기반 스칼라 비를 `mode="ratio_scalar"` 로 두고 비교.  l=0 은 동일, 텐서 성분에서는 성분별이 낫다 — 현재 무해함 확인 | |

---

## 4. R 티어 — 이식·성능 (**정확도 개선 뒤에**)

| ID | 내용 | 비고 |
|---|---|---|
| **R1** | tilted 적분 Rust (diffsol **질량행렬 ODE**) | M 이 이미 닫힌형이라 조립은 짧다.  RK4 → 암묵적 BDF 로 강성 대응도 얻는다 |
| **R2** | 나머지 차트 RHS 이식 (class_b_tilted, exceptional, type_ix_d) — 기존 C2 | K1 이 끝나면 결합계 전체를 Rust 로 굴릴 수 있다 |
| **R3** | rank-6 PSTF codegen + Rust (V1 이 요구) | |

★ 순서 원칙 (이미 두 번 확인됨): **이식은 빠르게만 할 뿐 정확해지지 않는다.**
H5-e3 에서 지배 오차가 i-닫힘으로 드러났을 때 이식을 미룬 판단이 6800배를 벌었다.

---

## 5. 기존 잔여 티어 (변동 없음)

- **B2b** 다준위 재결합 → Lyα 복사전달 Λ_α (β/Λ = 635/157/51 인 포화영역이라
  α_B 가 아니라 Λ_α 가 표적임을 이미 확인)
- **D1** 이벤트 검출 (Kasner 바운스·재붕괴·유형전이; diffsol 내장 기능)
- **D2** 구속 사영/감시, **D3** 혼합정밀 (특이점 근방)
- **E1** GPU (diffsol 실험적 CUDA)

---

## 6. 권장 순서와 근거

```
1) K1 → K3        결합.  프로젝트 목표에 직결 + K3 는 오라클 재사용이라 공짜
2) L3 → L1 → L2   유질량 tilted 를 먼저 **측정**하고, 그 결과가 L1/L2 의 필요성을 정한다
3) V2             유일하게 오라클 없는 부분 (몬테카를로로 메움)
4) K2             on-shell v — 물리적으로 크지만 K1 이 선행돼야 의미가 있다
5) R1 → R2        이식
6) V1, B2b, D1…   나머지
```

**한 가지 반대 의견도 적어둔다**: K1 을 먼저 하면 결합계의 새 오차원(구속 표류,
강성)이 tilted 계층의 남은 오차와 섞여 진단이 어려워질 수 있다.  그래서 K3(보존
게이트)를 K1 과 **동시에** 넣는 것을 전제로 한다 — 게이트 없이 결합하면 안 된다.
