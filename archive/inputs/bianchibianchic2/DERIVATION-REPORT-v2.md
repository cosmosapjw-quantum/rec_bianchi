# 유도 라운드 결과 (v2.0 착수 전) — 확정 수식과 검증 잔차

> **⚠ v1.3 정오표 (2026-07-29 적대적 감사 후).** 이 문서의 v1.2 판에는 네 가지
> 결함이 있었고 모두 아래 본문에 정정 반영되었다. 상세는 `AUDIT-REPORT-v1.3.md`.
>
> 1. **상반성 잔차 1.5e−3 은 적분오차가 아니라 수렴한 실결함이었다** — 스크린
>    기저를 공간 성분만 전송하고 E_A⁰ = 0 으로 고정한 것 (평행이동 위반, Σ² 에
>    비례). 4-벡터 전송으로 수정 → 4.1e−12. "2500스텝" 표기도 허위였다 (실제
>    120000스텝에서도 동일 잔차 = 스텝수 무관 결함의 증거).
> 2. **D13/D9 의 dP 식은 엔진 R(generator) 로 적혀 있었다** — 보고서 규약(commutator)
>    으로는 회전항이 +(R×P) 다. 좌표기저 독립검산(회전 프레임 포함)으로 확정.
> 3. **§D 의 "E=0 극한 일치" 주장은 검증 산출물과 모순이었다** — 참조식이
>    R_gen→R_comm 변환을 빠뜨려 −2(R×B) 잔차가 기록돼 있었다. 엔진·코드·좌표검산
>    3자 모두 +(R_comm×B) 로 일치 확인, 참조식 수정 후 잔차 정확히 0.
> 4. 검증 게이트가 계수를 보지 않고 span 만 보았다 (엔진 부호 변이가 41게이트 중
>    12개만 걸림) → v1.3 에서 계수 게이트 11개 포함 22개 게이트 증설.


`DERIVATION-DAG.md` 의 D1–D24 중 이번 라운드에서 실행한 항목의 결과.
유도는 전부 v1.1 감사 엔진(`audit/einstein_frame.py`: 구조상수 → 4차원 프레임 접속 →
Riemann/Ricci/Einstein)에서 나왔고, 새 규약은 하나도 도입하지 않았다.

실행 스크립트: `audit/d_transport.py`, `d_optical.py`, `d_kinetic.py`, `d_matter.py`.

---

## 0. 이번 라운드에서 발견된 결함 2건

### (i) 회전 표기 — v1.1 보고서가 자기모순 (문서만, 코드는 무사)

v1.1 은 식 (1) 을 `[e_0,e_a] = −(Hδ + σ − Ω)e_b` 로 고치면서 마스터 규칙을
`Ẏ_a ⊃ +Ω_a^b Y_b = +(R×Y)_a` 라고 적었다. 그런데 `Ω_ab ≡ ε_abc R^c` 이면

```
Ω_ab Y_b = ε_abc R^c Y^b = −ε_abc R^b Y^c = −(R × Y)_a
```

이므로 두 진술은 서로 모순이다. 프레임 접속을 직접 뽑아 보면 결론이 나온다:

```
Γ^0_00 = 0                      (법선 합동은 측지)
Γ^0_ab = Γ^a_0b = H δ_ab + σ_ab (외재곡률)
Γ^a_b0 = −Ω_ab                  ← 잔차 정확히 0
```

따라서 평행이동되는 공간 벡터는 `Ẏ_a = +Ω_ab Y_b` 를 따르고, 이는 commutator 규약의
R 로 쓰면 `+(R×Y)_a` 다. **식 (1) 은 v1.0 의 부호가 옳았고, v1.1 의 "수정"이 오히려
부호를 뒤집었다.** 코드는 처음부터 `W = −Ω` 로 구현되어 있었으므로 영향 없음
(테스트 변경 0건). v1.2 보고서에서 정정.

> 교훈: `Ω` 와 `W` 는 부호가 반대다. 둘 다 쓰지 말고 하나만 쓸 것.

### (ii) 자기장 Ampère 구속의 상대부호 — 실제 결함 (v1.0 부터)

E 와 B 를 **동시에** 넣고 프레임 Maxwell 을 풀면:

```
Ė_a = −2H E_a + σ_ab E^b + (R×E)_a − (curl B)_a
Ḃ_a = −2H B_a + σ_ab B^b + (R×B)_a + (curl E)_a
(curl X)_a ≡ n_ab X^b + ε_abc a^b X^c          ← 상대부호 +
```

두 식이 정확히 쌍대 구조를 이루는 것이 이 부호의 교차검증이다.
v1.0/v1.1 보고서와 `MagneticField.constraints` 는 `− ε_abc a^b X^c` 였다.

영향 범위: `n = 0` (유형 V → 여전히 B=0) 과 `a = 0` (유형 II → 여전히 B₁=0) 에서는
두 부호가 같은 결론을 준다. **차이는 n≠0 이고 a≠0 인 class B (III, IV, VI_h, VII_h)
에서만** 드러난다. 따라서 기존 결론은 전부 살아남지만 코드는 고쳤고 회귀 테스트를
추가했다 (`test_magnetic_ampere_constraint_relative_sign`).

---

## 1. 확정된 수식

### A군 — 전송 (D1, D2, D13, D9)

**프레임 접속 블록** (모두 잔차 ≤ 6.7e−16):

| 블록 | 값 |
|---|---|
| Γ⁰₀₀ | 0 |
| Γ⁰_ab | H δ_ab + σ_ab |
| Γ^a_0b | H δ_ab + σ_ab |
| Γ^a_b0 | −Ω_ab |
| Γ^a_bc | 3차원 Levi-Civita 접속 (구조상수에서) |

**삼각대(triad) 전송** — 불변기저 공간 여벡터 `E^α_i` 에 대해

```
dE/dt = (H·1 + σ + Ω) E ,        tr(H·1 + σ + Ω) = 3H  →  det E = e^{3τ}
```

부호 판정: `+Ω` 는 잔차 1.1e−9 (유한차분 h² 한계), `−Ω` 는 **17.0 으로 실패**.
이로써 τ = ln(ℓ/ℓ₀) 와 `ℓ′/ℓ = 1` 이 정확히 성립한다 (D1).

**광자 (널 측지선, D13)** — 잔차 ≤ 3.6e−15:

```
dE/dt   = −E ( H + σ_ab n̂^a n̂^b )
dP^a/dt = −(H δ + σ)^a_b P^b + (R×P)^a − E · 𝒫^a(n̂)          [v1.3 표기 정정]
𝒫^a(x)  = ³Γ^a_bc x^b x^c = A^a x² − (A·x) x^a + ε^a_bc x^b (N x)^c
d n̂^a/dt = −[σ n̂ − (n̂·σ·n̂) n̂]^a + (R×n̂)^a − [𝒫(n̂) − (n̂·𝒫(n̂)) n̂]^a
```

`|n̂| = 1` 보존 잔차 1.4e−15, FLRW 극한 4.4e−16.
**𝒫^a 는 v1.1 의 tilt 방정식에 나온 바로 그 항이다** — 같은 함수를 쓴다.

**질량 입자 (D9)** — 잔차 ≤ 3.6e−15:

```
dE/dt   = −( H |P|² + σ_ab P^a P^b ) / E ,      E² = m² + |P|²
dP^a/dt = −(H δ + σ)^a_b P^b + (R×P)^a − 𝒫^a(P) / E
```

광자는 m=0 극한. **자유흐름 종족과 광선 추적이 같은 적분기를 쓴다** (설계상 핵심 이득).

**Bianchi I 정확해 (D10)**: 좌표(불변)기저 공변성분 `p_i = a_i P^i` 가 상수.
1차 정확도 적분기로 20000 스텝에서 상대오차 3.3e−5 (수렴 확인용).

### B군 — 광학 (D15, D16, D17)

```
스크린 기저 E_A (A=1,2):  k·E_A = 0, u·E_A = 0, 광선 따라 평행이동
조석행렬        T_AB = −R_{a μ b ν} k^μ k^ν E_A^a E_B^b
Jacobi          d²J/dλ² = T J ,   J(0)=0, J′(0)=1
각지름거리      d_A = |det J|^{1/2}
```

| 검증 | 잔차 |
|---|---|
| tr T_AB = −R_ab k^a k^b (Ricci 집속) | 2.8e−14 |
| T_AB 대칭 | 1.8e−14 |
| k 널이고 Riemann 커널에 있음 | 1.6e−15 |
| EdS 기준해 `d_A = (2/H₀)(1−(1+z)^{−1/2})/(1+z)` | **2.3e−13** |
| FLRW `1+z = 1/a` | 4.4e−14 |
| **상반성 `r_{s→o} = (1+z) r_{o→s}`** | ~~1.5e−3~~ → **4.1e−12** (v1.3 수정 후) |

상반성은 Etherington 거리-쌍대성 `d_L = (1+z)² d_A` 의 근거이며, 광자수 보존만
있으면 임의 시공간에서 성립한다. 이것이 **d_L 을 독립 유도하지 말고 검증에만 써야
하는 이유**다.

**방향 의존성의 크기** (Bianchi I, Σ_i = (0.25, −0.10, −0.15), Ω=0.9, z≈0.4):

| 방향 | z | d_A |
|---|---|---|
| x | 0.3985 | 0.1809 |
| y | 0.3994 | 0.2449 |
| z | 0.3990 | 0.2592 |

d_A 의 방향간 퍼짐 **34%**, 같은 적분 끝점에서 z 의 퍼짐은 **0.22%**.
즉 이 배치에서 이방성은 적색이동보다 **거리에 훨씬 크게** 나타난다 —
관측 대비에서 SNIa 광도거리가 z 자체보다 민감하다는 뜻이다.

### C군 — 운동론 / 자유흐름 (D8, D11, D12)

Bianchi I 에서는 고정 영역 3중 구적으로 닫힌다:

```
ρ     = (1/V) ∫ d³p_i  E f₀(p_i)
p     = (1/V) ∫ d³p_i (|P|²/3E) f₀
π_ab  = (1/V) ∫ d³p_i (P_a P_b − |P|²δ_ab/3)/E f₀
V = a₁a₂a₃ ,  P^i = p_i/a_i ,  E = √(m² + Σ(p_i/a_i)²)
```

| 검증 | 값 |
|---|---|
| 등방 극한 π_ab = 0 | 2.6e−13 |
| 무질량 w = p/ρ | 0.333333333333 |
| 무질량 ρ ∝ ℓ^{−n}, n | 4.000 |
| 비상대론 ρ ∝ ℓ^{−n}, n (m/T=50) | 3.0028 |
| π_ab trace-free | 1.7e−14 |
| **ρ̇ + 3H(ρ+p) + σ_ab π^ab = 0** (무질량/질량) | 3.9e−10 / 4.2e−10 |

**급작 응답(sudden response) 계수** — 등방 분포를 부피보존 변형 δ(ln a) 로 순간
찌그러뜨렸을 때

```
π_ab = −(8/15) ρ · δ(ln a)_⟨ab⟩          (초상대론적 기체)
```

수치: δ = 0.05 → −0.5127, 0.02 → −0.5254, 0.01 → −0.5295, 0.004 → −0.5318,
0.002 → **−0.53257** (−8/15 = −0.53333, 차이 7.6e−4 = O(δ) 보정).
질량 기체(m/T=60)에서는 −0.0024 → 0 으로 소멸 ✓.

이 −8/15 가 **Misner 중성미자 점성의 기원**이다. 직접 적분에서 자유흐름 무질량
종족은 동일 초기조건의 완전유체 대비 최종 shear 를 **3.2% 감소**시켰다
(Δln ℓ ≈ 0.5 구간, Σ: 3.775 → 3.655).

### D군 — 전자기 (D19)

위 §0(ii) 참조. 추가로 확정된 구속:

```
Gauss (E):  a·E = 0
Gauss (B):  a·B = 0
```

E=0 극한에서 v1.1 자기장 진화식과 (회전항 포함) 일치.

### E군 — 유체 합동 (D22)

`u = Γ(n + v)` 의 운동학량:

| 양 | v = 0 에서 | 기울어졌을 때 |
|---|---|---|
| θ_u | 3H (정확) | 0.92 ~ 8.60 (무작위 표본) |
| 가속도 u̇^a | 0 (정확) | ≠ 0 |
| vorticity ω_ab | 0 (정확) | 일반적으로 ≠ 0 |

**함정**: ω_ab 는 v̇ 에 의존해서는 안 되는데, 수치검증 결과 `f_om` 은 v̇ 에 의존한다
(잔차 1.6). 이는 ω 의 정의 자체가 아니라 **투영 h^a_b 를 mixed 지표로 짠 구현의
문제**이며, PR-42 착수 전에 완전 투영형(`h_ab = g_ab + u_au_b`, 두 지표 모두 내림)으로
다시 짜야 한다. **미해결 항목으로 남긴다.**

### F군 — 점성 (D21, 부분)

```
Eckart :  π_ab = −2η σ_ab ,   p → p − ζθ
정규화 :  Π_ab = −2(η/H) Σ_ab      → Σ 방정식의 순수 감쇠항
IS     :  τ_π Π̇_ab + Π_ab = −2η σ_ab   (Π_ab 를 독립 변수로 승격)
엔트로피 생성:  T Ṡ = 2η σ_ab σ^ab + ζ θ² ≥ 0
```

---

## 2. 남은 유도 항목

| ID | 상태 | 비고 |
|---|---|---|
| D3 | 미실행 | 좌표기저 h_ij 재구성 — D2 에서 거의 자동 |
| D4 | 미실행 | IX D-차트 물리량 복원 |
| D5–D7 | 미실행 | 엔트로피/온도/재결합 — 위험 낮음, PR 단계에서 |
| D11 | 부분 | 일반 Bianchi 운동량 전송은 D9 에서 확보. 구적 가중치의 일반화가 남음 |
| D18 | 미실행 | ΔT/T(n̂) — D13/D14 위에서 조립 |
| D20 | 미실행 | f(φ)²F² 결합 |
| D22 | **미해결** | 위 투영 구현 문제 |
| D23, D24 | 미실행 | 위험 낮음 / 관측자 프레임 사전은 PR-46 직전 필수 |

**임계경로는 뚫렸다**: D2 → D13 → D15 → D16 → D17 이 전부 검증됨.
PR-28, 29, 43, 44, 45 는 지금 바로 착수 가능하다.

---

## 3. 재현

```bash
cd audit
python d_transport.py     # -> d_transport.json
python d_optical.py       # -> d_optical.json   (~4분)
python d_kinetic.py       # -> d_kinetic.json
python d_matter.py        # -> d_matter.json    (~3분)
```

시드는 전부 `20260729` 고정. 기대 잔차는 위 표와 각 JSON 에 있다.
