# 제3자 적대적 감사 보고서 (v1.3, 2026-07-29)

**대상**: bianchi-solver v1.2 전체 — `bianchi/` 패키지, `tests/`, `audit/` 유도·검증
파이프라인, `report.tex` v1.2, `DERIVATION-REPORT-v2.md`.
**방법**: 상호 독립인 세 감사 트랙을 병렬 실행 후 교차 판정.

| 트랙 | 관점 | 핵심 기법 |
|---|---|---|
| A | 유도 파이프라인 공격 | 순환성 분류, **변이 실험**(엔진 부호를 고의로 깨고 게이트 통과율 측정), 수렴성 스캔, 시드 민감도, 주장-산출물 대조 |
| B | 코드·테스트 수색 | 가짜 계산 색출(`*0` 패턴, 죽은 중간값), 동어반복 테스트 census, 보고서-코드 전사 대조(무작위 상태 200개/계) |
| C | 완전 독립 검산 | 저장소 코드를 전혀 안 쓰는 좌표기저 실현(type VI_h, exp(xM)) + JAX 자동미분, **회전 프레임 포함** |

---

## 1. 판정 요약

| # | 발견 (트랙) | 심각도 | 판정 | 처분 |
|---|---|---|---|---|
| 1 | 상반성 잔차 1.5e−3: 스텝수 무관 평탄, Σ² 비례 → **스크린 기저를 공간 성분만 전송 (E_A⁰=0 고정)** — 평행이동 위반 (A-F1) | **치명** | 실결함 확정 | 4-벡터 전송으로 수정 → **4.1e−12**. 게이트 1e−2 → 1e−9. 직교성 잔차 0 게이트 추가 |
| 2 | `stability_of_CS_II` 가 `lam * 0.0` 으로 계산을 버리고 (7γ−10)/7 하드코딩; "numerically" 테스트는 어떤 구현 변경에도 통과 (B-#1,#2) | **치명** | 위장 계산 확정 | jax.jacfwd 실선형화로 교체. 구속 논증(유형 II 에서 q₁≡0 → v₁ tilt 금지, 물리 이중항 {v₂,v₃}) 명시. 독립 해석식 3(7γ−10)/8 대조 테스트 |
| 3 | 변이 실험: 엔진 회전부호를 깨도 41게이트 중 12개만 실패 — fit 게이트가 **계수가 아닌 span 만** 검사 (A-F2) | 중대 | 게이트 설계 결함 | 계수 게이트 11개 신설 (G_AB 네 블록의 전 계수). 게이트 41 → 63 |
| 4 | D19 "E=0 극한 일치" 주장 ↔ 산출물 −2(R×B) 모순 (A-F3) | 중대 | **검사 참조식 결함** (엔진·코드 무죄) | 수치 판정: 엔진 Ḃ = −(R_gen×B) = +(R_comm×B) — 코드·좌표검산과 3자 일치. 참조식의 R_gen→R_comm 변환 누락 수정 → 잔차 정확히 0, 게이트 추가 |
| 5 | `einstein_frame.py` `__main__` 자기검사가 낡은 오답 참조식(G00 의 −³R/2, Codazzi ε 역순 등)으로 실패 기록을 조용히 출하 (A-F4) | 중대 | 사문서 확정 | 수치 평가로 재작성 (fit 계수 기준) → 4블록 ≤2.1e−14, 게이트 4개 추가 |
| 6 | `make_projector` 가 미등록 차트(class_b_tilted, type_ix_D)에 **조용히 항등 투영** 반환 (B-#3) | 중대 | 실결함 확정 | 명시 레지스트리로 교체: tilted-B 는 C2–C4, IX-D 는 definition+trace 로 투영, 미등록은 ValueError. 회귀 테스트 (투영 전후 1000× 감소 확인) |
| 7 | 보고서의 "codazzi 가 부호 잔차 반환" 서술은 허위 — 코드는 2차식 C (시트 무구분) (B-#4) | 중대 | 문서가 코드 과대포장 | 진실로 정정: 축소 변수는 시트 부호를 원리적으로 담지 못함; 시트 선택은 리프트 시점 규약. 코드 docstring 동기화 |
| 8 | `charts/general` 이 tilted 물질 주입 시 비틸트 q 를 조용히 사용 — (2−γ)V²/G₊ 보정 누락, Σ-폐포 1e−1 실패 (B-#5) | 중대 | 실결함 확정 | `args["q"]` 훅 추가. **전 성분 폐포 테스트** (tilted 차트 ↔ general, ≤1e−12) + q 생략 시 불일치를 고정하는 음성 통제 테스트 |
| 9 | D13/D9 의 dP 식이 엔진 R(generator)로 표기 — 회전 프레임에서 O(1) 실패, 보고서 규약으로는 +(R×P) (C-E) | 중대 | 표기 결함 확정 (코드 무죄) | 문서 전부 (R×P) 표기로 정정. 좌표검산이 회전 프레임에서 +(R×P) 를 1.7e−16 으로 확정 |
| 10 | Maxwell curl 상대부호 +ε: 좌표기저 완전 독립 검산 (C-D) | — | **v1.2 수정 재확인** | 신부호 8.3e−17 vs 구부호 1.7e−1. 쌍대성 \|−L_EB−L_BE\|=2.2e−16 |
| 11 | −8/15 급작응답: 손계산 + 형상 보편성 (A-F5) | — | **생존** | 임의 등방 f₀ 에 대해 정확히 보편 (∫q³f₀ 인수분해). Richardson −0.5333347 |
| 12 | `M @ 0 == 0` 공허 테스트 (B-#6) | 경미 | 확정 | 실제 흐름 검증으로 교체: 구속 위반 진공 데이터를 ij-방정식으로 흘려 dC/dt = M·C 를 유한차분 대조 |
| 13 | 보고서 (cprop) 박스·§5.4 회전 3종의 generator 부호 잔재 (B-#7) | 경미 | 확정 (코드 무죄) | 보고서 정정, 코드 불변 |
| 14 | IX-D 'gauss' 모니터 항등 0, `total_emt_conserved` 는 잔차 아님, fluid.py 철회된 극한 docstring (B-#8,#10,#11) | 경미 | 확정 | 제거/개명/정정 |
| 15 | 스칼라장 √(3/2) 결합이 무테스트 (보존항등식은 계수 무관) (B-#12) | 경미 | 확정 | 스케일링 고정점 정지성 테스트 (계수 1% 변이 시 실패 확인 포함) |
| 16 | d_transport 공간블록 검사의 per-draw min/max 버그, "2500스텝" 허위 표기, Γ⁰_ab "0 (exact)" 과대표기, manifest version/시드 누락 (A-F7,F9) | 경미 | 확정 | 전부 수정 |
| 17 | FD 에너지보존 잔차 3.9e−10 은 h=1e−6 유한차분 바닥값 — 그 이하는 검사 불능 (A-F6) | 참고 | 확정 | 문서에 한계 명시 (게이트 1e−8 은 바닥 위라 적절) |
| 18 | 시드 민감도 (A-F10) | — | **생존** | 4개 시드에서 전 게이트 여유 2자릿수 이상 |

**전사(transcription) 대조는 무결**: class A/B/예외형/tilted-B/IX-D 의 모든 진화식·구속,
fluid/Magnetic/Scalar 가 보고서와 ≤1.8e−15 일치 (B-#14, 200 무작위 상태/계).
IX-D 의 정칙 q̄ 사용 확인 (특이 q 누출 없음). 좌표기저 검산(C)은 실현 검증(구조상수
재판독 0.0), 측지선(2.2e−16), Maxwell(위), Gauss 법칙(a·E, a·B)까지 전부 통과.

## 2. 순환성 판정 (A-F2 상세)

41개 게이트(v1.2)의 분류: 외부 앵커 5 (EdS d_A, FLRW 적색이동, WE S± 문헌식, 상반성,
−8/15), 준독립 2 (삼각대 부호 판별), 자기일관 ~30, 동어반복 4. **v1.3 처방**: 계수
게이트 11개 + 엔진 자기검사 4개 + 이전 미게이트 판별자 7개 = 게이트 63개. 잔여 한계
(정직 고지): 자기일관 게이트는 엔진의 공통 오류에는 여전히 눈멀며, 이를 보완하는 것은
트랙 C 류의 좌표기저 독립 검산이다 — 이번에 측지선·Maxwell·회전 프레임까지 커버했고,
스크립트는 `/tmp/auditC/`(세션 소멸) 기반이므로 **v2.0 에서 저장소 내 재현물로 이식할
것** (후속 과제 #1).

## 3. 남은 정직한 구멍 (후속 과제)

1. 트랙 C 좌표기저 검산의 저장소 내 이식 (현재 /tmp — 세션 휘발).
2. D22 vorticity 투영 구현 (v̇ 의존 잔차 1.6) — PR-42 선행조건, 미해결 유지.
3. 자기일관 게이트의 구조적 한계 (§2) — 엔진 대체 구현(예: 좌표기저 병행 유도)만이
   근본 해결.
4. tilted II 의 v₁–Σ₂₃ 구속 결합 블록 선형화는 미구현 (현재 지표는 물리 이중항
   {v₂,v₃} 만 — HBW 임계값 10/7 에는 충분).

## 4. 최종 상태

```
pytest -q          : 134 passed   (v1.2: 130; 신규 4, 공허 테스트 2건은 실검사로 교체)
audit/run_all.py   : 63/63 checks passed  (v1.2: 41/41 — 그중 1개는 허위 참조,
                                           4개는 span-맹검, 1개는 8자릿수 느슨한 게이트)
잔차 개선          : 상반성 1.5e-3 → 4.1e-12  (스크린 4-벡터 평행이동)
```
