# PR 구현 현황

| PR | 내용 | 상태 | 완료 기준 검증 |
|---|---|---|---|
| PR-01 | scaffold + x64 | ✅ | `test_pr01_scaffold` |
| PR-02 | `symbolic/frame.py` 유도 엔진 | ✅ | L0~L4 사다리 (`test_L0`~`test_L4`) |
| PR-03 | `conventions.py` (함정 4종) | ✅ | 9배 함정 검출, Σ 타입분리, 게이지 3회전 |
| PR-04 | `algebra.py` 분류기 | ✅ | 12개 유형, κ 두 경로 일치, 회전 불변 |
| PR-05 | `symbolic/oracles.py` | ✅ | **커널 오염이 A 통과 & B·D 검출**을 테스트로 박제 |
| ~~P0.5~~ | ~~HW93 원문 확보~~ | 삭제 | 유도로 이미 종결 |
| PR-06 | class A 차트 | ✅ | Kasner 원, N_i=0 정확보존, 유형보존 |
| PR-07 | class B 차트 (κ) | ✅ | (b3,c2,c)=(−4,2,6) 재유도, Codazzi 전파, 유형 V 2차원 |
| PR-08 | 예외형 VI\*₋₁/₉ | ✅ | g′ 항등, K=N₋²+4A², A 2배환산 없음, 5 params |
| PR-09 | `integrate.py` | ✅ | scipy Radau/LSODA/DOP853 대조, throw=False, vmap |
| PR-10 | Tier2 정확해 | ✅ | Kasner, CS(II) **Σ₊=(3γ−2)/8**, Heckmann–Schücking, I+Λ, Collins VI_h |
| PR-11 | Layer 0 `general.py` | ✅ | S³ 앵커, class A RHS·궤적 일치, 구속 0 |
| PR-12 | `constraints.py` 투영 | ✅ | 지수증폭 억제 실증 |
| PR-13 | Tier3 교차검증 | ✅ | Layer0↔Layer1 (PR-11 테스트에 포함) |
| PR-14/15 | tilted 유체 | ✅ | **Ω′ 분모 G₊**, V=0·V=1 불변, γ=4/3 전환, 가드 2종 |
| PR-16 | tilted class B (Hervik) | ✅ | Codazzi 비 −3, 회전 3개, Σ₂₃′ 계수 2√3, Σ₊′ 회전항 |
| PR-17 | γ 임계값 | ✅ | 14/9 포함, 6/7 금지, Σ₊·h 의존 함수 |
| PR-18 | 다중 유체 | ✅ | 상대운동 → 비대각 응력 실증 |
| PR-19 | 스칼라장 | ✅ | 보존 항등식, no-hair 등방화 |
| PR-20 | 자기장 | ✅ | EMT 구조, 제약이 유형 V 에서 B=0 강제 |
| PR-21 | Bianchi IX D-차트 | ✅ | G′ 전파(trace-free 필수), **재붕괴 통과** |
| PR-22/23 | 배치 하네스 | ✅ | 메모리 7.2GB 재현, 발산 낭비 6.9배, 청킹 |
| PR-24 | Rodas5 독립구현 | ⏸ 선택 | diffrax Kvaerno5 로 충분. GPL 회피 위해 미착수 |
| PR-25 | `physical.py` | ✅ | de Sitter/먼지 H, Σ_WE vs Σ_θ, BBN |
| PR-26/27 | `analysis/dynamics.py` | ✅ | 고정점·연속법·Lyapunov·극한주기 검출 |

**26/27 완료** (PR-24 는 의도적 보류).

## 다음 후보
- Mussel attractor 수치 재현 (PR-27 벤치마크; `detect_limit_cycle` 준비됨)
- GPU 대규모 스캔 실측 (PR-23 프로파일)
- tilted 유체를 Layer 0 `general` 차트에 결합

---

## v1.1 — 외부검토 대응 (2026-07-29)

외부검토 7개 항목을 **수용 전에 전부 독립 재유도**한 뒤 실제 결함만 수정.
재유도 엔진은 v1.0 파이프라인과 코드를 공유하지 않는 별도 모듈 (`audit/`).

### 확정된 실제 결함 (수정 완료)

| # | 위치 | 내용 |
|---|------|------|
| 1 | `conventions.gauge_rotation_classB` | GENERATOR 부호 반환 ↔ 소비 측은 COMMUTATOR. 저장소 내 유일한 규약 혼용 지점. 부호 3개 전부 반전. |
| 2 | `matter/components.MagneticField.rhs` | 회전항 `-eps R B` → `+eps R B` (마스터 규칙). |
| 3 | `matter/fluid.dv_general` | shear 항 `+2 Sigma.v` → `-Sigma.v`, `(A.v)v` 부호 반전, `-A V^2` 항 누락 → 전면 재유도로 교체. |
| 4 | 문서 §4 | `(gamma,V)->(0,1)` 가 제거가능 0/0 이라는 주장 → 경로의존이므로 삭제. |
| 5 | 문서 §4 | class B + A≠0 에서 비틸트 유체 불가 주장 → 거짓 (Bianchi V open FLRW 반례). |
| 6 | 문서 부록 B | gamma 임계값 표 대폭 오류 → 전면 재작성. |

### 추가된 것

* `conventions.rotation_apply_vector / rotation_apply_tensor` — 마스터 회전 규칙 단일 창구
* `conventions.constraint_propagation_matrix` — 축약 Bianchi 로 유도한 4x4 전파 행렬
* `charts/general.S3_closed` — ^3S_ab 명시 닫힌 형태 (Jacobi 불필요)
* `tests/test_rotation_sign.py` — 17개 회귀 테스트 (순수 회전 프레임 포함)
* `audit/` — 독립 재유도 엔진 + `run_all.py` + `manifest.json` (25/25 통과)
* `requirements.lock`

### 테스트

```
pytest -q      ->  129 passed   (v1.0: 112)
audit/run_all.py ->  25/25 checks passed
```

---

## v1.2 — 유도 라운드 (PR-28~52 착수 전, 2026-07-29)

`DERIVATION-DAG.md` 의 D1–D24 중 임계경로(D2 → D13 → D15 → D16 → D17)를 유도·검증 완료.
스크립트: `audit/d_transport.py`, `d_optical.py`, `d_kinetic.py`, `d_matter.py`.
결과 문서: `DERIVATION-REPORT-v2.md`.  감사 `run_all.py` → **41/41 통과**.

### 발견된 결함 2건

| # | 위치 | 내용 |
|---|------|------|
| 1 | 보고서 식 (1) + 마스터 규칙 | v1.1 이 `Ω_ab Y_b = +(R×Y)_a` 로 적었으나 `Ω_ab ≡ ε_abc R^c` 이면 부호가 반대. 식 (1) 은 v1.0 이 옳았고 v1.1 의 "수정"이 뒤집은 것. **코드는 무사** (`W = −Ω` 로 일관). |
| 2 | `MagneticField.constraints.ampere` | curl 결합의 상대부호가 `−` 였으나 정답은 `+`: `n_ab B^b + eps_abc a^b B^c = 0`. E,B 를 동시에 넣고 프레임 Maxwell 을 풀면 Faraday 와 쌍대 구조로 확정된다. **v1.0 부터의 실제 결함.** 차이는 n≠0 & a≠0 인 class B (III, IV, VI_h, VII_h) 에서만 드러난다. |

### 미해결로 남긴 것

* **D22 vorticity**: ω_ab 가 v̇ 에 의존하면 안 되는데 현재 구현은 의존 (잔차 1.6).
  mixed 지표 투영 대신 완전 투영형으로 다시 짜야 함 — PR-42 선행조건.

### 테스트

```
pytest -q          ->  130 passed   (v1.1: 129)
audit/run_all.py   ->  41/41 checks passed  (v1.1: 25/25)
```


---

## v1.3 — 제3자 적대적 감사 (2026-07-29)

세 독립 트랙 (파이프라인 변이실험 / 코드·테스트 수색 / 좌표기저 완전독립 검산).
상세: `AUDIT-REPORT-v1.3.md`.

### 실결함 수정 6건
1. `audit/d_optical.py` 스크린 전송 — 공간 성분만 전송 (평행이동 위반) → 4-벡터
   전송. 상반성 1.5e-3 → **4.1e-12**.
2. `thresholds.stability_of_CS_II` — 하드코딩 위장 계산 → jacfwd 실선형화
   (+ 유형 II 구속 논증, 독립 해석식 3(7γ−10)/8 대조).
3. `constraints.make_projector` — 미등록 차트 침묵 항등 → 레지스트리 + ValueError,
   tilted-B (C2–C4) 와 IX-D (definition+trace) 투영 구현.
4. `charts/general.aux` — tilted 물질에서 비틸트 q 조용 사용 → `args["q"]` 훅 +
   전 성분 폐포 테스트 + 음성 통제.
5. `audit/d_matter.py` D19 참조식 — R_gen→R_comm 변환 누락 (−2(R×B) 허위 잔차)
   → 수정 후 정확히 0. 엔진·코드·좌표검산 3자 일치 확정.
6. `audit/einstein_frame.py` __main__ — 낡은 오답 참조로 실패 기록 출하 → 수치
   자기검사로 재작성 (4블록 ≤2.1e−14) + 게이트.

### 문서 결함 수정 (코드는 무죄로 판정된 것들)
* 보고서 (cprop) 박스, §5.4 회전 3종, D13/D9 dP 식 — generator-Ω 잔재 → (R×·) 표기.
* "codazzi 부호 잔차" 과대포장 → 축소차트는 시트 무구분임을 명시.
* "2500스텝" 허위, Γ⁰_ab "0 (exact)" 과대표기 정정.

### 게이트·테스트 강화
* run_all 41 → **63 게이트** (계수 게이트 11, 엔진 자기검사 4, 이전 미게이트 판별자 7).
* 공허 테스트 2건 (`M@0==0`, isfinite-only sink) → 실검사로 교체.
* 신규: 스칼라 √(3/2) 고정점 판별, general↔tilted 폐포, 투영기 회귀.

### 생존 확인 (공격에서 살아남은 것)
−8/15 급작응답 (손계산 재유도, f₀ 보편), Maxwell curl +ε (좌표검산 8.3e−17 vs
구부호 1.7e−1), 전사층 전체 (≤1.8e−15), 시드 강건성.

### 정직한 잔여 구멍
좌표기저 검산의 저장소 이식 (현재 /tmp 휘발), D22 vorticity (미해결 유지),
자기일관 게이트의 공통모드 맹점 (구조적 — 독립 좌표 유도만이 근본 해결).

```
pytest 134 passed · run_all 63/63
```

---

## v2.0 구현 착수 (2026-07-29) — M8 기반 + M11 임계경로 척추

DAG (IMPL-DAG.md) 를 따라 착수.  검증된 유도 D1–D24 를 코드로 이식.

### 완료 (6 PR)
| PR | 모듈 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 28 | `physical/chart.py` | de Sitter H=const, 먼지 t=2/(3H), IX D-차트 재붕괴 | test_m8 4 |
| 29 | `physical/frame_transport.py` | det E=e^{3τ}, +Ω 부호, legacy 방향인자 일치 | test_m8 5 |
| 30 | `matter/species.py` | 3종족 어댑터 legacy 재현, ΣQ=0 강제 | test_m8 3 |
| 45 | `rays/weyl.py` + `generate_weyl.py` | tr T = -Ric_kk (2.8e-14), T 대칭 | test_m11 1 |
| 43 | `rays/geodesics.py` | FLRW 1+z=1/a, Kasner 방향의존, \|n̂\|=1 | test_m11 5 |
| 44 | `rays/optical.py` | EdS d_A (8e-9), Etherington, 방향 퍼짐 34% | test_m11 3 |

**임계경로 척추 PR-45→43→44 완성** — 여기가 가장 길고 위험했다.

### 구현 노트
- `physical.py` → `physical/` 패키지로 승격 (legacy re-export 로 하위호환).
- 프레임 Riemann 은 `generate_weyl.py` 가 einstein_frame 에서 144개 비영 성분을
  pickle 로 생성 → `weyl.py` 가 jax lambdify+jit.  좌표검산(D16)과 동일.
- 광학 적분기: 2-패스 (광선+스크린 → 배치 조석행렬 vmap → Jacobi ODE).
  4-벡터 스크린 평행이동(v1.4 부채), O(dt⁴) Jacobi, 6000스텝 relerr 3e-14.
  ★ 함정: z 와 d_A 의 스텝 registration 이 한 칸 어긋나면 O(dt) 오차 (수정됨).

### 상태
```
pytest 159 passed  (v1.4: 138; 신규 M8 12 + M11 rays 9)
```

### 다음 (DAG 순서)
PR-31 (units/initial) → PR-32/33 (g_*, T(ℓ)) → PR-46 (CMB 패턴; 첫 Planck Bianchi 대비)
→ PR-38 (자유흐름) → PR-34/35/36/37 (열역사) → M10 나머지 → PR-49/51/52.

---

## v2.0 진행 2차 (2026-07-29) — M9 열역사 + M11 CMB + M10 자유흐름

### 완료 (5 PR 추가; 누적 11/24)
| PR | 모듈 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 31 | `physical/units.py`+`initial.py` | ΛCDM 나이 13.8 Gyr, z_eq 3400, Ω_γh²=2.47e-5 | test_m9 4 |
| 32 | `thermo/dof.py` | g_*(T≫)=106.75, (g_*,g_*s)(T≪)=(3.36,3.94), 단조 | test_m9 2 |
| 33 | `thermo/temperature.py` | T_ν/T_γ=(4/11)^{1/3}, sℓ³=const, T∝1/ℓ | test_m9 3 |
| 46 | `observables/cmb_pattern.py` | **Bianchi I 순수 사중극** (C₂ 99.85%), 등방 dT/T=0 | test_m11_cmb 4 |
| 38 | `matter/freestream.py` | 등방 π=0, w=1/3, ∇T=0, **−8/15 급작응답** | test_m10 7 |

**첫 Planck-Bianchi 대비 도달** (PR-46): Bianchi I 이 순수 사중극 CMB 패턴을
만드는 것을 확인 (C₂ 지배, C₁=C₃=0).  Planck VII_h vorticity 제한 참조 노출.

### 구현 노트
- `physical.py` 패키지에 units/initial 추가; `thermo/`, `observables/` 신규 패키지.
- numpy 2.x: `np.trapz`→`np.trapezoid`, scipy 1.17: `sph_harm`→`sph_harm_y` 대응.
- 자유흐름: audit/d_kinetic 의 검증 구적을 Species 형태로 이식.  π_ab≠0 이므로
  일반차트 필수.  광자 특성곡선과 코드 공유 (설계 D4).
- CMB 패턴: healpy 없이 직접 구면조화 분해.

### 상태
```
pytest 179 passed  (v2.0-1차: 159; 신규 thermo 9 + CMB 4 + freestream 7)
```

### 다음
PR-34 (질량 중성미자 FD) → PR-35 (암흑에너지 CPL) → PR-36 (재결합 Saha+Peebles)
→ PR-37 (동결) → PR-39/40/41/42 (점성·게이지장·no-hair·congruence) → PR-47/48
(거리·SNIa) → PR-49/51/52 (회귀·통합·보고서).

---

## v2.0 진행 3차 (2026-07-29) — M9 열역사 완성

### 완료 (4 PR 추가; 누적 15/24)
| PR | 모듈 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 34 | `matter/neutrino.py` | m→0: ρ∝a⁻⁴ w=1/3; m≫T: ρ∝a⁻³; Ω_νh²=6.4e-4 | 4 |
| 35 | `matter/dark_energy.py` | Λ 상수, CPL 닫힌형=수치적분, w(a) | 4 |
| 36 | `thermo/recombination.py` | z_*=1090(가시함수), 잔존 x_e=2.3e-4, z_drag | 5 |
| 37 | `thermo/relics.py` | WIMP miracle Ωh²=0.08, **이방→잔존 증가** | 5 |

**M9 열역사 완성** (PR-31~37).  재결합은 RECFAST 급 (잔존 x_e 2.3e-4), 동결은
이방 팽창이 x_f 를 옮기는 Bianchi 고유 효과 포함.

### 구현 노트
- Peebles: n_H0 상수 오류(34× 과소) + α_B 계수(3.8× 과소) 수정 → 잔존 2.3e-4.
- 재결합 이방성은 H(z) 콜백으로만 (설계 D7): 빠른 팽창 → 잔존 전자 증가.
- 동결 Boltzmann ODE 는 강성이라 표준 해석해로 대체 (WIMP miracle 재현).

### 상태
```
pytest 179+27=... (M9 신규 22; 전체 재실행은 M10 후)
```

### 다음
PR-39/40/41/42 (점성·게이지장·no-hair·congruence) → PR-47/48 (거리·SNIa)
→ PR-49/51/52 (회귀·통합·보고서 v2.0).

---

## v2.0 진행 4차 (2026-07-29) — M10 완성 (점성·게이지·no-hair·congruence)

### 완료 (4 PR 추가; 누적 19/24)
| PR | 모듈 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 39 | `matter/viscous.py` | Eckart Π=-2(η/H)Σ 감쇠, TṠ≥0, IS→Eckart | 3 |
| 40 | `matter/gauge_field.py` | curl 상대부호 +, E↔B 쌍대, π trace-free, Σ/H=O(ε) | 6 |
| 41 | `analysis/no_hair.py` | **Wald 지수 d lnΣ/dτ→-3**, H→√(Λ/3), 반례 exp→0 | 6 |
| 42 | `physical/congruence.py` | on-shell ω (D22): u-직교·반대칭·EOS-무관·a∥v, CMB 쌍극 β=\|v\| | 8 |

**M10 완성.**  no-hair 는 Bianchi I+Λ 정확해로 Wald 지수 -3 재현, 이방 인플레이션
게이지장으로 반례(Σ/H→O(ε)≠0) 구성.  congruence 는 D22 v1.4 on-shell vorticity 를
프레임 접속 위에서 재현 (a∥v 잔차 4.3e-11, EOS-무관 <1e-8).

### 구현 노트 — frame_connection 규약 버그 수정 (중요)
- `rays/frame.frame_connection` 의 **공간 블록 지표 순서 오류** 발견·수정.
  `_connection` 이 ³Γ^a_{bc} 를 미분슬롯=**가운데** 지표로 반환하는데 frame.py 는
  마지막 지표로 가정했다.  PR-43/44 는 P_double 의 **대칭축약** x^b x^c 만 써서
  드러나지 않았고, v2.0 의 첫 **비대칭 소비자** congruence 에서 표출 (a∥v 잔차 2.7).
- 수정: 공간 블록에 `swapaxes(C3,1,2)` 적용 → 계약(미분슬롯=마지막) 준수.
  회귀: 전체 220 통과 (ray 9 + cmb 4 + 나머지 207), 광선/광학/CMB 영향 없음.

### 상태
```
pytest 220 passed  (신규 M10: viscous+gauge 9, no-hair+congruence 14)
```

### 다음
PR-47/48 (거리·SNIa 관측량) → PR-49 (FLRW 극한 회귀) → PR-51/52 (통합·보고서 v2.0).

---

## v2.0 진행 5차 (2026-07-29) — M11 관측 브리지 (거리·SNIa)

### 완료 (2 PR 추가; 누적 21/24)
| PR | 모듈 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 47 | `observables/distances.py` | r_s=147.8 Mpc(0.5%), BAO D_M/D_H/D_V vs BOSS ~3%, 이방 AP 사중극∝Σ | 7 |
| 48 | `observables/sn.py` | μ 앵커, shear→사중극·속도→쌍극 분리, Pantheon+ 형식 | 7 |

**독립검산 (BOSS/eBOSS 대조):**
```
z=0.38  D_M/r_s=10.37(10.3)  D_H/r_s=24.47(24.9)
z=0.51  D_M/r_s=13.43(13.4)  D_H/r_s=22.63(22.3)
z=1.48  D_M/r_s=30.07(30.2)  D_H/r_s=12.84(13.3)
```
r_s 는 모듈 자체 Eisenstein-Hu drag_redshift 사용 (자기일관, Planck 0.5% 내).
이방 BAO: 시선 팽창률 H_∥(n̂)=H(1+Σ_ab n̂n̂) → D_H 방향의존, AP 비 C₂∝Σ.
SNIa: shear=사중극·bulk flow=쌍극 (PR-42 CMB 쌍극과 동일 v).

### 상태
```
pytest 234 passed  (fast 221 + rays 9 + cmb 4;  신규 M11: distances+sn 14)
```

### 다음
PR-49 (FLRW 극한 회귀) → PR-51 (batch 비용 갱신) → PR-52 (보고서 v2.0).

---

## v2.0 진행 6차 (2026-07-29) — PR-49 FLRW 극한 회귀

### 완료 (1 PR 추가; 누적 22/24)
| PR | 파일 | 검증 오라클 | 테스트 |
|---|---|---|---|
| 49 | `tests/test_flrw_limit.py` | CLASS/CAMB 대조 + 이방신호 소멸 | 15 |

**독립검산 (Planck2018/CAMB 대조):**
```
D_M(z*)   = 13867.0 Mpc  (CAMB 13869.6, 0.02%)
age t0    = 13.795 Gyr   (Planck 13.797, 0.01%)
r_drag    = 147.07 Mpc   (Aubourg2015 보정, Planck 147.09, 0.01%)
r_drag    = 147.82 Mpc   (물리적분, 0.5%)
Ω_ν h²    = 6.44e-4      (0.06 eV)
z_* (가시) = 1077.6       (Planck 1089.9, 1.1%)
```
정직성 노트: 음향지평 물리적분은 반해석적 ~2% 오차(θ_* 상속) — Aubourg2015 보정식을
정밀 오라클로 병기(`sound_horizon_fit`, CAMB 0.1%).  완전정밀은 Boltzmann 필요.
부수 수정: `recombination_redshift` docstring 정정(Saha x_e=0.5 는 ≈1370, 관측 z_*≈1090
아님 — 가시함수 최대점이 관측값).

### 상태
```
pytest 249 passed  (fast 236 + rays 9 + cmb 4;  신규 FLRW 회귀 15)
```

### 다음
PR-51 (batch 비용 갱신) → PR-52 (보고서 v2.0).  구현 22/24 완료.

---

## v2.0 진행 7차 (2026-07-29) — PR-51/52 통합 + 보고서

### 완료 (2 PR; 누적 24/24 계획 중 22 실질 완료)
| PR | 파일 | 내용 | 테스트 |
|---|---|---|---|
| 51 | `batch.py` (확장) | 광선추적 실측 비용모델 (측지 0.7ms/st, 광학 1.6ms/st) | 6 |
| 52 | `report_v2.tex/pdf` | v2.0 보고서 (수식·검증표·CAMB대조·규약결함·한계·비용) | — |

**비용 실측:** 전천 CMB ΔT/T 143 min, d_A 지도 328 min (numpy 직렬) → GPU 이식 동기.
**보고서:** 6쪽 PDF (xelatex, Noto Serif CJK KR). §5 CAMB 대조, §7 frame_connection 규약
결함 기록, §8 정직한 한계(음향지평 2%).

### 상태 (최종)
```
pytest 255 passed  (fast 242 + rays 9 + cmb 4)
구현: M8-M11 완료.  22개 실질 PR + 보고서.
```

---

## v3.0 진행 2차 (2026-07-29) — R1 완결 + R4 (광학 d_A Rust 이식)

### 완료
| 단계 | 산출 | 차등테스트 | 성능 (2코어) |
|---|---|---|---|
| R1(측지) | `rays/geodesic.rs` + PyO3 + Rayon | z **비트-정확 (0.00e+00)** | 전천 139분 → **0.7초** (≈12,000×) |
| R1(광학) | `rays/optical.rs` 3-패스 + Jacobi RK4 | z 비트-정확, d_A **2.3e-16** | 전천 400분 → **5.2초** (≈4,650×) |
| R4 | 조석행렬 **닫힌형** (탈-sympy) | Riemann 4.4e-16, T_AB 1.4e-14 | JAX/sympy 제거 |

### R4 — 조석의 닫힌형 (핵심 성과)
대각 Bianchi I (N=A=R=0, ³R=0) 에서 Gauss-Codazzi 로 프레임 Riemann 이 닫힌다:
```
K = Hδ+σ,  M = -(K̇+K²)
R_{0a0b}=M_ab,  R_{abcd}=K_ac K_bd - K_ad K_bc,  R_{0abc}=0        (오라클 4.4e-16)
T_AB = -[ w_A·M w_B + (u_A·K u_B)(κ·Kκ) - (u_A·Kκ)(Kκ·u_B) ]        (오라클 1.4e-14)
   w_A = α_A κ - k⁰ u_A,  E_A=(α_A,u_A),  k=(k⁰,κ)
```
→ 144성분 sympy pickle + JAX vmap 을 순수 산술로 대체.  일반 유형은 후속 codegen.

**스크린 직교성 1.1e-15** — v1.4 부채(공간성분만 전송 시 상반성 1.5e-3 편향)가
Rust 포트에서도 4-벡터 전송으로 유지됨을 확인.
**물리 오라클**: EdS 기준해 d_A=(2/H0)(1-(1+z)^{-1/2})/(1+z) <1e-6 (RK4 이산화 수준).

### 상태
```
pytest 276 passed  (fast 263 + rays 9 + cmb 4;  차등테스트 21)
cargo test 2 passed
```

### 다음
R2/R3 (차트 RHS + diffsol BDF/ESDIRK 단일·배치 적분) → 일반유형 조석 codegen →
`bianchi._rustcore` 통합.

---

## v3.0 진행 3차 (2026-07-29) — R2/R3 + 통합 (R-DAG 완주)

### 완료
| 단계 | 산출 | 차등테스트 | 성능 (2코어) |
|---|---|---|---|
| R2 | `ode/charts.rs` (class_a/class_b) + `ode/solve.rs` (diffsol BDF) | RHS **비트-정확 (0.0)**, 궤적 2.4e-10 vs diffrax | — |
| R3 | Rayon 배치 스캔 + 낙오자 격리 | 배치=단일 (1e-12) | K=256 배치 8.5초(JAX warm) → **33 ms** (≈260×) |
| 통합 | `bianchi/backend.py` 단일 디스패치 + 폴백 | 두 경로 일치 8개 시험 | — |

### R3 낙오자 격리 — 실측 검증
JAX batched while_loop 은 `batch × max_steps` 비용(끝난 원소도 본문 실행).  Rayon 은
원소별 독립.  K=256 (15% Kasner 근방) 실측: Rust 229/256 완료, **실패 27개는 전부
Ω<0 비물리 초기조건**이었고 JAX 도 같은 원소에서 발산(0/8).  실패는 솔버 약점이 아니라
초기데이터 발산이며 Rust 는 원소 단위로 격리·보고한다.

### 보존 성질 (Rust 에서도 유지)
- `N_i'=(…)N_i` 곱셈 구조 → N_i=0 과 부호가 **정확히** 보존 (유형 안전성)
- Class B Codazzi C = Σ̃Ñ-Δ²-Σ₊²Ã 궤적 위 <1e-8
- Gauss 항등식 Ω = 1-Σ²-K 궤적 위 <1e-13

### 통합 (계획 §2)
`bianchi.backend` 가 단일 진입점.  검증된 Python 모듈은 **미수정**, 디스패치만 추가.
모든 함수에 `force_python=True` 로 오라클 강제 가능 → 차등테스트가 영구 안전망.

### 상태 (R-DAG: R0·R1·R2·R3·R4 완료, R5 GPU 유예)
```
pytest 292 passed  (fast 279 + rays 9 + cmb 4;  차등 29 + 통합 8)
cargo test 7 passed
```

---

## v3.0 진행 4차 (2026-07-29) — 잔여 DAG 수립 + A티어(물리 정확도) 착수

### 질문 답: "Rust 로 열역학을 더 정밀하게?" — **측정 후 답변**
| 항목 | Python 비용 | Rust 필요? |
|---|---|---|
| Peebles 재결합 (700pts) | 15 ms | ✗ |
| 중성미자 FD 적분 (1 call) | 1.0 ms | **경계** (ODE RHS 안에서면 궤적당 8초) |
| 동결 Boltzmann λ=1e10 LSODA | **실패** | 솔버 선택 문제 |
| 동결 Boltzmann λ=1e10 Radau/BDF | 267/114 ms **성공** | ✗ Python 으로 충분 |

**결론**: 정밀도 향상 자체는 대체로 Rust 문제가 아니다(모델링·데이터·솔버선택).
Rust 가 진짜 푸는 것은 (a) ODE RHS 안 비싼 적분, (b) 다준위 재결합(강성 대규모),
(c) 대량 스캔 — 세 가지뿐. 그래서 **A티어(물리)를 Python 으로 먼저, B티어만 Rust**.

### 완료 — A1: g_*(T) 격자QCD 표 교체
구표(18점)에 **실제 오류**가 있었다:
```
T=1 GeV   : 구 106.75 → 참 76.34   (40% 과대!)  ← WIMP 동결이 놓이는 구간
T=200 keV : 구 3.909  → 참 7.66    (2배 과소)   ← e± 소멸 구간
T=100 GeV : 구 106.75 → 참 103.53
```
Husdal 2016 (Galaxies 4, 78; arXiv:1609.04979) Table A1 **36점**으로 교체.
기존 시험 2건이 구 **근사**(1 TeV=106.75)를 박제하고 있어 정정 — 106.75 는 10 TeV
점근값이고 1 TeV 참값은 106.72. QCD 구간 회귀 가드 시험 추가.

### 완료 — A2: 동결 Boltzmann 복원 (해석식 대체)
이전 후퇴 사유("강성이라 불가")는 **오진**이었다 — LSODA 선택 실수.
Radau/BDF 로 Python 에서 그대로 풀린다. `freeze_out_boltzmann()` 추가.
```
Boltzmann(Radau) Ωh² = 0.0795   해석식 0.0805 (1.3% 일치)   WIMP miracle ~0.1 ✓
Radau ≡ BDF (1e-4),  Ωh² ∝ 1/⟨σv⟩,  이방 boost 2.0 → 0.154 (잔존 증가)
```
★ 구현 중 λ 계수 5× 오류를 오라클 대조로 검출·수정:
  λ = (2π²/45)/1.66 · (g_*s/√g_*) M_Pl m ⟨σv⟩ = 0.2642(…)  — Kolb-Turner 1/3.79 와 일치.

### 상태
```
pytest 285 passed (fast; A1/A2 신규 6)
```
### 다음 (REMAINING-DAG-v3.md)
C1 일반유형 조석 codegen (VII_h — 현재 Rust 경로는 대각 I 만!) → A3 r_s → B1/B2/B3 Rust 열역학

---

## v3.0 진행 6차 (2026-07-29) — A3 r_s 물리 개선 (θ_* 부채 해소)

### 진단: 2% 오차의 정체는 "반해석적 한계"가 아니라 **적분 절단**이었다
`sound_horizon` 이 z_max=1e5 에서 잘랐는데, 복사기 꼬리가 2.7 Mpc(≈2%)나 된다.
z ≫ z_eq 에서 R→0, H→H₀√Ω_r(1+z)² 이므로 꼬리는 **해석적으로 닫힌다**:
```
∫_{z_max}^∞ c_s/H dz = (c/√3)/(H₀√Ω_r)/(1+z_max)
   z_max=1e5 → 2.673 Mpc (수치적분 2.646 과 일치)
```

### ★ 함정 발견: 기존 "0.5% 일치"는 두 오차의 상쇄였다
이전 기본값 Eisenstein-Hu `drag_redshift`(≈1021)는 참값(1060)보다 낮아 적분 구간을
넓혀 **빠진 꼬리를 우연히 벌충**했다 → 147.8 (0.5% 처럼 보임).  꼬리를 고치자 그
조합은 +2.3% 로 어긋난다.  따라서 z_drag 도 자기일관 값으로 교체.

### 수정
- `sound_horizon(..., include_tail=True)` — 복사기 해석 꼬리 추가
- `sound_horizon_tail(z_max, ...)` — 꼬리 닫힌형 (한계: 이른 시기 g_* 증가분 <0.2%)
- `drag_redshift_tau(...)` — **τ_drag=1 자기일관** z_drag (우리 Peebles x_e 사용)

### 결과
| 양 | 구 | 신 | Planck | 개선 |
|---|---|---|---|---|
| **100θ_*** | 1.021 (−2%) | **1.04006 (−0.10%)** | 1.04109 | **20×** |
| r_s(z_*) | 141.55 (−2.0%) | **144.23 (−0.14%)** | 144.43 | 14× |
| r_drag | 147.8 (상쇄된 우연) | 146.14 (−0.64%) | 147.09 | 자기일관 |
| z_drag | 1020.9 (E-H) | 1068.0 (τ=1) | 1059.94 | 자기일관 |

잔여 0.1~0.6% 는 3-준위 Peebles 재결합의 한계 — **B2(다준위)의 표적**이다.

### 상태
```
pytest 292 passed (fast; A3 신규 2, 기존 2건 강화)
```
### 다음
B1 (Rust 열역학 커널) → B2 (다준위 재결합) / B3 (이방 스캔) → C2 → D → E

---

## v3.0 진행 10차 (2026-07-29) — B2a 재적용 + B2b 표적 재확정 (구조 진단)

세션 간 업로드가 B2a 이전 스냅샷이어서 B2a(헬륨 + H(z) 복사항)를 재적용.

### ★ 구조적 발견 — B2b 의 표적이 바뀌었다
"3-준위 모형 한계" 를 막연히 지목하던 것을 **정량 진단**으로 좁혔다.

Peebles 억제인자 C = Λ/(Λ+β) 에서 β ∝ α_B 이므로, β ≫ Λ 인 영역에서
**C·α_B ≈ Λ·α_B/β 는 α_B 와 무관**해진다.  실측 β/Λ:
```
z=1400 → 635      z=1200 → 157      z=1100 → 51      z=900 → 2.3
C      1.6e-3           6.3e-3           1.9e-2          3.1e-1
```
즉 재결합 **급강하 구간은 탈출률 (Λ_2γ + Λ_α) 지배**이고 재결합계수는 무관하다.

**관측 가능한 귀결 (확인함)**: RECFAST fudge F 를 0.7→1.3 으로 바꿔도
x_e(1100) 는 0.1443→0.1435, z_* 는 **1079.3 불변**.  fudge 가 급강하 구간에서
효과가 없다 (꼬리 z≲900 에서만 작동 — 거기서는 우리 x_e/RECFAST ≈ 1.01 로 이미 일치).

**따라서 B2b 의 표적은 α_B 정밀화가 아니라 Lyα 복사전달 (Λ_α)** 이다.
Λ_2γ = 8.2245809 s⁻¹ 는 잘 측정된 원자상수이므로 남는 자유도는 Λ_α 뿐이며,
이는 HyRec/CosmoRec 의 주요 개선 항목(2광자 보정·되먹임·확산)과 정확히 일치한다.

산출물: `peebles_rate_regime()` 진단 함수 + 시험 2건으로 이 성질을 박제
(구조가 깨지면 B2b 논거가 흔들리므로 회귀 가드).

### 상태
```
pytest 307 passed   cargo test 14 passed
```

---

## v3.0 진행 11차 (2026-07-30) — H0 완료 + **H1-a** (Lewis-Challinor 계층 착수)

출처: Lewis & Challinor, PRD 66, 023531 (astro-ph/0203507v2).  사용자가 TeX 소스를
제공해 **식 (12) 원문 확보**.  계획서 `PLAN-H-boltzmann-hierarchy.md`.

### H0 (완료) — 전사·축약·자체검증
- 부호규약 **(+---)** 확인 (소스 247행), `A_a = u̇_a` = **가속도** (구조상수 a_a 아님)
- 균질 축약: 공간미분항은 **Bianchi I 에서 정확히 0**, 일반유형에서는 미분→3-접속 대수항
- **자체검증 1**: l=0,i=0 → `ρ̇ + 3H(ρ+p) ∓ π·σ = 0` (우리 검증식과 σ↔π 부호만 반대)
- **자체검증 2**: l=2,i=0 → `(2/15)[J^(2) − 5J^(1)] = −(8/15)ρ` (Misner 계수)

### H1-a (완료) — 상태 표현·초기조건
`matter/hierarchy.py`:

**★ 핵심 발견 — 정규화 인자가 상쇄된다.**  논문 식(10)의 `4π(−2)^l(l!)²/(2l+1)!` 가
PSTF 역변환 인자와 **정확히 상쇄**되어, F_{A_l} 을 거치지 않고 직접 모멘트로 쓸 수 있다:
```
J^(i)_{A_l} = ∫ dλ dΩ  λ² E (λ/E)^n  e_<A_l>  f          (계수 정확히 1)
```
구현이 크게 단순해졌고, 논문이 "the numerical factor is introduced so that … are
simply ρ=J^(0), q_a=J^(0)_a, π_ab=J^(0)_ab, p=⅓J^(1)" 라 적은 이유가 확인됐다.

**PSTF 사영**: 반복 대각합 제거는 **l≥4 에서 수렴 실패**(이중 대각합 구조) → 대칭 rank-l
공간의 `PSTF ⊕ sym(δ⊗rank(l−2))` 직교분해를 이용한 **직교사영**으로 교체.
l=2..6 에서 대각합 잔여 ≤5e−15, 독립성분이 정확히 2l+1.

### 검증 (전부 freestream.py 정확 구적해 대조)
```
식(11) 대응:  ρ 비 = 1.000000000000     p 비 = 1.000000000000
              π_ab 최대|Δ| = 2.2e-13     q_a = 3.2e-15 (비틸트 ⇒ 0)
PSTF 차원:    l=2..6 → 5,7,9,11,13 = 2l+1 (정확)
무질량 i-무관: 잔차 0.00e+00  (l=0,2,4)
★ Misner σ-소스 계수 = -0.533333333333 = -8/15,  상대오차 0.00e+00
   (freestream 독립 측정 -0.532569 와 구적·차분 수준 일치)
```
−8/15 는 계수 확정에 쓰지 않은 **독립 게이트**다 — 전사·축약·구현이 옳다는 증거.

### 상태
```
pytest 335 passed (fast; H1-a 신규 28)   cargo test 14 passed
```
### 다음
**H1-b**: 식(12) 균질 축약을 (l,i) 격자 RHS 로 구현 + σ-결합 부호를 오라클로 확정
(`audit/h_hierarchy.py`).  이어 H1-c 게이트(정확 구적 궤적 대조) → H2 절단수렴 → H4 viscous 유도.

---

## v3.0 진행 12차 (2026-07-30) — **H1-b** 계층 RHS + σ-부호 오라클 확정

### 완료 — 식 (12) 균질 축약 RHS
`matter/hierarchy.py` 에 `hierarchy_rhs(J, H, σ, l, i)` 추가.  세 σ-결합 항군:
```
(A) l→l 지표교체 :  (l/(2l+3))[(2n+3)J^(i)_{a<A_{l-1}} + (2-2n)J^(i+1)_{a<A_{l-1}}] σ_{a_l>}^a
(B) l+2→l 이중축약:  [(l-n)J^(i-1)_{abA_l} + (n-1)J^(i)_{abA_l}] σ^{ab}
(C) l-2→l 외적    :  (l(l-1)/(4l²-1))[(n-1)J^(i+2)_{<A_{l-2}} - (l+n+1)J^(i+1)_{<A_{l-2}}] σ_{a_{l-1}a_l>}
```

### ★ σ-부호를 오라클로 확정 — 유일성 마진 **1.5×10⁶ 배**
논문 (+---) ↔ 우리 (-+++) 의 부호 자유도를 추상지표로 단정하지 않고, **정확 구적해의
수치 시간미분**(Bianchi I 은 p_i=const 이므로 임의 시각의 J 를 정확히 계산 가능)과
8가지 조합을 대조:
```
A=+1 B=-1 C=-1  →  1.16e-09   ★ 확정
나머지 7 조합    →  ≥ 1.71e-03   (최소 1.5e6 배 열등)
```
PLAN-H §0b 의 해석 예측(B·C 반전, A 불변)과 **일치**.  `audit/h_hierarchy.py` 로 박제.

### 검증
```
dt 수렴:   5.81e-06 (dt=1e-3) → 5.82e-08 (1e-4) → 1.36e-09 (1e-5)   정확히 O(dt²)
           ⇒ 잔차는 모델오차가 아니라 차분오차
스윕 (81 조합: 질량 0/0.8/4, 무작위 배경 3종, (l,i) 9종, 분포 2종):
   방향의존 f₀ 전체 (l,i)  : 5.97e-09
   등방 f₀ 짝수 l          : 1.15e-08
   등방 f₀ 홀수 l |J|/ρ    : 5.06e-17  (항등적 0)
l=0,i=0 → EMT 보존 `ρ̇+3H(ρ+p)+σ·π=0` 재생산 (1e-10 내)
등방 배경(σ=0) → π_ab RHS = 0
```

### 구현 노트 2건
1. **방향의존 f₀ 가 필요했다.** 등방 f₀ 는 f(p)=f(−p) 라서 **홀수 l 다극이 항등적으로 0**
   (실측 |J_a|~3e−15) → (A) 항군과 q_a 섹터가 시험되지 않는다.  Vlasov 는 f=f₀(불변기저
   p_i) 면 임의 방향의존을 허용하므로 `f_dipole()` 을 추가해 홀수 l 을 켰다 (|J_a|=8.0).
   이로써 세 항군 모두 독립 판정.
2. **잔차 지표에 물리적 스케일 하한.** 등방 홀수 l 은 0/0 이라 상대잔차가 1.0 으로
   허위 실패했다 → 주도 규모 H·ρ 의 1e−8 을 하한으로 (배정밀도+구적에서 그 이하는
   분해 불가라는 정직한 선언).

### 상태
```
pytest 356 passed (fast; H1 누적 49)   cargo test 14 passed
```
### 다음
**H1-c/H2**: 절단·닫힘 규칙 + l_max 수렴, 그리고 계층을 실제로 **적분**해 정확 구적
궤적과 대조.  이후 H4(viscous 유도) → H3(충돌항) → H5(tilted).

---

## v3.0 진행 13차 (2026-07-30) — **H2** 절단·적분 + **Rust 이식** (구적·계층)

### H2 (완료) — 절단·닫힘과 궤적 검증
`matter/hierarchy.py` 에 닫힘·RK4 적분 추가.

**★ 구조적 발견 — 계층은 i 아래로 스스로 닫힌다.**  (B) 항의 J^(i−1) 계수가
`(l−n) = l−(l+2i) = **−2i**` 이므로 **i=0 에서 정확히 0**.  즉 i=0 방정식은 J^(−1) 을
참조하지 않아 아래로 무한 퇴행이 없다 (i_min=0 으로 충분).

**★ i 위쪽 닫힘은 기하 외삽이어야 한다.**  0-절단은 **무질량에서 망가진다** —
(λ/E)=1 이라 J^(i) 가 i 무관인데 0 으로 자르면 그 성질이 깨진다.  측정:
```
무질량 ρ 오차:  0-절단 1.10e-02   vs   기하외삽 1.63e-09     (7자리 차이)
```

**수렴 (정확 구적 궤적 대조)**:
```
무질량 l_max:  2 → π 4.92e-05    4 → 8.16e-09    6 → 6.91e-09   ⇒ l_max=4 충분
유질량 i_max:  1 → ρ 1.8e-03  2 → 2.3e-04  3 → 4.2e-05  4 → 9.5e-06  (진짜 2D 계층)
```
유질량이 i_max 를 요구하는 것은 물리적 사실이다 (λ/E<1 이라 속도가중 방향에 구조가 있음).

### Rust 이식 (H6 조기 완료) — 구적 + 계층
`_rustcore/src/kinetic/{pstf,quad,hierarchy}.rs` + `scripts/codegen_pstf.py`.
- **PSTF 사영**: 직교분해 데이터(A_l, S_l)를 codegen (l≤4, 1152 floats).
  l≤4 제한 근거는 위 l_max 수렴 측정 — 추측이 아니라 데이터.
- **구적**: freestream 격자(GL 반경 + GL·균일 각도)를 그대로 포트.  `thermo/fd.rs` 의
  Gauss-Legendre 노드 생성기를 재사용.
- **계층**: RHS 3항군 + 닫힘 + RK4, Rayon 병렬 ((l,i) 및 구적 반경축).

**차등테스트 17건** (ρ 로 정규화 — 등방 홀수 l 이 항등적 0 이라 상대오차 무의미):
```
J^(i)_{A_l} (l=0..4, i=0..2, 등방/쌍극)  ≤ 1e-12
freestream moments (ρ, p, π_ab)          ≤ 1e-12
PSTF 사영 (l=2,3,4)                      ≤ 1e-10
계층 궤적 ρ(t), π_ab(t)                  1.8e-14 / 1.4e-12
Rust 단독 물리게이트: 궤적 = 정확구적    < 1e-6
```

### 성능 — **정직하게: 광선추적만큼 극적이지 않다**
```
J_moment(l=4)   : Python 71.0 ms → Rust  8.98 ms   ( 7.9x)
freestream mom  : Python  7.1 ms → Rust  2.37 ms   ( 3.0x)
계층 적분(40st) : Python 2014 ms → Rust   132 ms   (15.3x)
긴 적분(2000st) : Rust 4.6 s  (Python 추정 ~1 분)
```
**왜 12,000× 가 아닌가**: 구적은 numpy 가 이미 큰 einsum 으로 잘 벡터화한다 —
R1 광선추적처럼 스텝당 스칼라 오버헤드가 지배하는 구조가 아니다.  계층 적분이 15×
나오는 것은 Python 쪽 dict·소형배열 오버헤드 때문이다.
(1차 구현은 2× 였는데, 내부 루프의 지표 분해를 테이블화 + Rayon 으로 7.9× 로 개선.)

### 상태
```
pytest 381 passed (H1·H2 누적 57, 차등 kinetic 17)   cargo test 22 passed
```
### 다음
H4 (viscous 를 계층 l=2 절단으로 유도) → H3 (Thomson 충돌항) → H5 (tilted 합동).

---

## v3.0 진행 14차 (2026-07-30) — **H4** imperfect fluid 를 유도 (두 경로 분리)

### 결과 — Eckart/IS 가 계층 l=2 절단의 귀결임을 보였다
`matter/viscous_derived.py`.  `viscous.py` 는 η, ζ, τ_π 를 **자유 파라미터**로 받았는데,
이제 계층에서 **유도**된다:
```
σ=0 :  π̇_ab = -4H π_ab                 ⇒  1/τ_π = 4H   ⇒  τ_π = 1/(4H)
π=0 :  π̇_ab = -(8/15) ρ σ_ab            ⇒  2η/τ_π = (8/15)ρ  ⇒  η = ρ/(15H)
Eckart 극한(π̇→0) 도 같은 η:  0 = -4Hπ-(8/15)ρσ ⇒ π = -(2/15)ρσ/H = -2ησ ✓
```
**자유 파라미터가 사라졌다** — η, τ_π 가 분포함수에서 나온다.

### 두 경로를 **분리**해 측정하고 교차검증 (사용자 요청)
| 경로 | 방법 | 무질량 감쇠 | 무질량 소스 |
|---|---|---|---|
| **A · freestream** | 정확 구적의 유한차분.  **계층 수식 미사용** | −4.000000 | −0.532569 |
| **B · PSTF 계층** | 식(12) l=2 평가.  손세팅 판은 **구적 미사용** | **−4.000000000** (정확) | **−8/15** (정확) |

```
교차검증 상대차 (m=0/0.5/1/4):
  감쇠  2.3e-09 / 1.9e-09 / 2.6e-09 / 2.6e-10     ← σ=0 에서 방정식이 절단 없이 정확
  소스  1.4e-03 / 1.4e-03 / 1.5e-03 / 1.7e-03     ← 구적 급작응답의 차분 한계
```

### 구조적 사실 2건
1. **σ=0 이면 l=2 방정식이 절단 없이 정확히 닫힌다** — (A),(B),(C) 세 σ-결합 항군이
   모두 σ 에 비례하므로.  무질량은 추가로 J^(1)_ab = π_ab (i-무관) 이라
   π̇ = −H[5π−π] = −4Hπ 가 **정확**하다.  두 경로가 −4.000000 으로 일치하는 이유.
   (시험: l=4 를 넣든 빼든 감쇠율 불변 <1e−14)
2. **유질량은 τ_π 가 스칼라가 아니다** — J^(1)_ab ≠ π_ab 이므로 감쇠율이 커지고
   **성분마다 다르다**.  측정 1/τ_π = 4.012H (m=0.5), 4.044H (m=1), 4.345H (m=4);
   이방성 >1e−3.  정직하게 평균과 이방성을 함께 보고한다.

### 구현 노트 (버그 1건)
경로 B 의 초기 판은 기본 `a_vec=(1,1,1)` 등방이라 π_ab≡0 이 되고, 재스케일 로직이
쓰레기를 만들어 m=1 에서 23% 불일치가 났다.  등방 a_vec 을 **예외로 거부**하고
경로 A 와 같은 비등방 상태에서 비교하도록 수정 → 전 질량에서 1e−9 일치.

### 상태
```
pytest 402 passed (H4 신규 21)   cargo test 22 passed
```
### 다음
H3 (Thomson 충돌항) → H5 (tilted 합동: ω, u̇) → H4 의 Rust 이식(계수는 이미 확정).

---

## v3.0 진행 15차 (2026-07-30) — **H3** Thomson 충돌항 + **H3/H4 Rust 이식**

### H3 — 무충돌(Vlasov) 계층을 진짜 Boltzmann 계층으로
`matter/collision.py`.  Lewis-Challinor 는 암흑물질(무충돌)을 다루며 *"In the case of
collisional matter the right hand side would contain a collision term"* 이라고만 적는다.
그 충돌항을 Thomson 산란에 대해 구성했다:
```
C[J^(i)_{A_l}] = -(n_e σ_T)(1 - λ_l) J^(i)_{A_l}
```
★ 비편광 Thomson 은 전자 정지틀에서 **탄성**이라 광자 에너지를 바꾸지 않고 각분포만
등방화한다 ⇒ 충돌항이 (l,i) 중 **l 에만** 의존한다 (i = 속도가중에 무관).

### 정직한 한계 — 구적 오라클이 충돌항을 검증할 수 **없다**
H1·H2·H4 의 안전망이던 `freestream.py` 정확 구적은 여기서 쓸 수 없다: 충돌이
자유흐름 특성곡선(불변기저 p_i = const)을 깨뜨려 구적의 정확성 근거가 사라진다.
그래서 H3 의 두 경로는 H4 와 **다른 방식으로** 나눴다.

| 경로 | 방법 | 쓰지 않는 것 |
|---|---|---|
| **A · 수치 위상함수** | K̃(μ)=(3/8)(1+μ²) 를 GL 격자에서 직접 적분해 λ_l 측정 | 다극 대수 |
| **B · 해석 다극** | 1+μ² = 4/3 + (2/3)P₂ 에서 λ_l = 4π k_l/(2l+1) | 적분 |

```
  l   경로A(수치적분)   경로B(해석)     감쇠(1-λ)
  0     1.000000000    1.000000    +0.000000   ← 광자 수 보존
  1     0.000000000    0.000000    +1.000000
  2     0.100000000    0.100000    +0.900000   ← 9/10
  3     0.000000000    0.000000    +1.000000
  4    -0.000000000    0.000000    +1.000000
  최대 상대차 = 8.75e-15
```

### 유도 Thomson 점성 — 9/10 인자를 단정하지 않고 노출
```
1/τ_π = 4H + (1-λ₂) n_eσ_T,   2η/τ_π = (8/15)ρ  ⇒  η = (4/15)ρ τ_π
9/10 포함 :  η·n_eσ_T/ρ = 0.296296296 = 8/27
9/10 미포함:  η·n_eσ_T/ρ = 0.266666667 = 4/15   ← 문헌 통용값
비 = 정확히 10/9  ⇒  **차이의 전부가 9/10 인자**
```
어느 쪽이 "맞다"고 단정하지 않고 `include_thomson_9_10` 플래그로 노출한다
(문헌 관례가 갈리는 지점).

### 물리 게이트
```
자유흐름 극한 (n_eσ_T=0) 잔차 = 0.00e+00        ← 정확히 0 (H1/H2 회귀 보호)
긴밀결합 π/ρ:  rate 1e2 → 2.84e-4
                    1e3 → 2.95e-5
                    1e4 → 2.96e-6               ← 깨끗한 1/rate (완전유체화)
```

### H3/H4 Rust 이식 — 두 경로를 **양쪽 언어에서 각각** 유지
`_rustcore/src/kinetic/collision.rs`, `viscous.rs`.  단순 포팅이 아니다:
Rust 경로 A 는 **3항 점화** P_l, Python 은 `numpy legval` — 서로 다른 구현이라
차등테스트가 포팅 확인이 아니라 **독립 검산**이 된다.

| 항목 | Python | Rust | 배속 |
|---|---|---|---|
| H2 무충돌 계층 RK4 (40 스텝) | 1787 ms | 148 ms | **12.1×** |
| H3 충돌 계층 RK4 (200 스텝) | (폴백 없음, 추정 8936 ms) | 447 ms | ~20× |
| H4 교차검증 (경로 A+B, 4 질량) | 1437 ms | 558 ms | 2.6× |
| H3 λ_l 경로 A (l=0..7) | 47.97 ms | 3.35 ms | **14.3×** |
| 긴밀결합 게이트 스캔 (20점) | 10616 ms | 1325 ms | 8.0× |

### 발견 1건 — 경로 A 차등테스트 허용오차의 **정직한 근거**
경로 A 감쇠율의 Rust↔Python 불일치가 1e−9 문턱을 넘었다 (7.7e−9).  원인 추적:
```
구적 자체의 일치도       |Δπ|/ρ = 1.5e-14      ← 실제 패리티 수준
차분 증폭계수           1/(2·dt·|π|/ρ) ≈ 8e7   (dt=1e-5, |π|/ρ=6.2e-3)
⇒ 예상 불일치 ≈ 8e-7 이하.  측정 7.7e-9 은 범위 안.
dt 스캔:  1e-3 → 1.1e-10,  1e-5 → 3.1e-8,  1e-7 → 2.7e-6   (약 1/dt)
```
**계수 오류가 아니라 유한차분 조건수**다 — 불일치가 1/dt 로 커진다는 것이 그 증거
(계수 오류라면 dt 무관한 하한이 남는다).  허용오차를 조용히 늘리지 않고
`test_kin_route_a_damping_is_roundoff_limited` 로 **그 기제 자체를 시험으로 못 박았다**.
정밀 대조는 구적 층에서 이미 1e−12 로 이뤄져 있다.

### 통합
`bianchi/backend.py` 에 운동론 디스패치 추가 (H2/H3/H4 전부, 모두 `force_python` 지원).
★ 충돌 궤적은 Python 폴백을 **만들지 않고** `NotImplementedError` 로 명시한다 —
조용히 무충돌로 떨어지면 사용자가 충돌을 켰다고 착각하기 때문
(`test_backend_collisional_python_fallback_is_explicit` 로 고정).

### 상태
```
pytest 491 passed (직전 402 → H3 파이썬 +25, 차등테스트 +47, 백엔드 +17)
cargo test 44 passed (신규 22:  collision 11 + viscous 11)
차등테스트 파일 102개 / 백엔드 통합 25개 / H3 25개
```
### 다음
H5 (tilted 합동: on-shell ω_ab, u̇_a 주입) → B2b (Lyα 복사전달 Λ_α) → C2 (남은 차트 RHS).

---

## v3.0 진행 16차 (2026-07-30) — **H5-a·H5-b** tilted 부호를 보존법칙 오라클로 확정

### 0. 사고 1건 — 컨테이너 회수로 작업트리 소실, 스냅샷으로 복구
턴 사이에 세션 컨테이너가 회수되어 `/home/claude` 가 비었다.  트랜스크립트는 남았으나
직전 턴 분량(파일 3개)뿐이었고, 나머지는 **직전에 전달한 `bianchi-h3.tar.gz` 가
유일한 완전본**이었다 (사용자 재업로드로 복구).  교훈: 증분마다 전체 스냅샷을
전달하는 관행이 실제로 프로젝트를 구했다.  재구성 비용: 의존성 재설치 + `maturin`
재빌드 약 5분.

### 1. 식 (12) 전문 확보 — 지금까지 버려온 세 항
`refs_neutrinos_LewisChallinor.tex` 에서 원문 전사.  법선합동 가정으로 죽였던 항들:
```
+ D^a J^(i)_{aA_l}                                                (div-con)
− (l/(2l+1)) D_{⟨a_l} J^(i+1)_{A_{l−1}⟩}                          (div-free)
− l J^(i)_{a⟨A_{l−1}} ω_{a_l⟩}{}^a                                (Ω)   회전
+ (l/(2l+1))[(l+n+1)J^(i)_{⟨A_{l−1}} + (2−n)J^(i+1)_{⟨A_{l−1}}] u̇_{a_l⟩}  (D) 가속도
+ [(l−n)J^(i−1)_{aA_l} + (n−2)J^(i)_{aA_l}] u̇^a                   (E)   가속도
```
★ 논문의 `A_a` 는 **가속도**다 — Wainwright-Ellis 의 `A_a`(class B 구조상수)와 다른 양.
  코드에서는 `u_dot` 으로 쓴다 (계획 위험표에 있던 중의성).

### 2. ★ 핵심 — 부호를 논문 전사가 아니라 **보존법칙으로** 확정
구조적 사실을 먼저 찾았다: 식 (12) 의 **(l,i)=(0,0) 과 (1,0) 은 T^{μν} 변수만으로
닫힌다**.  l=1 에서 (1−n)J^(1)_a 계수가 0 이고 (B) 는 (l−n)=(n−1)=0 이라 **완전히
소멸**하기 때문이다.  즉 그 두 방정식이 곧 에너지·운동량 보존이다.

⇒ tilted Bianchi I 계량에서 ∇_μT^{μν}=0 을 **독립 유도**(sympy, 제트기호+lambdify)해
   무작위 표본 최소제곱 + 전수 마진으로 부호를 풀었다.

| 항 | 부호 | |
|---|---|---|
| (A) σ 1축약 | **+1** | ← **통제군**: H1 정확구적 오라클과 일치 |
| (B) σ 2축약 | **−1** | ← **통제군**: H1 과 일치 |
| (Ω) ω | **+1** | 신규 |
| (D) u̇ 자유첨자 | **+1** | 신규 |
| (E) u̇ 축약 | **−1** | 신규 |
| (div-con) | **+1** | 신규 |
| (div-free) | **−1** | 신규 |

```
최소제곱해 전부 정확히 ±1 (|편차| < 1e−15)
잔차 절대 1.33e−15 / 상대 7.68e−16
유일성 마진 (2^7 = 128 조합 전수)  1.35e+14 배
사틀 정규직교성 잔차 4.44e−16
통제군 재생산 ✓ — seed 1/2/7 에서도 동일
```
★ **통제군 설계가 이 결과의 근거다**: 이미 확정된 두 부호를 **다른 방법**으로
  재생산했으므로 같은 절차가 준 신규 5개도 신뢰한다.  깨졌다면 채택하지 않았다.

### 3. 정정 1 — tilted 에서 공간미분은 **살아난다** (기존 전제 오류)
"균질성으로 D_a 가 죽는다"는 **법선합동에서만** 참이다.  균질성은 법선 합동의
초곡면에 대한 것이고 tilted 관측자의 초곡면은 다르다:
```
D_A ρ = E_A^μ∂_μρ = E_A^t ρ̇ = γ v_A ρ̇ ≠ 0     (순수 boost 사틀에서 E_A^t = γv_A, 정확)
```
발견 경로가 중요하다: 이 항을 빼고 맞추려 했을 때 최소제곱해가 s_A=+1.11, s_B=−0.88
로 어긋나고 잔차 0.40 이 남았다.  **±1 에서 벗어난 것을 "수치오차"로 넘기지 않은 것**이
누락 항을 찾아줬다.  항을 넣자 잔차가 1e−15 로 떨어졌다.

### 4. 정정 2 — tilted 계층은 **음함수(질량행렬) ODE** 다 (구현 설계 변경)
D 항이 이웃 l 의 시간미분을 품는다:
```
∂(D^a q_a)/∂q̇_A  →  v_A        (v→0, 해석 확인)
```
⇒ `hierarchy_rhs` 처럼 dJ/dt 를 바로 반환하는 명시적 RHS 로는 **구현할 수 없다**.
   M(v)·J̇ = F 를 풀어야 한다.  H5-c 설계를 이에 맞춰 바꿨다.
   ★ Rust 쪽 diffsol 이 질량행렬 ODE 를 지원하므로 이식 단계에서는 오히려 유리하다.

### 5. 구조적 발견 — 축정렬 tilt 는 ω 를 켜지 못한다
```
다축 tilt   max|ω_AB| = 4.77e−02   max|u̇_A| = 4.67e−01
단일축 tilt max|ω_AB| = 0.00e+00   max|u̇_A| = 3.40e−01
단일축 표본만 쓴 선형계 rank = 6/7  →  s_Ω 미결정
```
비등방 주축과 boost 방향이 정렬되면 D_au_b 의 반대칭부가 사라진다.
**(Ω) 시험에는 다축 tilt 필수** — 모르고 단일축만 쓰면 s_Ω 을 확정했다고 착각한다.
가속도는 단일축에서도 켜지므로 (D),(E) 는 단일축으로 충분하다.

### 6. 정직한 한계
보존법칙 오라클은 **l ≤ 1 만** 통제한다.  l ≥ 2 의 (Ω),(D),(E),(div) 계수는 아직
독립 검증되지 않았다 — H5-d 의 boosted 정확구적 오라클이 필요하다.
(C) 항은 l≥2 전용이라 이 오라클 범위 밖이며, H1 확정값을 그대로 쓴다.

### 상태
```
pytest 503 passed (H5 신규 12)   cargo test 44 passed
신규: audit/h5_tilted_conservation.py, tests/test_h5_tilted_signs.py, PLAN-H5-tilted.md
```
### 다음
H5-c (질량행렬 조립: `matter/tilted.py`, M(v)·J̇=F, v→0 에서 M→I) →
H5-d (l≥2 boosted 구적 오라클) → H5-e (Rust, diffsol 질량행렬).

---

## v3.0 진행 17차 (2026-07-30) — **H5-c** tilted 질량행렬, 닫힌형 발견

### 1. 결과 — l≤1 블록의 질량행렬은 해석적이다
`matter/tilted.py`.  §16차에서 tilted 계층이 음함수 ODE 임을 확인했고, 이번에 조립했다.

```
M = γ (I + e₀ vᵀ)          M⁻¹ = γ⁻¹ (I − e₀ vᵀ)          ← 상삼각
l=0 행:  γ(ρ̇ + v·q̇)   ← ⊥ρ̇ = γρ̇ 와 D^aq_a 의 γv_B q̇_B 가 합쳐진 것
l=1 행:  γ q̇_A 뿐      ← D^bπ_ba 는 π̇ (블록 밖) 로 가므로 q̇ 결합 없음
```
따라서 이 블록은 **선형해가 필요 없다**.  조건수도 온순하다:
```
|v|   0.0    0.1    0.3    0.5    0.7    0.9
cond  1.000  1.105  1.348  1.640  1.987  2.392
max|ω| 0     1.6e−3 5.5e−3 1.1e−2 2.4e−2 8.4e−2
```
극단 tilt (|v|→1) 에서도 질량행렬 자체는 병적이지 않다 — 유효범위를 조용히 쓰지 않고
`tilt_scan()` 으로 측정해 보고한다.

### 2. ★ 방법론 — 질량행렬을 **손으로 옮겨 적지 않았다**
좌변이 미지 시간미분 (ρ̇, q̇_A) 에 대해 **정확히 아핀**이므로
```
F = −EQ(0),      M[:, c] = EQ(e_c) − EQ(0)      (5회 평가, 오차 0)
```
로 뽑는다.  그리고 **아핀성 자체를 시험으로 못 박았다**
(`EQ(2x)−EQ(0) = 2[EQ(x)−EQ(0)]`, 그리고 "스텝 h = 1, 1e−3, 1e3 에서 같은 열").
아핀이 아니면 이 추출은 유한차분 근사가 되어버리므로, 전제를 검증하지 않으면
정확성 주장이 근거를 잃는다.

### 3. ★ 독립 교차검증 — 두 모듈이 서로를 임포트하지 않는다
`tilted.py` 는 (Ω),(D),(E),(div) 항을 **조립**하고,
`audit/h5_tilted_conservation.py` 는 T^{μν} 의 **발산을 직접** 계산한다.
두 결과가 항등적으로 같음을 4 seed 에서 확인 (상대차 ≤ 1e−11).
★ 이것이 항등식임에 주의: 좌변이 0 이라는 게 아니라 ∇_μT^{μν} 사영과 **같다**는 것
  (off-shell).  그래서 무작위 도함수 표본으로 시험할 수 있다.

### 4. 성능 — 초기 판이 쓸 수 없을 만큼 느렸다 (정직하게)
방정식 전체를 하나의 sympy 식으로 세워 lambdify 한 첫 구현은
```
임포트 147.8 s      state() 호출 107 ms       (cse=True 로도 개선 없음)
```
였다.  원인은 `D^bπ_ba` 가 B×m×n×l = 192항 중첩합이고 그 위에 M 추출용 편미분이
얹혀 표현식이 조합폭발한 것.  **사틀·접속 같은 작은 닫힌형만 기호로 두고 지표축약을
numpy(einsum) 로** 옮겨 재작성:
```
임포트 0.48 s (308배)      호출 1.06 ms (101배)      결과는 비트 동일
```
공개 API 를 바꾸지 않아 기존 시험이 그대로 회귀 보호 역할을 했다.

### 5. 정직한 한계 — l ≥ 2 는 여전히 미검증
보존법칙 오라클은 l ≤ 1 만 통제한다.  일반 l 에서는 (div-con) 이 l+1, (div-free) 가
l−1 (그리고 i+1) 로 결합하므로 M 이 l 에 대해 **삼중대각**이 되고 상삼각성이 깨진다.
이 블록의 상삼각성은 l≤1 절단의 성질이며 일반 l 로 외삽하면 안 된다.
H5-d (boosted 정확구적 오라클) 가 필요하다.

### 상태
```
pytest 520 passed (H5-c 신규 17)   cargo test 44 passed
신규: bianchi/matter/tilted.py, tests/test_h5c_tilted_mass_matrix.py
```
### 다음
H5-d (l≥2 boosted 구적 오라클: 정확해를 boost 해 J′ 를 구적, 유한차분으로 검증.
★ q′_a 가 boost 만으로 켜지므로 인공 쌍극자 `f_dipole` 불필요) → H5-e (Rust, diffsol 질량행렬).

---

## v3.0 진행 18차 (2026-07-30) — **H5-d** boosted 정확구적 오라클, l ≥ 2 검증

### 0. 사고 재발 — 컨테이너 회수 (같은 날 두 번째)
업로드 디렉터리까지 비어 완전 손실.  사용자 재업로드로 복구.
**대응**: `bootstrap.sh` 를 저장소에 추가 — 압축 해제 후 한 명령으로 의존성·Rust
빌드·회귀 시험까지 복원한다 (`--fast` 로 Rust 재빌드 생략 가능).  다음 회수 때
턴을 통째로 날리지 않기 위한 고정 절차.

### 1. tilted 에도 정확 오라클이 있다
Bianchi I 정확해 f = f₀(q_i) 는 **법선** 프레임 것이고, tilted 관측자는 같은 f 를
다르게 분해할 뿐이다.  `d³P/E` 가 로런츠 불변이므로 `d³P′ = (E′/E)d³P` 이고,
**기존 격자 위에서 피적분함수만 바꾸면** tilted 모멘트가 정확히 나온다:
```
J′^(i)_{A_l} = ∫ (d³q/V)·(E′/E)·E′(λ′/E′)^n·e′_{⟨A_l⟩}·f₀(q)
E′ = γ(E − v·P),   P′^A = P^A + v_A[k(v·P) − γE],   k = γ²/(γ+1)
```

### 2. 게이트 — 계층을 경유하지 않는 것부터
| 게이트 | 측정 | 뜻 |
|---|---|---|
| λ′² = E′² − m² (격자 전점) | 1.3e−15 | boost 대수에 오타 없음 |
| v→0 환원 | **비트-정확** | 피적분함수가 문자 그대로 동일해짐 |
| **T^{μν} 프레임 무관성** | ρ 4e−16, q 3e−15, π 1e−14 | ★ boost+구적 동시 검증 |
| \|q′\|/ρ′ 닫힌형 v(ρ+p)/(ρ+pv²) | 1e−12 | 구적 미사용 독립 검산 |

★ 유도를 믿지 않고 측정했다: `boost_algebra_residual` 한 줄이 부호 오타를 전부 잡는다.

### 3. ★ 약속대로 — 인공 쌍극자가 필요 없다
H1 은 등방 f₀ 가 f(p)=f(−p) 라 홀수 l 이 항등적 0 (4e−17) 이어서 `f_dipole` 을
**인공 도입**해야 했다.  boost 는 **물리적** 쌍극자원이다:
```
v=0.25, 등방 f₀ + 등방 a_vec:  l=1 → 3.3e−1,  l=3 → 7.0e−3,  l=5 → 8.1e−5
같은 설정 법선 프레임:          l=1 → 4.1e−17 (항등적 0)
```
분포를 왜곡하지 않고 홀수 l 과 (A),(Ω),(D),(E) 를 시험한다.

### 4. ★★ l ≥ 2 잔차 — 절단 없이
잔차 시험은 이웃 (l±2, i−1…i+2) 을 **모두 구적으로 직접 공급**하므로 절단 오차가
섞이지 않는다.  L_MAX=5 로 l ≤ 3 을 시험:
```
(l,i)  (0,0)    (1,0)    (2,0)    (3,0)    (2,1)
잔차   9.5e−11  4.8e−11  1.5e−10  1.4e−11  1.5e−10        (무질량)
       9.7e−11  3.6e−11  4.7e−11  3.8e−11  1.7e−10        (m=1)
dt 스캔 (l=2): 1e−3→3.9e−8,  1e−5→1.5e−10,  1e−7→1.2e−8   ← U자 = 차분 한계
부호 유일성 마진: 5.8e5 배 (무질량) / 5.4e5 배 (m=1)
```
★ **두 독립 오라클이 일치**: H5-b 는 ∇_μT^{μν}=0 (l≤1), H5-d 는 boosted 정확구적 (l≤3).

### 5. 함정 2건 — 둘 다 실제로 밟았고 시험으로 고정했다
**(a) (E) 가 (l,i)=(2,0) 에서 항등적 0.**  n=2 가 (n−2) 를, i=0 이 (l−n)=−2i 를
동시에 죽인다.  하필 그 칸으로 스캔해 **마진 1.000** 이 나왔다 — 이는 실패가 아니라
**미결정**인데, 활성도 진단 없이 마진만 보면 오독한다.  `term_activity` 를 만들어
각 항을 꺼 보며 크기를 재고, 스캔을 여러 칸 합산으로 고쳤다 (→ 마진 5.8e5).

**(b) 무질량은 i 구조를 시험하지 못한다.**  J^(i) 가 i-무관(5.7e−14)이라 각 항군의
계수합이 n-무관으로 붕괴한다:
```
(A) (2n+3)+(2−2n) = 5      (D) (l+n+1)+(2−n) = l+3      (B) (l−n)+(n−1) = l−1
(E) (l−n)+(n−2)   = l−2    (C) (n−1)−(l+n+1) = −(l+2)
```
⇒ 무질량 잔차는 **l 구조만** 시험한다.  (E) 는 계수합이 (l−2) 라 무질량에서 **l=2
전체**가 죽는다 (i=1 도).  i 의존성 검증에 유질량이 필수임을 확인하고 m=1 로 재시험했다.

### 6. 사틀 회전항 — 함정이 실재함을 못 박았다
J′_{A_l}(t) 는 t-의존 사틀 성분이라 ⊥J̇ ≠ dJ′/dt.  `deup` 을 0 으로 두면 (=성분을
직접 미분하면) ⊥q̇ 가 실제로 틀린다는 것을 시험으로 고정했다
(`test_tetrad_rotation_term_is_not_negligible`).  H1 에서 프레임 회전 규약 때문에
부호 사고가 났던 지점과 같은 함정이다.

### 7. 정직한 한계
- l ≤ 3 까지만 절단 없이 시험된다 (L_MAX=5; l=4 는 rank-6 필요, pstf 100 ms/회).
- 잔차 바닥은 중앙차분 한계 ~1e−10 이다 (기계정밀도가 아니다).

### 8. ★ 결함 1건 — Rayon 축약이 **비트 재현성을 깨뜨리고 있었다**
전체 스위트에서 `test_kin_integrate_collisional_reduces_to_free_at_zero_rate` 가
실패했는데 **단독 실행은 통과**했다.  값을 직접 비교해도 차이가 0 이었다 → 순서 의존.

원인: `quad::j_moment` 이 Rayon `reduce` 로 부분합을 합치는데, reduce 는 **작업훔치기에
따라 축약 트리 모양이 실행마다 달라져** 부동소수 합산 순서가 바뀐다.  측정:
```
유휴 상태  200회 호출 → 서로 다른 비트패턴 1개
동시 부하  200회 호출 → 서로 다른 비트패턴 **2개**      ← 비결정적
```
즉 H3 턴의 "비트-정확" 주장은 **근거가 없었다** — 기계가 한가해서 통과한 것이다.

시험을 느슨하게 하는 대신 **원인을 고쳤다**: 부분합을 노드 순서대로 `collect` 한 뒤
순차 합산 (비용은 96×3^l 배열 하나).  물리 코드에서 "같은 입력 → 같은 출력" 은
지켜야 하는 성질이다.
```
수정 후  동시 부하 400회 → 비트패턴 1개,  무충돌 vs rate=0 충돌 30/30 비트-일치
```
재발 방지로 **성질 자체를 시험**했다 (`test_quadrature_is_bit_reproducible_under_load`,
동시 부하를 걸고 120회 호출의 비트패턴이 1개인지 확인 + 반복 비교판).

### 상태
```
pytest 573 passed (H5-d 신규 51 + 재현성 2)   cargo test 44 passed
신규: bianchi/matter/{tilted_moments,tilted_terms}.py,
      audit/h5d_tilted_residual.py, tests/test_h5d_{tilted_moments,residual}.py,
      bootstrap.sh
```
### 다음
H5-e (Rust 이식 — diffsol 질량행렬 ODE) → 그 다음 B2b (Lyα 복사전달) 또는 C2 (남은 차트).

---

## v3.0 진행 19차 (2026-07-31) — **H5-e1** tilted 구적 Rust 이식 + 잠복결함 2건

### 1. 무엇을 이식했나 (그리고 왜 이것부터인가)
H5-d 의 병목은 **boosted 구적**이다 (잔차 시험 파일 91 초의 대부분).  계층 ODE 적분은
일반 l 질량행렬이 아직 없어 시기상조이므로, 이식 순서를 **구적 먼저**로 잡았다.
`_rustcore/src/kinetic/quad.rs` 에 `j_moment_tilted`, `moments_tilted`,
`boost_shell_residual` 을 추가 (기존 격자·색인표·PSTF 재사용).

```
차등  tilted  l=0..5, i=−1..2, m=0/0.7/3.0   최대 |Δ|/ρ = 5.0e−15
차등  법선    l=0..5 (기존 경로 회귀)          최대 |Δ|/ρ = 4.4e−15
v=0 이면 `kin_j_moment` 와 **비트-정확**
boost 자기검증 λ′²=E′²−m² 격자 전점          1.1e−15
성능  moment_grid (l=0..5, i=−1..3, 30 모멘트)  1967 ms → 411 ms  = **4.8×**
```

### 2. ★ 잠복결함 (a) — Rust **panic 이 FFI 를 넘어왔다**
`kin_j_moment(l=5)` 가 `PanicException` 을 던졌다.  **내 신규 코드가 아니라 기존 결함**
이다 (tilted 판을 l=5 로 부르다 드러났을 뿐).  한계가 **세 곳에 흩어져** 있었다:
```
① pstf_gen 의 L_MAX = 4      → quad::index_table 범위
② pstf::basis 의 match 팔    → l=2,3,4 만
③ pstf::symmetrize 의 [0usize; 4]  고정 스택버퍼   ← 실제 panic 지점 (경계 밖 쓰기)
```
①만 올려도 ②③에서 막힌다 — 실제로 그렇게 겪었다.  조치:
- codegen 을 **L_MAX=5** 로 재생성 (6012 floats; H5-d 잔차가 l+2=5 를 요구하므로 필요)
- ②에 l=5 팔 추가, ③을 `MAX_RANK = 8` 로 교체
- **컴파일 시 어서션** `const _: () = assert!(G::L_MAX <= MAX_RANK);` 로 재발 차단
- 경계에서 `check_l()` 로 깨끗한 `ValueError` (panic 이 FFI 를 넘지 않게)

### 3. ★ 내 실수 (b) — 경계검증을 과하게 걸어 7개 시험을 깨뜨렸다
`kin_integrate` 에 `check_l(l_max + 2)` 를 걸었는데, 계층 RHS 의 (B) 항은
`l + 2 <= l_max` 로 **단순절단**되어 격자 밖 rank 를 건드리지 않는다.  필요한 한계는
`l_max` 자체였다.  기본 `l_max=4` 가 거부되어 궤적·충돌·백엔드 시험 7개가 실패했고,
회귀 스위트가 즉시 잡았다.  `check_l(l_max)` 로 정정.

### 4. 통합
`backend.j_moment_tilted / moments_tilted / boost_shell_residual` 추가 (전부
`force_python` 지원).  ★ **오라클 순수성**: `audit/h5d_tilted_residual.py` 는 계층을
시험하는 오라클이므로 **Python 경로를 기본**으로 유지한다.  디스패치는 생산 경로용이며
두 경로 일치는 차등테스트가 고정한다.

### 상태
```
pytest 588 passed (H5-e 신규 15: 차등 6 + 백엔드 9)   cargo test 49 passed (신규 5)
```
### 다음
H5-e2 (일반 l 질량행렬 → diffsol 질량행렬 ODE) → B2b (Lyα 복사전달) 또는 C2 (남은 차트).

---

## v3.0 진행 20차 (2026-07-31) — **H5-e2** 일반 l 질량행렬

### 1. 닫힌형
H5-c 의 l≤1 상삼각 구조는 절단의 성질이었다.  일반 l 에서는 (div-con) 이 l+1 로,
(div-free) 가 l−1 **그리고 i+1** 로 결합한다:
```
M[(l,i) ← (l,i)]     = γ · P
M[(l,i) ← (l+1,i)]   = s_dc · γ · P∘(v 를 첫 지표에 축약)
M[(l,i) ← (l−1,i+1)] = s_df · (−l/(2l+1)) · γ · P∘(· ⊗ v)
```
근거는 순수 boost 사틀의 **E_A^t = γ v_A** 다 (D_B 가 시간미분을 γv_B 배로 끌어온다).
`P` 는 PSTF 사영자 — `equation_lhs` 가 l ≥ 2 에서 출력을 사영하므로 M 도 그래야 한다.

### 2. ★ 두 경로 교차검증
경로 A = 위 닫힌형, 경로 B = `equation_lhs` 를 J̇ 단위벡터로 때린 **일반 아핀 추출**
(닫힌형 미사용).  두 모듈은 서로를 임포트하지 않는다.
```
닫힌형 vs 일반추출        9.5e−15
M 의 물질 무관성 (m=0 vs 1.5)   1.4e−14      ← J̇-선형부가 J 를 포함하지 않는다
v=0 → M = I               6.7e−16
l≤1 에서 H5-c 닫힌형 재생산   0.0e+00 (정확)
cond(M):  |v|=0.1 → 1.18,  0.5 → 2.38,  0.9 → 5.25
```

### 3. ★ 구조적 발견 — 상태공간은 3^l 이 아니다
초기 판은 3^l 저장공간에 그대로 M 을 세웠고 닫힌형이 일반추출과 **0.84 어긋났다**.
원인은 PSTF 사영이었고, 더 근본적으로는 상태공간을 잘못 잡은 것이었다:
```
rank(P) = 2l+1  (정확)   l=2: 9 중 5,  l=3: 27 중 7,  l=5: 243 중 11
```
3^l 좌표에서 M 은 대각합 방향이 영공간이라 **특이**하다 (det(P) = 0).
`np.linalg.solve` 가 조용히 쓰레기를 낼 자리였다.  물리 상태공간인 **2l+1 차원 PSTF
부분공간** 좌표로 다시 세웠다 (논문의 "2l+1 독립성분" 규약과도 일치).

### 4. ★★ 폐쇄 시험 — 정확 구적과 연결
```
정확 구적 J̇ 가 M·J̇ = F 를 만족       1.5e−10 (m=0) / 1.7e−10 (m=1)
역으로 M·J̇ = F 를 풀면 J̇ 복원        1.1e−10 / 1.3e−10
```
바닥은 H5-d 와 같은 **중앙차분 한계**다 (계수 오류가 아니다).

### 5. ★ 함정 — 격자 밖 J̇ 를 조용히 버리면 8자리 나빠진다
l=3 행은 J̇_{A_4} 를, i=1 행은 J̇^(2) 를 참조한다.  이들은 상태공간 밖이므로
**F(강제항)로 가야 한다**.  0 으로 버리면:
```
격자 밖을 F 로 넘김:  잔차 1.5e−10
격자 밖을 0 으로 버림: 잔차 5.5e−02        ← 8자리 악화
```
처음에 이걸 버리고 재보다 5.5e−2 를 얻어 "닫힌형이 틀렸나" 의심했다.  절단을
"그냥 0" 으로 두는 것이 무해하지 않다는 **정량적 증거**이며 시험으로 고정했다.

### 상태
```
pytest 608 passed (H5-e2 신규 20)   cargo test 49 passed
신규: bianchi/matter/tilted_mass.py, tests/test_h5e2_tilted_mass.py
```
### 다음
H5-e3 (diffsol 질량행렬 ODE 로 시간적분, Rust) → B2b (Lyα 복사전달) 또는 C2 (남은 차트).

---

## v3.0 진행 21차 (2026-07-31) — **H5-e3** tilted 계층 시간적분 (Python)

### 0. 리팩터 — 의존 방향 정정
식 (12) 좌변 조립을 `audit/h5d_tilted_residual.py` → `bianchi/matter/tilted_equation.py`
로 옮겼다.  적분기가 그것을 필요로 하는데 라이브러리가 audit 을 임포트하는 것은 거꾸로다.
audit 은 재수출만 하므로 기존 시험 API 는 그대로 (41개 통과로 무변경 확인).

### 1. 닫힘의 근거를 **가정하지 않고 측정**했다 (PLAN-H5d 위험표 항목)
```
close_i 근거(무질량 i-무관성)가 boost 후에도 성립하는가
  m=0    |J′^(i)|/|J′^(0)| = 1.0000, 1.0000, 1.0000   (l=0/2/3 전부)  ← 정확히 유지
  m=0.5  0.967, 0.938, 0.912        m=2  0.698, 0.531, 0.423          ← 기하급수 감쇠
이유: 무질량은 E′ = λ′ 이라 (λ′/E′)^n ≡ 1 → n(=i) 의존성 소멸.  boost 가 이를 깨지 않는다.

tilted 의 새 l+1 결합 절단오차 (기존 σ 의 l+2 절단과 비교)
  l=2:  l+1 절단 2.10e−4  vs  l+2(σ) 절단 7.32e−5      → 약 3배
  l=3:  1.64e−4          vs  4.50e−5
```

### 2. ★★ 종단 게이트 — 궤적을 정확 구적과 대조
H2 가 법선합동에 쓴 것과 같은 게이트.  초기조건도 정확 구적이므로 오차는 순전히
닫힘 + 시간적분에서 온다.
```
m=0, l_max=2, i_max=2, t_end=0.2:   rho 9.2e−5   q 7.7e−5   pi 2.6e−4
m=1, 같은 설정:                       비슷한 수준
```

### 3. ★★ 구조적 발견 — 지배 오차가 **i-닫힘**이다 (H2 와 반대)
```
i_max  1 → 2 → 3 → 4   :  5.3e−4 → 9.2e−5 → 2.1e−5 → 6.3e−6   (5.7×, 4.4×, 3.3× 개선)
l_max  2 → 3           :  5.271e−4 → 5.258e−4                  (거의 무변화)
nsteps 10 → 80         :  변화 없음                              (RK4 이미 수렴)
```
법선합동(H2)에서는 **l_max 가 한계**였다 (π 가 l_max=4 에서 8.2e−9 수렴).
tilted 는 **(div-free) 가 (l,i) → (l−1, i+1) 로 결합해 i 를 능동적으로 퍼뜨리므로**
i-닫힘이 한계가 된다.  ⇒ 다음 개선은 Rust 이식이 아니라 **i 방향 닫힘**이다.

★ 남은 오차를 "적분 정밀도" 로 오해하면 안 된다는 것도 시험으로 못 박았다
(`test_time_step_is_already_converged`) — dt 를 8배 줄여도 오차가 그대로다.

### 4. 오차가 tilt 크기와 함께 ~v² 로 커진다
```
|v| ~ 0.000 → rho 1.6e−7      ← 법선합동 수준
     0.038 → 6.1e−6
     0.095 → 3.8e−5
     0.190 → 1.3e−4
로그-로그 기울기 p = 1.79  (v² 에 가깝다)
```
v=0 에서 법선합동 정확도로 돌아가는 것이 tilt 가 원인임을 확정한다 (q 는 2.8e−18).

### 상태
```
pytest 623 passed (H5-e3 신규 15)   cargo test 49 passed
신규: bianchi/matter/{tilted_equation,tilted_integrate}.py,
      tests/test_h5e3_tilted_integrate.py
```
### 다음
**i 방향 닫힘 개선** (지배 오차) → 그 다음 Rust 이식 (diffsol 질량행렬).
이식을 먼저 하면 빨라지기만 하고 정확해지지 않는다.

---

## v3.0 진행 22차 (2026-07-31) — **문헌 기반 정확도 개선**: 무질량 1차원 환원 (ρ 6800배)

### 0. 문헌 조사부터 (사용자 요청)
`LITERATURE-moment-closure.md` 로 정리.  핵심은 **원논문이 이미 답을 적어놓았다**는 것:
Lewis & Challinor (astro-ph/0203507) 는 "For massless particles E=λ … the moments are
identical, J^(i)=J^(i′).  The momentum-integrated equations then **reduce to the usual
one-dimensional Boltzmann hierarchy**" 라고 명시한다.

### 1. ★★ 지배 오차의 정체 — "닫힘 부정확" 이 아니라 "축퇴 파괴"
H5-e3 에서 i-닫힘이 지배 오차임을 측정했는데, 무질량은 J^(i)=J^(0) 가 **정확**하므로
닫힘이 부정확할 이유가 없다.  이 모순을 추적해 기전을 찾았다:
```
무질량 축퇴 위반 |J^(i)−J^(0)|/|J^(0)|
   t=0 : 3.1e−15   ← 초기조건은 정확
   t=T : 2.2e−01   ← 2차원 격자를 **독립 적분**하면 21% 까지 깨진다
```
궤적오차(1e−4)보다 **큰** 값이다 — 즉 오차의 원천이었다.  i_max 절단이 i 마다 다르게
되먹임되어 정확한 축퇴를 수치가 깨뜨린 것이다.

### 2. 조치 — 축퇴를 강제 (1차원 환원)
`rhs_degenerate` / `integrate_degenerate`: 상태를 J_l ≡ J^(0)_{A_l} 만 두고, 방정식이
요구하는 모든 J^(i) 에 같은 값을 공급한다 (무질량에서 **정확**).  질량행렬도 i 지표가
접힌다.
```
2차원 (l_max=2, i_max=1):  rho 5.3e−4   q 3.0e−4   pi 8.9e−4
1차원 환원 (l_max=2):       rho 1.6e−6   q 1.5e−5   pi 1.5e−4
1차원 환원 (l_max=3):       rho 7.8e−8   q 2.3e−6   pi 1.5e−5     ← ρ 약 **6800배**
```
★ 환원 후 오차가 다시 **l_max 지배**로 돌아간다 (l_max 2→3 에서 ρ 21배, π 10배).
2차원 판에서는 l_max 를 올려도 무변화였다 — 진단이 옳았다는 증거다.

### 3. 문헌에서 얻은 **미검증 예측** (다음 증분 1순위)
같은 논문이 두 가지를 더 말한다:
- 절단은 **총 속도가중 n_* = l+2i 의 삼각형**이어야 한다 (우리는 직사각형을 썼다).
- ★ 그 절단은 **n_* 가 홀수일 때만 프레임 불변**이다:
  "the truncation condition J^(i)_{A_l}=0 for l+2i > n_* is only frame-invariant at
  linear order for n_* **odd**" — l=1 에서 변환이 속도가중에 대해 비동차이기 때문.

  tilt 는 문자 그대로 fiducial velocity 변경이므로, 우리 오차가 ∝v² (측정 1.79) 로
  커진 것과 맞물릴 개연성이 크다.  **검증 가능한 예측**: 유질량 삼각절단에서
  n_* 홀수가 짝수보다 뚜렷이 정확해야 한다.  **아직 시험하지 않았다** — 정직하게 명시한다.

### 4. 검토했으나 채택하지 않은 대안 (근거와 함께)
| 대안 | 판단 |
|---|---|
| q-샘플링 (CLASS/CAMB 표준) | i-계층 회피의 정공법이나, 우리는 정확 구적이 있어 과함 |
| 적분방정식 정식화 (CLASSIER, <0.01%) | 우리 배경은 이미 특성곡선 정확해를 가짐 — 적분기 교체는 향후 검토 |
| 일반화 Boltzmann 계층 (PRD 104,083535) | 우리와 같은 2지표 구조지만 보고 정확도 ≲0.5% — 목표(1e−7)에 부족 |
| Grad/최대엔트로피/Levermore 닫힘 | **우리는 정확해가 있어 닫힘을 추측할 필요가 없다** → 우선순위 낮음 |
| 절단 반사 인공물 (τ~2l_max/k) | k-모드가 있는 섭동 계산의 병리 — 우리 배경엔 k 가 없어 해당 없음 |

★ 요점: 문제는 "닫힘을 잘 추측하는 것" 이 아니라 **아는 구조를 수치가 깨뜨리지 않게
하는 것** 이었다.  그래서 정교한 닫힘 이론보다 원논문의 1차원 환원이 6800배를 벌었다.

### 상태
```
pytest 627 passed (환원 신규 4)   cargo test 49 passed
신규: LITERATURE-moment-closure.md,  tilted_integrate 의 환원 경로
```
### 다음
n_* 삼각절단 + **홀짝 프레임불변성 시험** (§3 예측 검증) → 비율 닫힘 r≈5w → Rust 이식.

---

## v3.0 진행 23차 (2026-07-31) — **K1** 계층 ↔ Einstein 결합 (test-field 탈출)

### 0. 무엇이 바뀌었나
지금까지 계층은 **고정 배경 위의 test-field** 였다.  `charts/general.py` 에 `Pi` 훅이
있었으나 아무도 채우지 않았고 `Background` 는 a(t) 를 처방했다.  이제 π_ab 가 기하를
움직인다 (`matter/kinetic_einstein.py`, Bianchi I · 법선합동):
```
H = √((ρ + σ²)/3)          ← Friedmann **구속** (진화식이 아니다)
ȧ_i = (H + σ_i) a_i
σ̇_i = −3H σ_i + π_i         ← 계층의 π 가 전단을 움직인다
```

### 1. 규격화를 **측정으로 확정** (문헌 인용 대신)
Π 가 π/H² 인지 π/(3H²) 인지 ½π/H² 인지는 문헌마다 갈린다.  Bianchi I 계량에서
Einstein 텐서를 직접 계산해 쟀다 (`audit/k1_shear_source_normalization.py`):
```
통제군  Friedmann G_00 = 3H² − σ²                       잔차 2.1e−14
측정    σ̇^i + θσ^i = 1.000000000000 × (무대각합 공간 Einstein)  잔차 4.8e−16
검증    Σ' = −(2−q)Σ + Π  에서  **Π = π/H²**             잔차 6.7e−16 (독립 수치 경로)
```

### 2. ★ 함정 — Raychaudhuri 적합이 **rank 결손**이다
`Ḣ = c_H H² + c_s σ² + c_ρ ρ + c_p p` 를 최소제곱으로 재니
(−0.409, −0.864, −0.364, −0.500) 이 나와 교과서형 (−1, −2/3, −1/6, −1/2) 과 달랐다.
원인은 **Friedmann 3H² = ρ+σ² 이 {H², σ², ρ} 를 종속**시켜 설계행렬 rank 가 3/4 인 것.
계수 자체가 유일하지 않으므로 그대로 읽으면 오독한다.  올바른 판정은 "교과서형과의
차가 영공간 (3,−1,−1,0) 에 평행한가" 이고, **수직성분 6.1e−16** 로 동치임을 확인했다.

### 3. ★★ K3 게이트 — 그리고 그 게이트가 **내 버그를 잡았다**
Ḣ 를 두 경로(구속미분+에너지보존 / Raychaudhuri)로 계산해 비교한다.
처음 실행에서 잔차가 **1e−5** 였다.  추적하니 차이가 정확히 `−σ² + σ·π/(6H)` 로
떨어졌고, 원인은 내가 `d(σ²)/dt` 를 **2배로** 쓴 것이었다 (σ² ≡ **½**σ_abσ^ab 인데
σ_abσ^ab 로 착각).  고치니 **1.5e−16**.
```
m=0/0.7/3.0 × σ=3가지 조합 전부:  1.2e−16 ~ 3.0e−16
```

### 4. ★★ 그러나 K3 는 **Π 규격화에 눈이 멀었다** (처음 주장이 틀렸다)
게이트에 이빨이 있는지 시험하려고 π 를 3배로 틀리게 넣었더니 **잔차가 그대로**였다.
π 를 0배·−5배로 해도 1.5e−16 로 **구별조차 못 한다**:
```
6HḢ = ρ̇ + σ·σ̇ = [−3H(ρ+p) − σ·π] + [−3Hσ·σ + σ·π]      ← σ·π 가 정확히 상쇄
```
즉 K3 는 **보존만** 검사한다.  docstring 에 "규격화가 틀리면 즉시 드러난다" 고 적었던 것을
**철회**하고, 규격화를 보는 게이트를 따로 만들었다:

**`einstein_residual`** — (a, ȧ, ä) 를 결합식에서 만들어 G_μν 에 넣고 물질 응력과 대조.
```
π×1.0    → G_tf 8.8e−17      π×1.0001 → 9.2e−6      ← 0.01% 오차도 잡는다
π×1.1    → 9.2e−3            π×3.0    → 1.8e−1
```

### 5. 결합이 실제로 작동하는가
```
π 를 끄면:  a 4.8% , σ 100% 달라진다                    ← 결합 주장의 최소 조건
결합 궤적 vs 정확구적 기준 (nsteps=100):
  m=0 → a 1.2e−7, σ 8.3e−6      m=1 → a 1.9e−4, σ 1.7e−3
G_μν = T_μν 가 결합 흐름 위에서 기계정밀도 (m=0/0.7/3.0)
```

### 6. ★ 순진한 기대 하나가 반증됐다
"이방응력은 늘 전단을 감쇠시킨다" 는 **틀렸다**.
- **등방 a_vec** 시작: π 를 σ 가 만들고 σ 를 감쇠 (6.42e−3 → 3.56e−3) ✓ Misner 점성
- **비등방 a_vec** 시작: π 가 σ 와 무관하게 이미 존재해 σ 를 **키운다** (0.0065 → 0.147)

감쇠를 기대하는 시험을 썼다가 부딪혔고, 두 경우를 각각 시험으로 고정했다.

### 상태
```
pytest 660 passed (K1 신규 33)   cargo test 49 passed
신규: bianchi/matter/kinetic_einstein.py, audit/k1_shear_source_normalization.py,
      tests/test_k1_kinetic_einstein.py, PLAN-NEXT-v4.md
```
### 다음
K2 (on-shell v(t) — tilted Euler 로 tilt 속도를 풀어낸다) → L3 (유질량 tilted 측정) → V2.

---

## v3.0 진행 24차 (2026-07-31) — **K2** on-shell tilt 속도 (처방 → 풀어냄)

### 1. tilt 의 well-posed 정의를 잡았다
지금까지 `Background` 는 v(t) 를 **임의로 처방**했다 (식 12 는 어떤 합동에서도 성립하므로
검증용으로는 정당했다).  물리적 tilt 는 자유롭지 않다 — **에너지틀(Landau)** 이다:
```
q′_a(v) ≡ J′^(0)_a(a_vec, v, m, f₀) = 0        ← v 에 대한 3원 방정식
```
boosted 정확 구적이 있으므로 **직접 푼다** (Newton, 야코비안은 수치미분).

### 2. tilt 가 언제 물리적으로 존재하는가
```
등방 f₀:  |q|/ρ = 4.6e−17   ← f(p)=f(−p) 라 법선틀이 이미 에너지틀, tilt 없음
쌍극 f₀:  |q|/ρ = 8.5e−02   ← 벌크 운동량이 있어야 tilt 가 산다
```
★ H1 에서 `f_dipole` 은 홀수 l 을 켜려는 **인공 장치**였는데, 여기서는
  "움직이는 물질" 이라는 **물리적 설정**이 된다.

### 3. 근찾기 결과 (오라클)
```
m=0.0: v = (0,0,0.067717)   잔차 1.2e−17   Newton 3회
m=0.7: v = (0,0,0.066371)   잔차 1.9e−16
m=2.0: v = (0,0,0.059087)   잔차 1.3e−16     ← 무거울수록 tilt 가 작다
팽창을 따라 v 가 거의 보존: 0.06772 → 0.06829 (+0.8%, t=0.3)
```

### 4. ★★ 두 독립 경로 교차검증
에너지틀에서 q_a ≡ 0 이므로 계층의 l=1 방정식이 **q̇ 방정식이 아니라 u̇ 를 정하는
대수식**이 된다 (tilted Euler).  u̇ 는 v̇ 에 아핀이므로 v̇ 를 풀 수 있다.
```
경로 A — 근찾기를 시간에 대해 FD 한 v̇   (계층 미사용)
경로 B — tilted Euler 를 v̇ 에 대해 푼 값  (근찾기 미사용)
m=0.0:  상대차 1.6e−08        m=0.7:  상대차 2.1e−06        (둘 다 FD 한계)
```

### 5. ★ 손 전사가 또 틀렸고, 교차검증이 잡았다
처음에 l=1 항들을 **손으로 다시 적었다**.  (div-free) 항이
`−(l/(2l+1))·D_{⟨a_l}J^(i+1)_{A_{l−1}⟩}` 인데 J^(1) = 3p 대신 p 를 넣어 **3배** 틀렸다.
교차검증이 **24배 어긋나며**(부호까지 반대) 즉시 드러냈다:
```
v̇(근찾기) = +0.00193      v̇(Euler, 손전사) = −0.04603
```
손 전사를 버리고 **이미 두 오라클(∇T·boosted 구적)로 검증된
`tilted_equation.equation_lhs` 를 그대로 재사용**하니 1.6e−8 로 일치했다.
회귀 시험으로 "손 전사 금지" 를 고정했다 (`test_euler_reuses_verified_assembly...`).

★ 교훈이 반복된다: **이미 검증된 조립이 있으면 다시 적지 말 것.**

### 6. 정직한 범위
여기서 배경 a(t) 는 여전히 **처방**이다.  tilt 와 Einstein 결합을 **동시에** 푸는
K1+K2 통합은 다음 단계다 (K1 은 법선합동 결합, K2 는 처방 배경 위의 tilt).

### 상태
```
pytest 675 passed (K2 신규 15)   cargo test 49 passed
신규: bianchi/matter/tilted_frame.py, tests/test_k2_tilted_frame.py
```
### 다음
K1+K2 통합 (tilted 물질이 기하를 움직이는 완전 결합) → L3 (유질량 tilted 측정) → V2.

---

## v3.0 진행 25차 (2026-07-31) — **K2b** tilt 허용조건: Bianchi I 은 tilt 를 담을 수 없다

### 0. K1+K2 통합을 시도하기 전에 **존재 여부를 먼저 물었다**
계획은 "tilted 물질이 기하를 움직이는 완전 결합" 이었다.  짓기 전에 운동량 구속을
확인했더니 **그런 대상이 type I 에는 없었다**.

### 1. 운동량 구속(Codazzi)이 q = 0 을 강제한다
```
C^a = 3 A_b Σ^{ab} + ε^{abc} N_{bd} Σ_c{}^d − q^a = 0
Bianchi I: N = A = 0  ⇒  C^a = −q^a  ⇒  **q^a = 0**
```
계량 쪽 표현도 같다: 대각 Bianchi I 에서 **G_{0i} ≡ 0** (기호계산으로 확인).
기하에 운동량밀도를 받을 자리가 아예 없다.

⇒ K2 의 tilt 는 **운동학적으로 옳은 test-field 구성**이지만
  **type I 의 Einstein 해가 아니다**.  측정된 위반량은 정확히 |q| 다:
```
쌍극 f₀:  |q|/ρ = 8.46e−2   →  Codazzi 잔차 |C| = 8.46e−2
등방 f₀:  |q|/ρ = 4.6e−17   →  |C| = 4.6e−17   ← K1 결합이 정당한 이유
```

### 2. ★ 더 뾰족한 사실 — "type II 로 올리면 된다" 는 **틀렸다**
대각 N · 대각 Σ 이면 **class A 전체가 용량 0** 이다:
```
Bianchi I  (N=0,A=0)      용량 0        ★ 못 담는다
Bianchi II (N=diag)       용량 0        ★ 못 담는다
Bianchi VII₀ (N=diag)     용량 0        ★ 못 담는다
Bianchi IX (N=diag)       용량 0        ★ 못 담는다
Bianchi V  (A≠0)          용량 8.4e−2      담는다
Bianchi II (N **비대각**)  용량 8.0e−2      담는다
```
ε^{abc}N_{bd}Σ_c{}^d 가 N·Σ 둘 다 대각일 때 항등적으로 사라지기 때문이다.
⇒ tilt 를 담으려면 **A ≠ 0 (class B, 최소 type V)** 이거나 **비대각 N/Σ** 가 필요하다.

### 3. no-go 로 끝내지 않았다 — 구성적 증거
주어진 q 에 대해 type V 에서 3Σ^{ab}A_b = q^a 를 A 로 풀면 **정확해가 존재**한다:
```
q = (0,0,0.084556),  Σ = diag(0.06,−0.02,−0.04)
→ A = (0, 0, −0.704633),   Codazzi 잔차 1.4e−17,  rank 3
```
tilt 를 담는 일관된 배경이 **있다** — 다만 type I 이 아닐 뿐이다.

### 4. 로드맵 정정
"K1+K2 통합" 은 **type I 에서는 불가능**하다.  가능한 경로는 둘:
- **(a) class B (type V) 로 확장** — 정확 구적 오라클을 type V 로 옮겨야 한다
  (불변기저 운동량이 type I 처럼 단순히 1/a 로 적색이동하지 않는다).  실제 작업.
- **(b) 비대각 Σ/N 을 허용하는 general 차트** — 이미 `charts/general.py` 가 지원한다.
  다만 tilt 를 담으려면 Σ 나 N 이 비대각이어야 하므로 계량이 비대각이 되고,
  구적 오라클도 그에 맞춰 일반화해야 한다.

어느 쪽이든 **정확 구적 오라클의 일반화**가 선행 작업이다.  그것 없이 결합하면
검증 수단이 없다 — 이 프로젝트가 지금까지 지켜온 원칙에 어긋난다.

### 상태
```
pytest 687 passed (K2b 신규 12)   cargo test 49 passed
신규: bianchi/matter/tilt_admissibility.py, tests/test_k2b_tilt_admissibility.py
```
### 다음 (계획 정정 반영)
L3 (유질량 tilted 측정 — type I 안에서 가능) → V2 (충돌 몬테카를로 오라클) →
그 다음에 type V 구적 오라클 (tilt 결합의 선행조건).

---

## 진행 26차 — L3 · 유질량 tilted 정확도: **닫힘은 J 가 아니라 J̇ 이 문제였다**

### 0. 계획대로 먼저 **측정**했다 (L3 은 "측정된 적이 없다" 는 항목이었다)
```
i_max 1→4  (l_max=3):  m=0  5.3e−4 → 4.7e−6
                       m=1  1.4e−4 → 3.1e−6
                       m=2  6.3e−4 → 4.8e−7
l_max 2→4  (i_max=3):  m=0  2.10e−5 → 1.93e−5      ← 이미 포화
                       m=1  1.39e−5 → 1.17e−5
```
⇒ **유질량에서도 지배 오차는 i-닫힘**이다.  무질량에서 얻은 결론이 질량을 켜도 유지된다.

### 1. 구조 — i-탑은 Hausdorff 모멘트열이다
n = l+2i 이므로 i 한 칸이 (λ′/E′)² 한 번이다:
```
J^(i)_{A_l} = ∫ dμ_{A_l}(x) x^i ,   x = λ′²/(λ′²+m²) ∈ [0,1]
```
운동량이 무한대까지 있어 x 의 상한이 1 이므로 **비 r_i = J^(i+1)/J^(i) 는 단조 증가**한다.
측정:
```
m=4, l=0:  0.4068  0.5269  0.5996  0.6487  0.6844      (증가)
m=1, l=0:  0.8895  0.9067  0.9169  0.9240  0.9294
m=0    :  1 (정확)                                      ← 축퇴
```
기존 `close_i` 는 **r 을 얼려** 기하외삽하므로 구조적으로 과소평가한다.
(1−r) 을 기하외삽하도록 고치니 **닫힘 예측오차가 3–8배** 좋아졌다.

### 2. ★ 그런데 그것만으로는 궤적이 좋아지지 않았다 — 반증
```
m=1, i_max=2  궤적오차     rho        q         pi
  기존(얼린 비)          4.6e−5   6.2e−5   2.0e−4
  (1−r) 외삽             1.1e−4   9.4e−5   2.1e−4      ★ 오히려 나쁨
```
그래서 **오라클 실험**을 했다 — 격자 밖 J 를 *정확 구적*으로 채워 넣는다:
```
  기존                   4.8e−5   5.5e−5   2.3e−4
  격자 밖 J 를 정확히    8.1e−5   1.7e−4   1.6e−3      ★★ 7배 나빠짐
```
⇒ **한계는 J 값이 아니다.**  방정식 안에서 J 항과 J̇ 항이 상당부분 상쇄하므로
  한쪽만 정확하게 만들면 균형이 깨진다 — **정확성보다 일관성이 먼저**다.

### 3. ★★ 진짜 원인: 격자 밖 **J̇** 만 버리고 있었다
(div-free) 는 (l,i) ← (l−1,i+1) 로 결합하므로 i=i_max 에서 격자 밖 J̇ 이 필요하다.
지금까지 J 는 닫힘으로 채우면서 **J̇ 만 0 으로 버렸다** — 닫힘이 J 와 J̇ 사이에서
어긋나 있었다.  같은 비로 J̇ 도 닫으면 (질량행렬 안에 넣는다):
```
J^(i_max+1) = r·J^(i_max)   ⇒   J̇^(i_max+1) ≈ r·J̇^(i_max)
```
```
무질량 (l_max=3, i_max=1):   rho 5.26e−4 → 7.76e−8   (6800배)
                              q   3.02e−4 → 2.29e−6   (132배)
                              pi  8.62e−4 → 1.52e−5   (57배)
```
★ 이 값이 `integrate_degenerate` (무질량 1차원 환원) 와 **마지막 자리까지 동일**하다
  (상대차 정확히 0.0).  즉 **지난 증분의 6800배는 J̇ 닫힘의 r=1 특수경우였다** —
  무질량 전용 특례가 아니라 일반 원리였던 것이다.  이제 유질량에도 쓸 수 있다.

또한 무질량에서는 결과가 **i_max 와 무관**해진다 (r=1 이면 i 방향이 완전 축퇴).
`degeneracy_violation` 도 2.2e−1 → 기계정밀도로 내려간다 (V3 감시자).

### 4. 유질량 — 정직한 표 (i_max=3, l_max=3, T=0.2)
```
        기존(frozen, J̇ 버림)          새 기본값(ratio + J̇ 닫힘)
  m      rho      q      pi           rho      q      pi
 0.5   1.86e−5 2.60e−5 5.60e−5      8.4e−7  1.6e−6  1.4e−5     (22 / 16 / 3.9배)
 1.0   1.18e−5 2.02e−5 4.97e−5      2.6e−6  2.4e−6  1.3e−5     (4.6 / 8.6 / 3.9배)
 2.0   6.8e−7  5.97e−6 2.81e−5      3.9e−6  2.9e−6  7.6e−6     ★ rho 는 나빠짐
 4.0   3.24e−6 1.26e−6 6.69e−6      1.8e−6  8.0e−7  4.3e−6     (1.8 / 1.6 / 1.6배)
```
★ **m=2 의 rho 가 나빠지는 칸을 감추지 않는다**: 기존 6.8e−7 은 수렴열의 일부가
  아니라 **부호 상쇄로 우연히 지나간 영점**이다 (i_max=2 에서 2.9e−5, i_max=4 에서
  4.8e−7).  시험으로 고정해 두었다 (`test_m2_rho_gets_worse_and_we_say_so`).

★ 또 하나: **두 고침은 함께라야 한다.**  i_max=1 에서 J̇ 닫힘만 켜면 유질량이
  오히려 나빠진다 (m=2: 6.3e−4 → 8.9e−4) — 얼린 비 r 자체가 부정확하기 때문이다.

### 5. 부수 정리 (V4)
`close_i` 의 성분별 비 문제(거의 0 인 성분에서 비가 무의미)를 노름 기반 스칼라 비
(`mode="ratio_scalar"`) 대안과 나란히 두고 비교 가능하게 만들었다.  측정: l=0 은 동일,
텐서 성분에서는 성분별이 낫다 — 현재 무해함을 **확인**했다.

### 6. 기본값 변경과 대조군
`tilted_integrate` 의 기본값을 `mode="ratio", jdot_closure=True` 로 바꿨다.
H5-e3 의 기전 시험 7개는 `CTRL = dict(mode="frozen", jdot_closure=False)` 를 명시하는
**대조군**이 되었다 — 옛 거동을 지우지 않고 재현 가능하게 남긴다.

### 상태
```
pytest 726 passed (L3 신규 26, H5-e3 기전시험 7개는 대조군화)   cargo test 49 passed
신규: bianchi/matter/tilted_closure.py, tests/test_l3_massive_closure.py
수정: tilted_integrate.py (mode/jdot_closure), tilted_mass.py (matvec i_ratio)
```
### 다음
V2 (충돌 몬테카를로 오라클 — 유일하게 오라클 없는 부분) → L1 (n_* 삼각절단 홀짝) →
type V 구적 오라클 (tilt 결합의 선행조건).

---

## 진행 27차 — V2 · 충돌항 오라클: **결정론적 각도구적**, 그리고 미결 항목 하나가 닫혔다

### 0. 설계부터 했다 (사용자 지적에서 출발)
> "Bianchi universe 에서는 oscillative decay/expansion 이 자주 일어나서 몬테카를로
>  이용 적분법이 적합하지 않을 수도 있다"

문헌조사 6편 + 사전측정 4종으로 확인했다 (`PLAN-V2-collision-oracle.md`):
- **진동은 실재한다**: Bianchi VII₀ 는 늦은 시각에 진동수가 τ 에 대해 **지수적으로 증가**
  (`ψ ≈ ψ̂ + (√3/M)e^{ρτ}`, M ∝ e^{−τ}).  문헌 저자들도 진동/세속 분리 변수변환 + 강성 솔버를 쓴다.
- **같은 설정의 선행연구는 MC 를 안 쓴다**: Pontzen & Challinor (2007) 는 Bianchi VII_h +
  Thomson 을 **다극 계층**으로 풀고 l=60(120 으로 검증), 매 스텝 `τδη ≪ 1` 을 강제한다.
- **MC 가 가장 못하는 양이 우리 표적**: Richers 외 (2017) — MC 는 "차감 상쇄가 지배하는 양
  (예: Eddington 텐서 **비대각** 성분)" 에서 잡음을 못 낮추고, 근사 닫힘의 오차가
  **DO−MC 차이와 같은 수준**이다.
- 측정: MC 광자 10⁶ → p₂ 상대오차 **9 %** / 격자 32점 → **4e−15**.
  상관표본은 무력하다 (상관계수 0.10 — **강한 감쇠 자체가 분산감소의 전제를 파괴**).
  진동 배경에서 스텝 256배에 MC 는 16배만 개선 (결정론은 6만배).
  잡음이 `1/(a√N)` 이라 우주론적 a~10⁻⁵ 에서 N~10¹⁶.

⇒ **판정**: 주 오라클 = 결정론 각도구적.  그리고 더 중요한 재정의 —
  오라클을 **궤적이 아니라 국소 연산자**로 잡는다.  그러면 배경이 등장하지 않으므로
  **진동 문제가 설계 단계에서 소거**된다.

### 1. 순환 차단 — 두 가지를 인용하지 않고 재구성했다
```
위상함수 :  dσ/dΩ ∝ ½ Σ_{ij}|ê_i·ê′_j|²  (쌍극복사에서 조립)
            규격화 상수도 **측정**: ∫P_raw dΩ = 8.377580409572783
                                    8π/3    = 8.377580409572781   (2.1e−16)
            ⇒ (3/16π)(1+μ²) 를 어디에도 적어 넣지 않았다.
PSTF 기저:  n_{⟨A_l⟩} = (−1)^l/(2l−1)!!·r^{l+1}∂_{A_l}(1/r)  를 **기호미분**
            `hierarchy.pstf` (최소제곱 사영) 와 대조: l=2 1.1e−16, l=3 3.3e−16, l=4 4.4e−16
```
오라클 파일이 `collision`/`hierarchy.pstf` 를 임포트하지 않는지 **AST 로 검사**하는
회귀시험을 넣었다 (K2 에서 손 전사를 금지한 것과 같은 방식).

### 2. ★ 핵심 결과 — λ_l 을 격자에서 직접 잰다
```
 l      격자 측정          H3      고유벡터 잔차
 0   1.00000000000000    1.0        4.4e−16
 1  -0.00000000000000    0.0        8.4e−17
 2   0.10000000000000    0.1        3.8e−16
 3  -0.00000000000000    0.0        1.4e−16
 4   0.00000000000000    0.0        4.6e−16
```
32점 / 288점 / 1152점이 모두 0.100000000000000 — 저차에서 맞는 게 우연이 아니다
(Thomson 위상함수가 μ 의 2차식이라 낮은 차수 구적이 **정확**하다).

**대조군도 살아 있다**: 위상함수에 δ·P_l 을 더하면 λ_l 이 선형으로 움직인다
(l=2: δ=0.1 → 0.12, 즉 Δ=0.2δ;  l=3: Δ=0.0857δ).

### 3. ★ 전체 사슬 — "λ 값" 이 아니라 "계층에 붙인 결과"
실제 Bianchi I 분포에 산란을 걸고 모멘트를 재조립해 계층의 `C[J] = −(1−λ_l)J` 와 대조:
```
m=0:  최대 잔차 3.2e−15      m=1:  최대 잔차 7.9e−15     (l≤4, i≤2 전부)
```
**정직성 장치**: 등방 f₀ 는 홀수 l 이 정확히 0 이라 (|J₁|/ρ = 2.3e−17) 게이트가 0/0 이 된다.
그래서 `odd_l_is_switched_on()` 이 **게이트 전에 어떤 l 이 살아 있는지 먼저 보고**하고,
쌍극 성분으로 홀수 l 을 켠 뒤(7.5e−2, 5.2e−3) 시험한다.  (첫 판에서 실제로 l=1 잔차가
1.55 로 나와 잡아냈다 — 0/0 이었다.)

**탄성 가정 확인**: 계층은 "탄성이라 i 에 무관" 을 가정한다.  측정 (m=1, l=2):
i=0,1,2,3 모두 감쇠비 **0.100000000000**.

### 4. ★★ 편광 — H3 이 미결로 남긴 `8/27 vs 4/15` 가 닫혔다
`phase_unnormalised` 는 출사 편광을 **합산**한다.  합산하지 않고 2×2 결맞음 행렬을
그대로 나르는 연산자를 **같은 쌍극복사 조립에서** 만들었다.  l=2 불변 부분공간:
```
차원 = 2,     블록 = [[1/10,        √6/10     ]
                      [√6/10,       3/5       ]]
비대각 측정 = 0.244948974278318      √6/10 = 0.244948974278318
고유값 = {7/10, 0}
```
★ **√6 을 어디에도 적어 넣지 않았는데 격자에서 나왔다** — CMB 편광 계층의 그 √6 이다.

준정적 균형 (소스는 팽창·전단이라 **강도 채널에만** 들어간다) 을 풀면:
```
유효 사중극 감쇠 = 0.750000000000000  =  3/4        (비편광은 9/10)
비 (9/10)/(3/4)  = 1.200000000000     =  6/5
```
Hu 의 강의노트 *"polarization also increases the viscosity of the fluid by a factor
of 6/5"* 와 **정확히 일치**한다 — 우리는 그 6/5 를 측정으로 얻었다.

⇒ 광자 전단점성 η·n_eσ_T/ρ:
```
각구조 무시 (감쇠 1)    4/15  = 0.2666666667   ← H3 이 "문헌 관례" 로 적어둔 값
비편광    (감쇠 9/10)   8/27  = 0.2962962963   ← 우리 비편광 계층의 자기정합 값
편광 포함 (감쇠 3/4)    16/45 = 0.3555555556   ← ★ 물리적으로 옳은 값
```
`collision.thomson_viscosity(..., polarised=True)` 로 노출했고, 옛 플래그는 재현용으로 남겼다.

### 5. 겪은 결함 (기록)
- **내적을 틀렸다**: 축소 블록을 뽑을 때 결맞음 행렬의 내적 `⟨J,J′⟩=∫Tr(JJ′)` 에서
  J₁₂ 성분의 무게가 **2** 인데 1 로 썼다.  고유값은 기저 무관이라 멀쩡했지만
  (0.7 / 0) 블록이 비대칭이 되어 (0.24503 vs 0.24486) √6/10 이 8.6e−5 어긋났다.
  무게를 고치니 비대칭 5.6e−17.  ★ **구조를 읽으려면 내적이 맞아야 한다**.
- **0/0 게이트**: 위 §3.
- 독립성 시험을 문자열 검사로 짰다가 docstring 의 'pstf' 에 걸렸다 → **AST 검사**로 교체.

### 상태
```
pytest 756 passed (V2 신규 30)   cargo test 49 passed
신규: bianchi/matter/collision_oracle.py, audit/v2_collision_oracle.py,
      tests/test_v2_collision_oracle.py, PLAN-V2-collision-oracle.md
수정: collision.py (thomson_viscosity 에 polarised 플래그 + 측정 근거)
```
### 남은 V2 (계획서 §5)
V2b(Doppler l=1 분리 — 현재 `dipole_source` 가 docstring 에만 있고 **구현이 없다**),
V2c(tilted 프레임 충돌항), V2e(강성 스캔 — 문헌의 `τδη≪1` 을 우리 수치로),
V2f(MC 구조 오라클, 5 % 게이트).

---

## 진행 28차 — V2b/V2c · **움직이는 전자**의 충돌항: 약속만 있던 `dipole_source` 를 측정으로 채웠다

### 0. 갚은 부채 두 개
1. `collision.py` docstring 이 "λ₁ = 0 → 감쇠 1 (쌍극; 실제로는 바리온 속도로 완화
   → `dipole_source`)" 라고 적어 두었는데 **`dipole_source` 는 구현이 없었다**.
   즉 계층에 광자–바리온 결합의 원천항이 통째로 빠져 있었다.
2. V2a 는 전자 **정지틀**만 검증했다.  tilted 배경에서는 전자가 움직인다.

### 1. 구성 — 새 물리 입력 없이 boost 로만 유도
Thomson 이 **전자 정지틀에서 탄성**이라는 사실 하나만 쓴다.  f 가 스칼라이므로:
```
C/E = (E′/E) · n_eσ_T · [ ⟨f⟩_P(λ′, n̂′) − f(λ, n̂) ]
⟨f⟩_P 안의 f 는 (λ′, n̂″) 를 **역boost** 해 실험실틀에서 평가한다 — 이 한 줄이 Doppler 를 전부 만든다.
```
자기검증: |n̂′|−1 = 4.4e−16, λ 왕복 5.6e−16, 방향 왕복 6.7e−16.
**v_e = 0 에서 V2a 의 격자 행렬과 4.0e−16 일치** (코드 경로가 전혀 다르다).

### 2. ★ 대조군이 처음에 **아무것도 잡지 못했다** (기록)
평형 게이트(정지틀 등방 분포는 산란으로 불변, 잔차 8.9e−16)를 세우고
"boost·위상함수·측도 중 하나라도 틀리면 깨진다" 고 적었다.  **틀린 주장이었다.**
```
위상함수에 0.1 % 상수배    →  8.95e−16   ★ 그대로 (죽은 대조군)
```
`collision_rate_density` 가 **같은 상수로 나눈 행합**으로 규격화하므로 정확히 상쇄된다.
⇒ 평형 게이트는 **전체 규격화에 둔감**하다.  실제로 무는 것을 찾아 다시 만들었다:
```
되돌리는 boost 를 0.1 % 어긋냄  →  8.9e−16 → 1.32e−08   (1.5 × 10⁷ 배)
```
역할 분담을 문서와 시험에 명시했다:
```
전체 규격화  →  V2a `phase_norm`            (8π/3, 2e−16)
각도 구조    →  V2a `eigenvalue_from_grid`  (λ_l, 1e−13)
boost 왕복   →  여기 `equilibrium_residual`
```
둔감성 자체도 시험으로 고정했다 (`test_the_equilibrium_gate_is_insensitive_to_overall_normalisation`)
— **죽은 게이트를 살아 있다고 오인하지 않기 위해서**다.

### 3. ★★ V2b — 쌍극 원천 계수를 **측정**했다
`q̇_a = −τ̇[q_a − c·ρ·v_{e,a}]` 의 c 를 인용하지 않고 격자에서 뽑았다:
```
|v_e| = 1e−4 :  c = 1.333333354302   (편차 2.10e−08)
        3e−4 :      1.333333514302          1.81e−07
        1e−3 :      1.333335334304          2.00e−06
        3e−3 :      1.333351334505          1.80e−05
```
편차가 정확히 **v_e² 로** 간다 ⇒ **c = 4/3 + O(v²)**, 원천이 v_e 1차임이 확인된다.
`collision.dipole_source(rho, v_e, n_eσ_T)` 로 구현하고 `collision_term(..., v_e=...)`
에 연결했다 (v_e=None 이면 옛 거동 그대로 — 회귀시험으로 고정).

### 4. 보존량 — 광자수와 에너지를 **구별해서** 잰다
```
전자 정지  :  ṅ/n = −5.5e−18    ρ̇/ρ = −8.9e−18     (둘 다 보존, 탄성)
v_e = 1e−2 :  ṅ/n = +3.3e−13    ρ̇/ρ = −8.7e−04     (수는 보존, 에너지는 Doppler)
```
뭉뚱그리면 "보존이 깨졌다" 고 오독하게 된다.

### 5. V2c — l=2 감쇠는 v_e **1차에서 변하지 않는다**
```
v_e = 0      :  π̇/π = −0.900000000000   (편차 6.7e−16)
      1e−3   :        −0.900008303982          8.3e−06
      1e−2   :        −0.900830535081          8.3e−04
      3e−2   :        −0.907484781541          7.5e−03
```
편차가 v_e² ⇒ 계층이 π 감쇠에 v_e 보정을 넣지 않는 것이 **1차까지 정당**하다.
(2차 보정은 필요하면 이 표가 크기를 준다.)

### 상태
```
pytest 770 passed (V2b 신규 14)   cargo test 49 passed
신규: bianchi/matter/collision_moving.py, audit/v2b_collision_moving.py,
      tests/test_v2b_collision_moving.py
수정: collision.py (dipole_source 구현 + collision_term 의 v_e 인자)
```
### 남은 V2
V2e (강성 스캔 — 문헌의 `τδη ≪ 1` 을 우리 수치로), V2f (MC 구조 오라클, 5 % 게이트).
그 다음은 L1 (n_* 삼각절단 홀짝) 과 type V 구적 오라클.

---

## 진행 29차 — V2e/V2f · 강성의 "≪ 1" 을 숫자로, MC 는 구조 오라클로 (V2 티어 완결)

### 0. V2e — 문헌 제약을 우리 수치로
Pontzen & Challinor 는 매 스텝 `τ̇ δη ≪ 1` 을 강제하는데 "≪ 1" 이 얼마인지는 안 준다.

**안정한계를 인용하지 않고 찾았다** (|R(−z)| = 1 이분법):
```
측정 2.7852935634   고전값 2.7852935635      (10자리 일치)
```
그리고 τ̇ 를 5자릿수에 걸쳐 올리며 **실제 적분으로** 임계 스텝을 찾으면:
```
      τ̇         Γ      δt_crit      Γ·δt_crit
       1       4.9   5.684e-01    2.7852935634
      10        13   2.143e-01    2.7852935634
     100        94   2.963e-02    2.7852935634
    1000       904   3.081e-03    2.7852935634
   1e+04      9004   3.093e-04    2.7852935634
```
⇒ **`τ̇ δη ≲ 1.86`** (편광 유효감쇠 d₂=3/4, 안전계수 0.5).  문헌의 "≪ 1" 이 숫자가 됐다.

### 1. ★ 두 번의 측정 결함 (둘 다 기록)
**(a) 임계 스텝 스캔이 이산화 잔재를 재고 있었다.**  t_end=1 로 고정했더니 작은 τ̇ 에서
스텝이 한두 번뿐이라 불안정이 드러날 시간이 없었고 Γδt_crit 가 **10.0, 6.5** 로 나왔다.
적분구간을 Γ 에 맞추고, 판정 기준도 "|y| < 1e3" 에서 **"소스를 끄고 |R| = 1"** 로 바꿨다
(전자는 60스텝에 걸친 1000배 성장을 허용해 |R| = 10^{3/60} = 1.122 지점을 재고 있었다 —
그래서 2.8813 이 나왔다).  고치니 5자릿수에 걸쳐 2.7852935634 로 상수.

**(b) 정확도 시험이 퇴화해 있었다.**  상수 소스로 짰더니 z 와 무관하게 오차가
1e−14…0 이었다.  "정확도 손실이 없다" 가 아니라 **시험이 아무것도 재지 않았던 것**이다 —
S 가 상수면 RK4 는 안정하기만 하면 정확한 준정적값 S/Γ 에 그대로 앉는다.
물리적으로 맞는 **진동 소스**로 바꾸니 (Bianchi VII₀ 를 생각하면 오히려 현실적이다):
```
 Γδt = 0.01 → 9.0e−14     0.30 → 8.9e−08     2.00 → 7.9e−04
       0.03 → 7.4e−12     1.00 → 1.8e−05     2.70 → 2.6e−02   ← 안정한계 바로 아래
```
오차가 z⁴ 로 간다 (RK4 4차).  **"발산하지 않았으니 괜찮다" 는 판단이 위험하다** —
안정영역 안에서 조용히 2.6 % 가 틀린다.  두 퇴화 사실 모두 시험으로 고정했다.

### 2. 전환의 진짜 이유는 정확도가 아니라 **비용**이다 (정직한 반대 결과)
준정적 근사가 명시적 RK4 보다 나은 구간을 찾으려 했는데 **없었다**:
```
 Γ/ω      RK4(z=1)      준정적
  0.3     7.8e−03     1.6e−01
   10     1.8e−05     5.2e−02
  100     1.8e−08     5.0e−03
```
z=1 을 감당할 수 있으면 RK4 가 항상 낫다.  넘어가는 근거는 **스텝 수**다:
```
  τ̇/H        Γ       δt_max     스텝수 (T = 1/H)
     1     4.75    2.93e-01              4
   100       79    1.76e-02             57
 1e+04     7504    1.86e-04          5,389
 1e+06    7.5e+05  1.86e-06        538,546
```
`check_step()` 이 한계를 넘으면 **경고**하고 (조용히 넘어가지 않는다) 권장 δt 를 준다.

### 3. V2f — MC 의 역할을 문서와 코드에서 확정
정확도 오라클이 아니라 **마르코프 커널 구조**만 본다:
```
[1] 위상함수 표본추출이 실제 pdf 인가 (KS, n=200,000)
    D = 0.00154   5 % 임계 0.00304   통과
[2] 커널이 확률분포인가:  최소값 0.375 (≥0),  ∫ = 0.999999999999999
[3] 광자 수 보존 (정수 등식):  [100000] × 6회 불변
[4] p₂ (5 % 게이트, N=10⁶ × 12회):  0.10279,  상대오차 2.79 %
[5] 잡음 바닥:  N=10⁴ → 105 %,  10⁵ → 18.6 %,  10⁶ → 6.7 %   (1/√N)
```
★ 곁가지 하나 더: **잡음의 크기를 재는 것부터가 잡음이 크다.**  반복 6회로 지수를
재려 했더니 σ 추정이 0.169/0.213 로 나와 지수가 **+0.17** 이 됐다 (비 추정량의 두꺼운
꼬리).  24회로 올려야 −0.52 가 나온다.  게이트를 넓게 잡고 그 사실도 docstring 에 남겼다.

### 상태
```
pytest 788 passed (V2e/V2f 신규 18)   cargo test 49 passed
신규: bianchi/matter/collision_stiffness.py, audit/v2_monte_carlo.py,
      tests/test_v2ef_stiffness_mc.py
```
**V2 티어 완결** — V2a(격자 오라클) · V2b(Doppler 원천) · V2c(tilted) · V2d(편광 16/45)
· V2e(강성) · V2f(MC 구조).  H3 이 "충돌항에 오라클이 없다" 고 적었던 자리가 다 메워졌다.

### 다음
L1 (n_* 삼각절단 홀짝 프레임불변성 — 문헌의 미검증 예측), 그 다음 type V 구적 오라클.

---

## 진행 30차 — L1 · 문헌의 홀짝 예측: **확인됐다, 그리고 논문 식에 항이 빠져 있었다**

계획서가 "아직 시험하지 않았다 — 다음 증분의 1순위" 로 남겨둔 항목.
원논문은 절단조건 `J = 0 for l+2i > n_*` 가 **n_* 홀수에서만** 프레임 불변이라 적는다.

### 1. ★★ 논문의 변환식은 우리 배경에서 **불완전하다**
분포족 8종에 걸쳐 dJ̃^(i)_a/dv 를 boosted 정확 구적에서 재고 소스 기저에 최소제곱:
```
 i |     J0[n−1]      J0[n+1]      J2[n−1]      J2[n+1] |    잔차   | 대조군(n±3)
 0 | −1.00000000  −0.33333333   0.00000000  −1.00000000 | 4.6e−12 |   1.3e−09
 1 | −1.66666667  +0.33333333  −2.00000000  +1.00000001 | 1.0e−12 |   4.9e−09
 2 | −2.33333333  +1.00000000  −4.00000000  +3.00000000 | 4.4e−12 |   2.0e−09
 3 | −3.00000000  +1.66666667  −6.00000002  +5.00000004 | 2.5e−12 |   2.0e−08
```
앞 두 열이 **논문의 −(3+2i)/3, −(1−2i)/3 그대로** — 논문이 맞다.
그런데 **논문에 없는 l=2 항 두 개**가 필요하고 계수가 −2i, (2i−1) 로 깔끔하다:
```
J̃^(i)_a = J^(i)_a − ⅓v_a[(3+2i)J^(i) + (1−2i)J^(i+1)] − 2i·J^(i−1)_{ab}v^b + (2i−1)·J^(i)_{ab}v^b
```
★ 논문이 틀린 게 아니다: **선형섭동론**에서는 π_ab 가 1차라 π·v 가 2차로 떨어진다.
**Bianchi 배경에서는 π_ab 가 0차**라 같은 차수로 살아난다.  정량화하면
```
4항 전체 vs 정확 구적:  1.1e−10 (유한차분 한계)
논문 2항만            :  6.2 ~ 8.3 %
등방 a_vec (π=0)      :  1.2e−10   ← 경계 확인: 등방이면 논문 2항으로 충분
```
i=0 은 잘 알려진 `q̃ = q − (ρ+p)v − π·v` 다.

### 2. ★★ 홀짝 예측은 **정확히** 확인된다
네 소스의 가중이 전부 n∓1 (n = 1+2i) 이고 l=1 가중은 홀수이므로:
```
n_* 홀수 → 절단 넘는 최소 l=1 가중 n_*+2 ⇒ 소스 가중 n_*+1 도 잘려 있다 ⇒ 불변
n_* 짝수 → 최소 가중 n_*+1 ⇒ 소스 가중 n_* 가 **남아 있다** ⇒ 깨짐
```
측정 (절단상태를 무한소 boost 했을 때 금지영역 누출, ρ 규격화):
```
n_* = 2 (짝):  1.3335e+00      n_* = 3 (홀):  0.0000e+00
n_* = 4 (짝):  1.6025e+00      n_* = 5 (홀):  0.0000e+00
n_* = 6 (짝):  1.8236e+00      n_* = 7 (홀):  0.0000e+00
```
기전까지 확인: n_*=4 에서 새는 것은 가중 5 의 l=1 행이고, 남은 소스가
`J0[n−1]`, `J2[n−1]` (둘 다 가중 4 = n_*) 이다.

### 3. ★ 궤적오차에는 홀짝 계단이 **없다** (계획서 기대의 반증)
삼각 절단을 적분기에 넣고 (`tilted_mass.layout(n_star=...)`) 궤적오차를 재면:
```
n_* = 2,3,4,5,6,7  →  1.4e−2, 1.5e−2, 3.0e−3, 2.5e−3, 6.9e−4, 5.4e−4
```
n_* 수렴만 보이고 계단이 없다.  2→3 에서는 오히려 나빠지는 성분도 있다.
**"홀수가 뚜렷이 정확할 것" 이라는 기대는 이 형태로는 틀렸다.**

### 4. ★★ 올바른 형태 — 홀짝은 **tilt 를 켜야** 나타난다
프레임 불변성은 *틀 의존성*의 성질이므로 그걸 직접 재야 한다:
```
 n_*      v=0         v=0.15      오염비
   2   1.5435e−02   1.4886e−02   −0.036   (짝)
   3   1.5435e−02   1.3117e−02   −0.150   (홀)
   4   2.8881e−03   3.8741e−03   **+0.341** (짝)
   5   2.8881e−03   2.6079e−03   −0.097   (홀)
```
★ **v = 0 에서 n_*=4 와 5 는 상대차 0.000e+00 로 완전히 같다** — 법선틀에서는 홀수 l 이
항등적으로 0 이라 가중 n_*+1 상태가 아무것도 하지 않는다.  tilt 를 켜야 비로소
홀수가 낫다 (v=0.15 에서 3.87e−3 vs 2.61e−3, **1.5배**).

⇒ 문헌 예측은 **확인**되지만 크기는 "계단" 이 아니라 **유한 배수**이고,
  **드러나는 방식이 궤적오차가 아니라 tilt 오염도**다.  이 구별을 시험으로 고정했다.

### 상태
```
pytest 815 passed (L1 신규 27)   cargo test 49 passed
신규: bianchi/matter/frame_transform.py, audit/l1_frame_parity.py,
      tests/test_l1_frame_transform.py
수정: tilted_mass.py (layout/pack/unpack/dense/solve 에 n_star 삼각절단),
      tilted_integrate.py (closure/rhs/integrate/trajectory_error 에 n_star)
      — n_star=None 이면 옛 경로와 **비트 단위로 동일** (회귀시험으로 고정)
```
### 다음
L2 (비율 닫힘 r ≈ 5w / √(3w) 를 `close_i` 와 비교), 그 다음 type V 구적 오라클.

---

## 진행 31차 — L2 · 비율 닫힘: 문헌 공식의 **적용 범위를 재고**, 닫힘으로는 **못 이긴다**

### 0. 표적
원논문: 초기 `r ≈ (3w)^{1/2}`, 후기 `r ≈ 5w`, 그리고 이 조건이
**"frame-invariant to the order of our velocity-weight truncation"**.
L1 에서 프레임 효과가 **tilt 오염도**로 드러남을 확인했으니 그 단서를 여기서 잰다.

### 1. ★ 먼저 함정 하나 — `r₀ = 3w` 는 **정의상 항등식**이다
첫 측정에서 `r₀/5w = 0.6000` 이 모든 질량에서 정확히 나왔다.  "5w 와 3/5 배로
일치" 로 읽을 뻔했는데, `J^(1) ≡ 3p` 이므로 `r₀ = 3p/ρ = 3w` 가 **정의**다.
물리가 아니라 규약이다.  l ≥ 1 만 잴 것이 있다 (그리고 법선틀 홀수 l 은 0 이라
l=1 에는 쌍극 f₀ 가 필요하다 — L1 에서 배운 것).

### 2. ★★ 문헌 공식의 적용 범위를 **측정으로 채웠다**
논문은 "초기/후기" 라고만 하고 경계를 주지 않는다.  상대오차 `|r/식 − 1|` 로:
```
l=2:  w=0.3226 에서 √(3w) 상대오차 **4e−5**  (4자리 일치)
      유효지수 α (r=(3w)^α) 가 0.50 → 0.82 로 이동  ← 문헌의 1/2→1 방향과 일치
                                                     (다만 우리 범위에서 1 에 닿지 않는다)
전환:  l=1 은 w ≈ 0.104 에서 √3w → 5w 로 **1회**
       l=2·3 은 **2회** (0.104 에서 5w, 0.082 에서 다시 √3w)
최저 w(0.0012) 에서 5w 상대오차:  l=1 **0.157** (계속 개선)  vs  l=2 **1.02** (발산)
```
⇒ **5w 는 l=1 전용이다** — 논문이 `J_a^{(1)} ≈ r J^{(0)}_a` 로 쓴 그대로다.
  l ≥ 2 에서는 √(3w) 계열이 훨씬 오래 맞는다.

### 3. ★ 측정 도구를 한 번 틀렸다 (기록)
전환점을 **절대오차 + 이분법**으로 찾게 짰다.  결과가 l=1 과 l=2 에서 16자리까지
동일하게 나와 이상해서 확인했더니, l=2 의 술어가 **단조가 아니었다** (m=6, 8 에서
다시 뒤집힌다).  이분법은 단조성을 가정하므로 첫 교차만 잡고 나머지를 놓쳤다.
**상대오차 + 전수 스캔**으로 바꾸니 l=1 1회, l=2·3 2회로 드러났다.
(절대오차는 r 이 작아지는 영역에서 스케일이 달라져 비교가 무의미해진다.)

### 4. ★★ 단일 r 처방의 한계를 정량화
`r_l = ⟨v^{l+2}⟩/⟨v^l⟩` 이므로 **l 마다 다르다**:
```
 w=0.297:  r₁ 0.8894  r₂ 0.9433  r₃ 0.9324   r₃/r₁ = 1.05
 w=0.177:  r₁ 0.5617  r₂ 0.7171  r₃ 0.7035   r₃/r₁ = 1.25
 w=0.053:  r₁ 0.1971  r₂ 0.3415  r₃ 0.3477   r₃/r₁ = 1.76
 w=0.005:  r₁ 0.0201  r₂ 0.0465  r₃ 0.0511   r₃/r₁ = **2.54**
```
비상대론으로 갈수록 나빠진다.  이것이 아래 `phys_5w` 가 적분기에서 무너지는 이유다.

### 5. 동역학적 끌개 — 논문의 **진짜** 주장
논문의 5w 는 배경 분포의 모멘트비가 아니라 "큰 스케일에서 절단된 계층의 해" 라는
**동역학적** 진술이다.  ★ 우리 배경에는 k-모드가 없으니 **큰 스케일 극한 그 자체**라
조건이 자동 충족된다 — 그래서 곧바로 시험된다.  n_*=3 절단으로 적분하며 잰 결과 (m=1):
```
   t     r_dyn      5w      √(3w)   r_exact
 0.00   0.9356   1.4801   0.9424   0.9273
 0.30   0.7345   1.0126   0.7794   0.8792
 0.60   0.5149   0.6444   0.6218   0.8071
```
끌개는 **정확 구적의 비를 잘 따라가고** (t=0 에서 0.9356 vs 0.9273), 5w 는 1.58배
과대평가하다가 시간이 가며 1.25배로 줄어든다 (w 가 떨어져 5w 의 영역에 가까워진다).
√(3w) 가 내내 더 가깝다.

### 6. ★★ 그런데 닫힘으로 쓰면 **이기지 못한다** (정직한 부정 결과)
`tilted_closure` 에 `phys_sqrt` / `phys_5w` / `phys_interp` 를 넣고 겨뤘다 (m=1):
```
 모드          v=0        v=0.15      오염비
 frozen      2.19e−5    3.47e−5     +0.581
 ratio       1.89e−5    3.72e−5     +0.970
 phys_sqrt   1.88e−5    3.71e−5     +0.975
 phys_interp 4.52e−5    8.48e−5     +0.876
 phys_5w     3.79e−4    1.14e−3     +1.998
```
- `phys_sqrt` 는 데이터 구동 `ratio` 와 **사실상 동률** (더 낫지 않다).
- `phys_5w` 는 **20배 나쁘다** — 단일 r 을 모든 l 에 적용하는데 5w 는 l=1 전용이라서다.
- ★★ **"frame-invariant" 단서가 tilt 오염도 감소로 나타나지 않는다.**
  오히려 `frozen` 이 가장 둔감하다 (+0.581).  선형이론의 프레임 불변성이 우리
  비선형 배경에서 자동으로 이득이 되지는 않는다 — **반증으로 기록**한다.

⇒ 실용적 결론: 기본값은 그대로 데이터 구동 `ratio` 를 쓴다.  물리 처방은
  **모드로 남겨** 두되 (재현·비교용) 기본으로 승격하지 않는다.

### 상태
```
pytest 829 passed (L2 신규 14)   cargo test 49 passed
신규: audit/l2_ratio_closure.py, tests/test_l2_ratio_closure.py
수정: tilted_closure.py (phys_sqrt / phys_5w / phys_interp 모드 + L2 진단 5종)
      — 기본값 `ratio` 의 결과는 불변 (회귀시험으로 고정)
```
### 다음
L 티어 완결.  남은 것은 **type V 정확구적 오라클** (K4 — tilt 결합의 선행조건),
그 다음 R 티어(Rust 이식)와 D 티어(이벤트 검출·혼합정밀).

---

## 진행 32차 — K4 · type V 자유흐름 오라클: **tilt 결합의 문이 열렸다**

### 0. 왜 필요했나
K2b 에서 확립한 no-go: Codazzi 구속이 Bianchi I 에서 q = 0 을 강제하고, 대각 N·Σ 이면
class A 전체가 그렇다.  tilt 를 담으려면 **A ≠ 0 그리고 Σ ≠ 0** 이 동시에 필요하다
(등방 type V 는 용량이 다시 0).  그런데 type V 에서는 **불변기저 운동량이 보존되지
않아** type I 정확 구적의 근거(p_i = const)가 사라진다.  구조상수 C²₁₂ = C³₁₃ = A 로
```
ṗ₁ = (A/E)(p₂p² + p₃p³),   ṗ₂ = −(A/E)p¹p₂,   ṗ₃ = −(A/E)p¹p₃
```
가 결합한다.  자유흐름 해를 **특성곡선으로 다시 세워야** 했다.

### 1. ★★ 두 경로로 확정 (부호를 단정하지 않는다)
- **경로 A**: 구조상수 공식으로 조립
- **경로 B**: 계량 `ds² = −dt² + a₁²dx² + e^{2Ax}(a₂²dy² + a₃²dz²)` 에서 **기호 측지선**
  `dp_μ/dλ = ½∂_μ g_{αβ}p^αp^β` 을 뽑고 좌표 → 불변기저 변환
```
x = 0.00 → 잔차 2.05e−16      x = +0.37 → 4.10e−16      x = −0.50 → 2.05e−16
```
(질량 0 / 0.6 / 2.0 에서도 동일 수준.)

### 2. 보존량 게이트 — 넷 다 통과, 대조군도 살아 있다
```
A → 0 에서 p_i = const        0.00e+00      ← 비트 단위 (type I 근거 복원)
p₂/p₃ 보존                    4.58e−16      ← ṗ₂/p₂ = ṗ₃/p₃ 에서 곧바로
등방 Σp_i² 보존               6.77e−14      ← open FRW 의 λ ∝ 1/a 와 같은 말
  이방 대조군 (깨져야)         1.96e−03      ← 게이트가 죽지 않았음
★ Killing 운동량 p_y, p_z     1.53e−15 / 3.03e−15   (x 가 실제로 0.2329 이동)
등방 무질량 ρ ∝ a⁻⁴           1.23e−12
```
★ Killing 게이트가 특히 강하다: y, z 가 순환좌표라는 사실만 쓰고 **불변기저 ODE 를
  전혀 안 쓴다** — 좌표 그림에서 온 완전 독립 확인이다.

### 3. ★ 측도의 함정 (측정으로 잡았다)
불변기저 측도 d³p 는 **보존되지 않는다**:  `∂ṗ_i/∂p_i = −2A p¹/E ≠ 0`.
(좌표 측도는 보존되고 `d³p_inv = e^{−2Ax}d³p_coord` 이라 정합한다.)
⇒ 순방향으로 입자를 밀며 야코비안을 따라다니면 오차가 쌓인다.
그래서 **역방향 특성곡선**을 쓴다: 시각 t 의 구적 격자점마다 t=0 으로 거슬러 올라가
f₀ 를 읽는다.  f 가 특성곡선을 따라 상수이므로 **야코비안이 전혀 필요 없고**, 격자는
t 에서 고정이라 측도는 type I 과 같은 `d³P = d³p/V` 를 그대로 쓴다.

**종단 게이트**: A→0 에서 모멘트가 type I 정확 구적과 **9.54e−17** 일치
(역추적·격자·측도를 한 번에 검증).

### 4. ★ 사전 경고가 **너무 비관적이었다** (정직하게 기록)
설계 때 "닫힌형이 아니라 정확도가 ODE 솔버에 걸리므로 type I 오라클보다 약할 것" 이라
적어 두었다.  실제로는:
```
역추적 nsteps =   8 :  3.428e−09
                 32 :  1.353e−11
                 64 :  8.470e−13
                128 :  5.281e−14      수렴차수 4.00 (RK4)
```
**약한 오라클이 아니다.**  64스텝에 이미 1e−12 아래고 비용도 작다.
경고를 남겨 두되 틀렸다는 사실을 시험으로 고정했다
(`test_the_pessimistic_prediction_was_wrong`).

### 5. 부수 — bootstrap 에 스냅샷 절차 추가
컨테이너 회수가 잦아져 `bash bootstrap.sh --snapshot NAME` 을 넣었다.
★ 처음에 스크립트 **끝**에 붙였다가 회귀시험(13분)을 다 돌고서야 스냅샷이 나왔다 —
"빨리 저장" 용도에 맞게 **앞으로 옮기고 즉시 종료**하게 고쳤다.

### 상태
```
pytest 845 passed (K4 신규 16)   cargo test 49 passed
신규: bianchi/matter/type_v.py, audit/k4_type_v.py, tests/test_k4_type_v.py
수정: bootstrap.sh (--snapshot)
```
### 다음
K4 의 남은 조각: **type V 에서의 boosted 모멘트**(tilt 를 실제로 켠 T^{μν} 프레임
무관성 게이트) → 그 위에서 K1+K2 결합.  그 다음이 R 티어(Rust 이식)와 D 티어.

---

## 진행 33차 — K4c · type V boosted 모멘트: **자유흐름이 스스로 열유속을 만든다**

### 0. 구조는 H5-d 와 같고, 딱 한 군데가 다르다
- **같은 것**: boost 대수는 순수 대수라 `tilted_moments.boost_momentum` 을 그대로 쓰고,
  측도도 `d³P′ = (E′/E)d³P` 그대로.
- **다른 것**: 격자에서 f 값을 얻는 방법.  type I 은 `f₀(q)` 를 바로 읽지만 type V 는
  **역방향 특성곡선**으로 t=0 까지 거슬러야 한다 (K4b 의 `back_trace` 재사용).

### 1. 환원 게이트 둘
```
v = 0 에서 K4b 와            0.000e+00     ← 비트-정확 (E′=E, P′=P)
A → 0 에서 type I boosted    2.170e−16     ← 두 독립 오라클이 만난다
                                             (역방향 특성곡선 vs 닫힌형 1/a 적색이동)
```

### 2. ★ 게이트가 실패했고, 그게 물리를 가리켰다
T^{μν} 프레임 무관성 게이트를 세웠는데 잔차가 **1e−3 에서 멈췄다**.
진단 필드가 원인을 짚었다: `normal_flux = 6.3e−3`.

`tilted_moments.project_stress_to_tilted` 는 **법선틀 q = 0 을 가정**한다.
type I 등방 f₀ 에서는 홀수 l 이 항등적으로 0 이라 옳았지만,
**type V 는 자유흐름만으로 q ≠ 0 을 만든다** — 특성곡선이 p₁ 의 홀짝 대칭을 깨기 때문이다.
q 를 포함한 사영(`project_stress_with_flux`)으로 바꾸니:
```
★★ T^{μν} 프레임 무관성:  rho 3.8e−16   p 4.8e−17   q 1.9e−15   pi 2.0e−15
```
(v = (0.15,−0.1,0.2) / (0,0,0.3) / (−0.2,0.05,0.1) 전부.)
계층을 전혀 안 쓰므로 특성곡선·측도·boost 를 한 번에 검증한다.

### 3. ★★ 물리 결과 — K2b 가 연 자리가 **실제로 채워진다**
등방 f₀(|p|) 로 출발해도 type V 자유흐름이 법선틀 열유속을 만든다:
```
   t      |q|/ρ              m     A     |q|/ρ
 0.00   2.67e−17           0.0   0.0   2.70e−17     ← A=0 이면 **정확히** 0
 0.10   1.48e−03           0.0   0.2   1.95e−03
 0.20   3.27e−03           0.0   0.4   3.88e−03
 0.35   6.31e−03           0.0   0.7   6.67e−03
 0.50   9.47e−03           2.0   0.7   4.39e−03     ← 무거우면 작다
```
A 에 대해 거의 **선형**(측정 지수 0.9~1.05), t=0 에서 정확히 0 ⇒ 초기조건이 아니라
**자유흐름이 만든다**.  K2b 가 "type V 는 tilt 를 담을 수 있다" 고 한 자리에
실제 물질이 스스로 들어간다.

### 4. Codazzi 로 그 유속을 담는 A 를 푼다 — 그리고 정직한 한계
```
|q|/ρ = 6.31e−3  →  A_solved = (0.035030, 0, 0),  Codazzi 잔차 8.67e−19, rank 3
```
K2b 는 **임의로 준** q 로 구성적 해를 보였는데, 이번엔 **실제 물질에서 나온** q 다.

★ 다만 푼 A(0.035)가 넣은 A(0.7)와 **약 20배** 다르다.  배경을 처방했으니 당연하지만,
  그 차이를 숫자로 남긴다 — "지금은 test-field 구성" 이라는 사실의 정량적 표현이다.
  자기정합 해를 얻으려면 **결합계를 함께 풀어야** 한다 (다음 증분).

### 상태
```
pytest 860 passed (K4c 신규 15)   cargo test 49 passed
수정: bianchi/matter/type_v.py (boosted 모멘트 + 프레임 게이트 + 자발적 유속 진단),
      audit/k4_type_v.py;  신규 tests/test_k4c_type_v_tilted.py
```
### 다음
**K5 — type V 결합계**: 자발적 q 를 Codazzi 로 담으면서 Σ, a, A 를 함께 진화시킨다.
K1 의 Einstein 결합 + K3 의 Bianchi 항등식 게이트를 그 위에 얹는다.

---

## 진행 34차 — K5a · type V 의 Einstein 텐서를 **계량에서 직접**, 그리고 정합 구성 발견

### 0. 왜
K4c 까지 `3Σ^{ab}A_b = q^a` 와 type V 곡률을 **인용해** 썼다.  결합계를 세우기 전에
계량 `ds² = −dt² + a₁²dx² + e^{2Ax}(a₂²dy² + a₃²dz²)` 에서 직접 확정한다.

### 1. 자기검증부터 — 사영 성분에 **x 가 하나도 안 남는다**
정규직교 불변기저 `e₀=∂_t, e₁=(1/a₁)∂_x, e_{2,3}=e^{−Ax}/a_{2,3}∂_{y,z}` 로 사영한
G_{âb̂} 에서 x 잔존 성분 **0개**.  하나라도 남으면 그 뒤 계산은 볼 필요가 없다.

### 2. 구속식을 측정으로 얻었다
```
Friedmann:  G₀₀ − 3H² + σ² = **−3A²/a₁²**      ⇒  3H² = ρ + σ² + 3A²/a₁²
Codazzi  :  G₀₁ = 3σ₁·A/a₁
등방 극한 :  G₀₁ = 0,  곡률항 −3A²/a²          ← open FRW ✓
A → 0     :  G₀₀ − (3H² − σ²) = 0              ← Bianchi I 재생산 ✓
```

### 3. ★ 부호 논쟁을 **교환자 계산으로** 끝냈다
측정한 `G₀₁ = +3σ₁A/a₁` 이 프로젝트 규약 `CODAZZI_G0A_SIGN = −1`
(`G_0a = −3a_bσ^{ab}`) 과 어긋나 보였다.  규약이 틀린 게 아니라 **a_a 의 부호**였다:
```
[e₁,e₂] = −(A/a₁)·e₂,   [e₁,e₃] = −(A/a₁)·e₃      ⇒  a_a = (−A/a₁, 0, 0)
```
우리 계량이 `e^{+2Ax}` 이면 Ellis-MacCallum 구조상수가 **음수**다.
그 값을 넣으니 규약이 **잔차 정확히 0** 으로 재생산됐다.  두 쪽 다 맞았고,
어긋난 것은 중간 부호 하나였다 — 논쟁 대신 교환자를 계산해 끝냈다.

### 4. ★ K4c 의 "20배 차이" 는 **정규화 문제였다**
K4c 는 확장정규화(q/3H² 등)로 자기정합성을 쟀는데 규격화 인자가 불확실했다.
K5a 로 확정한 **물리 단위** Codazzi (`q₁ = 3σ₁a₁`, `a₁ = −A/a₁(t)`) 로 다시 재면:
```
   t      q₁          a₁(실제)    a₁(필요)    비
 0.10  +7.35e−02   −0.62960   +0.40844   −0.6487
 0.35  +1.17e−01   −0.48303   +0.65140   −1.3486
 0.50  +9.79e−02   −0.41202   +0.54393   −1.3201
```
|비| 가 **O(1)** 이다 — 20배가 아니라.  물리가 아니라 정규화가 문제였다.
(유속은 x 축 전용, 횡성분 1.4e−17.)

남은 것은 **부호**였다: σ₁ 의 부호가 정합 여부의 부호를 정한다 (σ₁>0 이면 비가 음수).

### 5. ★★ 구성적 결과 — 정합 구성이 **존재한다**
q 가 σ 에도 의존하므로 이것은 고정점 문제다.  σ₁ 을 이분법으로 풀면:
```
σ₁ = −0.0271202,  q₁ = 0.0405165,  a₁ = −0.4979862
⇒ q₁ − 3σ₁a₁ = 0  (ρ 로 규격화한 잔차 **0.0**),  비 = 1.0
```
**자유흐름이 만든 열유속을 기하가 그대로 담는 type V 구성**이다.
K2b 의 no-go → K4c 의 자발적 유속 → 여기서 정합 구성까지 이어졌다.

★ 정직한 범위: 이것은 **운동량 구속만** 만족시킨 구성이다.  Friedmann 구속과
  진화방정식까지 함께 푸는 것은 K5 결합계(다음)의 몫이다.

### 상태
```
pytest 870 passed (K5a 신규 10)   cargo test 49 passed
신규: audit/k5_type_v_einstein.py, tests/test_k5_type_v_einstein.py
수정: bianchi/matter/type_v.py (물리단위 자기정합성 + σ 고정점 풀이)
```
### 다음
**K5b — 결합 진화**: 이 정합 구성을 초기조건으로 삼아 a_i, A, σ 를 Friedmann·
Raychaudhuri·전단 진화와 함께 굴리고, K3 의 Bianchi 항등식 게이트로 감시한다.

---

## 진행 35차 — K5b · **결합 진화: 운동론이 드디어 기하를 움직인다**

K5a 는 운동량 구속**만** 만족하는 type V 구성을 찾고 멈췄다.  여기서 Friedmann·
Codazzi·공간 Einstein 식과 Liouville 방정식을 **함께** 굴린다.  프로젝트가 처음부터
겨냥한 자리다 — 배경을 처방하지 않고 물질이 스스로 만든다.

### 1. 운동론을 순방향으로 — 측도 규칙을 **대조로** 확정
K4b 는 매 시각 t=0 까지 역추적했다 (O(nsteps²)).  결합 진화에는 못 쓴다.  노드와
무게를 함께 미는 Liouville 판으로 바꾼다:
```
ṗ_i = 특성곡선 (K4a),      d ln W/dt = −2A p¹/E
```
둘째 항은 불변기저 측도가 보존되지 않아서 생긴다.  **가정하지 않고** 역추적 판과
대조했다:
```
규칙 사용 : 1.421e−11
규칙 제거 : 1.204e−01      배율 8.5e+09
```
두 판은 격자마저 다르다 (역추적은 시각 t 의 고정 구면격자, 순방향은 t=0 격자를
실어 나른 찌그러진 격자) — 그래서 이 일치는 특성곡선·야코비안·구적을 한꺼번에 잡는다.

### 2. ★ 반증 기록 — 1/V 를 적분기에 실으면 안 된다
처음엔 무게에 측도 d³P = d³p/V 를 통째로 넣어 `−2Ap¹/E − 3H` 로 굴렸다.  그런데
**A = 0 대조군이 0 이 아니라 1.7e−09** 를 냈다.  원인을 예측식으로 못 박았다:
```
옛 판 오차   1.732e−09
예측 n·z⁵/120 1.702e−09      (z = 3H·dt, RK4 지수함수 상대오차)
새 판        0.000e+00      ← 비트-정확
```
V 는 어차피 a_i 에서 정확히 나온다.  W 는 공변무게 f·d³p 만 나르고, 모멘트를 잴 때
V=∏a_i 로 나눈다.  **적분기가 이미 정확히 아는 것을 다시 적분하지 않는다.**

### 3. 기하 — K5a 기호식과 **왕복**
`accelerations` 는 공간 Einstein 식을 ä_i/a_i 에 대해 푼다.  세 방향 모두 곡률항이
**같은** A²/a₁² 다 (1 축이 특별한데도 — K5a 기호 계산에서 읽었다):
```
G11 = A²/s1² − h2h3 − u2 − u3
G22 = A²/s1² − h1h3 − u1 − u3
G33 = A²/s1² − h1h2 − u1 − u2
```
기호식 → 물질 역산 → `accelerations` → u 되찾기 왕복 잔차 **1.3e−15**.

### 4. ★★ 구속을 **부과하지 않는다** — 그리고 4차로 사라진다
Friedmann 도 Codazzi 도 진화식에 넣지 않았다 (넣으면 게이트가 자기 자신을 검사한다).
그런데 잔차가 RK4 4차로 수렴한다:
```
nsteps   F/ρ         C/ρ         비
   30  1.323e−06   1.933e−10
   60  8.141e−08   1.153e−11   16.3 / 16.8
  120  5.042e−09   7.029e−13   16.1 / 16.4
  240  3.136e−10   4.335e−14   16.1 / 16.2
```
특성곡선·측도·모멘트 정의·공간 Einstein 식 중 하나라도 어긋나면 dt→0 에서 상수로
남는다.  **역학이 구속을 보존한다** — 우리가 강제한 게 아니다.

### 5. ★★ 물리 결론 — **자발적 열유속을 기하가 흡수한다**
등방 f₀ 로 출발하므로 t=0 에 q₁ = 0 이고, Codazzi 가 σ₁ = 0 을 강제한다.
자유흐름이 q₁ 을 만들어내면 σ₁ 이 **따라간다**:
```
  t      q₁            σ₁            −q₁a₁/3A (예측)
0.000  −1.9e−15     +0.000000e+00   +9.1e−16
0.050  +3.093465e−03 −1.805522e−03  −1.805522e−03
0.100  +3.011662e−03 −2.032940e−03  −2.032940e−03
0.200  +2.062329e−03 −1.711805e−03  −1.711805e−03
0.300  +1.385464e−03 −1.333670e−03  −1.333670e−03
```
상대 일치 **1.0e−07**.  K4c 가 발견한 "type V 자유흐름은 열유속을 스스로 만든다" 가
K5a 의 Codazzi 를 거쳐 여기서 **닫혔다**: 유속이 생기면 전단이 그만큼 기운다.

### 6. 대조군 셋
```
A=0 에서 K1 독립 적분기와 궤적 일치   a 5.5e−11,  σ₁ 2.1e−12  (4차 수렴)
A=0 에서 q₁ = 1.9e−15                 (유속의 원인이 A 임을 확정)
σ₁(0)=0.05 로 깨면 C/ρ = 1.4e−03
```
K1 은 H 를 구속에서 풀고 σ̇=−3Hσ+π (1계) 를 쓰며 물질은 닫힌형 적색이동 구적이다.
K5b 는 ä 를 공간 Einstein 에서 풀고 (2계) Friedmann 을 아예 안 쓰며 물질은 특성곡선
노드다.  공유하는 게 거의 없는 두 경로가 만난다.

★ **반증 기록 (b)**: "A=0 이면 σ₁ 도 0" 이라고 적었는데 틀렸다.  σ₁ 은 2.1e−03 까지
자란다.  A=0 에서 Codazzi 는 `3σ₁·0/a₁ + q₁ = q₁ = 0` 으로 **퇴화**해 σ₁ 을 전혀
구속하지 않기 때문이다 — σ₁ 은 비등방 압력 π₁₁ 이 끈다 (K1 과 같은 물리).

### 7. ★★ 구속 위반은 **번진다**
§4 의 4차 수렴이 "구속을 몰래 부과해서" 나온 게 아님을 반대편에서 확인한다.
σ₁(0) = 0.05 로 일부러 Codazzi 를 깨면, Friedmann 은 t=0 에 **정확히 만족**(7.6e−17)
하는데도 자란다:
```
  t     F/ρ          C/ρ
0.00  +7.55e−17    +1.4288e−03
0.10  +1.91e−04    +1.3944e−03
0.20  +3.64e−04    +1.3648e−03
0.30  +5.21e−04    +1.3377e−03
```
Bianchi 항등식 ∇_μG^{μν}=0 과 ∇_μT^{μν}=0 이 두 잔차를 **닫힌 선형계**로 묶는다.
C ≠ 0 이 F 를 민다 — 교과서적 구속 전파를 그대로 본다.

### 상태
```
pytest 884 passed (K5b 신규 14)   cargo test 49 passed
신규: bianchi/matter/type_v_coupled.py, tests/test_k5b_type_v_coupled.py
수정: audit/k5_type_v_einstein.py (spatial_einstein_symbols/numeric), bootstrap.sh
```
### 다음
· **K5c** — 지금은 A 를 좌표상수로 고정했다.  물리 성분 A/a₁ 은 진화하지만
  ȧ_a 진화식을 독립으로 검증하지 않았다.  N=0 이 유지되는지도 감시 대상이다.
· **D2** — 여기서 만든 상시 구속 감시자를 일반 Bianchi 로 올린다 (구속 투영).
· **R1–R3** — 정확도가 확보됐으니 Rust 이식.

---

## 진행 36차 — K5c · **A 를 상수로 둔 것은 근사가 아니라 게이지였다**

K5b 는 계량의 A 를 좌표상수로 고정하고 굴렸다.  그런데 Ellis-MacCallum 형식에서
a_a 는 `ȧ_a = −(Hδ + σ)·a` 로 **진화하는 변수**다.  그 식을 K5b 에 넣은 적이 없다.
넣지 않아도 되는지를 계량에서 확정한다.

### 1. 전체 교환자표 — K5a 는 두 개만 봤다
```
[e₀,e₁] = −(ȧ₁/a₁)e₁     [e₁,e₂] = −(A/a₁)e₂
[e₀,e₂] = −(ȧ₂/a₂)e₂     [e₁,e₃] = −(A/a₁)e₃
[e₀,e₃] = −(ȧ₃/a₃)e₃     [e₂,e₃] = 0
기저 밖으로 새는 성분: 0개
```
[e₀,e_α] 에 **가속도항도 회전항도 없다** — 법선 측지 합동이고 Ω = 0 이다.
공간 구조상수를 프로젝트 자신의 `algebra.decompose` 에 넣으면
```
n_ab = 0.0 (정확히),   a_a = (−0.53846154, 0, 0) = (−A/a₁, 0, 0),   Jacobi n·a = 0.0
```
sympy 교환자 → numpy 대수 분해, 두 경로가 만난다.  ṅ 은 n 에 선형이므로 **유형이
흐르지 않는다**.

### 2. ★★ 결론 — A = const 는 **진화식의 정확해**
```
a_1(t) = −A/a₁(t)  ⇒  ȧ_1 + (H + σ₁)a_1 = 0        잔차 = 0 (항등식)
확장정규화 차트:      A₁′ − (q − Σ₁)A₁ = 0          잔차 = 0
```
K5b 가 Ellis-MacCallum 진화식을 빠뜨린 게 아니었다 — ansatz 가 이미 만족하고 있었다.
차트 쪽 잔차도 0 이라 **물리 단위와 확장정규화 사이 규격화 인자**가 여기서 잠긴다.

그리고 x → λx 에서 (A, a₁) → (λA, λa₁) 이므로 **a_a = −A/a₁ 은 불변**이다.
A 를 상수로 고정한 것은 물리적 제약이 아니라 **게이지 고정**이었다.
수치로도 확인: λ=1.4 에서 ρ, H, σ₁, q₁ 이 1e−9 이내로 같다.
(λ=2.5 의 1e−4 는 **각 구적** 오차다 — NANG 24→32→40 에서
6.1e−05 → 1.9e−06 → 5.6e−08 로 스펙트럼 수렴한다.)

### 3. ★★ Ḣ 이중경로 — 정확한 대수 항등식
Ḣ 를 두 경로로 잰다.  (A) 공간 Einstein 식 `Ḣ = ⟨u_i⟩ − ⟨h_i²⟩` (Friedmann 미사용),
(B) Raychaudhuri `Ḣ = −H² − ⅔σ² − (ρ+3p)/6`.  대수적으로 차이가 **정확히 −F/6** 이다:
```
nsteps=30   gap 4.011e−06   예측 4.011e−06   잔차 1.755e−16
nsteps=60   gap 2.473e−07   예측 2.473e−07   잔차 2.542e−16
```
두 진단자(공간식과 Friedmann 잔차)를 묶는 식이라 어느 한쪽이 틀리면 기계정밀도로
맞을 수 없다.

### 4. ★★ 정직한 한계 — 대각 계량은 대각 물질만 담는다
비대각 Einstein 성분이 **항등적으로 0** 이므로 물질도 q₂=q₃=π_{i≠j}=0 이어야 한다.
어떤 f₀ 가 허용되는지 숫자로 못 박았다:
```
등방 f₀            q⊥ 9.7e−18    π_off 2.4e−17     허용
쌍극자 p₁          q⊥ 5.8e−18    π_off 3.3e−17     **허용**
쌍극자 p₂          q⊥ 1.0e−01                      거부
사중극자 p₁p₂                     π_off 5.9e−02     거부
사중극자 p₂p₃                     π_off 5.7e−02     거부
```
★ 필요한 것은 **등방성이 아니라 횡방향 반사대칭 두 개**다 (p₂→−p₂, p₃→−p₃).
p₁ 쌍극자는 q₁ ≠ 0 을 만들지만 그건 G_{0̂1̂} 이 담는다.

특성곡선이 그 대칭을 보존하므로 허용된 물질은 **진화 내내** 허용된다 (2.4e−17).
그리고 `evolve_coupled` 에 t=0 검사를 넣어 **거부**한다 — 검사가 없으면 비대각
성분을 조용히 버리고 틀린 답을 낸다 (`check_ansatz=None` 로 끌 수 있다, 대조군용).

### 상태
```
pytest 899 passed (K5c 신규 15)   cargo test 49 passed
신규: audit/k5c_type_v_structure.py, tests/test_k5c_type_v_structure.py
수정: bianchi/matter/type_v_coupled.py (ansatz 검사 + Ḣ 이중경로 기록 + 게이지 도구)
```
### 다음
**D2 — 구속 사영/감시**: K5b §7 이 "구속 위반은 번진다" 를 관찰만 했다.
성장률을 예측·측정하고, 물리 단위 투영기를 만들어 무엇을 고치고 무엇을 못 고치는지 잰다.

---

## 진행 37차 — D2 · **구속 위반의 전파 법칙을 유도하고, 투영기를 만든다**

K5b §7 은 "Codazzi 를 깨면 Friedmann 이 따라 자란다" 를 **관찰만** 했다.
`bianchi/constraints.py` 는 class B 증폭률 `4(q + Σ₊ − 1)` 을 **인용**해 두었을 뿐
어디서도 재지 않았고, 일반 차트의 `amplification_rate` 는 그냥 0.0 을 돌려준다.

### 1. ★★ 전파 법칙을 계량에서 유도
구속 잔차를 텐서 하나로 묶는다.  E^{μν} ≡ G^{μν} − T^{μν} 이고, 결합 진화가 공간
성분 E^{îĵ} = 0 을 매 순간 강제하므로 (ä_i 를 거기서 푼다) 남는 성분이 정확히 두
감시자다: E^{0̂0̂} = F, E^{0̂1̂} = C.  ∇_μG^{μν} ≡ 0 과 ∇_μT^{μν} = 0 이므로
∇_μE^{μν} = 0 이 닫힌 계를 준다.  sympy 로 직접:
```
(∇E)² = (∇E)³ = 0  (항등적으로)          x 가 남은 성분: 0개

Ḟ = −3H·F − 2(A/a₁)·C
Ċ = −(4H + σ₁)·C                        ← M[1,0] = 0, **삼각행렬**
```
**Friedmann 잔차는 Codazzi 로 되먹임하지 않는다.**  닫힌형도 대입으로 확인했다
(잔차 0): `C ∝ 1/(a₁²a₂a₃)`, `F 의 제차해 ∝ 1/V`.

### 2. ★★ 같은 Φ 를 섭동 실험으로 **측정**해 대조
독립 섭동 둘로 기본해 행렬 Φ(t) 를 만들고, 유도한 닫힌형과 비교한다.  측정 쪽은
결합계를 굴려 얻고, 예측 쪽은 궤적의 a_i(t) 만 쓴다.
```
ε = 4e−3 → 8.603e−05      Φ_CF(되먹임) 측정 2.6e−11  (유도 예측: 정확히 0)
ε = 2e−3 → 4.303e−05
ε = 1e−3 → 2.153e−05      ← ε 에 **정비례**: 남은 건 O(ε) 비선형뿐
```
그리고 **(F, C) 가 닫힌 선형계인지** 반증 가능하게 물었다: 셋째 독립 섭동을 Φ 로
예측해 1.8e−05 로 맞았다.  다른 자유도가 끼어들었다면 어긋났을 것이다.

★ **반증 기록**: 첫 판은 Φ_FC 의 **부호**가 반대였다 (상대차 6.8e−02, 대각 성분은
1e−4 로 맞음).  유도는 반변 E^{μν} 로 했는데 K5b 의 감시자는 공변 G_{0̂1̂} 기준이고
η^{0̂0̂}η^{1̂1̂} = −1 이다.  부호를 논쟁 대신 **지표 올리기로** 정했다.

### 3. ★★ 인용식을 유도로 바꾼다
확장정규화 (𝒞 = C/H², dτ = H dt) 로 옮기면
```
d ln𝒞/dτ = −(4H+σ₁)/H + 2(1+q) = 2(q + Σ₊ − 1)        잔차 0
```
`constraints.py` 가 인용한 `4(q + Σ₊ − 1)` 은 정확히 **두 배**다 — class B 의 Codazzi
잔차 `Σ̃Ñ − Δ² − Σ₊²Ã` 가 선형 자료의 **2차형식**이기 때문이다.  인용이 맞았고,
이제 그 이유까지 안다.

### 4. ★★ 증폭은 **방향에 달렸다**
`constraints.py` 는 "구속은 지수 증폭한다" 고 적어 두었는데, 유도한 법칙은 더
구체적이다: C ∝ 1/(Va₁) 이므로 **팽창이면 감쇠, 수축이면 증폭**이다.
```
팽창 t∈[0,0.15]   V 4.007배   C 0.157배    닫힌형 잔차 9.5e−09
수축 t∈[0,0.08]   V 0.094배   C 23.3배     닫힌형 잔차 3.4e−07
```
같은 닫힌형이 두 방향 다 맞는다.  (특이점에 더 다가가면 — V 0.001배 — 잔차가
4e−03 로 커진다.  RK4 자체가 거기서 정확도를 잃는 것이고, 정직하게 기록해 둔다.)

### 5. 물리 단위 투영기
h 만 움직이는 최소노름 Gauss-Newton.  야코비안은 해석적이다 (물질은 노드가 나르므로
순간적으로 h 와 무관):
```
∂F/∂h_i = 2H − σ_i          ∂C/∂h_i = 3(A/a₁)(δ_{i1} − ⅓)
중심차분과 1.6e−09,  영공간 성분 6.5e−10 (최소노름)
2차 수렴: 2.07e−03 → 5.30e−05 → 6.89e−10 → 4.11e−16
```
### 6. ★★ 투영이 무엇을 고치고 무엇을 못 고치는가
```
정합 자료에 매 스텝 투영     궤적 차 4.3e−10          ← 무해
수축(증폭) 구간, 투영 off    C 5.81e−04,  F 5.91e−05
수축(증폭) 구간, 투영 on     C 3.61e−17,  F 6.47e−16  ← 잡는다
깨진 자료를 투영하며 굴리면  구속 5.5e−17 인데 a_i 는 정합 궤적과 7.2e−04 차이
```
★ **정직한 몫**: 투영은 **적분 오차를 지우는 도구**지 잘못된 초기자료를 고쳐 주지
않는다.  구속면 위로 되돌리지만 원래 해로 되돌리지는 않는다 (σ₁ 차 3.7e−05).

★ 부수적으로 K5b 의 지표 하나를 고쳤다: 기록이 스텝 **앞**에서 일어나므로 k=0 은
투영 전 값이다.  그걸 최대값에 넣었더니 투영판이 "안 고쳤다"(5.7e−04)로 보였다.

### 상태
```
pytest 921 passed (D2 신규 22)   cargo test 49 passed
신규: audit/d2_constraint_propagation.py, bianchi/matter/type_v_constraints.py,
      tests/test_d2_constraint_propagation.py
수정: bianchi/matter/type_v_coupled.py (hsign 수축 초기자료, project 훅), bootstrap.sh
```
### 다음
· **D2b** — 유도한 삼각 전파계를 **일반 Bianchi 차트**로 올린다 (`amplification_rate`
  가 general 차트에서 0.0 을 돌려주는 자리를 메운다).
· **D1** — Kasner 튐 사건 검출.  · **R1–R3** — Rust 이식.

---

## 진행 38차 — D2b · **일반 차트의 구속 증폭** (0.0 을 돌려주던 자리)

D2 는 type V 를 **물리 단위**에서 계량으로 유도했다.  여기서 프로덕션 차트
(`charts/general`, 확장정규화, JAX) 로 올린다.  손대수가 아니라 **자동미분**이다.

★ 방법: Ċ = J f (J = ∂C/∂y) 이고, 계가 닫혔다면 M 이 있어 Ċ = M·C 다.  구속면
  위(C=0)에서 미분하면 `∂(Jf)/∂y = M·J` 이므로 M 을 최소제곱으로 풀고, **그 잔차가
  곧 닫힘 검사**다.  M 의 스펙트럼이 증폭률이다.

### 1. ★★ 공허한 감시자 둘
```
구속별 기울기 노름: gauss=2.02  trace=0  cod1=2.73  cod2=3.13  cod3=2.70
                    jac1=0.869  jac2=0.923  jac3=0.899
rank(J) = 7   (8 이 아니다)
```
· `trace` 는 기울기가 **항등적으로 0** 이다.  상태를 `tracefree_from_5` 로 짜므로
  tr Σ 는 애초에 위반될 수 없다 — 감시해도 정보가 0 이다.
· `gauss` 는 args 에 Ω 를 안 주면 **항등적으로 0** 이다 (`aux` 가 Gauss 로 Ω 를 소거).
  기본 설정에서 Gauss 감시는 공허하다 (잔차 0.0, 기울기 0.0).

### 2. ★★ 물질을 얼리면 구속계가 닫히지 않는다
차트는 Ω, q_flux, Π 를 **args 로 얼린다**.  그 상태의 닫힘 잔차:
```
6.33e−01   1.00e+00   9.84e−01   1.00e+00      (Jacobi 만족 무작위 배위 4개)
```
### 3. ★★ 닫힘이 **물질 법칙을 결정**한다
잔차 R(c) = Gv(c)(I − J⁺J) 는 계수 c 에 선형이므로 최소제곱 한 번이면 된다.
후보 11개를 넣고 풀면:
```
Q  −2.000000000   ΣQ  −1.000000000   qQ  +2.000000000   ΠA  +3.000000000
εNΠ +1.000000000   NQ  +0.000000000   εAQ −0.000000000
Ω  −2.000000000   qΩ  +2.000000000   A·Q +0.666666667   tr(ΣΠ) −0.333333333
```
즉
```
Q̇^a = (2q − 2)Q^a − Σ^a_b Q^b + 3Π^{ab}A_b + ε^{abc}N_{bd}Π^d_c
Ω̇   = (2q − 2)Ω + (2/3)A_aQ^a − (1/3)Σ_abΠ^{ab}
```
이 계수로 닫힘 잔차 **1.0e−15 ~ 1.6e−15**.  넣어 본 `N Q`, `ε A Q` 는 계수가 **정확히
0** 으로 나왔다 — 기저가 축퇴하지 않았다는 확인이다.

★ **반증 기록**: 첫 적합은 계수가 1~15% 어긋나고 (`NQ`=+0.126) 잔차가 1e−1 에서
  멈췄다.  무작위 배위가 **Jacobi (N^{ab}A_b = 0) 를 위반**하고 있었다 — 구속면
  위가 아니었으니 "구속면 위에서 미분한다" 는 전제가 통째로 무너져 있었다.
  N 의 A-방향 행·열을 0 으로 두자 계수가 정확한 정수·분수로 떨어졌다.
  (대조군으로 남겨 뒀다: Jacobi 를 깨면 같은 계수로도 잔차가 1.4e−01 ~ 3.8e−01.)

### 4. ★★ D2 와 만나는 지점
닫은 뒤 type V 배위(N=0, Π=0)의 Codazzi 대각 증폭률:
```
M[cod1,cod1]  = −0.832733333
2(q + Σ₊ − 1) = −0.832733333        차 2.2e−16
```
D2 가 **계량에서 sympy 로** 얻은 값과 차트가 **JAX 자동미분으로** 준 값이 만난다.
`constraints.py` 의 인용식 4(q+Σ₊−1) 은 그 잔차가 2차식이라 정확히 두 배다.

`amplification_rate` 는 이제 general 차트에서 **조용한 0.0 을 돌려주지 않는다**.
M 의 스펙트럼 상한을 내고, 닫힘 잔차가 크면 (물질이 얼어 있으면) 0.0 대신 **NaN**
을 낸다 — 못 믿을 수를 조용히 내놓지 않기 위해서다.

### 5. ★★ 운동론 해가 그 법칙을 **이미** 만족한다
§3 이 요구한 `3Π^{ab}A_b` 항은 물리 단위에서 `−3(A/a₁)π₁₁` 이다.  K5b 의 해로 재면
(q̇₁ 을 노드에서 정확히 계산 — 유한차분 없이):
```
q̇₁ = −(4H + σ₁)q₁ − 3(A/a₁)π₁₁          잔차 6.3e−12
a·π 항을 빼면                              100% 틀림
   t=0:  참값 +1.615221e−01,  π 없는 판 +3.9e−14
```
t=0 에서는 a·π 항이 **유일한** 원천이다 — 보정이 아니다.  Liouville 수송에 운동량
보존을 넣은 적이 없는데도 성립한다.

### 상태
```
pytest 932 passed (D2b 신규 11)   cargo test 49 passed
신규: bianchi/constraint_rates.py, audit/d2b_chart_amplification.py,
      tests/test_d2b_chart_amplification.py
수정: bianchi/constraints.py (general 차트 증폭률), bianchi/matter/type_v_coupled.py
      (q̇₁ 노드 계산 + 기록), bianchi/matter/type_v_constraints.py, bootstrap.sh
```
### 다음
· **D2c** — 적합한 물질 법칙을 차트에 **실제 상태변수로** 넣는다 (지금은 args 로
  얼어 있어 `solve_with_projection` 이 그 위에서 돈다).
· **D1** — Kasner 튐 사건 검출.  · **R1–R3** — Rust 이식.

---

## 진행 39차 — R1/R2/R3 · **이식과 가지치기** (정확도는 오라클로 지킨다)

"코드가 너무 느리다" 는 지적을 받아 먼저 **프로파일**했다.  옛 R-목록(tilted 적분)이
아니라 K5b/D2 가 만든 새 병목이 지배적이었다.

### 1. 프로파일이 지목한 자리 (55296 노드)
```
moments_from_nodes(l≤2,i≤1)  11.2 ms      kinetic_rhs             4.1 ms
characteristic_rhs_vec        3.3 ms      evolve_coupled 한 스텝   68 ms
```
둘 다 노드 배열을 여러 번 훑는 **메모리 대역폭** 문제다 — 융합 순회 + rayon 이 듣는다.

### 2. R1 — type V 결합 진화 커널 (`_rustcore/src/kinetic/typev.rs`)
RK4 한 스텝 전체(기하 + 운동론 + 진단)를 Rust 안에서 돈다.  노드가 Python 으로
되돌아오지 않는다.
```
evolve_coupled(60스텝)   python 3.94 s → rust 0.38 s     10.4배
```
### 3. R2 — 특성곡선 (배경 처방형)
```
back_trace(256스텝)      python 2.78 s → rust 0.27 s     11배   (차 1.9e−16)
forward_vs_backward      python 1.61 s → rust 0.47 s      3.5배
```
### 4. ★★ 오라클을 **지우지 않았다**
`backend="python"` 이면 numpy 판이 그대로 돈다.  신규 시험 14개가 두 경로를 대조한다:
```
궤적·구속·진단 전 항목   ≤ 1e−11 (상대),  F/C 절대차 ≤ 1e−14/ρ
투영 경로도 일치,  back_trace 1.9e−16,  push_nodes 측도 on/off 양쪽
```
그리고 **물리 결론이 안 바뀐다**: 구속 4차 수렴이 Rust 경로에서 같은 수를 낸다
(1.323e−06 → 8.141e−08 → 5.042e−09 → 3.136e−10, 비 16.3/16.1/16.1).

### 5. R3 — 이식보다 먼저, **낭비를 잘라냈다**
전체 회귀에서 가장 느린 단일 시험이 `test_critical_step_scales_inversely_with_rate`
(103 초) 였다.  원인은 강성이 아니라 이등분 초반의 δt ≈ 1e−6 에서
`t_end/δt = 2.4e+07` 스텝을 **실제로 돌고 있던** 것이다.

S = 0 이면 해가 정확히 `y_n = R(−Γδt)^n y₀` 이므로 `|y_n| ≤ |y₀| ⇔ |R| ≤ 1` 이고
**n 은 판정에 전혀 영향이 없다**.  스텝수를 64 로 잘랐다:
```
103 s → 0.03 s   (3400배)
Γδt_crit = 2.785294 (다섯 τ̇ 전부) — 측정값은 **한 자리도 안 바뀌었다**
```
★ 교훈: 이식 전에 프로파일을 봐야 한다.  가장 큰 한 건은 Rust 가 아니라 **불필요한
  2400만 스텝**이었다.  (프로젝트 원칙 "이식은 빠르게만 할 뿐 정확해지지 않는다" 의
  반대편 짝이다 — 느린 게 항상 계산량 때문은 아니다.)

부수적으로 D2b 의 AD 배위 생성에 `lru_cache` 를 걸었다 (50 s → 35 s).

### 6. 전체 효과
```
회귀 전체   1392 s → 1232 s      (신규 시험 14개를 더하고도 −12%)
K5/D2 계열   242 s →  122 s      (−50%)
V2e          103 s →    6 s
```
### 상태
```
pytest 946 passed (R1/R2 신규 14)   cargo test 53 passed (typev 신규 4)
신규: _rustcore/src/kinetic/typev.rs, tests/test_r1r2_typev_rust.py
수정: _rustcore/src/lib.rs (tv_* 바인딩 6개), bianchi/matter/type_v.py,
      bianchi/matter/type_v_coupled.py (backend 디스패치),
      bianchi/matter/collision_stiffness.py (NS_CAP),
      audit/d2b_chart_amplification.py (캐시), bootstrap.sh
```
### 다음
· 남은 느린 자리: L1/L2/L3 tilted 적분(≈95 s, 옛 R1 목표), M-계열 차트 적분(≈230 s)
· **D1** Kasner 튐 사건 검출,  **D2c** 물질 법칙을 차트 상태변수로 승격

---

## 진행 40차 — R3 · **뜨거운 경로 정리** (L1–L3 tilted 적분, M-계열 차트)

R1/R2 뒤 남은 회귀시간은 세 군데에 몰려 있었다.  프로파일해 보니 셋 다 **이식이
필요한 계산량이 아니라 낭비**였다 — 이번에도 Rust 를 한 줄도 안 늘렸다.

### 1. tilted 적분 (L1/L2/L3):  34.3 s → 2.3 s  (15배)
프로파일이 지목한 자리는 `hierarchy.pstf` 였다 — **호출 87093회에 18.7 초**.
```
pstf 안: l! 개 전치 평균 루프  +  np.linalg.lstsq  (매 호출)
```
둘 다 **입력에 무관한 선형연산**이다.  직교사영을 고정 행렬로 굳혔다:
```
Q_l = (I − A·A⁺)·S_l      (S_l 대칭화, A = 순수 대각합 부분공간)
l=2  40.6 µs → 13.9 µs (3배)   l=4  138 µs → 15 µs (9배)   l=6  4.0 ms → 144 µs (28배)
```
그리고 질량행렬 조립 `dense` — 단위벡터마다 **전체** dict 를 만들어 넘기니 빈 블록에도
0 배열을 만들고 사영까지 했다.  희소 dict 로 바꾸고, 기여가 없는 블록은 사영을 건너뛴다
(pstf(0)=0 이므로 수학은 동일).  **M 은 비트-정확 동일**함을 시험으로 고정했다.

### 2. ★ 정직한 대가 하나 — 비트-일치가 깨졌다
L3 이 고정했던 "1차원 환원 = J̇ 닫힘(r=1)" 의 **상대차 정확히 0.0** 이 1.21e−13 이 됐다.
원인을 **이분법으로 확정**했다 (pstf 만 바꾼 트리 1.21e−13, tilted_mass 만 바꾼 트리 0.0).
```
비트-일치는 수학적 항등식이 아니라 두 경로의 **연산 순서가 같아서** 생긴 것이었다.
남는 차이는 오차값 기준 1.8e−18 절대 — 궤적량이 O(1) 이므로 배정밀도 eps 의 1/100.
```
게이트를 "정확히 0" 대신 **절대 1e−16** 으로 바꿨다 (느슨하게 한 게 아니라 척도를 맞췄다).
시험 docstring 에 경위를 그대로 남겼다.

### 3. Lyapunov·극한주기 (M4/M7):  75.2 s → 1.1 s / 26.7 s → 0.27 s
`for i in range(steps)` 안에서 JAX 를 **즉시 실행**했다 (스텝당 f 4회 + jacfwd 1회 →
디스패치 1만 번).  같은 알고리즘·같은 재정규화 규칙을 `lax.scan` 두 겹으로 옮기고 jit.
```
λ = −0.7265523307057665 (옛)  vs  −0.7265523307057664 (새)   ← 1 ulp
```
### 4. CMB 패턴 (M11):  59.2 s → 0.01 s  (5900배)
`temperature_pattern_diag_bianchi` 가 방향마다 Python 루프로 광선을 적분하고 있었다 —
**R1 때 만든 배치 백엔드(`backend.ray_final_z_batch`)를 안 쓰고 있었다.**
그쪽으로 넘기니 z 가 8.4e−16 로 같고 max|ΔT/T| 는 12자리까지 동일하다.
(`force_python=True` 로 옛 경로를 남겨 두 백엔드를 시험이 대조한다.)

### 5. scipy 대조 (M1):  46 s → 6.6 s
`solve_ivp` 가 RHS 를 수천 번 부르는데 그때마다 JAX 를 즉시 실행했다.  RHS 를 jit 로
한 번 컴파일해 재사용한다 (같은 함수·같은 tolerance).

### 6. ★ 남은 최장 시험은 **계산이 아니라 컴파일**이다
`test_projection_suppresses_exponential_growth` (50.6 s) 를 쪼개 재보니:
```
첫 호출 59.5 s → 둘째 2.6 s → 셋째 0.59 s
```
전부 diffrax 의 일회성 jit 컴파일이다 (그 시험의 cfg 조합이 세션에서 유일해 캐시가
비어 있다).  알고리즘 비용이 아니므로 손대지 않고 **정체만 기록해 둔다**.

### 7. 전체 효과
```
회귀 전체   1232 s → 977 s     (신규 시험 11개를 더하고도 −21%)
39차 시작점 1392 s 대비          −30%
```
### 상태
```
pytest 957 passed (R3 신규 11)   cargo test 53 passed
신규: tests/test_r3_hot_paths.py
수정: bianchi/matter/hierarchy.py (pstf_operator), bianchi/matter/tilted_mass.py
      (희소 조립 + 빈 블록 사영 생략), bianchi/analysis/dynamics.py (lax.scan),
      bianchi/observables/cmb_pattern.py (배치 백엔드), tests/test_m1.py,
      tests/test_l3_massive_closure.py (비트-일치 정정), bootstrap.sh
```
### 다음
· **D1** Kasner 튐 사건 검출,  **D2c** 물질 법칙을 차트 상태변수로 승격

---

## 진행 41차 — D1 · **Kasner 튐 사건 검출** (BKL 사상을 인용이 아니라 측정으로)

`bianchi/integrate.py` 에 이벤트 도구(`float_event`, `blowup_event`,
`singularity_event`)가 있었지만 시험 한 곳에서만 쓰였다.  물리에 쓴다.

### 1. 튐의 정의를 규약에서 확정
chart `class_a` (Wainwright-Hsu) 에서
```
Σ = diag(−2Σ₊, Σ₊+√3Σ₋, Σ₊−√3Σ₋),   p_i = (1 + Σ_i)/3
Kasner 원 (Ω=0, N=0, Σ₊²+Σ₋²=1) 에서 q = 2
d ln N₁/dτ = q − 4Σ₊ = 2 − 4Σ₊      ⇒ N₁ 벽은 Σ₊ < 1/2 에서 자란다
```
**튐 = 벽의 성장률이 0 을 아래로 지나는 순간**이고, 그 근을 Newton 으로 정확히 푼다.
Kasner 매개변수는 u = p_max/p_mid (표준 매개화의 역) — 왕복 잔차 ≤ 2.2e−16.

### 2. ★★ Bianchi II 를 오라클로 — Kasner 사상을 **측정**한다
벽이 하나라 튐도 정확히 한 번이고, 그 사상이 BKL 사상의 기본 벽돌이다.
τ 를 **키우며** 재므로 (특이점에서 **멀어지는** 방향) 나오는 것은 BKL 의 **역**이다:
```
  u_in     u_out(측정)   bkl(u_out)    |차|
  1.300     1.7692308    1.3000000   7.2e−14
  1.700     1.5882353    1.7000000   7.6e−14
  2.400     1.4166667    2.4000000   1.0e−13
  3.100     1.3225806    3.1000000   8.4e−14
  4.600     1.2173913    4.6000000   1.5e−13
  7.200     1.1388889    7.2000000   1.9e−13     (원 잔차 ≤ 8e−15)
```
인용식을 쓰는 곳은 `bkl_map` 한 줄뿐이고 나머지는 전부 적분이다.

### 3. ★★ 축 배정이 **사상의 가지**를 정한다
BKL 사상은 두 가지(u≥2 / 1<u<2)라 2-대-1 이고, 역은 두 값이다.  어느 축이 벽을
갖느냐가 그 둘을 가른다 (u_in = 3.7):
```
배정 (1,0,2) → u_out = u_in + 1     = 4.70000000     |차| 1.2e−12
배정 (2,0,1) → u_out = 1 + 1/u_in   = 1.27027027     |차| 1.2e−13
```
둘 다 `bkl(u_out) = u_in` 을 되돌린다.

### 4. ★★ D1 ↔ D2 연결 — 진공면 Ω = 0 의 **구속 증폭**
차트가 Ω 를 Gauss 로 소거해 두었으므로 Ω=0 면의 수치 안정성이 γ 에 달렸다:
```
Ω′ = [2q − (3γ−2)]Ω,  Kasner 원에서 q=2  ⇒  지수 = 6 − 3γ
γ=0     예측 +6.0   |Ω(τ=12)| = 1.16e+10   ← 터진다
γ=4/3   예측 +2.0   |Ω(τ=12)| = 8.93e−07
γ=2     예측  0.0   |Ω(τ=12)| = 4.56e−15   ← 중립
```
D2 가 물리 단위에서 잰 구속 증폭이 여기서는 **적분이 터지느냐**로 나타난다.
진공에서 물리는 γ 와 무관하므로 γ=2 는 **수치적 선택**이고, 그걸 명시해 둔다.

★ **반증 기록 (a)**: 처음엔 Kasner 원 위에 벽을 그냥 켰다.  N₁≠0 이 Ω = −K ≠ 0 을
  만들고 γ=0 의 지수 +6 에 실려 Σ² 가 1e+10 으로 터졌다 — 궤적을 찍어 보고서야
  알았다.  **진공 궤도 위로 사영**하는 것이 옳았다.

★ **반증 기록 (b)**: 고정 τ 로 끊고 u_out 을 읽었더니 벽이 덜 죽어 4.70107 (참값
  4.7) 이 나왔다.  벽이 1e−9 아래로 죽을 때까지 이어 적분하게 고쳤다.

### 5. ★★ 이벤트가 격자 훑기보다 낫다 (숫자로)
같은 튐 시각을 두 방법으로 잰다.  `argmax N₁` 은 출력 격자에 걸려 **1차**로만 준다:
```
이벤트(Newton)  τ_bounce = 2.8190511838
격자   40점 (Δτ=0.1446)  오차 7.228e−02
격자   80점 (Δτ=0.0714)  오차 3.568e−02
격자  160점 (Δτ=0.0355)  오차 1.773e−02
격자  320점 (Δτ=0.0177)  오차 8.837e−03
```
Δτ 를 반으로 줄이면 오차도 반 — 가장 촘촘한 격자도 이벤트에 못 미친다.

### 6. 대조군
```
Bianchi I (N = 0)        튐 발생 False
Σ₊ > 1/2 (벽이 죽는 쪽)  튐 발생 False
u_out 이 u_in 과 0.2 이상 다르다 (사상이 자명하지 않다)
```
### 상태
```
pytest 974 passed (D1 신규 17)   cargo test 53 passed
신규: bianchi/analysis/kasner.py, tests/test_d1_kasner_bounce.py
수정: bootstrap.sh
```
### 다음
· **D1b** — Bianchi IX 의 **연속 튐**.  τ 를 줄이는(특이점 방향) 적분이 필요하고,
  세 벽이 번갈아 켜지므로 이벤트를 벽마다 걸어 가장 이른 근을 골라야 한다.
  u 수열의 Gauss 사상 통계(불변밀도, Lyapunov π²/6ln2)까지 가면 완결된다.
· **D2c** — D2b 가 적합으로 결정한 물질 법칙을 차트의 **실제 상태변수**로 승격
  (지금은 args 로 얼어 있어 `solve_with_projection` 이 그 위에서 돈다).

---

## 진행 42차 — D2c · **물질을 차트의 상태변수로 승격**

D2b 는 "물질(Ω, Q, Π)이 args 로 얼려 있으면 구속계가 닫히지 않는다 (잔차 ~1)" 를
**한 점에서** 보이고, 닫히게 하는 법칙을 적합으로 결정했다.  그런데 그 위에서
`amplification_rate` 와 `solve_with_projection` 이 돌고 있었다.  이제 법칙을 넣는다.

### 1. 새 차트 `charts/general_matter`
Ω 와 Q^a 를 **상태로** 올린다 (Σ 5 + N 6 + A 3 + Q 3 + Ω 1 = 18).  진화식은 D2b 가
닫힘을 요구해 적합으로 결정한 것 그대로 (`constraint_rates.CLOSURE_COEFFS`):
```
Q̇^a = (2q−2)Q^a − Σ^a_b Q^b + 3Π^{ab}A_b + ε^{abc}N_bd Π^d_c
Ω̇   = (2q−2)Ω + (2/3)A·Q − (1/3)Σ·Π
```
★ 기하는 `charts/general.rhs` 를 **그대로 호출**한다 — 한 줄도 다시 쓰지 않았다
  (두 판이 갈라지면 어느 쪽이 맞는지 알 수 없게 된다).  환원 게이트가 이를
  **정확히 0.0** 으로 고정한다 (Q=0, Π=0 에서 기하 RHS 비트-동일).

### 2. ★★ 같은 초기자료, 갈라지는 결말
```
   τ      상태(D2c)     얼림(기존 general)      Ω
  0.00   1.388e−17     1.388e−17            +0.591634
  0.50   7.496e−14     3.209e−01            +0.353791
  1.00   6.442e−13     9.272e−01            +0.171445
  1.50   4.787e−13     2.316e+00            +0.075804
  2.00   3.696e−12     2.335e+01            +0.031983
  2.50   6.534e−13     **NaN**              +0.010813
  3.00   2.676e−14     **NaN**              −0.001492
```
얼린 판은 τ=2.5 에 터진다.  상태로 올린 판은 세 구속(Gauss·Codazzi·Jacobi)이
**진화 내내 1e−12 이하**다.

### 3. ★★ 닫힘이 궤적 위에서 유지된다 (한 점이 아니라)
D2b 는 손으로 만든 배위 한 점에서 닫힘을 봤다.  적분 궤적 위 여러 점에서 다시 잰다:
```
τ=0.00  닫힘 1.33e−15   증폭률(max Re λ) +1.620432
τ=0.50  닫힘 3.04e−14                    +1.029647
τ=1.00  닫힘 2.25e−13                    +0.720267
τ=1.50  닫힘 1.50e−13                    +0.607945
τ=2.00  닫힘 5.73e−15                    +0.570262
```
`constraints.amplification_rate` 가 이 차트에서는 **NaN 을 낼 일이 없다** (D2b 에서
얼린 차트는 닫힘이 깨져 NaN 을 돌려주게 해 뒀었다).

### 4. ★ 잔차가 **적분 tolerance 를 따라간다**
```
rtol=1e−07  최악 잔차 1.901e−10
rtol=1e−09  최악 잔차 2.774e−12
rtol=1e−11  최악 잔차 2.284e−14
```
구조적 표류가 아니라 **적분오차**라는 뜻이다 — 얼린 판의 O(1) 표류와 성격이 다르다.

### 5. 투영 — 확장 상태 위에서
`constraints.make_projector` 에 이 차트를 **명시적으로** 등록했다 (조용한 항등 투영
금지 규약).  기하만 고치면 Ω, Q 가 어긋난 채 남기 때문이다.
```
깨진 자료 (Q, Ω 를 흔듦)  4.0e−03 → 2.2e−16   (보정 크기 1.9e−03)
닫힌 계에서 매 청크 투영   2.2e−14 → 2.8e−17   ← 거의 무동작
```
D2 가 물리 단위에서 얻은 교훈("투영은 적분오차를 지우는 도구")이 그대로 재현된다.

### 6. ★ 정직한 한계 — Π 는 여전히 처방 입력이다
D2b 의 적합이 Π 방향 잔차까지 0 으로 만들었으므로 **어떤 Π 를 주든 구속계는 닫힌다**.
하지만 닫힘과 물질 모형의 무모순성은 다른 이야기다: 처방 Π 는 Ω 를 음수(−1.5e−03)로
끌 수 있다.  물리적 Π 는 운동론 계층(l=2 모멘트)의 몫이고 이 차트의 범위 밖이다.
숨기지 않고 시험으로 고정해 뒀다.

### 상태
```
pytest 985 passed (D2c 신규 11)   cargo test 53 passed
신규: bianchi/charts/general_matter.py, audit/d2c_matter_state.py,
      tests/test_d2c_matter_state.py
수정: bianchi/constraints.py (make_projector·amplification_rate 에 general_matter 등록),
      bootstrap.sh
```
### 다음
· **D1b** — Bianchi IX 연속 튐 (특이점 방향 적분, 세 벽 이벤트, Gauss 사상 통계)
· **B2b** 다준위 재결합 → Lyα,  **V1** l≥4 tilted 부호 (rank-6 PSTF),  **D3** 혼합정밀

---

## 진행 43차 — D1b · **Mixmaster 연속 튐** (u 수열이 BKL 사상을 따른다)

D1 은 Bianchi II 로 **한 번의 튐**을 오라클 삼아 Kasner 사상을 측정했다.  여기서는
벽이 셋인 IX/VIII 에서 **연속으로** 튀는 것을 잡는다.

### 1. 방향이 D1 과 반대다
Mixmaster 의 튐은 **특이점 쪽**(τ 감소)에서 일어난다.  부호 실수를 피하려고
τ̃ = −τ 로 **뒤집은 RHS** 를 써서 D1 의 사건 정의를 그대로 재사용한다:
```
d ln N_i/dτ̃ = −(q − 4Σ₊),   Kasner 원에서 q = 2
⇒ 특이점 쪽으로는 Σ₊ > 1/2 인 벽이 자란다      (D1 의 정반대)
```
D1 이 잰 것이 BKL 의 **역**이었던 것과 앞뒤가 맞는다.

★ u 는 **튐 지점에서 읽으면 안 된다** — 그 순간 벽이 최대라 Kasner 원에서 멀다.
  튐 사이 구간에서 max_i N_i 가 최소인 곳(가장 Kasner 다운 순간)에서 읽는다.

### 2. ★★ 연속 튐 검출과 u 수열 (u₀ = 3.7, 씨앗 1e−3, IX)
세 벽에 각각 이벤트를 걸고 **가장 이른 근**을 고른다.
```
  #   τ̃_bounce  벽    u(에라)      BKL 예측     |차|      maxN
  0     6.5017  N1   3.7000004                          1.0e−03
  1    18.2832  N2   2.7000019   2.7000004  1.44e−06   1.5e−04
  2    30.8984  N1   1.7000019   1.7000019  2.31e−08   4.0e−06
  3    49.2605  N2   1.4285676   1.4285676  7.98e−12   8.3e−11
```
**두 가지가 한 수열에 다 나온다**: u−1 감소 세 번, 그리고 u<2 에서 1/(u−1) 재주입
(1/(1.7−1) = 1.4285714).  에라 구조도 따로 확인했다 — u₀ = 5.4 에서
5.4 → 4.4 → 3.4 → 2.4 → 1.4 (한 에라가 통째로).

### 3. ★★ 반증된 기대 — **혼돈 증폭이 아니라 오차가 줄어든다**
"Kasner 사상은 양의 Lyapunov 라 u 오차가 튐마다 커진다" 고 적어 두고 쟀는데
**정반대**였다:
```
오차   1.44e−06 → 2.31e−08 → 7.98e−12      (줄어든다)
maxN   1.0e−03 → 1.5e−04 → 4.0e−06 → 8.3e−11
```
비활성 벽이 에라마다 **기하급수로 죽어** 구간이 점점 더 Kasner 다워지기 때문이다.
이 구간의 한계는 혼돈이 아니라 **벽의 언더플로**다 (배정밀도 바닥에 닿으면 다음 벽이
자랄 씨앗이 없다).  Lyapunov 증폭은 벽이 O(1) 로 유지되는 훨씬 긴 수열에서야 지배할
것이고, 그건 이 초기화로는 도달할 수 없다 — 숨기지 않고 시험으로 고정했다.

### 4. ★ 오차를 정하는 것은 **씨앗**이지 적분 정밀도가 아니다
```
씨앗 1e−2 → 첫 오차 1.36e−04       씨앗 1e−3 → 1.44e−06   (10배 → ~100배)
rtol 1e−12 vs 1e−10 → u 가 9자리까지 동일
   3.700000440 / 2.700001876 / 1.700001899 / 1.428567554
```
적분을 더 조여도 얻을 게 없다.  씨앗을 줄이면 정확해지지만 튐 하나에 드는 τ̃ 가
길어져 같은 span 으로는 튐을 덜 잡는다 — 교환관계를 숫자로 남겼다.

### 5. 대조군
```
VIII (n = +,+,−)  튐 4회, 오차 3.6e−06 / 2.3e−08 / 8.0e−12   ← IX 만의 성질이 아니다
II   (벽 하나)     튐 1회                                     ← D1 오라클과 정합
I    (벽 없음)     튐 0회
```
### 상태
```
pytest 997 passed (D1b 신규 12)   cargo test 53 passed
신규: bianchi/analysis/mixmaster.py, tests/test_d1b_mixmaster.py
수정: bootstrap.sh
```
★ 회귀 시간이 19 → 26 분으로 늘었다.  원인은 명확하다: 튐 하나마다 세 벽을 각각
  span 만큼 적분해야 하고 (가장 이른 근을 고르려면), 에라가 진행될수록 τ̃ 간격이
  길어진다 (6.5 → 11.8 → 12.6 → 18.4).  정밀도를 풀어도 안 줄어든다 (§4) — 줄이려면
  벽별 이벤트를 **한 번의 적분**으로 합쳐야 한다 (diffrax 다중 이벤트).  다음 후보.

### 다음
· **D1c** — 다중 이벤트로 튐 검출을 한 번의 적분으로 (회귀 시간 회수)
· **B2b** 다준위 재결합 → Lyα,  **V1** l≥4 tilted 부호 (rank-6 PSTF),  **D3** 혼합정밀

---

## 진행 44차 — R4 · **Mixmaster 튐 수열을 Rust 로**, 그리고 그제서야 보인 혼돈

D1b 의 Python/diffrax 판은 튐 하나마다 세 벽에 각각 이벤트를 걸어 긴 구간을 적분하고
상태를 되감아 다시 시작했다 (튐 4번에 12번의 긴 적분).  JAX 쪽에서 다중 이벤트로
다듬는 대신 **바로 Rust 로** 갔다 — 수치는 이미 D1b 에서 검증됐으니까.

### 1. 한 번의 적분으로 세 벽을 동시에 감시
`_rustcore/src/ode/mixmaster.rs` — 직접 짠 Dormand-Prince 5(4) 적응 스텝 위에서
스텝마다 세 사건함수의 부호변화를 보고, 그 스텝 **안에서** 이분법으로 근을 조인다.
되감기도 중복 적분도 없다.  class_a RHS 는 이미 있던 `ode/charts.rs` 를 그대로 쓴다.
```
u_sequence(n_bounce=4)   python 105.3 s → rust 0.0030 s      35109배
```
### 2. ★★ 차등시험 — 적분기가 아예 다른데 답이 같다
Python 은 diffrax **Kvaerno5(암묵)**, Rust 는 **DP54(명시)** 다.
```
u 차   0.0 / 5.5e−12 / 1.6e−12 / 8.2e−12
τ̃ 차   4.5e−11 / 1.1e−09 / 2.6e−08 / 2.3e−05
```
D1b §4 가 "오차는 적분 정밀도가 아니라 씨앗이 정한다" 고 잰 것과 정확히 일관된다.

### 3. ★★ Rust 가 생기고서야 잴 수 있게 된 것 — **혼돈의 정체**
D1b 는 튐 4번에서 끊겨 "혼돈 증폭이 아니라 오차가 줄어든다" 고 기록했다.  10번까지
가니 **둘 다 맞다**.  국소 사상오차는 벽이 죽으며 줄지만, 이상적 BKL 수열과의
**누적 표류**는 사상 미분의 곱으로 자란다:
```
 k   u(궤적)         이상 BKL        누적차     |d bkl/du| at k
 0   3.700000440    3.700000440    0.00e+00   1.00
 3   1.428567554    1.428570530    2.98e−06   5.44
 4   2.333354431    2.333338224    1.62e−05   1.00     ← 2.98e−06 × 5.44
 5   1.333354431    1.333338224    1.62e−05   9.00
 6   2.999810142    2.999955982    1.46e−04   1.00     ← 1.62e−05 × 9.00
 8   1.000189924    1.000044020    1.46e−04   2.77e+07 ← u → 1 (Taub 점)
 9   2584.0011      22716.9611     2.01e+04
```
자릿수가 아니라 **값이 맞는다**.  추적이 깨지는 지점은 궤적이 u = 1 근처를 지나
`d(bkl)/du = 1/(u−1)²` 가 2.8e+07 이 되는 곳이다 — 그게 BKL 혼돈이다.
이상 수열과 **9번** 같이 간다 (누적 표류 < 1e−3).

### 4. ★ 멀리 가니 하나 더 — γ=2 의 중립성은 **Kasner 원 위에서만**이다
D1 §3 은 `Ω′ = [2q−(3γ−2)]Ω` 의 지수가 q=2 에서 6−3γ 이고 γ=2 면 0 이라 했다.  맞다 —
다만 **튐을 지날 때는 q ≠ 2** 라 그 구간에서 지수가 0 이 아니다.  τ̃ ≈ 1242 까지
굴리면 |Ω| 가 1e−16 → **4.0e−08** 로 샌다.  Python 판의 τ̃ ≈ 49 에서는 안 보이던 양이라
처음엔 시험이 1e−8 게이트에 걸렸다 — 게이트를 고치는 대신 **원인을 적고** 측정을 남겼다.

### 5. 회귀 시간 회수
D1b 의 시험들이 이제 전부 Rust 경로로 가면서 **1초 미만**이 됐다.  남은 비용은
차등시험 하나(109 s, Python 오라클 쪽)뿐이다.
```
회귀 전체   26.2 분 → 22.5 분   (신규 시험 10개 + D1b 12개를 그대로 두고)
```
### 상태
```
pytest 1007 passed (R4 신규 10)   cargo test 57 passed (mixmaster 신규 4)
신규: _rustcore/src/ode/mixmaster.rs, tests/test_r4_mixmaster_rust.py
수정: _rustcore/src/lib.rs (mx_bounce_sequence), _rustcore/src/ode/mod.rs,
      bianchi/analysis/mixmaster.py (backend 디스패치 + bkl_tracking +
      map_derivative_explains_the_blowup), bootstrap.sh
```
### 다음
· **D1c** — Gauss 사상 통계 (불변밀도 1/((1+x)ln2), Lyapunov π²/6ln2).  Rust 커널이
  튐 하나에 0.3 ms 이므로 이제 통계가 가능하다 — 다만 §3 이 보였듯 궤적 하나는
  9번 뒤 이상 수열에서 벗어나므로, **여러 초기조건의 앙상블**로 재야 한다.
· **B2b** 다준위 재결합 → Lyα,  **V1** l≥4 tilted 부호 (rank-6 PSTF),  **D3** 혼합정밀

---

## 진행 45차 — R5a · **tilted 텐서 커널을 Rust 로**, 그리고 **왜 그것만으론 부족한지**

"core backend 를 대부분 Rust 로" 라는 방향으로, 남은 가장 큰 Python 덩어리인 tilted
계층을 친다.  먼저 **프로파일**했다 (tilted 적분 2.45 s, l_max=3, i_max=3, 20 스텝):
```
tensordot           52480 회  1.49 s (누적)   ← to_coord / covariant_derivative
equation_lhs                  2.46 s (누적)
covariant_derivative           0.58 s
to_coef / pack / matvec        0.90 s
```
★ **어느 한 곳도 지배하지 않는다.**  작은 배열에 대한 numpy 호출이 수만 번이라
  오버헤드가 고르게 깔려 있다 — R1(노드 55296개 융합순회)과는 성격이 정반대다.

### 1. 텐서층 포트 (`_rustcore/src/kinetic/tilted_terms.rs`)
⊥Ẋ, D_B X, D^aJ_{aA_l}, D_{⟨a_l}J_{A_{l−1}⟩} 를 동적 rank 로 옮겼다.  지표 규약은
**Python 에서 실측해** 맞췄다 (문서만 믿지 않았다):
```
eup/edn/deup (3,4)=[A][μ],  Γ (4,4,4)=Γ[μ][λ][ρ]
to_coord 는 축0(A)를 소비하고 μ 를 뒤에 붙인다 (r 번 반복 → 순서 보존)
spatial_derivative 반환 축은 (B, A₁…A_r),  div_contracted 는 (B, A₁) 을 축약
```
정확도: rank 0~3 에서 **5.3e−16** 이내, 적분 궤적오차는 **비트-정확 동일** (0.00e+00).

### 2. ★★ 그런데 속도는 **1.32배**뿐이다 — 이번 증분의 진짜 결과
```
trajectory_error(l=3,i=3,20스텝)   python 2.45 s → rust 1.86 s   1.32배
```
R1/R2 의 type V 커널이 10~35109 배였던 것과 대조된다.  차이는 **경계를 넘는 횟수**다:
type V 는 RK4 루프 전체가 Rust 안이라 노드가 Python 으로 돌아오지 않았는데, 여기서는
(l,i) 조합마다 작은 배열을 주고받아 **FFI 왕복과 배열 생성이 이득을 먹는다**.

⇒ **결론이자 R5b 의 근거**: tilted 계층은 층별로 떼어 옮기면 안 된다.
  `equation_lhs` + 질량행렬 조립/해 + RK4 루프를 **통째로** 옮겨야 하고, 이번 커널이
  그 토대다.  (숫자를 재 보기 전에 통째로 갔으면 이 교훈을 못 얻었을 것이다.)

### 3. ★ 버그 하나를 만들었고, 기존 시험이 잡았다
geo 배열 마샬링을 줄이려고 `geo["_rc"]` 에 연속배열을 캐시했다.  그런데 시험이
`dict(geo, deup=0)` 으로 **복사·수정**해 대조군을 만드는데, 복사본이 옛 캐시를
물려받아 Rust 가 **낡은 deup 를 썼다** — 사틀 회전항 대조군이 조용히 무력해졌다.
`test_h5d_residual.py::test_tetrad_rotation_term_is_not_negligible` 이 잡았다
(0.0 > 2.0e−06 실패).  캐시 키를 배열 **객체 동일성**으로 바꾸고, 회귀시험을 새로 넣었다.
★ 성능 캐시가 **대조군을 무력화**하는 전형적 사고다 — 그래서 대조군을 남겨 두는 것이다.

### 상태
```
pytest 1025 passed (R5a 신규 18)   cargo test 60 passed (tilted_terms 신규 3)
신규: _rustcore/src/kinetic/tilted_terms.rs, tests/test_r5a_tilted_terms_rust.py
수정: _rustcore/src/lib.rs (tt_* 바인딩 4개), _rustcore/src/kinetic/mod.rs,
      bianchi/matter/tilted_terms.py (backend 디스패치 + 캐시), bootstrap.sh
```
### 다음
· **R5b** — `equation_lhs` + 질량행렬(PSTF 좌표) + RK4 루프를 **한 호출**로.
  §2 가 그 필요성을 숫자로 보였다.  PSTF 기저는 Python 것(SVD)을 넘겨 받아 쓰면
  좌표계까지 같아져 비트 비교가 가능하다.
· **R5c** — `hierarchy.pstf` 고정연산자와 `type_v.moments_type_V_tilted` (남은 numpy 열점)
· **D1c** Gauss 사상 통계 (앙상블),  **B2b** Lyα,  **V1** rank-6 PSTF,  **D3** 혼합정밀

---

## 진행 46차 — R5b · **tilted 계층을 통째로 Rust 로**, 그리고 **대조군을 세어 보고 뒤집은 것**

45차의 결론("층별로 옮기면 FFI 왕복이 이득을 먹는다")을 그대로 실행했다:
`equation_lhs` + 질량행렬 조립 + 선형해 + RK4 루프를 **한 호출**에 넣고 J 가 Python 으로
돌아오지 않게 했다 (`_rustcore/src/kinetic/tilted_hier.rs`).

### 1. 다시 구현하지 않고 **넘겨 받은 것**
· 시각별 기하 — `tilted._geometry` 는 sympy lambdify 닫힌형이라 포트 표면을 넓힐 이유가
  없다.  RK4 가 쓰는 시각을 Python 이 만들어 넘긴다.
· PSTF 사영 연산자 Q_l 과 정규직교기저 B_l — **Python 것 그대로**.  기저가 SVD 라
  구현마다 달라질 수 있는데, 같은 기저를 쓰면 질량행렬 좌표까지 같아져 M 을 성분별로
  대조할 수 있다 (실제로 그 시험이 붙었다).

### 2. ★ 질량행렬 조립이 진짜 병목이었다 (첫 판을 스스로 반증)
첫 판은 `tilted_mass.dense` 를 그대로 옮겨 **단위벡터 n 개마다 전 블록을 훑었다**.
그런데 소스 블록 하나는 목표를 **최대 셋**만 건드린다 (대각 / div-con l₀−1 /
div-free l₀+1).  희소 조립으로 바꾸니:
```
Rust 커널 (l=3, i=3, 20스텝)   0.133 s  →  0.061 s
Python 루프                    1.678 s          → 28배
```
값은 그대로다 (건너뛴 블록은 pstf(0) = 0).

### 3. ★★ **그런데 시험 벽시계는 9% 밖에 안 줄었다** — 세어 보고 알았다
기본 경로(`mode="ratio"` + J̇ 닫힘)만 옮겼더니, tilted 시험 **1260 적분스텝 중 310
스텝(25%)** 만 Rust 로 갔다.  나머지는 전부 **대조군**이었다:
```
frozen + J̇닫힘끔   450     ratio + n_* 삼각절단   260
phys_sqrt/phys_5w  160     frozen + J̇닫힘         60     ratio + J̇닫힘끔  20
```
이 프로젝트는 대조군을 남기는 것이 규율이라 대조군이 **시험시간의 다수**를 차지한다.
그것을 Python 에 두면 벽시계는 그대로다.  그래서 `tilted_closure.MODES` 전부 +
J̇ 닫힘 on/off + 삼각 절단까지 옮겼다:
```
tilted 시험 5 파일   115.5 s  →  70.6 s
```
★ "커널이 28배 빨라졌다" 와 "시험이 빨라졌다" 는 **다른 명제**다.  프로파일이 아니라
  **호출 계수**가 그 차이를 드러냈다.  회귀시험(`test_no_supported_mode_silently_falls_back_to_python`)을 붙였다.

### 4. ★★ 비트-정확의 경계 — 처음 적은 설명이 **틀렸고** 이분법으로 고쳤다
처음엔 "PSTF 를 안 쓰는 l < 2 층은 비트-정확" 이라고 적고 게이트를 세웠다.
i_max=1 에서는 통과했는데 i_max=2 의 (l,i)=(1,1) 에서 3.6e−15 로 깨졌다.
블록을 하나씩 0 으로 만드는 이분법으로 좁히니 원인은 PSTF 가 아니라
```
np.einsum("a,ba->b", J, σ)   대   순차합 Σ_a J_a σ_{ba}   →  1.4e−17
```
였다.  (그 자리에서 마침 같았던 `np.tensordot` 도 무작위 배열에서는 어긋난다 —
"안전한 커널" 같은 분류는 성립하지 않는다.)  ⇒ 경계는 **"PSTF 를 쓰는가" 가 아니라
"작은 numpy 축약 커널을 타는가"** 이고, 그 커널을 전혀 안 타는 **l=0 블록만** 비트-정확이다.

측정된 층별 표 (모두 게이트로 고정):
```
F (좌변)  l = 0 블록             비트-정확        (6 가지 (l_max,i_max,m,n_*) 전부)
F (좌변)  나머지                 ≤ 1.5e−17  (ρ 규격)
M (질량행렬)                     2.2e−16
같은 (M,F) → LAPACK 대 Rust-LU   2.6e−17
전 경로 Python 대 Rust           5.1e−17     ← cond(M)=1.37 이라 증폭이 없다
궤적오차 (전 모드 × J̇닫힘)      ≤ 8.7e−13 상대 (절반은 비트-정확)
```
⇒ **항 조립은 정확하고, 남는 차이는 전부 선형대수·축약의 합 순서**다.  선형해는
LAPACK `dgesv` 대신 자체 부분추축 LU 를 썼다 — 의존성이 아니라 **결정성** 때문이다.

### 5. ★ 함정: `(step+1)*dt` ≠ `step*dt + dt`
기하를 미리 만들어 넘기므로 시각을 Python 루프와 **같은 산술**로 만들어야 한다.
스텝당 2 개(끝을 다음 시작으로 재사용)로 줄이면 마지막 비트가 어긋난다.  스텝당 3 개를
넘기고, 그 차이가 실재함을 시험이 확인한다.

### 6. ★★ 다음 열점을 **재서** 확정했다 — 그리고 이미 있는 Rust 를 안 쓰고 있었다
R5b 후 프로파일 (`test_l3_*` + `test_h5e3_*`, 54.6 s):
```
tilted_moments.J_moment_tilted   1815 회   43.4 s (누적)   ← 압도적 1위
```
그런데 **`bianchi_rustcore.kin_j_moment_tilted` 가 이미 있다** (H5-e 에서 만들었다).
`backend.j_moment_tilted` 가 그것을 부르는데 **아무도 backend 를 안 부른다** —
적분기·시험이 전부 numpy 오라클을 직접 쓴다.  실측:
```
numpy 28.5 ms/call   rust 3.63 ms/call   → 8배,  두 경로 최대차 3.3e−14 (ρ 규격)
```
이번 증분에서 **일부러 갈아끼우지 않았다**: 오라클을 바꾸는 변경과 커널 포트를 같은
증분에 섞으면 게이트가 움직였을 때 어느 쪽 탓인지 가릴 수 없다.  R5c 로 분리한다.

### 상태
```
pytest 1061 passed (R5b 신규 36)   cargo test 65 passed (tilted_hier 신규 5)
전 시험 22분 37초 — 45차와 **같다**.  tilted 에서 45 초를 벌고 신규 36 개로 되돌려
놨다.  전체 시간은 이제 tilted 가 아니라 M2/D1/D2c/rustcore-차등이 지배한다.
신규: _rustcore/src/kinetic/tilted_hier.rs, bianchi/matter/tilted_rust.py,
      tests/test_r5b_tilted_hier_rust.py
수정: _rustcore/src/lib.rs (th_integrate / th_rhs / th_force_and_matrix),
      _rustcore/src/kinetic/mod.rs, bianchi/matter/tilted_integrate.py
      (backend 디스패치 + rust_available), bootstrap.sh
```
### 다음
· **R5c** — §6 이 재서 확정한 것: `J_moment_tilted` 를 이미 있는 Rust 구적으로
  라우팅한다 (8배).  **오라클 순수성**을 따로 판정해야 한다 — audit 계열은 Python 을
  기본으로 남기고, 생산 경로만 갈아끼운다.  같이 볼 것: `hierarchy.pstf` 고정연산자,
  `type_v.moments_type_V_tilted`.
· **D1c** Gauss 사상 통계 (앙상블),  **B2b** Lyα,  **V1** rank-6 PSTF,  **D3** 혼합정밀

---

## 진행 47차 — R5c · **정확구적 라우팅** — 커널을 새로 만들지 않은 최적화

46차 §6 이 재서 확정한 그대로: `J_moment_tilted` 가 tilted 시험 54.6 s 중 43.4 s 를
먹는데, `kin_j_moment_tilted` 는 H5-e 때 **이미 Rust 에 있었고** 차등테스트로 묶여
있었다.  아무도 디스패치(`backend.py`)를 부르지 않았을 뿐이다.  이번 증분은 디스패치를
`tilted_moments.J_moment_tilted` / `hierarchy.J_moment` **안으로** 옮겨 모든 호출자가
자동으로 Rust 를 타게 했다.  Rust 쪽은 한 줄도 바꾸지 않았다 (cargo 65 그대로).

### 1. ★★ 두 함수를 **함께** 갈아끼워야 했다 — v=0 비트-정확 쌍
`test_zero_tilt_is_bit_exact` 는 J(v=0) ↔ J′(v=0) 의 비트-동일을 요구한다.  이 불변식은
numpy 쌍끼리, Rust 쌍끼리 각각 성립하고 (피적분함수가 문자 그대로 같다; Rust 쌍은
cargo 게이트) **섞으면 깨진다** (백엔드 차 ~1e−15·ρ).  J′ 만 라우팅했으면 그 시험이
깨졌을 것이다.  둘을 함께 옮겼고, 섞으면 깨진다는 사실 자체를 게이트로 넣었다.

### 2. f₀ 인식 규칙 — 조용히 틀린 분포를 적분하지 않는다
Rust 커널은 f₀ 콜백을 받지 않는다.  라우팅은 f₀ 를 **알아볼 수 있을 때만**:
페르미-디랙 그 자체, 그리고 base 가 기본인 `f_dipole(eps, axis)` (클로저에
`_rust_dipole` 표식을 붙였다).  base ≠ 기본인 쌍극, 커스텀 f₀, l > 5 는 numpy 폴백 —
폴백이면 두 호출이 비트-동일함을 시험이 확인한다.

### 3. ★★ 오라클 고정이 취향이 아니라 **산술적 필요**인 지점 — 그리고 크기 추정의 반증
`audit.h5d_tilted_residual.moment_grid` 는 dt=1e−5 중앙차분의 원료라 backend="python"
에 못박았다.  근거를 측정하다 **추정이 틀렸음을 발견**했다:
```
소박한 추정: |Δ|_max/(2dt) = 3.3e−14/2e−5 ≈ 1.6e−9    (게이트 1.5e−10 의 10배)
한 점 (2,0) 측정:  9.4e−11   ← 게이트 **아래** — 첫 게이트가 이걸로 깨졌다
격자 전체 측정:    최악 (1,0) 1.78e−10   ← 게이트를 넘는다 (1.2배)
```
성분별 백엔드 차가 최악값보다 대체로 작아 추정이 9배 비관적이었다.  결론(못박아야
한다)은 살아남았고 크기는 고쳤다 — 시험이 격자 전체를 재고, 최악이 게이트의 1/10
아래로 내려가면 고정을 풀라고 알린다.  같은 이유로 차등테스트의 python 참조도
명시적으로 못박았다 (안 그러면 Rust 대 Rust 공허 비교).

### 4. 효과 (측정)
```
J_moment_tilted        numpy 21.0 ms → rust 2.8 ms   (7.4배)
J_moment               numpy 18.4 ms → rust 2.7 ms   (6.8배)
기준 5파일 블록        115.5 s (R5a) → 70.6 s (R5b) → 16.9 s (R5c)
L3+H5e3 프로파일       78.4 s → 15.6 s;  J_moment_tilted 누적 43.4 s → 7.9 s
전체 스위트            22분 37초 → **14분 36초**  (Rust 코드 0줄 추가로 −36%)
```

### 5. ★ 46차가 지목한 나머지 후보 둘은 **재보니 이득이 없다** — 옮기지 않았다
· `hierarchy.pstf` 고정연산자: 생산 경로(R5b 커널)는 이미 넘겨받은 Q_l 을 쓴다.
  남은 numpy pstf 는 오라클 안이고 tottime 0.6 s — 포트할 것이 없다.
· `type_v.moments_type_V_tilted`: K4c 전체가 4.4 s 이고 그마저 (이미 Rust 인)
  `back_trace` 가 지배한다.  커스텀 f 격자를 받는 구조라 포트 표면만 넓어진다.
· 새로 보이는 것: `tilted._geometry` (sympy lambdify, 1.3 ms/호출) 가 남은 두 파일에서
  3.6 s — R5b 의 "포트하지 않는다" 결정과 상충하고 상한이 작다.  보류로 기록.

### 상태
```
pytest 1076 passed (R5c 신규 15) — 14분 36초   cargo test 65 passed (변경 없음)
이제 상위는 diffrax-오라클(82 s)·jax 차트(M2 37 s, D2c 24+21+16+14 s)·구적 배치
차등(21+19 s) — 전부 오라클 대조 시험이라 "생산 경로가 느려서" 가 아니다.
신규: tests/test_r5c_quadrature_routing.py
수정: bianchi/matter/hierarchy.py (J_moment 디스패치 + rust_f0_args + f_dipole 표식),
      bianchi/matter/tilted_moments.py (J_moment_tilted/moments_tilted 디스패치),
      audit/h5d_tilted_residual.py (moment_grid 를 python 에 못박음 + 근거 주석),
      bianchi/backend.py (force_python 이 실제 python 에 닿게),
      tests/test_rustcore_differential.py (참조를 오라클에 명시 고정), bootstrap.sh
```
### 다음
· **D1c** — Gauss 사상 통계 (불변밀도 1/((1+x)ln2), Lyapunov π²/6ln2).  R4 커널이
  튐 하나 0.3 ms 이므로 앙상블이 현실적이다.  성능 작업이 아니라 물리 증분.
· **B2b** 다준위 재결합 → Lyα,  **V1** l≥4 tilted 부호 (rank-6 PSTF),  **D3** 혼합정밀
· 성능 잔여: 스위트 상위는 이제 diffrax-오라클 대조(R4 122 s)와 jax 차트 적분(M2/D2c)
  — 전자는 오라클이라 빠르게 만드는 것 자체가 무의미하고, 후자는 D3(혼합정밀)와
  묶어서 보는 편이 낫다.

---

## 진행 48차 — D1c · **Gauss 사상 통계** — Mixmaster 혼돈의 보편 상수를 앙상블로

R4 커널(튐당 0.3 ms)이 열어 준 물리 증분.  D1b 는 궤적 **하나**가 BKL 사상을 따름을
보였고, 여기서는 초기조건 **500 개 앙상블**로 사상의 통계적 보편량을 잰다.

### 1. 에라 분해에 floor() 가 없다
에라의 끝 에폭은 u ∈ (1,2) 이므로 소수부분을 **x = u_끝 − 1** 로 읽고 digit 은 에폭
개수를 **센다**.  (u_시작 = a+x 에서 frac 으로 읽으면 u 가 정수 근처일 때 ODE 오차
1e−4 로 digit 이 뒤집힌다 — 설계로 피했고, x ∈ (0,1) 불변식과 u_시작 ≈ digit+x
자기검증을 게이트로 뒀다.)  병리 행(D1b 의 벽-바닥 잔재)은 국소 사상잔차 tol 로
접두부를 잘라 배제한다 (잔차 중앙값 실측 8e−9, tol 0.02 — 여유 6자릿수).

### 2. ★★ 세 경로 — 검열을 분리한다
① ODE (물리, τ·에폭 예산으로 **검열됨**) ② 미러 (같은 예산의 순수 사상) ③ 무검열 사상.
③↔닫힌형이 이론을, ①↔②가 "ODE 가 사상이다" 를, ②↔③의 차가 **검열 편향 자체**를 잰다.
```
                        ① ODE          ② 미러      ③ 무검열     닫힌형
λ (에라당)              2.4358±0.066   2.4193      2.3760±0.005  π²/6ln2 = 2.3731
KS(era-x → Gauss)       0.0256 [<.041]             0.0032
digit1                  0.5230         0.5575      0.4141        GK 0.4150
digit≥6                 0.0637         0.0420      0.2245        log₂(7/6)=0.2224
Khinchin                1.7153         1.5950      2.6915        K₀ = 2.6855
혼합률(균일→Gauss 비)                              0.356, 0.322  Wirsing 0.3037
```
· **머리기사**: 물리 앙상블의 λ 가 π²/(6 ln2) 와 1σ — 균일밀도 대조값 2.0 은 6.6σ 배제.
· 에라-시작 u 밀도 1/(ln2·u(u+1)) 는 ρ_G 에서 **유도**했고, 구간질량 = Gauss–Kuzmin
  항등을 수치 자기검증으로 고정했다 (기억 인용이 아니다 — 처음 기억했던 "에폭당
  밀도 1/(u(u+1))" 는 전달연산자 검증에서 **탈락**했다).
· digit·K 의 ①② 편향은 검열(예산 안에 끝나는 에라의 길이-편향)이고 **숨기지 않는다**:
  digit-1 과잉 7σ 를 못박는 시험, ②가 방향·크기급을 재현하는 시험, "ODE 로 K₀ 를
  확인했다고 주장하지 않기 위한" 게이트가 각각 있다.  ②의 정직한 한계도 기록:
  ODE 예산은 내생적이라 주변분포 미러에는 2~3σ 잔차가 남는다.

### 3. ★★ 반증 둘 — 혼돈이 시험 자신을 먹은 자리
· **황금비 대조군**: n=60 으로 λ=2lnφ 를 확인하려다 1.2786 이 나왔다.  배정밀도
  오차가 스텝당 φ²≈2.618 배로 자라 n*≈ln(1/ε)/2lnφ≈38 에서 측도-0 궤도를 떠나
  전형 궤도로 돌아간다 — **실측 이탈 37**.  λ 는 이탈 전(n=25)으로 재고 (2lnφ 와
  7e−8 일치), 이탈 지점을 예측 [30,45] 와 대조하는 시험으로 뒤집었다.
· **에폭 풀링의 함정**: 에라 (a,x) 의 에폭 a 개가 같은 x 를 공유 → 풀링 밀도
  ∝ Σ_j 1/(j+x) **발산** (평균 에라 길이 무한).  실측 KS(풀링→Gauss) 0.110 ≫
  에라당 0.005, 풀링은 오히려 균일에 가깝다 (0.080) — 통계가 전부 에라당 1 표본인 이유.

### 4. 설계 세부 (측정으로 고른 것)
· 벽 씨앗 1e−2: 완결에라/궤적 3.5 로 최다 (3e−3: 2.8, 1e−3: 2.5) — 통계에는 사상잔차
  1e−4 로 충분해서 추적시험(1e−3 게이트)과 달리 벽이 오래 사는 쪽이 이득.
· 초기조건: 에라-시작 정상법칙 u₀ = 1/x′, x′ = 2^ξ−1 (역CDF 정확표본), u₀>12 재추출
  + **첫 에라 폐기** (잔류 편향은 Wirsing 0.30 배).
· 비용: 500 궤적 2.3 s (Rust) — 앙상블이 "이제 현실적" 이라는 R4 의 약속 그대로.

### 상태
```
pytest 1095 passed (D1c 신규 19)   cargo test 65 passed (변경 없음)
신규: bianchi/analysis/gauss_map.py, audit/d1c_gauss_statistics.py,
      tests/test_d1c_gauss_statistics.py
수정: bootstrap.sh (기대값)
```
### 다음
· **B2b** 다준위 재결합 → Lyα,  **V1** l≥4 tilted 부호 (rank-6 PSTF),  **D3** 혼합정밀
· D1 계열은 이것으로 닫힌다 (사상 측정 → 연속 튐 → Rust → 앙상블 통계).

---

## 진행 49차 — D3 · **혼합정밀 (특이점 근방)** — 표현이 죽는 곳을 재고, 표현 혼합으로 고쳤다

D-티어의 마지막 항목.  D1b 의 반증 기록("한계는 혼돈 증폭이 아니라 **벽의 언더플로**")
이 지목한 지점을 직접 공격했다.  고침은 정밀도 상향이 아니라 **표현 혼합**이다:
Σ± 는 f64 그대로, 벽만 w = ln|N| — 곱셈 구조 dN/dτ = rate·N 이 덧셈 dw/dτ = rate 가
되므로 물리가 근사 없이 옮겨진다 (항등 실측 **8.9e−16**, `log_rhs_identity`).

### 1. Rust 로그-벽 커널 (`mx_bounce_sequence_log`)
R4 커널을 제네릭 코어(`bounce_core`)로 리팩터해 선형/로그가 DP54·사건 기계를 공유한다.
u₀ = 2+1/√2 (은비 주기궤도 — **유리수 CF 종결 함정**을 D1c 에서 배운 대로 회피), 8000 τ̃:
```
로그 최저 골 w = −2197    (선형 표현한계 ln 1e−308 ≈ −708 의 3배)
겹침 구간 |Δu| = 6.2e−6   (오라클 사슬: diffrax ↔ 선형(R4) ↔ 로그(D3))
```
★ **반증 기록 (h_max)**: 첫 판은 3번째 튐 뒤 수열이 끊겼다.  순수 Kasner 구간에서
로그 RHS 는 **정확히 상수**라 DP54 오차가 0 — h 가 스텝마다 5배로 폭주, 거대 스텝
하나에 사건 쌍(+→−→+)이 통째로 들어가 끝점 부호변화가 사라졌다.  선형판은 지수적으로
변하는 N 이 오차제어로 h 를 묶어 **우연히** 안전했던 것 — 표현을 바꾸면 적분기의
보호막도 함께 사라진다.  h_max = 1 로 고정했다.

### 2. ★★ 측정이 가설을 두 번 고쳤다
**(a) 선형 벽의 실효 바닥은 표현(−708)이 아니라 atol 이다.**
```
선형 maxN(골): 1e-3, 5e-5, 1e-8, 1e-13, 1e-13, 1e-13, …   ← atol = 1e−13 에 핀
로그 w(골)   :  −7,  −9,  −18,  −101,  −157,  −330, −1048, −2197
```
골이 atol 에 핀되면 회복 τ 가 인위 단축되어 **가짜로 이른 튐**이 생긴다:
τ(선형) = 372, 558, 764 vs τ(참) = 520, 1387, 3201 (비 0.71 → 0.24).
★ 함정: 그 구간에서도 u 는 6e−6 으로 맞는다 — 사상 강건성이 시간 조작을 가려
**u 만 보면 못 잡는다**.  τ 간격의 기하급수성이 판별량이고, 게이트로 못박았다.

**(b) "로그벽이면 D1c 앙상블이 깊어진다" 는 기대도 반증됐다.**  같은 τ 예산에서
에라가 890 < 1130 으로 **준다** — 선형의 atol-핀이 깊은 에라를 인위적으로 싸게 만들어
수확을 부풀렸던 것.  λ (2.4358±0.066 vs 2.4537±0.073)·KS 는 오차 내 일치 —
**D1c 의 u-기반 통계 결론은 표현에 강건**하고, τ 에 기대는 양만 선형에서 못 믿는다.

### 3. ★★ 로그가 드러낸 다음 한계 — 이번엔 물리다
```
|w_골| 인접비: 1.56, 2.10, 3.18, 2.10      Δτ 인접비: 1.55, 2.09, 3.16, 2.09
```
Δτ 비가 |w| 비를 1% 이내로 따른다 (**회복 τ ∝ 골 깊이**의 실측).  골이 에라마다
기하급수로 깊어지므로 **튐 수 ~ O(ln τ_budget)** — 어떤 표현도 못 고친다.
(고치려면 시간변수 자체를 바꿔야 한다 — 별개 증분으로만 가능.)

### 4. f32 결정표 (E1 GPU 의 입력)
```
혼돈 그림자 지평 (Gauss 사상): f32 8.0 에라 / f64 16.6  (예측 ln(1/ε)/λ = 6.7/15.2)
u 판독 조건수: |du/dΣ| = 4 (u=2) → 1473 (u=50) ~ O(u²) → f32 잡음 ≤ 8.8e−5  [허용]
구속감시 Ω = 1−Σ²−K: f32 상쇄바닥 1.19e−7  → 1e−9 급 게이트 **불가**
⇒ f32 는 u 급 판독·짧은 구간 전용.  구속·장기수열은 f64 + 로그벽.
```

### 상태
```
pytest 1106 passed (D3 신규 11)   cargo test 67 passed (mixmaster 신규 2)
신규: bianchi/analysis/precision.py, audit/d3_mixed_precision.py,
      tests/test_d3_mixed_precision.py
수정: _rustcore/src/ode/mixmaster.rs (bounce_core 제네릭화 + 로그판 + h_max),
      _rustcore/src/lib.rs (mx_bounce_sequence_log),
      bianchi/analysis/mixmaster.py (walls="log" + rhs_past_log_np),
      bianchi/analysis/gauss_map.py (ode_ensemble walls=), bootstrap.sh
```
### 다음
· **B2b** 다준위 재결합 → Lyα — ★ 사용자 별도 프로젝트로 진행 중, **통합양식 대기**.
  통합 시점에 이쪽의 접합면은 `bianchi/thermo`(재결합 이력 소비자)와
  `audit/` 게이트 관례 (두 경로 교차검증 + 반증 기록) 를 그대로 쓰면 된다.
· **V1** l≥4 tilted 부호 (rank-6 PSTF),  **E1** GPU (위 f32 결정표가 입력).
· D 티어 (D1/D1b/D1c/D2/D2b/D2c/D3) 는 이것으로 **닫힌다**.

---

## 진행 50차 — V15+V1 · **물리 총감사 (기호 + CRAG)** 와 **l=4 tilted 부호 (rank-6 PSTF)**

두 검증층을 보탰다: 지금까지의 수치 오라클과 **독립인** 기호(sympy) 항등, 그리고
제한적 web CRAG 로 문헌값 대조.  그 위에서 V 계열의 마지막 항목(V1)을 닫았다.

### 1. V15 · 기호 항등 — 반올림·격자 무의존의 계수 심판 (`audit/v15_physics_audit.py`)
```
class A  dΩ/dτ − Ω(2q−(3γ−2))   = 0   (구속면 밖에서도 항등 — RHS 전 계수 심판)
class A  dK/dτ − (2qK + 2Σ·S)    = 0   ← 교환항 부호는 반증으로 확정 (아래)
class B  Ċ − 4(q+Σ₊−1)C          = 0   (D2 의 측정 전파율이 기호로도 닫힌다)
Kasner   Σp−1, Σp²−1, p₃/p₂−u    = 0
```
★ **감사자가 자신을 반증했다**: 전단–곡률 분해를 K̇ = 2qK − 4Σ·S 로 적었더니 잔차가
정확히 +6Σ·S — 총항등이 0 이므로 틀린 것은 코드가 아니라 **감사의 분해식**이었다.
참형 K̇ = 2qK + 2Σ·S (전단→곡률 교환)로 고치고, 옛 식이 틀렸음 자체를 감시하는
시험을 두었다.

### 2. web CRAG (제한적 — 2026-08)
```
Khinchin K₀ = 2.685452001…  (MathWorld)      코드와 |Δ| = 0
Wirsing  λ  = 0.3036630029… (MathWorld)      코드와 |Δ| = 0
L-C 식(13) 선형구조 (arXiv PDF): H[(1−n)J^(i+1)+(3+n)J^(i)] − (l/2l+1)D⟨⟩J^(i+1) + D^aJ^(i)
  — 코드의 H·divcon·divfree 부호와 일치.  무질량 문장("moments are identical")도 재확인.
```
코드 결함 수확: `tilted_closure.predict` 의 도달불능 중복 phys_ 블록 제거 (물리 무영향).

### 3. V1 · l=4 tilted 부호 — rank-6 PSTF 격자, 절단 없는 잔차 (`audit/v1_high_l_signs.py`)
H5-d 는 l ≤ 3 까지였다 ("l=4 는 rank-6 필요" 로 기록).  l_max=6 격자를 정확구적으로
채워 l=4 방정식을 심판했다:
```
잔차 (ρ 규격, FD 한계 ~1e−10):  m=0: 1.4e−11/1.4e−11   m=0.7: 5.5e−11/3.9e−11
                                m=3: 1.1e−11/6.1e−12    (i=0/1)
부호 반전 8종 전부:             잔차 ×2.1e5 ~ ×4.4e7
rank-6 PSTF:                    멱등 6e−16, rank 13 = 2·6+1 (구축 2 s)
적분기 l_max=4:                 R5b 커널 그대로 소화, 궤적오차 l_max=3 동급, 백엔드 일치
```
⇒ l≤3 이중 오라클로 확정한 부호·계수가 l=4 에서 그대로 성립 — l-의존 계수는 이제
  네 값의 l 로 과결정이다.  l=5 는 범위 밖으로 명시 (rank-7 대칭화 5040×2187² 는
  시간 단위 비용인데 계수는 이미 과결정이라 정보가 없다).

### 상태
```
pytest 1120 passed (V1 신규 8, V15 신규 6)   cargo test 67 passed (변경 없음)
신규: audit/v15_physics_audit.py, audit/v1_high_l_signs.py,
      tests/test_v15_physics_audit.py, tests/test_v1_high_l_signs.py
수정: bianchi/matter/tilted_closure.py (도달불능 블록 제거), bootstrap.sh
```
### 다음 (남은 로드맵)
· **B2b** 다준위 재결합 → Lyα — 사용자 별도 프로젝트, **통합양식 대기**.  접합면:
  `bianchi/thermo` + audit 관례 (두 경로 교차검증·반증 기록).
· **E1** GPU — D3 의 f32 결정표가 입력 (u 급 판독만 f32, 구속·장기수열 f64+로그벽).
  diffsol CUDA 재활용이 계획서의 경로.  CPU 가 이미 12,000× 라 수익 체감은 기록된 사실.
· C2 나머지 차트 RHS 이식 (class_b_tilted, exceptional, type_ix_d) — 커버리지 항목.
· V/D 티어는 **완결**: 수치견고성(D1–D3) + 검증(V13/V14/V15/V1) 전부 닫힘.

---

## 진행 51차 — C2 · **차트 커버리지** — 남은 세 차트 RHS 를 Rust 로, 세 겹 검증으로

C 티어의 마지막 항목.  Rust 고속 경로(diffsol BDF + Rayon 배치)가 class_a/class_b 에
갇혀 있었다 — class_b_tilted(11 상태), exceptional VI*_{−1/9}(6), type_ix_d(7, ±방향)
를 `Chart` enum 에 올리고 solve 층을 MAX_STATES=11 로 일반화했다 (기존 5-상태 경로는
버퍼만 커지고 값은 그대로 — R 계열 차등게이트가 지킨다).

### 1. 세 겹 검증
```
RHS 패리티 (무작위 40점):   BT 1.3e−16   EX 3.6e−16   XD(±) 0.00e+00
기호 항등 (sympy):          EX g′ = 2(q+Σ₊−1)g → 0,  Ω′−[2q−(3γ−2)]Ω+4Ag → 0
                            XD G′+2(qH+F)G: 일반 ≠ 0, trace-free 부과 → 0
                            (Python 문서의 조건부 주장이 기호로 확정됐다)
궤적 패리티 (BDF/diffrax):  EX 1e−7,  XD 1e−7,  BT 1e−6  + 구속 수송 전 구간 ≤1e−8
```

### 2. ★★ 감사 수확 — tilted-B 의 `dOmega()` 는 **Codazzi 온셸 전용**이다
사슬법칙 dΩ 와 닫힌형을 정확 유리수 점에서 대조하니 오프셸에서 ~1e−1 로 달랐다.
C₂=C₃=C₄=0 을 v 로 풀어 온셸에 올리면 1e−14 (4점 실측).  class B 의 b3=−4 가 온셸
전용인 것과 같은 구조인데 **문서에 단서가 없었다** — 단서를 달고 시험으로 못박았다.
(오프셸 잔차가 사라지면 그것대로 기록을 고쳐야 하므로 오프셸 ≠ 0 도 게이트다.)

### 3. 이식 세부
· `aux_bt` 는 Ω 를 Gauss 로 소거 (Python 기본 경로와 동일), G± 가드는
  `fluid.GUARD_EPS = 1e−12` 를 그대로.
· type_ix_d 의 trace-free 유지(평균 제거)와 `future` 부호 반전이 연산 순서까지 같아
  RHS 패리티가 **비트-정확**(0.00e+00)이다.
· `chart_aux` 의 둘째 반환값 = 차트별 **대표 구속** (BT: C₂, EX: g, XD: 정의식) —
  의미를 문서화했다.

### 상태
```
pytest 1132 passed (C2 신규 12)   cargo test 70 passed (차트 신규 3)
신규: tests/test_c2_chart_coverage.py
수정: _rustcore/src/ode/charts.rs (enum 3 변형 + aux_bt + rhs/omega/codazzi 팔),
      _rustcore/src/ode/solve.rs (MAX_STATES 일반화), _rustcore/src/lib.rs
      (make_chart 6 이름 + to_yn + n-일반 마샬링),
      bianchi/charts/class_b_tilted.py (dOmega 온셸 단서),
      audit/v15_physics_audit.py (EX/XD 항등 추가), bootstrap.sh
```
### 다음
· **B2b** — 사용자 별도 프로젝트, 통합양식 대기 (접합면: bianchi/thermo + audit 관례).
· **E1** GPU — D3 f32 결정표가 입력.  C2 로 배치 커버리지가 전 차트로 넓어졌으므로
  B3-급 대규모 스캔 수요가 생기면 그때 착수 권장.
· A/B/C/D/V 티어 **전부 완결** — 계획서(REMAINING-DAG-v3)의 미결은 B2b·E1 뿐이다.
