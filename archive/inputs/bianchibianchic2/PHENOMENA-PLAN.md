# 배경 수준 우주론 현상 구현 계획 (v2.0 로드맵)

**대상 저장소**: `bianchi-solver` v1.1 (11개 Bianchi 유형 + 예외형, tilted, 129 tests, audit 25/25)
**참조**: Piattella, *Lecture Notes in Cosmology* (arXiv:1803.00070) — 목차 전체를 배경/섭동으로 분류하여 대응시킴. 이에 한정하지 않고 Bianchi 고유 배경 현상을 추가.
**작성일**: 2026-07-29

---

## 0. 확정된 범위 (사용자 선택 반영)

| 축 | 선택 |
|---|---|
| 관측 브리지 | **전체 포함** — 널 측지선 + Sachs 광학 → z(n̂), d_L(n̂,z), d_A(n̂,z), ΔT/T(n̂) |
| 정밀도 | **준정밀** — g_*(T) 테이블 보간, Saha + Peebles(RECFAST 급), 질량 중성미자 FD 정확 적분, r_s 정확도 ~0.1–1% |
| 우선 축 | 물리 차트+열역사 → 관측량 산출 → 이방성 고유 현상 (동역학계 확장은 후순위) |

### 0.1 "background level" 의 작업 정의

> **배경 수준 = 공간 균질 계량과 균질 물질만으로 결정되는 모든 것.**
> 즉 시간 t 에 대한 ODE/구적으로 닫히는 모든 양.

이 정의는 상식보다 **넓다**. 특히 다음 셋은 흔히 "섭동"으로 오해되지만 순수 배경이다.

1. **널 측지선과 적색이동.** 배경 계량의 측지선일 뿐이다. FLRW 에서는 1+z = 1/a 라는 대수식으로 축약되어 보이지 않을 뿐, Bianchi 에서는 방향마다 다른 ODE 를 풀어야 한다.
2. **CMB 온도 패턴 ΔT/T(n̂).** Bianchi 템플릿(Collins–Hawking, VII_h 나선)은 **섭동이 아니라 배경 이방 팽창의 적색이동 패턴**이다. Planck 의 Bianchi VII_h 탐색이 정확히 이것을 쓴다.
3. **무충돌(자유흐름) 종족의 이방응력.** 균질 Vlasov 방정식은 k-모드가 없다. 분포함수 f(t, p_α) 의 특성곡선 + 구적으로 ρ, p, π_ab 가 나온다 (Misner 중성미자 점성).

### 0.2 명시적 제외

| 제외 | 이유 |
|---|---|
| BBN 반응망 | 사용자 지시. 대신 Σ² → ΔN_eff 프록시만 유지 (`physical.bbn_expansion_anisotropy`). |
| k-공간 섭동 (Ch. 5–8, 10, 11) | 배경 아님. 전달함수, C_ℓ, P(k) 전부 제외. |
| 인플레이션 요동 생성 (§9.4–9.7) | 양자 요동 = 섭동. **배경 슬로우롤·e-folds·재가열은 포함.** |
| CMB 편광 | 톰슨 산란 + 사중극 섭동 필요. |
| 수정중력 (f(R), 스칼라-텐서) | 별도 프로젝트. 단 `Species` 인터페이스는 유효 유체로 흉내낼 여지를 남긴다. |

---

## 1. 현상 인벤토리

### 1.1 Piattella 목차 → 배경 판정 → Bianchi 일반화

| 절 | 현상 | 배경? | Bianchi 일반화 | PR |
|---|---|:--:|---|---|
| 2.2–2.4 | FLRW 계량, 광원뿔, 측지선 | ✅ | 계량은 이미 있음. **측지선은 방향 의존 ODE** | 43 |
| 2.5–2.6 | Friedmann, 연속방정식 | ✅ | 이미 구현 (Gauss/Raychaudhuri/보존) | — |
| 2.7 | H₀, q, Ω_i | ✅ | q 는 있음. **Ω_i 의 차원 복원 + H₀ 단위** 필요 | 28, 31 |
| 2.8 | ΛCDM, 우주 나이, 평탄성 문제 | ✅ | Λ + CDM + 복사 종족, t₀ 구적 | 31, 35 |
| 2.9 | 해석해 (de Sitter, 복사/먼지 지배) | ✅ | Bianchi 대응해 (Kasner, Collins–Stewart) = 회귀 기준 | 49 |
| 2.10 | 거리 측도, 지평선, lookback time | ✅ | **전부 방향 의존.** d_A 는 Sachs 방정식 필요 | 44, 47 |
| 3.1–3.4 | 열평형, g_*, 엔트로피 밀도, FD/BE 적분 | ✅ | 스칼라 열역학 → **평균 스케일 ℓ 로 그대로 이식** | 32, 33 |
| 3.5–3.6 | 광자·중성미자 생성, T_ν, 질량 중성미자, 등가시각 | ✅ | 동일. 단 자유흐름 중성미자는 π_ab 를 만든다 | 33, 34, 38 |
| 3.7 | Boltzmann 방정식 (균질) | ✅ | 동결(freeze-out) 계산에 사용 | 37 |
| 4.1 | BBN | ❌ | 제외 (프록시만) | — |
| 4.2 | 재결합·탈결합 | ✅ | Saha+Peebles. H → θ/3 치환만 | 36 |
| 4.3 | 열적 잔존물 (WIMP 동결) | ✅ | **이방 팽창이 동결 시점을 옮긴다** — Bianchi 고유 효과 | 37 |
| 5–8, 10, 11 | 섭동, Boltzmann 계층, CMB C_ℓ | ❌ | 제외 | — |
| 9.1–9.3 | 평탄성/지평선 문제, 슬로우롤, e-folds, 재가열 | ✅ | 스칼라장 있음. **재가열(Γ) + no-hair 수치검증** 추가 | 41, 35 |
| 12.1–12.4 | 등급, 거리사다리, 적색이동, SNIa 베이지안 | ✅ | μ(z, n̂) → **쌍극/사중극 분해** | 48 |

### 1.2 Bianchi 고유 배경 현상 (강의노트에 없음)

| 현상 | 왜 배경인가 | 물리적 중요도 | PR |
|---|---|---|---|
| **방향 의존 적색이동** dE/dt = −E(H + Σ_ab n̂^a n̂^b) | 배경 계량의 널 측지선 | 모든 관측량의 뿌리 | 43 |
| **널 다발의 Sachs 광학** (팽창 θ, shear σ_null) | 배경 Riemann 의 조석행렬 | d_A, 렌즈 없는 순수 기하 왜곡 | 44, 45 |
| **CMB 이방 패턴** (Collins–Hawking 사중극, VII_h 나선) | 최종산란면까지의 적색이동 패턴 | Planck 제한 대비의 유일한 경로 | 46 |
| **자유흐름 중성미자 이방응력** (Misner 점성) | 균질 Vlasov = ODE + 구적 | shear 를 실제로 감쇠시킴. 복사시대 Σ 진화를 바꾼다 | 38 |
| **점성 유체** (Eckart π=−2ησ; Israel–Stewart 인과 변형) | 구성방정식이 대수/ODE | 등방화 메커니즘, 인과성 함정 | 39 |
| **이방 인플레이션** (Kanno–Soda f(φ)²F²) | 배경 게이지장 + 인플라톤 | **우주 no-hair 정리의 반례** — Σ/H 가 0 이 아닌 끌개로 감 | 40, 41 |
| **기울어진 유체 합동의 vorticity·가속도** | tilt v_a 로부터 대수적으로 | Planck (ω/H)₀ < 7.6×10⁻¹⁰ 대비 | 42 |
| **이방 BAO** (r_s 가 방향 의존 거리) | r_s = ∫c_s dτ/(방향 의존 팽창) | D_M/D_H 이방성 → BAO 사중극 | 47 |
| **관측자 tilt 로 인한 CMB 쌍극** | v_a 로부터 대수적으로 | 쌍극 = 순수 배경 효과일 수 있음 | 42, 46 |

---

## 2. 아키텍처 결정 (착수 전 확정)

### D1 — 물리 시간 변수: **τ 를 그대로 쓴다** (새 시간 변수 도입 금지)

핵심 관찰:

```
ℓ ≡ (a₁a₂a₃)^{1/3},   ℓ'/ℓ = 1   (정의상 정확)
⟹  τ = ln(ℓ/ℓ₀)  —  즉 팽창정규화 τ 는 이미 "평균 스케일인자의 e-fold 수"다.
```

따라서 물리 차트는 **새 적분기가 아니라 τ 위의 부수량 묶음**이다.

```
H(τ)   = H₀ exp(−∫₀^τ (1+q) dτ')            ← physical.integrate_H (이미 있음)
t(τ)   = t₀ + ∫ dτ/H                         ← physical.cosmic_time (이미 있음)
ρ_i(τ) = 3H(τ)² Ω_i(τ)
T_γ(τ) : g_*s(T) T³ ℓ³ = const  →  T(ℓ)      ← 신규
```

필요 τ 범위 (수치 확인):

| 목표 | Δτ |
|---|---|
| 재결합 (T=3000 K) | 7.0 |
| e± 소멸 | 21.5 |
| QCD 전이 | 27.2 |
| 전약 스케일 | 33.7 |
| 인플레이션 종료 | 60–70 |

**함정**: H = 0 (Bianchi IX 재붕괴) 에서 τ 차트가 깨진다. → IX 는 D-정규화 차트에서 적분하고, 물리량은 `d τ = H dt` 대신 `dt = −dτ₋/D` 로 복원한다. `physical/` 은 두 경로를 모두 지원해야 한다.

### D2 — 종족(Species) 아키텍처 일반화

현재 `MultiFluid / ScalarField / MagneticField` 는 각자 다른 시그니처를 갖는다. 통일 프로토콜:

```python
class Species(Protocol):
    def rho(self, ctx) -> Scalar          # 법선 프레임 에너지밀도
    def p(self, ctx) -> Scalar
    def pi(self, ctx) -> Array[3,3]       # 이방응력 (trace-free)
    def q(self, ctx) -> Array[3]          # 운동량 flux (Codazzi 소스)
    def rhs(self, ctx) -> "Species"       # 자체 진화
    def Q(self, ctx) -> tuple[Scalar, Array[3]]   # 다른 종족과의 교환 (Q^A)
```

* `ctx` 는 (H 또는 D, Σ_ab, N_ab, A_a, R_a, τ, T_γ, 다른 종족 핸들) 묶음.
* **불변식**: `sum_i Q_i^A = 0` 을 런타임에서 강제 (총 EMT 보존은 축약 Bianchi 항등식이 요구하는 것 — v1.1 §3 참조).
* 신규 종족: `TabulatedEoS(w(a))`, `MassiveNeutrino`, `FreeStreaming`, `ViscousFluid`, `GaugeField`, `DecayingSpecies(Γ)`.

### D3 — 열역학과 기하의 결합 방향: **단방향 우선, 예외 명시**

| 섹터 | 기하 → 열 | 열 → 기하 | 처리 |
|---|:--:|:--:|---|
| g_*(T), T(ℓ) | ✅ | ρ_r 를 통해 ✅ | **양방향** — 결합계에 포함 |
| 재결합 x_e | ✅ | 무시 가능 (ρ 기여 ~10⁻⁹) | **단방향 2-pass** |
| 잔존물 동결 | ✅ | Ω_DM 초기값으로 ✅ | 단방향 + 최종 정규화 |
| 자유흐름 π_ab | ✅ | **✅ (shear 감쇠)** | **반드시 결합계** |
| 점성 π_ab | ✅ | ✅ | 결합계 |

단방향인 것은 별도 stiff 서브솔버로 분리한다 (Kvaerno5, `throw=False`, vmap 안전). 이게 대규모 스캔 비용을 지배한다.

### D4 — 자유흐름 종족: 특성곡선 + 구적 (모멘트 계층 아님)

균질 시공간의 Vlasov 방정식은 좌표(불변) 기저에서 분포함수가 특성곡선을 따라 보존된다.

* **Bianchi I**: p_i = const (공변 성분), 물리 운동량 p² = Σ_i p_i²/a_i². ρ, p, π_ab 가 **2차원 구적**으로 정확히 나온다.
* **일반 Bianchi**: 구조상수 때문에 dp_α/dt ≠ 0. → 표본 운동량 N_p 개를 상태에 실어 함께 적분하고, 구적 가중치로 모멘트를 만든다.

**설계상 이득**: 이 특성곡선 적분기는 **광자(m=0)의 널 측지선 적분기와 동일한 코드**다 (PR-43). 하나만 만들어 두 곳에 쓴다.

**대안(폴백)**: 모멘트 계층 + 닫힘(closure). 싸지만 근사. Bianchi I/LRS 에서 정확해와 대조해 오차를 표로 남긴다.

### D5 — 광선 서브시스템

법선 프레임에서 광자 4-운동량을 (E, n̂) 로 분해:

```
dE/dt  = −E ( H + Σ_ab n̂^a n̂^b )                          ← 이방 적색이동
dn̂^a/dt = −( Σ^a_b − n̂^a n̂^c Σ_cb ) n̂^b + (곡률·회전항)      ← 방향 회전
1+z    = E_emit / E_obs
```

거리는 Sachs/Jacobi 방정식으로:

```
d²J/dλ² = −R(λ) J ,   R_AB = 광학 조석행렬 = R_{aμbν} k^μ k^ν 의 스크린 사영
d_A     = |det J|^{1/2}
```

조석행렬에는 **Ricci 집속(물질) + Weyl shear** 가 모두 들어간다. Bianchi 는 Weyl ≠ 0 이므로 널 shear 가 반드시 생긴다. → 프레임 Riemann 이 필요하고, 이는 v1.1 감사 엔진 `audit/einstein_frame.py` 가 이미 심볼릭으로 만든다. **코드 생성으로 JAX 함수화**한다 (PR-45).

**강력한 오라클**: Etherington 거리-쌍대성 `d_L = (1+z)² d_A` 는 광자수 보존만 있으면 **어떤 시공간에서도 정확히 성립**한다. d_L 을 독립 유도하지 말고 검증에 쓴다.

### D6 — 관측자 프레임 고정 (최대 오류원)

관측량은 **어느 합동**에서 재는가를 먼저 못 박는다.

* 기본: **법선 합동** n^A (Σ, N, A 가 정의된 프레임).
* CMB 쌍극이 0 이 되는 관측자 = "CMB 정지 프레임" 은 tilt 가 있으면 법선 합동과 다르다.
* `observables/` 의 모든 함수는 `frame: Literal["normal","fluid","cmb_rest"]` 를 **필수 인자**로 받는다. 기본값 금지 (v1.1 회전 규약 사고의 재발 방지).

### D7 — 단위계

* 내부: 자연단위 8πG = c = 1, 시간은 τ (무차원).
* 경계: `physical/units.py` 가 (H₀ [km/s/Mpc], h, T₀ [K], Mpc, eV, Gyr) 변환을 한 곳에서 담당.
* **ShearScalar 와 같은 뉴타입 패턴**을 온도·거리에도 적용할지 검토 (Σ_WE vs σ/θ 사고의 일반화).

---

## 3. PR 리스트

각 PR = 1 모듈 + 테스트 + 오라클. 괄호는 선행 의존.

### M8 — 물리 차트 기반

| PR | 내용 | 핵심 오라클 |
|---|---|---|
| **28** | `physical/chart.py` — τ ↔ (H, t, ℓ) 왕복, IX 의 D-차트 경로, 차원 복원 | de Sitter: H=const, ℓ=e^τ; 먼지 FLRW: t = 2/(3H) |
| **29** | `physical/frame_transport.py` — 삼각대 P(τ) 전송 `dP/dτ = (I+Σ+Ω)P`, a_i(τ), 비대각·회전 프레임 지원 (28) | det P = e^{3τ}; 대각 게이지에서 기존 `directional_scale_factors` 와 일치 |
| **30** | `matter/species.py` — 공통 프로토콜, 교환항 Q^A, `sum Q = 0` 런타임 강제 (—) | 기존 3종족 리팩터 후 129 tests 그대로 통과 |
| **31** | `physical/units.py` + `physical/initial.py` — CODATA 상수, Ω_i0 파라미터화, **오늘→과거 역적분 빌더** (28,30) | ΛCDM 재현: t₀ = 13.80 Gyr, z_eq = 3400 |

### M9 — 열역사

| PR | 내용 | 핵심 오라클 |
|---|---|---|
| **32** | `thermo/dof.py` — g_*(T), g_*s(T) 테이블 (SM, QCD 교차 포함) + 단조 보간 (—) | T≫m_t: g_*=106.75; T≪m_e: g_*=3.36, g_*s=3.94 |
| **33** | `thermo/temperature.py` — 엔트로피 보존 → T_γ(ℓ), T_ν, e± 소멸, N_eff (28,32) | **T_ν/T_γ = (4/11)^{1/3}**; N_eff = 3.044 (근사 3.046 허용) |
| **34** | `matter/neutrino.py` — 질량 중성미자 FD 정확 적분 ρ_ν(a), p_ν(a), w_ν(a) (30,33) | m→0: ρ ∝ a⁻⁴, w=1/3; m≫T: ρ ∝ a⁻³, w→0; Σm_ν=0.06 eV → Ω_νh²=6.4×10⁻⁴ |
| **35** | `matter/dark_energy.py` — Λ, CPL w(a)=w₀+w_a(1−a), 임의 테이블, 상호작용 Q (30) | w=−1 → Λ 와 비트 단위 일치; Ω'  항등식 |
| **36** | `thermo/recombination.py` — Saha(He III→II→I, H) + Peebles ODE(fudge), x_e(τ), z_*, z_drag, 가시함수 (33) | z_* = 1090±2, z_drag = 1060±3, x_e(잔존) ≈ 2×10⁻⁴ |
| **37** | `thermo/relics.py` — 동결 Boltzmann, ⟨σv⟩ 입력 → Ω_DM; **이방성이 x_f 를 옮기는 효과** (33) | 표준 WIMP: Ω h² ≈ 0.1 pb/⟨σv⟩; Σ→0 에서 FLRW 값 복원 |

### M10 — 이방성 고유 현상

| PR | 내용 | 핵심 오라클 |
|---|---|---|
| **38** | `matter/freestream.py` — Vlasov 특성곡선 + 구적 → ρ, p, **π_ab** (30, 43의 전송코드 공유) | (i) 등방 극한 π=0; (ii) **∇^aT_ab = 0** 을 v1.1 감사 엔진으로 검증; (iii) Bianchi I 정확해 대조 |
| **39** | `matter/viscous.py` — Eckart π=−2ησ, ζθ; Israel–Stewart 완화시간 τ_π (30) | η→0 극한; IS 의 인과성 조건; shear 감쇠율 |
| **40** | `matter/gauge_field.py` — 전기 섹터 추가, f(φ)²F² 결합, 이방 인플레이션 (30, 35) | f=1 에서 기존 `MagneticField` 재현; Kanno–Soda 끌개 Σ/H ∝ ε |
| **41** | `analysis/no_hair.py` — Wald 정리 수치검증 + 반례 재현 (40) | Λ 지배 Bianchi(IX 제외): Σ ∝ e^{−3Hτ/H}; PR-40 켜면 Σ/H → 상수 ≠ 0 |
| **42** | `physical/congruence.py` — 유체 합동의 vorticity ω_ab, 가속도 u̇_a, CMB 쌍극 (29,30) | 법선 합동 ω=0 (구성상); class A tilted 에서 ω 공식 대조 |

### M11 — 광선 추적 / 관측량

| PR | 내용 | 핵심 오라클 |
|---|---|---|
| **43** | `rays/geodesics.py` — (E, n̂) 전송, z(n̂), 역방향 적분, vmap over 방향 (29) | FLRW: 1+z = 1/a 정확; Kasner 정확해; \|n̂\|=1 보존 |
| **44** | `rays/optical.py` — Jacobi 행렬 ODE, d_A(n̂,z), 널 팽창·shear (43,45) | **Etherington d_L=(1+z)²d_A**; FLRW 극한; 집속 정리 |
| **45** | `rays/generate_weyl.py` — `audit/einstein_frame.py` 에서 프레임 Riemann/Weyl → JAX 코드 생성 (—) | 생성 코드 vs 심볼릭 잔차 < 1e−12; Petrov 유형 |
| **46** | `observables/cmb_pattern.py` — HEALPix 방향 → ΔT/T(n̂), a_ℓm, C_ℓ^{Bianchi} (43) | Bianchi I → 순수 사중극; VII_h → 나선 패턴; Planck (ω/H)₀<7.6×10⁻¹⁰ 재현 |
| **47** | `observables/distances.py` — r_s(z_drag)(방향), D_M, D_H, D_V, 이방 BAO (36,44) | FLRW: r_s = 147.1 Mpc; D_V/r_d 표준값 |
| **48** | `observables/sn.py` — μ(z,n̂), 쌍극/사중극 분해, Pantheon+ 형식 예측 (44) | FLRW μ(z); Bianchi I 의 μ 사중극 진폭 해석식 |

### M12 — 검증·통합

| PR | 내용 |
|---|---|
| **49** | **FLRW 극한 회귀 스위트** — 모든 신규 모듈에 Σ→0, N→0, A→0 극한 테스트. CLASS/CAMB 기준값을 상수로 박아 대조 |
| **50** | `thresholds.py` **v1.1 반영** (부록 B 수정본이 아직 코드에 없음 — 즉시 처리 가능한 부채) + 관측 한계 카탈로그 (Planck, Pantheon+, DESI) |
| **51** | 대규모 스캔 통합 — `batch.py` 에 광선 비용 반영, 메모리/시간 재추정, 난이도 점수 갱신 |
| **52** | 보고서 v2.0 — 신규 방정식 전부 + 감사 엔진 확장 + 검증표 |

---

## 4. 의존 그래프

`phenomena-dag.mermaid` 참조. 요약:

```
PR-30 (Species) ─┬─> M9 전부
PR-28 (chart) ───┴─> PR-29 (frame transport) ──> PR-43 (rays) ──┬─> PR-44 ──> PR-47, 48
                                                                └─> PR-46
PR-45 (Weyl codegen) ──> PR-44
PR-43 (전송코드) ──> PR-38 (freestream)
```

**임계경로**: 28 → 29 → 43 → 44 → 47/48. 여기가 가장 길고 위험하다.

---

## 5. 검증 오라클 카탈로그

기존 오라클(§v1.1 보고서)에 더해 신규:

| 오라클 | 무엇을 잡는가 | 사각지대 |
|---|---|---|
| **Etherington** d_L=(1+z)²d_A | d_A, z 의 상대 오류 | 둘 다 같은 인자로 틀리면 통과 |
| **T_ν/T_γ = (4/11)^{1/3}** | g_*s 테이블, e± 소멸 처리 | QCD 전이 부근 오류는 못 잡음 |
| **∇^aT_ab = 0 (자유흐름)** | π_ab 구적의 정확성 | 구적 노드가 너무 적으면 둘 다 틀림 |
| **Wald no-hair 감쇠율** | Λ 종족의 부호·크기 | IX 는 예외이므로 별도 |
| **FLRW 극한 (Σ,N,A→0)** | 모든 신규 모듈 | 이방성 고유 항은 못 잡음 → 별도 필요 |
| **Bianchi I 정확해** (Kasner, 자유흐름 정확 적분) | 이방성 고유 항 | I 이외 유형은 못 잡음 |
| **엔트로피 보존 s ℓ³ = const** | T(ℓ) 관계 | tilted 종족에서는 성립 안 함 (아래 함정) |
| **Petrov 유형** (PR-45) | Weyl 코드 생성 | — |

### 5.1 교차 검증 전략

v1.1 에서 효과가 입증된 **계수 동정(coefficient identification)** 을 그대로 확장한다: 새 닫힌 형태를 수백 개 난수 상태에서 텐서 기저에 최소제곱 적합 → 계수가 정수로 떨어지면 옳고, 잔차가 크면 기저(=식)가 불완전하다. v1.0 의 `dv_general` 버그를 잡은 방식이다. `audit/` 을 그대로 확장한다.

---

## 6. 알려진 함정 (착수 전 각인)

1. **온도를 두 번 이방화하지 말 것.** 열역학적 T 는 평균 스케일 ℓ 만 따른다 (T ∝ ℓ⁻¹). 방향 의존은 **광선의 적색이동**에서만 나온다. 둘을 곱하면 이방성을 이중 계산한다.
2. **엔트로피 보존은 tilted 종족에 적용되지 않는다.** s ℓ³ = const 는 종족이 법선 합동과 공동운동일 때만. tilted 면 Γ 인자가 붙는다.
3. **관측자 프레임을 기본값으로 두지 말 것** (D6). v1.1 회전 규약 사고와 같은 구조 — 생산자와 소비자가 다른 프레임을 쓰면 게이지 고정된 차트 안에서는 상쇄되어 테스트가 통과한다.
4. **VII_h 에서 "오늘의 Σ" 와 "CMB 패턴 진폭"은 교환 불가.** VII_h 의 나선은 N_× 회전이 만들며, Σ₀ 가 극도로 작아도 큰 패턴을 남길 수 있다. 제한을 서로 번역하지 말 것.
5. **Etherington 은 검증용이지 유도용이 아니다.** d_L 을 (1+z)²d_A 로 *정의*해 버리면 오라클이 자동 통과가 된다.
6. **널 측지선 역적분의 방향.** 관측 → 과거로 적분한다. τ 가 감소하는 방향이며 부호 실수가 나기 쉽다. 강제 테스트: z ≥ 0 이고 단조.
7. **π_ab 가 0 이 아니면 대각 shear 가정이 깨진다** — 이미 `components.py` 주석에 있는 사실이나, 자유흐름·점성·게이지장이 들어오면 **일반 차트(`charts/general`)가 기본**이 되어야 한다. 축약 차트로 스캔하던 관성을 버릴 것.
8. **stiff 구간의 vmap 배치 발산.** 재결합·동결은 stiff 하다. 배치 비용 = 배치크기 × 최대스텝. 단방향 분리(D3)가 이 문제의 핵심 완화책이다.
9. **`thresholds.py` 가 아직 v1.0 값이다.** 보고서 부록 B 는 v1.1 에서 고쳤으나 코드는 안 고쳤다 (10/9 를 VI₀ 에 할당한 값이 그대로 남아 있음). PR-50 이전이라도 즉시 처리 권장.

---

## 7. 규모 추정

| 마일스톤 | PR 수 | 신규 LOC(추정) | 위험 |
|---|:--:|:--:|---|
| M8 물리 차트 | 4 | ~600 | 낮음 (기존 `physical.py` 확장) |
| M9 열역사 | 6 | ~1200 | 중 (테이블 데이터, stiff ODE) |
| M10 이방성 고유 | 5 | ~1100 | **높음** (자유흐름 구적, 게이지장) |
| M11 광선/관측량 | 6 | ~1400 | **높음** (Sachs, Weyl 코드생성, HEALPix) |
| M12 검증·통합 | 4 | ~700 | 중 |
| **합계** | **25** | **~5000** | 현 저장소(4.3k) 규모의 약 2배 |

계산 비용: 광선 추적이 지배한다. HEALPix N_side=16 (3072 픽셀) × 방향당 ~10³ 스텝 → 모델 하나당 3×10⁶ RHS 평가. 대규모 스캔에서는 **저해상도 패턴(N_side=8) + 사중극만** 옵션을 기본으로 둔다.

---

## 8. 권장 착수 순서

사용자 우선축(관측량 · 이방성 고유 · 물리차트+열역사)을 반영하되, 임계경로를 먼저 뚫는다.

1. **PR-50 (부채 청산)** — `thresholds.py` v1.1 반영. 30분.
2. **PR-28, 29, 30** — 기반 3종. 이게 없으면 나머지 전부가 막힌다.
3. **PR-45, 43** — Weyl 코드 생성 + 널 측지선. 임계경로의 심장.
4. **PR-32, 33** — g_*, T(ℓ). 열역사의 뿌리.
5. **PR-44, 46** — d_A + CMB 패턴. 여기서 **첫 관측 대비 숫자**가 나온다 (Planck Bianchi 제한 재현이 마일스톤 검증).
6. **PR-38** — 자유흐름 중성미자. 이방성 고유 현상 중 물리적 영향이 가장 크다.
7. 나머지 M9/M10/M11 을 병렬로.
8. **PR-49, 52** — 회귀 + 보고서 v2.0.

---

## 부록: 참고문헌

- O. F. Piattella, *Lecture Notes in Cosmology*, arXiv:1803.00070 — 배경/섭동 분류의 기준 목차.
- Planck Collab. 2015 XVIII, *Background geometry & topology*, arXiv:1502.01593 — Bianchi VII_h 탐색, (ω/H)₀ < 7.6×10⁻¹⁰ (95% CL).
- McEwen et al., *Bayesian analysis of anisotropic cosmologies: Bianchi VII_h and WMAP*, MNRAS 436, 3680.
- Collins & Hawking (1973); Barrow, Juszkiewicz & Sonoda (1985) — Bianchi CMB 패턴.
- Misner (1968) — 중성미자 점성에 의한 이방성 감쇠.
- Watanabe, Kanno & Soda (2009) — 이방 인플레이션 끌개 (no-hair 반례).
- Wainwright & Ellis, *Dynamical Systems in Cosmology* — 팽창정규화 형식 (기존 기반).
- Heinzle & Uggla — Bianchi IX D-정규화 (기존 기반).
