# PLAN-H5 · tilted 합동 계층 — 유도·오라클 설계

작성 2026-07-30.  H1–H4 완료(법선합동), H3/H4 Rust 이식 완료 상태에서 다음 단계.
**2026-07-30 갱신 — H5-a·H5-b 완료.  §0 에 확정 결과와 두 건의 정정을 기록한다.**

---

## 0. 확정 결과 (H5-a·H5-b 완료)

### 0.1 식 (12) 전문 — ω·u̇ 항 포함 (논문 원문 전사)

```
⊥J̇^(i)_{A_l} + H[(3+n)J^(i)_{A_l} + (1−n)J^(i+1)_{A_l}]
  + D^a J^(i)_{aA_l}                                                          (div-con)
  − (l/(2l+1)) D_{⟨a_l} J^(i+1)_{A_{l−1}⟩}                                    (div-free)
  − l J^(i)_{a⟨A_{l−1}} ω_{a_l⟩}{}^a                                          (Ω)
  + (l/(2l+1))[(l+n+1)J^(i)_{⟨A_{l−1}} + (2−n)J^(i+1)_{⟨A_{l−1}}] u̇_{a_l⟩}   (D)
  + [(l−n)J^(i−1)_{aA_l} + (n−2)J^(i)_{aA_l}] u̇^a                            (E)
  + (l/(2l+3))[(2n+3)J^(i)_{a⟨A_{l−1}} + (2−2n)J^(i+1)_{a⟨A_{l−1}}] σ_{a_l⟩}{}^a  (A)
  + [(l−n)J^(i−1)_{abA_l} + (n−1)J^(i)_{abA_l}] σ^{ab}                        (B)
  + (l(l−1)/(4l²−1))[(n−1)J^(i+2)_{⟨A_{l−2}} − (l+n+1)J^(i+1)_{⟨A_{l−2}}] σ_{a_{l−1}a_l⟩} (C)
  = 0
```
★ 명칭 주의: 논문의 `A_a` 는 **가속도** u̇_a 다.  Wainwright-Ellis 의 `A_a` (class B
  구조상수) 와 완전히 다른 양이므로 코드에서는 `u_dot` 으로 쓴다 (기존 계획의 위험표 항목).

### 0.2 부호 확정 — 보존법칙 오라클 (`audit/h5_tilted_conservation.py`)

식 (12) 의 (l,i)=(0,0) 과 (1,0) 은 **T^{μν} 변수만으로 닫힌다**:
l=1 에서 (1−n)J^(1)_a 의 계수가 0 이고 (B) 는 (l−n)=(n−1)=0 이라 **완전히 소멸**한다.
즉 그 두 방정식이 곧 에너지·운동량 보존이다.  그래서 tilted Bianchi I 계량에서
∇_μT^{μν}=0 을 **독립 유도**해 부호를 확정했다 (논문 전사를 믿지 않는다).

| 항 | 부호 | 성격 |
|---|---|---|
| (A) σ, 1축약 | **+1** | ← 통제군: H1 정확구적 오라클과 일치 |
| (B) σ, 2축약 | **−1** | ← 통제군: H1 과 일치 |
| (C) σ, 외적 | −1 | (H1 확정, l≥2 이라 이 오라클 범위 밖) |
| (Ω) ω | **+1** | 신규 |
| (D) u̇ 자유첨자 | **+1** | 신규 |
| (E) u̇ 축약 | **−1** | 신규 |
| (div-con) D^aJ_{aA_l} | **+1** | 신규 |
| (div-free) D_{⟨a_l}J_{A_{l−1}⟩} | **−1** | 신규 |

```
최소제곱해 전부 정확히 ±1 (|편차| < 1e−15)
잔차 절대 1.33e−15 / 상대 7.68e−16
유일성 마진 (2^7 = 128 조합 전수)  1.35e+14 배
통제군 s_A=+1, s_B=−1 재생산 ✓   (다른 seed 3개에서도 동일)
```
★ **통제군이 이 결과의 근거다**: 이미 확정된 두 부호를 **다른 방법**(보존법칙 vs
  정확구적)으로 재생산했으므로 같은 절차가 준 신규 부호도 신뢰한다.
  통제군이 깨졌다면 신규 부호를 채택하지 않았을 것이다.

### 0.3 정정 1 — tilted 에서 공간미분은 **살아난다**

기존 계획(및 H1 전제)의 "균질성으로 D_a 가 죽는다"는 **법선 합동에서만** 참이다.
균질성은 법선 합동의 초곡면에 대한 것이고 tilted 관측자의 초곡면은 다르므로:

```
D_A ρ = E_A^μ ∂_μ ρ = E_A^t ρ̇ = γ v_A ρ̇  ≠ 0        (순수 boost 사틀에서 E_A^t = γv_A, 정확)
```
스칼라조차 공간구배를 갖는다.  이 항을 빼고 부호를 맞추려 하면 최소제곱해가
s_A=+1.11, s_B=−0.88 로 어긋나고 잔차 0.40 이 남는다 — **그 어긋남이 누락을 찾아줬다**
(±1 에서 벗어나는 것을 "수치오차"로 넘기지 않은 것이 핵심).

### 0.4 정정 2 — tilted 계층은 **음함수(질량행렬) ODE** 다

D 항이 이웃 l 의 **시간미분을 품는다**:

```
∂(D^a q_a)/∂q̇_A  →  v_A        (v→0 극한, 해석적으로 확인)
```
따라서 `hierarchy_rhs` 처럼 dJ/dt 를 바로 반환하는 명시적 RHS 형태로는 **구현할 수 없다**.
l 번째 방정식이 J^(i)_{A_{l±1}} 의 시간미분을 포함하므로 질량행렬 M(v)·J̇ = F 를 풀어야 한다.
⇒ H5-c 의 설계가 바뀐다 (아래 §6 갱신).  Rust 쪽 diffsol 은 질량행렬 ODE 를 지원하므로
  이식 단계에서는 오히려 유리하다.

### 0.5 구조적 발견 — 축정렬 tilt 는 ω 를 켜지 못한다

```
다축 tilt   (v₁,v₂,v₃ 모두≠0):  max|ω_AB| = 4.77e−02,  max|u̇_A| = 4.67e−01
단일축 tilt (v=(0,0,v₃)):        max|ω_AB| = 0.00e+00,  max|u̇_A| = 3.40e−01
단일축 표본만 쓴 선형계의 rank = 6/7  →  s_Ω 미결정
```
비등방 주축과 boost 방향이 정렬되면 D_au_b 의 반대칭부가 사라진다.
**(Ω) 항 시험에는 다축 tilt 가 필수**다 — 모르고 단일축으로만 시험하면 s_Ω 을
확정했다고 착각한다.  가속도는 단일축에서도 켜지므로 (D),(E) 는 단일축으로 충분하다.

시험 잠금: `tests/test_h5_tilted_signs.py` (12개).

이 문서는 **코드 없이 확정할 수 있는 부분**을 먼저 못 박는다: 문제 구조, 그리고 무엇보다
**ω/u̇ 계수를 단정하지 않고 확정할 오라클**.  H1 에서 σ-결합 부호 (A,B,C)=(+1,−1,−1) 을
정확 구적 오라클로 확정한 것과 **같은 방법**을 tilted 로 확장하는 것이 요점이다.

---

## 1. 무엇을 푸는가

지금까지의 계층은 **법선 합동**(normal congruence) 가정 위에 있다:

```
u^μ = n^μ  (균질성 초곡면에 직교)  ⇒  u̇_a = 0,  ω_ab = 0
```

Bianchi I 에서 n^μ 는 측지·비회전이므로 이 두 항이 죽고, 균질성으로 공간미분도 죽어서
식 (12) 가 σ-결합만 남은 형태로 축약됐다 (현재 구현):

```
J̇^(i)_{A_l} + H[(3+n)J^(i)_{A_l} + (1−n)J^(i+1)_{A_l}]
  + (l/(2l+3))[(2n+3)J^(i)_{a⟨A_{l−1}} + (2−2n)J^(i+1)_{a⟨A_{l−1}}] σ_{a_l⟩}{}^a    (A)
  + [(l−n)J^(i−1)_{abA_l} + (n−1)J^(i)_{abA_l}] σ^{ab}                               (B)
  + (l(l−1)/(4l²−1))[(n−1)J^(i+2)_{⟨A_{l−2}} − (l+n+1)J^(i+1)_{⟨A_{l−2}}] σ_{..⟩}    (C)
  = 0                                                    n ≡ l + 2i
```

**H5 는 u^μ ≠ n^μ (tilted) 를 허용한다.**  물리적 동기: 물질의 4-속도가 균질성 초곡면에
직교하지 않는 경우(tilted 유체)가 Bianchi 우주론의 일반적 상황이고, 이때 에너지 유속
q_a ≠ 0 이 배경 수준에서 살아난다.  현재 코드는 q_a 를 구조적으로 가질 수 없다.

---

## 2. 핵심 — H5 에도 **정확 오라클이 존재한다**

이것이 이 문서의 요점이다.  처음에는 tilted 가 오라클 없는 영역처럼 보인다(H3 의 충돌항이
그랬듯이).  그렇지 않다.

Bianchi I 무충돌 Vlasov 의 정확해는 **법선 프레임에서**

```
f(x, p) = f₀(p_i),        p_i = const  (불변기저 공변운동량)
```

이고, 이것이 H1–H4 의 구적 오라클 근거였다.  tilted 관측자는 **같은 분포함수 f** 를 보되
운동량을 다르게 분해한다.  boost 를

```
u^μ = γ(n^μ + v^μ),     v·n = 0,     γ = (1 − v²)^{−1/2}
E′ = γ(E − v·P),        P′^a = Λ(v)^a{}_b P^b + (…)E
```

로 두면, tilted 프레임 모멘트가

```
J′^(i)_{A_l} = ∫ dλ′ dΩ′  λ′² E′ (λ′/E′)^n  e′_{⟨A_l⟩}  f₀(p(λ′, n̂′))
```

**정확 구적으로** 계산된다 — f₀ 는 정확히 알고 boost 는 순수 대수이므로.

⇒ **tilted 계층의 ω/u̇ 계수·부호도 오라클 매칭으로 확정할 수 있다.**
논문 계수를 전사만 하고 믿는 대신, H1 과 같은 유일성 마진 스캔을 돌릴 수 있다.

### 2.1 부수 효과 하나 — 홀수 l 이 **인공 쌍극자 없이** 켜진다

H1 에서 등방 f₀ 는 f(p) = f(−p) 라 홀수 l 모멘트가 항등적으로 0 이었고 (|J_a| ~ 3e−15),
(A) 항군을 시험하려고 `f_dipole(ε, axis)` 을 인공적으로 도입해야 했다.

tilted 에서는 **boost 자체가 물리적 쌍극자원**이다:  등방 f₀ + v ≠ 0 이면
q′_a = J′^(0)_a ≠ 0 (에너지 유속).  즉 H5 는 (A) 항군과 홀수 l 을
**인공 분포 왜곡 없이** 시험할 수 있다 — H1 의 시험보다 물리적으로 정직한 게이트다.

### 2.2 계층과 무관한 강한 게이트 — T^{μν} 프레임 무관성

T^{μν} 는 텐서이므로 프레임 선택과 무관하다.  따라서 boosted 구적에서 얻은
(ρ′, q′_a, π′_ab) 는 법선 프레임 (ρ, q_a = 0, π_ab) 의 **Lorentz 변환과 정확히 일치**해야
한다.  이건 순수 대수 항등식이라 계층 방정식을 전혀 경유하지 않는 독립 검증이다:

```
ρ′ = γ²(ρ + 2 v^a q_a + v^a v^b (…)) …   (v·q = 0 이므로 단순화)
q′_a = −γ²(ρ + p) v_a + O(v·π) + …
```
정확한 형태는 π_ab 항 때문에 손으로 쓰기보다 **코드에서 대수적으로 확인**하는 게 안전하다
(부호 실수가 나기 쉬운 곳).  게이트로 만들면 자동으로 잡힌다.

---

## 3. 두 경로 (사용자 규약 — 모든 유도를 두 갈래로)

| 경로 | 방법 | 쓰지 않는 것 |
|---|---|---|
| **A · boosted 구적** | 정확해 f₀ 를 boost 해 J′ 를 구적, 시간 유한차분으로 dJ′/dt | 계층 수식 |
| **B · 확장 PSTF 계층** | ω/u̇-확장 식 (12) 평가 (상태 손세팅 판은 구적도 미사용) | 구적 |

H1 과 같은 구조다(H3 처럼 나눌 필요가 없다 — 여기서는 구적이 **살아있으므로**).

---

## 4. 단정하지 않고 코드로 확인할 것 (열린 질문)

1. **Bianchi I tilted 합동의 ω_ab 가 0 인가?**
   균질성으로 v 는 불변기저에서 공간상수지만, ω 는 공변 반대칭 미분이라 접속항을 먹는다.
   Bianchi I 은 구조상수가 0 (n_ab = 0, A_a = 0) 이라 공간접속이 죽지만, 외재곡률과 boost
   의 조합이 남는지는 **`congruence.py` 의 on-shell 값으로 측정**해야 한다.
   King–Ellis (1973) tilted 모델에서 vorticity 는 일반적으로 0 이 아니지만, class A 의
   특수성이 있을 수 있다.  **여기서 단정하지 않는다.**
2. **u̇_a 의 on-shell 값**: tilted 유체의 가속도는 압력 구배가 없으면 (균질) 0 일 수도 있고,
   π_ab 와 v 의 결합에서 나올 수도 있다.  역시 측정.
3. **v 의 진화 방정식**: v(t) 는 자유함수가 아니라 운동량 구속(tilted Euler)이 정한다.
   H5 에서 v(t) 를 (a) 시험함수로 규정해 계수만 확정하는 단계와,
   (b) on-shell v(t) 로 자기일관 적분하는 단계를 **분리**한다 — (a) 가 부호 확정에 충분하다.
4. **ω ≠ 0 이면 PSTF 구조가 깨지는가**: ω 결합은 반대칭이라 J′_{A_l} 의 PSTF 성질을
   보존해야 한다(사영 후에도).  이건 시험으로 못 박을 수 있다 (사영 잔차 = 0).

---

## 5. 필요한 입력

- **논문 식 (12) 의 ω_ab, u̇_a 항 원문** — `arXivastroph0203507v2.tar.gz` (이전에 업로드됨).
  부호규약 (+−−−) → (−+++) 변환을 σ 항과 **같은 절차**로 처리한다.
  ★ 다만 최종 부호는 전사에 의존하지 않고 §2 오라클로 확정한다 (전사는 후보 생성용).
- **저장소 스냅샷** — `bianchi-h3.tar.gz` (직전 턴에 전달한 688 KB).

---

## 6. DAG (§0.4 발견으로 갱신)

```
H5-a  ✅ 구조확정: eq(12) ω/u̇ 항 전사 (§0.1)
        │
H5-b  ✅ 부호 확정: ∇_μT^{μν}=0 보존법칙 오라클 (§0.2)
        │   `audit/h5_tilted_conservation.py`, `tests/test_h5_tilted_signs.py` (12)
        │   ★ 통제군으로 H1 의 s_A, s_B 재생산 — 신규 5개 부호 확정, 마진 1.4e14×
        │
H5-c  ✅ **질량행렬 조립** — `matter/tilted.py`, `tests/test_h5c_tilted_mass_matrix.py` (17)
        │   · 발견 닫힌형:  **M = γ(I + e₀vᵀ)**,  M⁻¹ = γ⁻¹(I − e₀vᵀ)  → 상삼각
        │   · cond(M) ≤ 2.4 (|v| ≤ 0.9) — 극단 tilt 에서도 온순
        │   · 계층 좌변 ≡ ∇_μT^{μν} 사영 (독립 모듈 교차검증, 4 seed, ≤1e−11)
        │   · v→0 에서 M = I, ω = u̇ = D = 0 (정확히 0)
        │
H5-d  ✅ **완료** — `matter/tilted_moments.py`, `matter/tilted_terms.py`,
        │   `audit/h5d_tilted_residual.py`, 시험 51개 (d1 30 + d2/d3 21)
        │   · T^{μν} 프레임 무관성 ≤ 1e−14 (계층 미경유, 가장 결정적 게이트)
        │   · v→0 **비트-정확** 환원,  λ′²=E′²−m² 격자 전점 1.3e−15
        │   · l ≤ 3 잔차 1e−11 ~ 1.5e−10 (중앙차분 한계, dt 스캔 U자 확인)
        │   · 부호 유일성 마진 **5.8e5 배** — H5-b 보존법칙 확정값과 **독립 일치**
        │   · ★ q′_a 가 boost 만으로 켜짐 ⇒ `f_dipole` 불필요 (l=1 3.3e−1, l=3 7.0e−3)
        │   · |q′|/ρ′ = v(ρ+p)/(ρ+pv²) 닫힌형과 1e−12 일치 (구적 미사용 독립 검산)
        │
H5-e1 ✅ **구적 이식** — `quad.rs` 에 `j_moment_tilted` 등, 차등 5e−15, **4.8×**
        │   · 잠복결함 2건 수정: FFI 를 넘던 Rust panic (한계가 세 곳에 흩어져 있었다),
        │     codegen L_MAX 4→5, 컴파일 시 어서션으로 재발 차단
        │
H5-e2 ✅ **일반 l 질량행렬** — `matter/tilted_mass.py`, 시험 20개
        │   · 닫힌형:  대각 γ·P,  l+1 → s_dc·γ·P∘(v 축약),
        │             l−1(i+1) → s_df·(−l/(2l+1))·γ·P∘(·⊗v)
        │   · 닫힌형 vs 일반 아핀 추출 9.5e−15,  M 은 **물질 무관** 1.4e−14
        │   · v→0 에서 M = I,  l≤1 에서 H5-c 닫힌형 정확 재생산
        │   · cond(M) ≤ 5.25 (|v| ≤ 0.9)
        │   · ★ 상태공간은 3^l 이 아니라 **2l+1 PSTF 부분공간** (3^l 은 특이)
        │   · ★ 정확 구적 J̇ 가 M·J̇ = F 를 만족 (1.7e−10), 역해도 J̇ 복원
        │
H5-e3 ✅ **시간적분 (Python)** — `matter/tilted_integrate.py`, 시험 15개
        │   · 종단 게이트: 궤적 vs 정확 구적 (i_max=2 에서 ~1e−4)
        │   · ★ 닫힘 근거를 **측정**: 무질량 i-무관성이 boost 후에도 정확히 성립(1.0000)
        │   · ★ 지배 오차가 **i-닫힘** — 법선합동(H2)에서 l_max 였던 것과 **반대**
        │     (i_max 1→4: 5.3e−4 → 6.3e−6;  l_max 2→3 은 거의 무변화)
        │   · dt 는 이미 수렴 (nsteps 10→80 불변) — 남은 오차를 적분정밀도로 오해 금지
        │   · 오차 ~v² (측정 지수 1.79), v=0 에서 1.6e−7 (법선합동 수준)
        │   · l+1 절단오차가 σ 의 l+2 절단보다 **약 3배** (2.10e−4 vs 7.32e−5)
        │
H5-e4 ✅ **무질량 1차원 환원** (문헌 근거: 원논문의 "reduce to the usual
        │   one-dimensional Boltzmann hierarchy") — ρ 오차 5.3e−4 → 7.8e−8 (약 6800배)
        │   · 기전: 2차원 독립적분이 정확한 축퇴 J^(i)=J^(0) 를 t=T 에서 21% 깨뜨렸다
        │   · 환원 후 오차가 다시 l_max 지배 (H2 와 같은 성격)
        │   · `LITERATURE-moment-closure.md` 에 문헌 기반 정리
        │
H5-e5 ⬜ **n_* 삼각절단 + 홀짝 프레임불변성 시험** (문헌의 미검증 예측)
        │   원논문: 절단 J=0 for l+2i>n_* 는 **n_* 홀수에서만** 프레임 불변
        │   tilt = fiducial velocity 변경이므로 직결 — 오차 ∝v² 와 맞물릴 개연성
        │
H5-e6 ⬜ Rust 이식 (diffsol 질량행렬 ODE) — **정확도 개선이 끝난 뒤**
```

임계경로가 H5-d → **H5-e (Rust 이식)** 로 옮겨졌다.  부호는 l≤1(보존법칙)과
l≤3(정확구적) 두 독립 오라클로 확정됐고, 음함수 구조도 확인됐다.

### H5-d 에서 드러난 두 가지 한계 (정직하게)
1. **(E) 는 (l,i)=(2,0) 에서 항등적 0** — n=2 가 (n−2) 를, i=0 이 (l−n)=−2i 를 동시에
   죽인다.  이 칸만으로 부호 스캔을 하면 마진이 **1.000** 으로 나오는데 이는 실패가
   아니라 **미결정**이다.  활성도 진단(`term_activity`) 없이 마진만 보면 오독한다.
   → 여러 (l,i) 를 합산해 스캔하도록 고쳤다.
2. **무질량은 i 구조를 시험하지 못한다** — J^(i) 가 i-무관이라 각 항군의 계수합이
   n-무관으로 붕괴한다:  (A)→5, (D)→l+3, (B)→l−1, (E)→l−2, (C)→−(l+2).
   무질량 잔차는 **l 구조만** 시험한다.  i 의존성 검증에는 유질량이 필요하다
   (m=1 에서 재확인: 잔차 ≤1.7e−10, 마진 5.4e5).
3. 격자 L_MAX=5 라 **l ≤ 3** 까지만 절단 없이 시험된다 (l=4 는 rank-6 필요, pstf 100 ms/회).

### H5-c 의 부수 성과 — 성능
방정식 전체를 하나의 sympy 식으로 세워 lambdify 한 초기 판은 **임포트 148 초 · 호출
107 ms** 였다 (`D^bπ_ba` 가 4중 합 192항이고 그 위에 편미분이 얹혀 조합폭발; `cse=True`
로도 개선 없음).  사틀·접속 같은 **작은 닫힌형만 기호로 두고 지표축약을 numpy 로**
옮겨 **임포트 0.48 초 · 호출 1.06 ms** — 각각 약 300배·100배.  결과는 비트 동일.

### H5-c 의 방법론 성과 — 질량행렬을 손으로 옮겨 적지 않았다
좌변은 미지 시간미분에 대해 **정확히 아핀**이므로
`F = −EQ(0)`, `M[:,c] = EQ(e_c) − EQ(0)` 로 5회 평가에 M, F 가 **오차 없이** 나온다.
아핀성 자체와 "스텝 크기를 바꿔도 같은 열"을 시험으로 못 박아, 이것이 유한차분 근사가
아니라 정확 추출임을 보장했다.

---

## 7. 예상 위험

| 위험 | 대응 |
|---|---|
| boost 후 구적 격자가 더 이상 최적이 아니다 (E′ 가 방향의존) | 격자 수렴 시험을 v 마다 다시 돌린다 — H2 의 l_max·i_max 수렴 시험 패턴 |
| v 가 크면 (γ ≫ 1) 구적 꼬리가 잘린다 | γ 스캔에서 ρ′ 대 해석 Lorentz 변환값 비교로 유효 v 범위를 **측정해 문서화** (조용히 쓰지 않는다) |
| ω 결합이 홀수/짝수 l 을 섞어 닫힘 규칙이 바뀐다 | `close_i` 기하외삽의 유효성을 tilted 에서 재측정 (H2 에서 0-절단 대비 7자리 우월했던 근거가 유질량 i-무관성이었으므로 재확인 필요) |
| 경로 A 유한차분 조건수 (H3R 에서 겪은 것) | 처음부터 dt 스캔으로 반올림 지배 구간을 확인하고 허용오차의 근거를 시험으로 고정 |
