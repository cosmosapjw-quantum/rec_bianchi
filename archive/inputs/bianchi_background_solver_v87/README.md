# Bianchi background solver v87 canonical input

Owner-supplied BASS project archive used only as an evidence-locked source for
PR-05C2C1B2B1D/v0.72.  The first provider implementation transcribes the
orthogonal class-A Bianchi-II equations from `bianchi/charts/class_a.py` into a
dependency-free NumPy/SciPy adapter.  The archive remains the byte authority;
all other family/tilt routes are tiered and fail closed until validated.

The archive's own bootstrap expects optional JAX/Diffrax/Equinox and Rust
components that are not required by the bounded Bianchi-II provider pilot.
