# 04 Hypothesis space

H_A direct nodal compilation and fixed-topology interpolation satisfy all structural gates. Survives on three nodes and selected withheld pairs. H_B scalar data instantaneously determine angular radiation. Rejected; characteristic initial-boundary evolution is required. H_C the entropy graph candidate improves the locked solve. Rejected by measured iterations and wall time. H_D the bounded stage can close while retaining source-adapter and multi-macro blockers explicitly. Selected.
