# Phase 5 — Independent Adversarial Review

## H1: REJECTED_SHORTCUT

It is numerically simple but silently changes photon energy from the interface
frequency to a broad-cell centroid. The resulting discrepancy is not roundoff
and has no physical collision event to absorb it.

## H2: DEFENDED, with bounded claim

Strengths: exact state ownership; no global remap; dimensional conversion is
unique for a scalar packet; number and energy ledgers are algebraic; log
variables support positivity; analytic JVP is available from the collision
operator. Required limitation: v0.55 red/blue rates are source-conditioned
trace data, not a reconstructed COM distribution. H2 closes the interface
operator at declared snapshots, not a full Bianchi recombination history.

## H3: REJECTED_SHORTCUT

An explicit transfer step can become negative and would need clipping or an
unpublished CFL policy. It also obscures whether conservation was checked on the
same residual solved by Newton.

## H4: INFORMATIVE_FAILURE fallback

Scientifically honest and preferable to forcing H1/H3, but only if H2 fails a
hard gate.

## Additional process vulnerability

The compiler-dependent HyRec binary-hash assertion recurred in two releases.
A shared fixture reduces duplication but does not prevent a third unconditional
assertion. Add an AST-based fail-fast policy scanner to the quick verifier.
